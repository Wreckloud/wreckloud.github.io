<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>PySide6 学习笔记</title>
    <url>/2023/10/28/PySide6-%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0/</url>
    <content><![CDATA[<blockquote>
<p>学了这么久的编程, 也没什么真正拿得出手的项目. 这次, 一定要完整做出一个能用的东西出来啊!<br>带着选修课程Python的期末作业, 顺便学习PySide6来做一个高颜值的音乐播放器吧!</p>
<p>另外吐槽一下这选修课的作业难度, 真是来真的啊, 选修课作业难度达到了毕业设计的高度, 老师你真的是看得起我…<br>课程笔记来源: <a class="link"   href="https://www.bilibili.com/video/BV1c84y1N7iL?p=1&vd_source=b19220b8f372f3a81aa8463fde694a63" >PySide6百炼成真，带你系统性入门Qt <i class="fa-regular fa-arrow-up-right-from-square fa-sm"></i></a><br>啊啊, 总觉得时间老不够呜呜呜.</p>
</blockquote>
<h1 id="基础框架"><a href="#基础框架" class="headerlink" title="基础框架"></a>基础框架</h1><div class="highlight-container" data-rel="Python"><figure class="iseeu highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> PySide6.QtWidgets <span class="keyword">import</span> QApplication, QMainWindow</span><br></pre></td></tr></table></figure></div>

<p><code>QT Widget</code> 之后的框架都来自于此<br><code>QMainWindow</code> 选择的窗体类型(主窗口)</p>
<p>导入后即可开始写类</p>
]]></content>
  </entry>
  <entry>
    <title>Godot引擎学习</title>
    <url>/2023/10/08/Godot%E5%BC%95%E6%93%8E%E5%AD%A6%E4%B9%A0/</url>
    <content><![CDATA[<h1 id="Godot-初识"><a href="#Godot-初识" class="headerlink" title="Godot 初识"></a>Godot 初识</h1><h3 id="Godot-的开发思想————围绕节点"><a href="#Godot-的开发思想————围绕节点" class="headerlink" title="Godot 的开发思想————围绕节点"></a>Godot 的开发思想————围绕节点</h3><ol>
<li><strong>节点</strong></li>
</ol>
<ul>
<li>节点是Godot内最常用最基本的开发组件,  </li>
<li>节点本身 <strong>不具备</strong> 实际功能.  </li>
<li>节点之间可以用上下级的方式组合, 一个节点可以有多个子节点</li>
</ul>
<ol start="2">
<li><strong>服务器</strong></li>
</ol>
<ul>
<li>服务器指的是Godot <strong>内置的各类Server</strong> </li>
<li>节点的主要功能都要使用Server实现.</li>
</ul>
<blockquote>
<p>节点往往直接涉及人类的试听感官, 因此开发思想贴近人类思想.<br>服务器编码接近底层, 保障了游戏运行效率.</p>
</blockquote>
<ul>
<li>节点和服务器通过场景树进行协调沟通.</li>
</ul>
<ol start="3">
<li><strong>场景树</strong></li>
</ol>
<ul>
<li>场景树是游戏的主循环对象.</li>
</ul>
<blockquote>
<p>只有在树下的节点才可以正常行使节点的主要功能,节点本身不具备实际功能.<br>但节点初始化 <strong>并不在</strong> 树下.   </p>
</blockquote>
<ul>
<li>节点是在场景树与服务器的控制下运行的</li>
</ul>
<ol start="4">
<li><strong>节点的组织方式————场景</strong></li>
</ol>
<ul>
<li>场景是若干节点的集合  </li>
<li>场景文件是记录若干节点集合的文件  </li>
<li>游戏实际运行时不存在”场景”, 场景时节点在文件系统中存储和加载的单位<br>游戏从主场景开始运行</li>
</ul>
<h3 id="GDScript————Godot的原生语言"><a href="#GDScript————Godot的原生语言" class="headerlink" title="GDScript————Godot的原生语言"></a>GDScript————Godot的原生语言</h3><ol>
<li><strong>GDScript入门介绍</strong></li>
</ol>
<ul>
<li>GDScript是Godot的原生语言, 是最适合Godot开发的语言</li>
<li>入门阶段, 只需要知道GDScript可以为节点添加拓展功能即可.</li>
</ul>
<ol start="2">
<li><strong>GDScript与节点的关系</strong></li>
</ol>
<ul>
<li><p>节点能让电脑做具体的功能.<br>例如显示图片、播放音频、模拟物理等, 但节点也仅限于此.</p>
</li>
<li><p>GDScript负责实现内在逻辑, 同时控制节点展示具体的功能.<br> 例如实现角色被怪物击中掉血死亡的功能.<br> GDScript除了要削减角色生命值之外, 还要判断角色存活, 以及控制节点播放死亡动画和音效.</p>
</li>
</ul>
<h1 id="Godot-入门"><a href="#Godot-入门" class="headerlink" title="Godot 入门"></a>Godot 入门</h1><p> 定义变量: var 变量名<br> 定义函数: func 函数名():<br> 类似于Python,缩进</p>
<p> 限定类型 var 变量名:限定类型 </p>
]]></content>
      <tags>
        <tag>学习笔记</tag>
      </tags>
  </entry>
  <entry>
    <title>Python基础</title>
    <url>/2023/09/23/Python%E5%9F%BA%E7%A1%80/</url>
    <content><![CDATA[<blockquote>
<p>大二上学期选修报名了Python,实际上也有点想学习的念头.早听说python处理一些重复性的事很在行?  </p>
<p>课程笔记来自:<a class="link"   href="https://www.bilibili.com/video/BV1qW4y1a7fU/?spm_id_from=333.999.0.0&vd_source=b19220b8f372f3a81aa8463fde694a63" >Python基础 <i class="fa-regular fa-arrow-up-right-from-square fa-sm"></i></a></p>
</blockquote>
<h1 id="Python-基础语法"><a href="#Python-基础语法" class="headerlink" title="Python 基础语法"></a>Python 基础语法</h1><h3 id="字面量、数据类型"><a href="#字面量、数据类型" class="headerlink" title="字面量、数据类型"></a>字面量、数据类型</h3><p>被写下来的固定值称之为字面量,有以下6种常用数据类型:</p>
<table>
<thead>
<tr>
<th>类型</th>
<th>描述</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>数字(Number)</td>
<td>整数(int)<br>浮点(float)<br>复数(complex)<br>布尔(bool)</td>
<td>复数形如4+3j,以j结尾表示的数</td>
</tr>
<tr>
<td>字符串(string)</td>
<td>描述文本的一种数据类型</td>
<td>由任意数量字符组成</td>
</tr>
<tr>
<td>列表(List)</td>
<td>有序的可变序列</td>
<td>Python中最频繁使用的数据类型</td>
</tr>
<tr>
<td>元组(Tuple)</td>
<td>有序的不可变序列</td>
<td>有序记录不可变的Python数据</td>
</tr>
<tr>
<td>集合(Set)</td>
<td>无序不重复集合</td>
<td>可无序记录不重复的数据</td>
</tr>
<tr>
<td>字典(Dictionary)</td>
<td>无序Key-Value集合</td>
<td>无序记录Key-Value型数据</td>
</tr>
</tbody></table>
<p><code>type()</code>能够查看字面量和变量的数据类型</p>
<div class="highlight-container" data-rel="Python"><figure class="iseeu highlight python"><table><tr><td class="code"><pre><span class="line"><span class="number">114</span></span><br><span class="line"><span class="number">5.14</span></span><br><span class="line"><span class="string">&quot;我去!&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 甚至都不用声明类型...</span></span><br><span class="line"></span><br><span class="line"><span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">另外关于注释,单行注释是一个井号就ok,</span></span><br><span class="line"><span class="string">多行注释是像这样三个引号.</span></span><br><span class="line"><span class="string">不过md的代码块好像没给显示出来</span></span><br><span class="line"><span class="string">&quot;&quot;&quot;</span></span><br></pre></td></tr></table></figure></div>

<h3 id="数据类型转换"><a href="#数据类型转换" class="headerlink" title="数据类型转换"></a>数据类型转换</h3><table>
<thead>
<tr>
<th>语句(函数)</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>int(x)</td>
<td>将x转换为整数</td>
</tr>
<tr>
<td>float(x)</td>
<td>将x转换为浮点数</td>
</tr>
<tr>
<td>str(x)</td>
<td>将x转换为字符串</td>
</tr>
</tbody></table>
<ul>
<li>任何类型都可以转换成字符串</li>
<li>字符串只有数字才能转换成数字</li>
<li>浮点转换整数会丢失精度(小数部分)</li>
</ul>
<h3 id="标识符"><a href="#标识符" class="headerlink" title="标识符"></a>标识符</h3><p>是用户在编程时用于给变量、类、方法等自定义的命名.<br>能自定义标识符的有:</p>
<ul>
<li>变量的名字</li>
<li>方法的名字</li>
<li>类的名字,等等…</li>
</ul>
<p>注意:  </p>
<ul>
<li>标识符不能以数字开头  </li>
<li>标识符不能使用关键字</li>
<li>标识符不推荐使用中文</li>
<li>标识符大小写敏感</li>
</ul>
<h3 id="运算符"><a href="#运算符" class="headerlink" title="运算符"></a>运算符</h3><p>除加减乘除外Python还有:</p>
<table>
<thead>
<tr>
<th>运算符</th>
<th>描述</th>
<th>实例</th>
</tr>
</thead>
<tbody><tr>
<td>&#x2F;&#x2F;</td>
<td>取整</td>
<td>返回商的整数部分</td>
</tr>
<tr>
<td>%</td>
<td>取余</td>
<td>返回商的余数部分</td>
</tr>
<tr>
<td>**</td>
<td>指数</td>
<td>就是指数运算</td>
</tr>
</tbody></table>
<p>除了基本运算,Python也支持赋值运算.<br><strong>注意: Python 中是没有 ++ 和 – 的</strong></p>
<h3 id="字符串"><a href="#字符串" class="headerlink" title="字符串"></a>字符串</h3><ol>
<li>单引号定义法: <code>name=&#39;雲之残骸&#39;</code></li>
<li>双引号定义法: <code>name=&quot;雲之残骸&quot;</code></li>
<li>三引号定义法: <code>name=&quot;&quot;&quot;雲之残骸&quot;&quot;&quot;</code></li>
</ol>
<p>其中,三引号定义法和多行注释写法相同,同样支持换行<br>当使用变量接收它时,会变成字符串.<br>不使用变量接收时,就可以当作多行注释.   </p>
<p>除了用 “+” 号拼接字符串, 我们还能使用占位符快速拼接字符串和变量  </p>
<div class="highlight-container" data-rel="Python"><figure class="iseeu highlight python"><table><tr><td class="code"><pre><span class="line">name = <span class="string">&quot;雲之残骸&quot;</span></span><br><span class="line">year = <span class="number">114514</span></span><br><span class="line">price = <span class="number">19.5</span></span><br><span class="line">massage = <span class="string">&quot;我去,是 %s.今年是 %d 年,衬衫的价格是 %2.1f&quot;</span> % (name,year,price)</span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(message)</span><br></pre></td></tr></table></figure></div>

<blockquote>
<p>输出: &gt; 我去,是 雲之残骸 .今年是 114514 年,衬衫的价格是 19.5</p>
</blockquote>
<p>其中,%s:</p>
<ul>
<li>% 表示:占位</li>
<li>s 表示:将变量变成字符串放入占位的地方</li>
</ul>
<p>这一点与c语言相同,包括支持 %f 浮点数.<br>使用格式 %m.nf 能实现控制精度.</p>
<p>快速写法:</p>
<div class="highlight-container" data-rel="Python"><figure class="iseeu highlight python"><table><tr><td class="code"><pre><span class="line">name = <span class="string">&quot;雲之残骸&quot;</span></span><br><span class="line">year = <span class="number">114514</span></span><br><span class="line">price = <span class="number">19.5</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(<span class="string">f&quot;我去,是<span class="subst">&#123;name&#125;</span>.今年是 <span class="subst">&#123;year&#125;</span> 年,衬衫的价格是 <span class="subst">&#123;price&#125;</span>&quot;</span>)</span><br></pre></td></tr></table></figure></div>

<p>此方法不限数字类型,也不关心精度控制.</p>
<h3 id="数据输入输出"><a href="#数据输入输出" class="headerlink" title="数据输入输出"></a>数据输入输出</h3><ul>
<li>输出: print<br>print输出默认换行,若想让输出换行,则可以:</li>
</ul>
<div class="highlight-container" data-rel="Python"><figure class="iseeu highlight python"><table><tr><td class="code"><pre><span class="line"><span class="built_in">print</span>(<span class="string">&quot;hello&quot;</span>,end=<span class="string">&#x27;&#x27;</span>)  </span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;world&quot;</span>,end=<span class="string">&#x27;&#x27;</span>)  </span><br><span class="line">```    </span><br><span class="line">可以在结尾加上 `end=<span class="string">&#x27;&#x27;</span>` 即可不换行.</span><br><span class="line">* 输入: <span class="built_in">input</span></span><br><span class="line"></span><br><span class="line">```Python</span><br><span class="line"><span class="comment"># 可以直接把提示语句写在input中:</span></span><br><span class="line"></span><br><span class="line">name = <span class="built_in">input</span>(<span class="string">&quot;你是?&quot;</span>)</span><br><span class="line"><span class="built_in">print</span>(<span class="string">&quot;明白了,你是 %s&quot;</span> % name)</span><br></pre></td></tr></table></figure></div>

<p>input输入的数据默认都会当成<strong>字符串</strong><br>可以使用强制转换: num &#x3D; input(num)</p>
<h1 id="Python-判断语句"><a href="#Python-判断语句" class="headerlink" title="Python 判断语句"></a>Python 判断语句</h1><p><strong>True &#x3D; 1<br>False &#x3D; 0</strong></p>
<h3 id="if-判断语句"><a href="#if-判断语句" class="headerlink" title="if 判断语句"></a>if 判断语句</h3><div class="highlight-container" data-rel="Python"><figure class="iseeu highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> 要判断的条件<span class="number">1</span>:</span><br><span class="line">    条件成立要做的事<span class="number">1</span></span><br><span class="line">    条件成立要做的事<span class="number">2</span></span><br><span class="line"><span class="keyword">elif</span> 要判断的条件<span class="number">2</span>:</span><br><span class="line">    条件成立要做的事</span><br><span class="line"><span class="keyword">elif</span> 要判断的条件<span class="number">3</span>:</span><br><span class="line">    条件成立要做的事</span><br><span class="line"></span><br><span class="line">...</span><br><span class="line"></span><br><span class="line"><span class="keyword">else</span>:</span><br><span class="line">    条件不成立做的事</span><br></pre></td></tr></table></figure></div>

<p>注意冒号不能省略,<br>以及归属于if的代码块,需要四个空格缩进.      </p>
<p>可将input( )写入if中简化代码:</p>
<div class="highlight-container" data-rel="Python"><figure class="iseeu highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> <span class="built_in">int</span>(<span class="built_in">input</span>(请输入数字:))&gt;<span class="number">3</span></span><br></pre></td></tr></table></figure></div>
<p>例如将输入的数字与 3 比较判断大小.</p>
<h1 id="Python-循环语句"><a href="#Python-循环语句" class="headerlink" title="Python 循环语句"></a>Python 循环语句</h1><p>条件满足,便会去做对应的操作</p>
<h3 id="while-循环语句"><a href="#while-循环语句" class="headerlink" title="while 循环语句"></a>while 循环语句</h3><div class="highlight-container" data-rel="Python"><figure class="iseeu highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">while</span> 条件:</span><br><span class="line">    条件满足,要做的事.</span><br></pre></td></tr></table></figure></div>

<p>嵌套循环时,一定要注意空格缩进来确定关系.</p>
<h3 id="for-循环"><a href="#for-循环" class="headerlink" title="for 循环"></a>for 循环</h3><div class="highlight-container" data-rel="Python"><figure class="iseeu highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> 临时变量 <span class="keyword">in</span> 待处理数据集:</span><br><span class="line">    循环满足条件时执行的代码</span><br></pre></td></tr></table></figure></div>

<ul>
<li>for循环无法定义循环条件,只能被动取出数据处理</li>
<li>依旧注意缩进格式</li>
</ul>
<h4 id="for-循环的变量作用域"><a href="#for-循环的变量作用域" class="headerlink" title="for 循环的变量作用域"></a>for 循环的变量作用域</h4><p>临时变量, 在 <strong>编程规范</strong> 上, 作用范围(作用域), 只限定 for 循环内部.</p>
<p>如果在 for 循环外部访问的临时变量:</p>
<ul>
<li>实际上是能访问的.</li>
<li>在规范上, 是不允许的. <strong>不建议</strong> 这样做.</li>
<li>如需访问, 可以在循环外定义它.</li>
</ul>
<h3 id="range-语句"><a href="#range-语句" class="headerlink" title="range 语句"></a>range 语句</h3><p>本质上是遍历: 序列类型</p>
<ul>
<li>语法 1</li>
</ul>
<div class="highlight-container" data-rel="Python"><figure class="iseeu highlight python"><table><tr><td class="code"><pre><span class="line"><span class="built_in">range</span>(num)</span><br></pre></td></tr></table></figure></div>

<p>获取一个从 0 开始, 到 num 结束的数字序列 (<strong>不含</strong>num本身)</p>
<ul>
<li>语法 2</li>
</ul>
<div class="highlight-container" data-rel="Python"><figure class="iseeu highlight python"><table><tr><td class="code"><pre><span class="line"><span class="built_in">range</span>(num1,num2)</span><br><span class="line">```  </span><br><span class="line"></span><br><span class="line">获得一个从 num1 开始, 到 num2 结束的数字序列 (**不含**num2本身)</span><br><span class="line"></span><br><span class="line">* 语法 <span class="number">3</span></span><br><span class="line"></span><br><span class="line">```Python</span><br><span class="line"><span class="built_in">range</span>(num1,num2,step)</span><br></pre></td></tr></table></figure></div>
<p>获得一个从 num1 开始,到 num2 结束的数字序列 (<strong>不含</strong>num2本身)<br>数字之间的步长为step的值 (step默认为 1 )</p>
<h3 id="continue-和-break-关键字"><a href="#continue-和-break-关键字" class="headerlink" title="continue 和 break 关键字"></a>continue 和 break 关键字</h3><ul>
<li>临时跳过: contine</li>
</ul>
<p>中断本次循环, 直接进入下一次循环  </p>
<ul>
<li>直接结束: break</li>
</ul>
<p>直接结束循环</p>
<h1 id="Python-函数"><a href="#Python-函数" class="headerlink" title="Python 函数"></a>Python 函数</h1><p>函数是 <strong>组织好的</strong>, <strong>可重复使用的</strong>, 用来<strong>实现特定功能的</strong> 代码段.</p>
<h3 id="定义函数"><a href="#定义函数" class="headerlink" title="定义函数"></a>定义函数</h3><ul>
<li>def关键字</li>
</ul>
<div class="highlight-container" data-rel="Python"><figure class="iseeu highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">函数名</span>(<span class="params">传入参数</span>):</span><br><span class="line">    函数体</span><br><span class="line">    <span class="keyword">return</span> 返回值</span><br></pre></td></tr></table></figure></div>
<p>def关键字, 可以定义带有名称的函数.<br>传入参数和返回值不需要可以省略.</p>
<ul>
<li>lambda关键字(匿名函数)</li>
</ul>
<div class="highlight-container" data-rel="Python"><figure class="iseeu highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">func</span>(<span class="params">add</span>):</span><br><span class="line">    result = add(<span class="number">1</span>,<span class="number">2</span>)</span><br><span class="line"></span><br><span class="line">func(<span class="keyword">lambda</span> x,y:x+y)</span><br></pre></td></tr></table></figure></div>

<p>函数体只能写一行</p>
<h3 id="函数的传入参数"><a href="#函数的传入参数" class="headerlink" title="函数的传入参数"></a>函数的传入参数</h3><p>作用是能让函数接收外部传入数据</p>
<div class="highlight-container" data-rel="Python"><figure class="iseeu highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">add</span>(<span class="params">x,y</span>): <span class="comment"># 多个参数用逗号隔开</span></span><br><span class="line"></span><br><span class="line"><span class="string">&quot;&quot;&quot;</span></span><br><span class="line"><span class="string">函数说明文档</span></span><br><span class="line"><span class="string">    :param x: 形参x的说明</span></span><br><span class="line"><span class="string">    :param y: 形参y的说明</span></span><br><span class="line"><span class="string">    :return: 返回值的说明</span></span><br><span class="line"><span class="string">&quot;&quot;&quot;</span></span><br><span class="line">    result = x + y <span class="comment"># &lt;--形式参数</span></span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&quot;<span class="subst">&#123;x&#125;</span>+<span class="subst">&#123;y&#125;</span>=<span class="subst">&#123;result&#125;</span>&quot;</span>)</span><br></pre></td></tr></table></figure></div>

<p>函数定义中的参数为 <strong>形式参数</strong><br>函数调用中的参数为 <strong>实际参数</strong></p>
<p>传入的参数要与形式参数一一对应.</p>
<ul>
<li><strong>多个传入参数</strong></li>
</ul>
<p>根据使用方式的不同, 函数有4种常见的参数使用方式.</p>
<ol>
<li><p><strong>位置参数</strong>: 调用函数时根据函数定义的参数位置来传递参数.</p>
<div class="highlight-container" data-rel="Python"><figure class="iseeu highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">user_info</span>(<span class="params">name,age,gender</span>):</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&quot;名字<span class="subst">&#123;name&#125;</span>,年龄<span class="subst">&#123;age&#125;</span>,性别<span class="subst">&#123;gender&#125;</span>&quot;</span>)</span><br><span class="line"></span><br><span class="line">user_info(<span class="string">&quot;TOM&quot;</span>,<span class="number">20</span>,<span class="string">&quot;男&quot;</span>)</span><br></pre></td></tr></table></figure></div>
</li>
<li><p><strong>关键字参数</strong>: 调用时用”键&#x3D;值”形式传递参数</p>
<div class="highlight-container" data-rel="Python"><figure class="iseeu highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">user_info</span>(<span class="params">name,age,gender</span>):</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&quot;名字<span class="subst">&#123;name&#125;</span>,年龄<span class="subst">&#123;age&#125;</span>,性别<span class="subst">&#123;gender&#125;</span>&quot;</span>)</span><br><span class="line"></span><br><span class="line">user_info(gender=<span class="string">&quot;男&quot;</span>,name=<span class="string">&quot;TOM&quot;</span>,age=<span class="number">20</span>)</span><br></pre></td></tr></table></figure></div></li>
<li><p><strong>缺省参数</strong>: 也就是设置默认值</p>
<div class="highlight-container" data-rel="Python"><figure class="iseeu highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">user_info</span>(<span class="params">name,age,gender=<span class="string">&quot;男&quot;</span></span>):</span><br><span class="line">    <span class="built_in">print</span>(<span class="string">f&quot;名字<span class="subst">&#123;name&#125;</span>,年龄<span class="subst">&#123;age&#125;</span>,性别<span class="subst">&#123;gender&#125;</span>&quot;</span>)</span><br><span class="line"></span><br><span class="line">user_info(<span class="string">&quot;TOM&quot;</span>,<span class="number">20</span>)</span><br></pre></td></tr></table></figure></div>
</li>
<li><p><strong>不定长参数</strong>: 也叫可变参数, 用于不确定调用的时候会传递多少个参数.</p>
</li>
</ol>
<p>(1) 位置传递:</p>
<div class="highlight-container" data-rel="Python"><figure class="iseeu highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">user_info</span>(<span class="params">*arg</span>):</span><br><span class="line">    <span class="built_in">print</span>(args)</span><br><span class="line"></span><br><span class="line">user_info(<span class="string">&quot;男&quot;</span>,<span class="string">&quot;TOM&quot;</span>,<span class="number">20</span>)</span><br></pre></td></tr></table></figure></div>
<p>传进的参数会被args变量收集, 成为一个元组.</p>
<p>(2) 关键字传递:</p>
<div class="highlight-container" data-rel="Python"><figure class="iseeu highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">user_info</span>(<span class="params">**kwargs</span>):</span><br><span class="line">    <span class="built_in">print</span>(kwargs)</span><br><span class="line"></span><br><span class="line">user_info(gender=<span class="string">&quot;男&quot;</span>,name=<span class="string">&quot;TOM&quot;</span>,age=<span class="number">20</span>)</span><br></pre></td></tr></table></figure></div>
<p>传进的参数会被kwargs变量收集, 成为一个”键&#x3D;值”的字典.</p>
<h4 id="函数作为参数传递"><a href="#函数作为参数传递" class="headerlink" title="函数作为参数传递"></a>函数作为参数传递</h4><p>传入函数的不仅可以是数据, 也可以是计算逻辑.  </p>
<p>传入数据:</p>
<div class="highlight-container" data-rel="Python"><figure class="iseeu highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">add</span>(<span class="params">x,y</span>):</span><br><span class="line">    <span class="keyword">return</span> x + y</span><br></pre></td></tr></table></figure></div>

<p>传入一个计算逻辑(函数):</p>
<div class="highlight-container" data-rel="Python"><figure class="iseeu highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">func</span>(<span class="params">add</span>):</span><br><span class="line">    result = add(<span class="number">1</span>,<span class="number">2</span>)</span><br><span class="line">    <span class="built_in">print</span>(result)</span><br><span class="line"></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">add</span>(<span class="params">x,y</span>):</span><br><span class="line">    <span class="keyword">return</span> x + y</span><br></pre></td></tr></table></figure></div>

<h3 id="函数的返回值"><a href="#函数的返回值" class="headerlink" title="函数的返回值"></a>函数的返回值</h3><p>函数的返回值能在函数执行完毕后, 返回 <strong>给调用者</strong> 结果.</p>
<ul>
<li>注意: 函数体在遇到return后就结束了, 不会执行return后的代码.</li>
</ul>
<p>当函数没有使用return语句返回数据时, 会返回 <strong>字面量 None</strong>,   </p>
<blockquote>
<p>无返回值的函数, 返回的内容是: None<br>无返回值的函数, 返回的内容类型是: &lt;class ‘NoneType’&gt;</p>
</blockquote>
<p>表示空的, 没有意义.</p>
<p>在if判断中, None等同于False  </p>
<ul>
<li><strong>多个返回值</strong></li>
</ul>
<p>要使函数返回多个返回值, 可以用逗号分隔.</p>
<div class="highlight-container" data-rel="Python"><figure class="iseeu highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">test_return</span>():</span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span></span><br><span class="line"></span><br><span class="line">x,y,z = test_return()</span><br><span class="line"><span class="built_in">print</span>(<span class="string">f&quot;<span class="subst">&#123;x&#125;</span>,<span class="subst">&#123;y&#125;</span>,<span class="subst">&#123;z&#125;</span>&quot;</span>)</span><br></pre></td></tr></table></figure></div>

<h3 id="变量的作用域"><a href="#变量的作用域" class="headerlink" title="变量的作用域"></a>变量的作用域</h3><p>变量的作用域指的是变量的<strong>作用范围</strong>.<br>主要分为两类: <strong>局部变量</strong> 和 <strong>全局变量</strong></p>
<ol>
<li><strong>局部变量</strong></li>
</ol>
<p>定义在函数体内部的变量, 只在函数体内部生效  </p>
<p>能在函数体内部临时保存数据, 函数调用完后销毁</p>
<ol start="2">
<li><strong>全局变量</strong></li>
</ol>
<p>能在函数体内外都能生效的变量.</p>
<ol start="3">
<li><strong>global关键字</strong></li>
</ol>
<p>可以让函数内定义的局部变量转换为全局变量以修改.</p>
<h1 id="Python-数据容器"><a href="#Python-数据容器" class="headerlink" title="Python 数据容器"></a>Python 数据容器</h1><p>数据容器是能够存储多个元素的Python数据类型</p>
<p><strong>list [列表]</strong><br><strong>tuple (元组)</strong><br><strong>str “字符串”</strong><br><strong>set {集合}</strong><br><strong>dict(字典)</strong>  </p>
<p>它们各自有各自的特点</p>
<h2 id="列表-list"><a href="#列表-list" class="headerlink" title="列表(list)"></a>列表(list)</h2><div class="highlight-container" data-rel="Python"><figure class="iseeu highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 字面量</span></span><br><span class="line">[元素<span class="number">1</span>,元素<span class="number">2</span>,元素<span class="number">3</span>,元素<span class="number">4</span>,...]</span><br><span class="line"></span><br><span class="line"><span class="comment">#定义变量</span></span><br><span class="line">变量名称 = [元素<span class="number">1</span>,元素<span class="number">2</span>,元素<span class="number">3</span>,元素<span class="number">4</span>,...]</span><br><span class="line"></span><br><span class="line"><span class="comment"># 定义空列表</span></span><br><span class="line">变量名称 = []</span><br><span class="line">变量名称 = <span class="built_in">list</span>()</span><br></pre></td></tr></table></figure></div>

<p>元素的数据类型没有任何限制, 甚至元素也可以是列表, 这样就定义了嵌套列表</p>
<h3 id="列表的下标索引"><a href="#列表的下标索引" class="headerlink" title="列表的下标索引"></a>列表的下标索引</h3><p>列表的每个元素, 都有编号称之为下标索引</p>
<p>从前向后看, 下标索引从 <strong>0 开始递增.</strong><br>从后向前看, 下标索引从 <strong>-1开始递减.</strong></p>
<p><code>列表名称[ 下标 ]</code>即可使用下标索引取出元素</p>
<h3 id="列表的方法"><a href="#列表的方法" class="headerlink" title="列表的方法"></a>列表的方法</h3><p>把一个函数封装到类当中, 这个函数就会被称作为 <strong>方法</strong></p>
<ul>
<li><p>查找某元素的下标<br><code>列表. index(要查找的元素)</code></p>
</li>
<li><p>修改特定下标索引的值<br><code>列表[下标] = 要修改的值</code></p>
</li>
<li><p>插入元素<br><code>列表.insert(下标, 插入的元素)</code></p>
</li>
<li><p>追加元素(到末尾)<br><code>列表.append(要插入的元素)</code><br>也可以批量追加元素:<br><code>列表.extend(其他数据容器)</code></p>
</li>
<li><p>删除元素(匹配下标)<br><code>del列表[下标]</code><br>也可以通过pop取出元素达到删除效果:<br><code>列表.pop(下标)</code><br>pop能得到取出的元素.</p>
</li>
<li><p>删除匹配(指定)的元素<br><code>列表.remove(元素)</code><br>只能删掉第一个匹配的元素.</p>
</li>
<li><p>清空<br><code>列表.clear()</code></p>
</li>
<li><p>统计指定元素在列表内的数量<br><code>列表.count(元素)</code></p>
</li>
<li><p>统计列表内总元素个数<br><code>len(列表)</code></p>
</li>
</ul>
<h3 id="列表的特点"><a href="#列表的特点" class="headerlink" title="列表的特点"></a>列表的特点</h3><ul>
<li>可以容纳 <strong>多个, 类型不同</strong> 的元素</li>
<li>元素是 <strong>有序存储</strong> 的</li>
<li>允许 <strong>增加删除</strong> 元素, 允许 <strong>重复数据</strong> 存在.</li>
</ul>
<h3 id="列表的遍历"><a href="#列表的遍历" class="headerlink" title="列表的遍历"></a>列表的遍历</h3><ol>
<li>while 循环</li>
</ol>
<div class="highlight-container" data-rel="Python"><figure class="iseeu highlight python"><table><tr><td class="code"><pre><span class="line">index = <span class="number">0</span></span><br><span class="line"><span class="keyword">while</span> index &lt; <span class="built_in">len</span>(列表):</span><br><span class="line">    元素 = 列表[index]</span><br><span class="line">    对元素进行处理</span><br><span class="line">    index += <span class="number">1</span></span><br></pre></td></tr></table></figure></div>
<p>while循环更灵活</p>
<ol start="2">
<li>for 循环</li>
</ol>
<div class="highlight-container" data-rel="Python"><figure class="iseeu highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> 临时变量 <span class="keyword">in</span> 数据容器:</span><br><span class="line">    对临时变量进行处理</span><br></pre></td></tr></table></figure></div>
<p>for循环更简单</p>
<h2 id="元组-tuple"><a href="#元组-tuple" class="headerlink" title="元组(tuple)"></a>元组(tuple)</h2><p>定义完成后, 就不可修改. 其他特点与列表类似.</p>
<div class="highlight-container" data-rel="Python"><figure class="iseeu highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 定义元组字面量</span></span><br><span class="line">(元素,元素,...,元素,)</span><br><span class="line"><span class="comment"># 定义元组变量</span></span><br><span class="line">变量名称 = (元素,元素,...,元素,)</span><br><span class="line"><span class="comment"># 定义空元组</span></span><br><span class="line">变量名称 = () <span class="comment"># 方式1</span></span><br><span class="line">变量名称 = <span class="built_in">tuple</span>() <span class="comment">#方式2</span></span><br></pre></td></tr></table></figure></div>

<p>虽然元组元素不能修改, 但是元组嵌套中的列表能修改.</p>
<h3 id="元组的方法"><a href="#元组的方法" class="headerlink" title="元组的方法"></a>元组的方法</h3><table>
<thead>
<tr>
<th>方法</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td><code>元组.index(&quot;元素&quot;)</code></td>
<td>查找某个数据, 如果数据存在返回对应的下标, 否则报错</td>
</tr>
<tr>
<td><code>元组.count(&quot;元素&quot;)</code></td>
<td>统计某个数据在当前元组出现的次数</td>
</tr>
<tr>
<td><code>len(元组)</code></td>
<td>统计元组内的元素个数</td>
</tr>
</tbody></table>
<h2 id="字符串-str"><a href="#字符串-str" class="headerlink" title="字符串(str)"></a>字符串(str)</h2><p>是字符的容器, 一个字符串可以存放任意数量的字符.</p>
<p>字符串同元组一样, 是 <strong>无法修改</strong> 的数据容器.</p>
<h3 id="字符串的方法"><a href="#字符串的方法" class="headerlink" title="字符串的方法"></a>字符串的方法</h3><ul>
<li><p>字符串的替换<br><code>字符串.replace(字符串1, 字符串2)</code><br>能将老字符串替换成一个新的字符串</p>
</li>
<li><p>字符串分割<br><code>字符串.split(分隔符字符)</code><br>字符串本身不变, 得到一个新的列表对象</p>
</li>
<li><p>字符串规整操作<br><code>字符串.strip()</code> 去掉前后空格<br><code>字符串.strip(字符串)</code> 去掉前后指定字符串</p>
</li>
<li><p>统计字符串某个字符串的出现次数<br><code>字符串.count(&quot;字符串&quot;)</code></p>
</li>
<li><p>统计字符串长度<br><code>len(字符串)</code></p>
</li>
</ul>
<h2 id="序列"><a href="#序列" class="headerlink" title="序列"></a>序列</h2><p>内容连续, 有序, 可使用下标索引的一类数据容器.  </p>
<p>列表, 元组, 字符串, 均可视为序列.</p>
<h3 id="序列的常用操作"><a href="#序列的常用操作" class="headerlink" title="序列的常用操作"></a>序列的常用操作</h3><ul>
<li>切片<br><code>序列[起始下标:结束下标:步长]</code><br>从一个序列中, 取出一个子序列, 会得到一个新字符串<br>步长表示取元素的间隔</li>
</ul>
<p>起始下标不写 表示从头<br>结束下标不写 表示到尾<br>步长为负 表示倒序</p>
<h2 id="集合-set"><a href="#集合-set" class="headerlink" title="集合(set)"></a>集合(set)</h2><p>不支持元素重复(自带去重功能), 并且内容无序.</p>
<p>内容无序, 导致不支持下标索引访问.</p>
<div class="highlight-container" data-rel="Python"><figure class="iseeu highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 定义集合字面量</span></span><br><span class="line">&#123;元素, 元素, ... , 元素&#125;</span><br><span class="line"><span class="comment"># 定义集合变量</span></span><br><span class="line">变量名称 = &#123;元素, 元素, ... , 元素&#125;</span><br><span class="line"><span class="comment"># 定义空集合</span></span><br><span class="line">变量名称 = <span class="built_in">set</span>()</span><br></pre></td></tr></table></figure></div>

<h3 id="集合的常用操作"><a href="#集合的常用操作" class="headerlink" title="集合的常用操作"></a>集合的常用操作</h3><ul>
<li><p>添加新元素<br><code>集合.add(元素)</code><br>将指定元素, 添加到集合内.<br>修改集合本身, 添加了新元素.</p>
</li>
<li><p>移除元素<br><code>集合.remove</code><br>将指定元素, 从集合中移除.<br>集合本身被修改, 移除了元素.</p>
</li>
<li><p>从集合中随机取出元素<br><code>集合.pop</code><br>由于集合无序, 无法指定取出. 因此达到随机取出的效果.<br>集合本身被修改, 元素被移除.</p>
</li>
<li><p>清空集合<br><code>集合.clear</code></p>
</li>
<li><p>取出2个集合的差集<br><code>集合1.difference(集合2)</code><br>原集合1,2不变, 得到一个新集合.</p>
</li>
<li><p>消除2个集合的差集<br><code>集合1.difference_updata(集合2)</code><br>集合1被修改, 集合2不变.</p>
</li>
<li><p>2个集合合并<br><code>集合1.union(集合2)</code><br>原集合1,2不变, 得到一个新集合.</p>
</li>
<li><p>统计元素个数<br><code>len(集合)</code></p>
</li>
</ul>
<h3 id="集合的遍历"><a href="#集合的遍历" class="headerlink" title="集合的遍历"></a>集合的遍历</h3><p>由于无序, 因此不支持while循环.  </p>
<p>但支持for循环遍历:</p>
<div class="highlight-container" data-rel="Python"><figure class="iseeu highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> element <span class="keyword">in</span> 集合:</span><br><span class="line">    要对element的操作</span><br></pre></td></tr></table></figure></div>
<h3 id="字典"><a href="#字典" class="headerlink" title="字典"></a>字典</h3><div class="highlight-container" data-rel="Python"><figure class="iseeu highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 定义字典字面量</span></span><br><span class="line">&#123;key:value,key:value,...,key:value&#125;</span><br><span class="line"><span class="comment"># 定义字典变量</span></span><br><span class="line">my_dict = &#123;key:value,key:value,...,key:value&#125;</span><br><span class="line"><span class="comment"># 定义空字典</span></span><br><span class="line">my_dict = &#123;&#125;</span><br><span class="line">my_dict = <span class="built_in">dict</span>()</span><br></pre></td></tr></table></figure></div>

<p>可以通过Key, 取到对应的Value.<br>存储的元素 Key:value 键值对.<br>不支持下标索引.</p>
<blockquote>
<p>字典嵌套:</p>
<table>
<thead>
<tr>
<th>姓名</th>
<th>语文</th>
<th>数学</th>
</tr>
</thead>
<tbody><tr>
<td>张三</td>
<td>95</td>
<td>92</td>
</tr>
<tr>
<td>李四</td>
<td>94</td>
<td>98</td>
</tr>
</tbody></table>
<div class="highlight-container" data-rel="Python"><figure class="iseeu highlight python"><table><tr><td class="code"><pre><span class="line">  socre_dict = &#123;</span><br><span class="line">   <span class="string">&quot;张三&quot;</span>:&#123;</span><br><span class="line">          <span class="string">&quot;语文&quot;</span>:<span class="number">95</span></span><br><span class="line">          <span class="string">&quot;数学&quot;</span>:<span class="number">92</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>
</blockquote>
<h3 id="字典的常用操作"><a href="#字典的常用操作" class="headerlink" title="字典的常用操作"></a>字典的常用操作</h3><ul>
<li><p>新增&#x2F;更新元素<br><code>字典[Key] = Value</code><br>对已经存在的Key执行, 就会更新Value值.</p>
</li>
<li><p>删除元素<br><code>字典.pop(Key)</code>  </p>
</li>
<li><p>清空元素<br><code>字典.clear()</code></p>
</li>
<li><p>获取全部的key<br><code>字典.keys()</code></p>
</li>
</ul>
<p>遍历字典  </p>
<p>第一种方法:</p>
<div class="highlight-container" data-rel="Python"><figure class="iseeu highlight python"><table><tr><td class="code"><pre><span class="line">keys = my_dict.keys()</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> key <span class="keyword">in</span> keys:</span><br><span class="line">    要执行的操作</span><br></pre></td></tr></table></figure></div>

<p>第二种方法:</p>
<div class="highlight-container" data-rel="Python"><figure class="iseeu highlight python"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">for</span> key <span class="keyword">in</span> my_dict:</span><br><span class="line">    要执行的操作</span><br></pre></td></tr></table></figure></div>

<ul>
<li>统计字典内元素数量:<br><code>len(字典)</code></li>
</ul>
<h2 id="数据容器总结"><a href="#数据容器总结" class="headerlink" title="数据容器总结"></a>数据容器总结</h2><p>数据容器可以从以下视角进行简单分类:  </p>
<ul>
<li><p><strong>是否支持下标索引</strong><br>序列类型(支持): 列表, 元组, 字符串<br>非序列类型(不支持):集合, 字典</p>
</li>
<li><p><strong>是否支持重复元素</strong><br>序列类型(支持): 列表, 元组, 字符串<br>非序列类型(不支持):集合, 字典</p>
</li>
<li><p><strong>是否可以修改</strong><br>支持: 列表, 集合, 字典<br>不支持: 元组, 字符串</p>
</li>
</ul>
<table>
<thead>
<tr>
<th>属性</th>
<th>列表</th>
<th>元组</th>
<th>字符串</th>
<th>集合</th>
<th>字典</th>
</tr>
</thead>
<tbody><tr>
<td>元素类型</td>
<td>任意</td>
<td>任意</td>
<td>仅字符</td>
<td>任意</td>
<td>key:除字典外任意类型 <br> value:任意类型</td>
</tr>
<tr>
<td>下标索引</td>
<td>-</td>
<td>-</td>
<td>-</td>
<td>不支持</td>
<td>不支持</td>
</tr>
<tr>
<td>重复元素</td>
<td>-</td>
<td>-</td>
<td>-</td>
<td>不支持</td>
<td>key不支持</td>
</tr>
<tr>
<td>可修改性</td>
<td>-</td>
<td>不支持</td>
<td>不支持</td>
<td>-</td>
<td>-</td>
</tr>
<tr>
<td>数据有序</td>
<td>-</td>
<td>-</td>
<td>-</td>
<td>无序</td>
<td>无序</td>
</tr>
<tr>
<td>使用场景</td>
<td>可修改的, <br>带重复的数据记录</td>
<td>不可修改, <br>带重复的数据记录</td>
<td>一串字符的记录</td>
<td>去重的数据记录</td>
<td>以Key检索Value的记录</td>
</tr>
</tbody></table>
<h1 id="Python-文件操作"><a href="#Python-文件操作" class="headerlink" title="Python 文件操作"></a>Python 文件操作</h1><p>文件操作步骤分为: 打开, 读写, 关闭</p>
<h3 id="打开"><a href="#打开" class="headerlink" title="打开"></a>打开</h3><p><code>open(name,mode,encoding)</code><br>name: 文件的名称&#x2F;路径<br>mode: 有只读(r), 写入(w), 追加(a)等.<br>encoding: 编码格式(推荐使用UTF-8)  </p>
<h3 id="读取"><a href="#读取" class="headerlink" title="读取"></a>读取</h3><ol>
<li><p>read()方法<br><code>文件对象.read(num)</code><br>num表示要从文件中读取的数据的长度(单位是字节), 如果省略, 就会读取文件中所有的数据.</p>
</li>
<li><p>readlines()方法<br><code>文件对象.readlines()</code><br>readlines可以按照行的方式把整个文件一次性读取, 返回一个列表, 其中每一行的数据为一个元素.</p>
</li>
<li><p>readline()方法<br><code>文件对象.readline()</code><br>一次读取一行内容</p>
</li>
</ol>
<h3 id="写入"><a href="#写入" class="headerlink" title="写入"></a>写入</h3><p><code>文件对象.write(&quot;写入内容&quot;)</code></p>
<div class="highlight-container" data-rel="Python"><figure class="iseeu highlight python"><table><tr><td class="code"><pre><span class="line">f = <span class="built_in">open</span>(<span class="string">&#x27;python.txt&#x27;</span>, <span class="string">&#x27;w&#x27;</span>)</span><br><span class="line"></span><br><span class="line">f.write(hello world)</span><br><span class="line"></span><br><span class="line">f.flush()</span><br></pre></td></tr></table></figure></div>
<p>为避免频繁操作硬盘而导致效率低, 内容会先积攒到程序的内存中(缓冲区), 当调用flush时, 内容才会真正写进文件.<br>.close()方法也会自带.flush()方法  </p>
<p>w模式下写入时, 文件不存在, 会创建新文件. 文件存在, 会清空原有内容.</p>
<h3 id="追加"><a href="#追加" class="headerlink" title="追加"></a>追加</h3><p>方法同w, 有:</p>
<p>wirte(), 写入内容<br>flush(), 刷新内容到硬盘</p>
<p>a同w, 文件不存在, 会创建新文件. 不同的是:文件存在, 会在原有内容后继续写入.</p>
<h3 id="关闭"><a href="#关闭" class="headerlink" title="关闭"></a>关闭</h3><ol>
<li><p><code>文件对象.close()</code><br>解除文件占用</p>
</li>
<li><p><code>with open() as 文件对象</code><br>自动关闭文件</p>
</li>
</ol>
<h1 id="Python-异常"><a href="#Python-异常" class="headerlink" title="Python 异常"></a>Python 异常</h1><h3 id="异常处理-捕获异常"><a href="#异常处理-捕获异常" class="headerlink" title="异常处理&#x2F;捕获异常"></a>异常处理&#x2F;捕获异常</h3><p>基本语法:</p>
<div class="highlight-container" data-rel="Python"><figure class="iseeu highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">try</span>:</span><br><span class="line">    可能发生错误的代码</span><br><span class="line"></span><br><span class="line"><span class="keyword">except</span>:</span><br><span class="line">    如果出现异常要执行的代码</span><br><span class="line"></span><br><span class="line"><span class="comment"># [ ]中括号代表代码可选</span></span><br><span class="line">[<span class="keyword">else</span>:]</span><br><span class="line">    没有异常时执行的代码</span><br><span class="line"></span><br><span class="line">[<span class="keyword">finally</span>:]</span><br><span class="line">    无论是否异常, 都会执行的代码</span><br></pre></td></tr></table></figure></div>

<p>若要捕获指定的异常:</p>
<div class="highlight-container" data-rel="Python"><figure class="iseeu highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">except</span> 错误类型 <span class="keyword">as</span> e(别名):</span><br><span class="line">    出现异常要执行的操作</span><br></pre></td></tr></table></figure></div>

<p>若要捕获多个异常, 可以通过元组书写:</p>
<div class="highlight-container" data-rel="Python"><figure class="iseeu highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">except</span> (错误类型<span class="number">1</span>,错误类型<span class="number">2</span>) <span class="keyword">as</span> e:</span><br><span class="line">    出现异常要执行的操作</span><br></pre></td></tr></table></figure></div>

<p>捕获所有异常:</p>
<div class="highlight-container" data-rel="Python"><figure class="iseeu highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">except</span> Exception <span class="keyword">as</span> e:</span><br><span class="line">    出现异常要执行的操作</span><br></pre></td></tr></table></figure></div>

<p>异常具有传递性.</p>
<h1 id="Python-模块"><a href="#Python-模块" class="headerlink" title="Python 模块"></a>Python 模块</h1><p>是一个Python文件(以.py结尾), 里面有类, 函数, 变量等. 我们可以导入模块使用.</p>
<p>导入方式:</p>
<div class="highlight-container" data-rel="Python"><figure class="iseeu highlight python"><table><tr><td class="code"><pre><span class="line">[<span class="keyword">from</span> 模块名] <span class="keyword">import</span> [模块|类|变量|函数|*][<span class="keyword">as</span> 别名]</span><br><span class="line"></span><br><span class="line"><span class="comment">#import会导入整个模块, 通过 . 能使用其中所有功能</span></span><br><span class="line"><span class="keyword">import</span> 模块名<span class="number">1</span>, 模块名<span class="number">2</span></span><br><span class="line">模块名.功能名()</span><br><span class="line"></span><br><span class="line"><span class="comment">#form import 能具体导入某个功能</span></span><br><span class="line"><span class="keyword">from</span> 模块名 <span class="keyword">import</span> 功能名</span><br><span class="line">功能名()</span><br><span class="line"></span><br><span class="line"><span class="comment">#使用 * 导入模块的全部功能, 只是写法不同而已. </span></span><br><span class="line">form 模块名 <span class="keyword">import</span> *</span><br><span class="line">功能名()</span><br></pre></td></tr></table></figure></div>
<p>如果使用这种方式导入, 模块文件含有<code>__all__</code>变量, 就只会导入列表all中记录的元素:</p>
<div class="highlight-container" data-rel="Python"><figure class="iseeu highlight python"><table><tr><td class="code"><pre><span class="line">__all__ = [函数<span class="number">1</span>,函数<span class="number">2</span>]</span><br><span class="line"></span><br><span class="line">函数<span class="number">1</span></span><br><span class="line"></span><br><span class="line">函数<span class="number">2</span></span><br><span class="line"></span><br><span class="line">函数<span class="number">3</span></span><br><span class="line"><span class="comment"># 只会导入 1 和 2</span></span><br></pre></td></tr></table></figure></div>
<h3 id="自定义模块"><a href="#自定义模块" class="headerlink" title="自定义模块"></a>自定义模块</h3><p>新建一个<code>my_module1.py</code>的文件,内容如下:</p>
<div class="highlight-container" data-rel="Python"><figure class="iseeu highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">test</span>(<span class="params">a, b</span>):</span><br><span class="line">    <span class="built_in">print</span>(a + b)</span><br></pre></td></tr></table></figure></div>

<p>在另一个文件中,使用这个Python模块:</p>
<div class="highlight-container" data-rel="Python"><figure class="iseeu highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> my_module1</span><br><span class="line"></span><br><span class="line">my_module1.test(<span class="number">10</span>, <span class="number">20</span>)</span><br></pre></td></tr></table></figure></div>

<p>当导入两个同名的模块时, 后调用的会覆盖先调用的.</p>
<p>如果想让模块调用时不执行, 而在测试时执行, 可以添加:</p>
<div class="highlight-container" data-rel="Python"><figure class="iseeu highlight python"><table><tr><td class="code"><pre><span class="line">导入模块要执行的代码</span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">&#x27;__main__&#x27;</span>:</span><br><span class="line">    导入模块不会执行的代码</span><br></pre></td></tr></table></figure></div>

<h3 id="Python-包"><a href="#Python-包" class="headerlink" title="Python 包"></a>Python 包</h3><p>本质是一个包含 <code>__init__.py</code> 文件的文件夹, 能存放模块.</p>
<p>没有 <code>__init__.py</code> ,就不是包.</p>
<p>在Python程序的生态中, 有许多第三方包,例如: </p>
<ul>
<li>科学计算的 numpu 包</li>
<li>数据分析的 pandas 包</li>
<li>大数据计算的 pyspark apache-flink 包</li>
<li>图形可视化的 matplotlib pyrcharts</li>
<li>人工智能的 tensorflow</li>
<li>等等</li>
</ul>
<h4 id="安装第三方包"><a href="#安装第三方包" class="headerlink" title="安装第三方包"></a>安装第三方包</h4><p>使用Python内置的pip程序, 在命令提示符中输入:</p>
<div class="highlight-container" data-rel="Cmd"><figure class="iseeu highlight cmd"><table><tr><td class="code"><pre><span class="line">pip install 包名称</span><br></pre></td></tr></table></figure></div>
<p>即可通过网络安装包.</p>
<h4 id="优化下载的方法"><a href="#优化下载的方法" class="headerlink" title="优化下载的方法"></a>优化下载的方法</h4><p>让其链接国内的网站进行下载</p>
<div class="highlight-container" data-rel="Cmd"><figure class="iseeu highlight cmd"><table><tr><td class="code"><pre><span class="line">pip instal -i https://pypi.tuna.tsinghua.edu.cn/simple 包名称</span><br></pre></td></tr></table></figure></div>

<p><a class="link"   href="https://pypi.tuna.tsinghua.edu.cn/simple" >https://pypi.tuna.tsinghua.edu.cn/simple <i class="fa-regular fa-arrow-up-right-from-square fa-sm"></i></a> 是清华大学提供的一个网站, 可供pip程序下载第三方包.</p>
<h1 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h1><blockquote>
<p>wow! 基础学完力!</p>
</blockquote>
]]></content>
      <tags>
        <tag>Python</tag>
      </tags>
  </entry>
  <entry>
    <title>Python进阶</title>
    <url>/2023/10/29/Python%E8%BF%9B%E9%98%B6/</url>
    <content><![CDATA[<h1 id="面向对象"><a href="#面向对象" class="headerlink" title="面向对象"></a>面向对象</h1><h3 id="数据的组织"><a href="#数据的组织" class="headerlink" title="数据的组织"></a>数据的组织</h3><p>生活中, 可以设计一个表格, 将数据整齐地统计出来, 在程序中也有相同的思路.</p>
<ol>
<li>在程序中设计表格, 称之为: <strong>设计类(class)</strong></li>
<li>在程序中打印生产表格, 称之为: <strong>创建对象</strong></li>
<li>在程序中填写表格, 称之为: <strong>对象属性赋值</strong></li>
</ol>
<h3 id="类的定义和使用"><a href="#类的定义和使用" class="headerlink" title="类的定义和使用"></a>类的定义和使用</h3><div class="highlight-container" data-rel="Python"><figure class="iseeu highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># classs是关键字, 表示要定义类了</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">类名称</span>:</span><br><span class="line">    <span class="comment"># 类的属性: 定义在类中的变量(成员变量)</span></span><br><span class="line">    name = <span class="literal">None</span> </span><br><span class="line"></span><br><span class="line">    <span class="comment"># 类的行为: 定义在类中的函数(成员方法)</span></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">say</span>(<span class="params">self</span>):</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">&quot;hello world!&quot;</span>)</span><br></pre></td></tr></table></figure></div>

<p>self关键字是成员方法定义时, 必须填写.</p>
]]></content>
      <tags>
        <tag>Python</tag>
      </tags>
  </entry>
  <entry>
    <title>hello websites</title>
    <url>/2023/09/17/hello-websites/</url>
    <content><![CDATA[<p>这是一篇hexo网页部署,以及Redefine主题配置指南</p>
<h1 id="快速开始"><a href="#快速开始" class="headerlink" title="快速开始"></a>快速开始</h1><h2 id="Redefine相关"><a href="#Redefine相关" class="headerlink" title="Redefine相关"></a>Redefine相关</h2><p><a class="link"   href="https://redefine-docs.ohevan.com/" >Redefine主题配置指南 <i class="fa-regular fa-arrow-up-right-from-square fa-sm"></i></a><br>不得不说,这个主题真的巨好看又很丰富的自定义,非常不错!</p>
<p>此外,主题作者还加入了一些方便的模块让笔记更好看:</p>
<div class="note-large default"><div class="notel-title"><p>文章属性自定义</p>
</div><div class="notel-content"><p><strong>文章时效性</strong><br>expires: 2023-08-31 23:59:59<br><strong>置顶文章:</strong><br>sticky: 值 (值越大,顶置文章越靠前)<br><strong>首页摘要:</strong><br>excerpt: “这是文章摘要”<br><strong>文章缩略图:</strong><br>thumbnail: “图片链接”<br><strong>文章头图:</strong><br>banner: “图片链接”<br>cover: “图片链接”</p>
</div></div>


<h2 id="hexo-快速指令"><a href="#hexo-快速指令" class="headerlink" title="hexo 快速指令"></a>hexo 快速指令</h2><ol>
<li>创建一篇文章</li>
</ol>
<div class="highlight-container" data-rel="Bash"><figure class="iseeu highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo new <span class="string">&quot;My New Post&quot;</span></span><br></pre></td></tr></table></figure></div>

<ol start="2">
<li>运行网页</li>
</ol>
<div class="highlight-container" data-rel="Bash"><figure class="iseeu highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo server</span><br><span class="line"><span class="comment"># 可以简写为</span></span><br><span class="line">$ hexo s</span><br></pre></td></tr></table></figure></div>

<ol start="3">
<li>修改网页内容后重新生成网页</li>
</ol>
<div class="highlight-container" data-rel="Bash"><figure class="iseeu highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo g</span><br></pre></td></tr></table></figure></div>

<ol start="4">
<li>部署到github的仓库</li>
</ol>
<div class="highlight-container" data-rel="Bash"><figure class="iseeu highlight bash"><table><tr><td class="code"><pre><span class="line">$ hexo d</span><br></pre></td></tr></table></figure></div>
<p> <a class="link"   href="https://hexo.io/" >关于hexo的更多信息 <i class="fa-regular fa-arrow-up-right-from-square fa-sm"></i></a><br> <a class="link"   href="https://moren5483.github.io/2022/07/17/github/" >关于hexo+github仓库搭建博客 <i class="fa-regular fa-arrow-up-right-from-square fa-sm"></i></a></p>
<blockquote>
<p>希望能在这里留下一些有意思的东西!</p>
</blockquote>
]]></content>
      <tags>
        <tag>指南</tag>
      </tags>
  </entry>
  <entry>
    <title>数据结构与算法(c语言)</title>
    <url>/2023/09/17/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E4%B8%8E%E5%9F%BA%E7%A1%80-c%E8%AF%AD%E8%A8%80/</url>
    <content><![CDATA[<p>上课老师真的好无聊,还是上b站自学吧,听说这门课还挺难学习的?<br>笔记源自于这套课程:<br><a class="link"   href="https://www.bilibili.com/video/BV1W64y1z7jh/?p=2&spm_id_from=pageDriver&vd_source=b19220b8f372f3a81aa8463fde694a63" >C语言描述《数据结构和算法》 <i class="fa-regular fa-arrow-up-right-from-square fa-sm"></i></a></p>
<h1 id="第零章-C语言基础"><a href="#第零章-C语言基础" class="headerlink" title="第零章 C语言基础"></a>第零章 C语言基础</h1><blockquote>
<p>补一补c语言有关指针和结构体的基础知识…<br>视频来源: <a class="link"   href="https://www.bilibili.com/video/BV1kV4y1C7z6/?spm_id_from=333.337.search-card.all.click&vd_source=b19220b8f372f3a81aa8463fde694a63" >数据结构所需要的c语言基础 <i class="fa-regular fa-arrow-up-right-from-square fa-sm"></i></a></p>
</blockquote>
<h3 id="指针"><a href="#指针" class="headerlink" title="指针"></a>指针</h3><p>类似于 定义int能存放整数, 定义char能存放字符;<br>指针能存放地址. </p>
<p>地址 可以类比理解成 寝室号.<br>“ * “ 可以类比为 寝室钥匙.</p>
<p>例如: “ *a “ 可以访问到a的值.</p>
<div class="highlight-container" data-rel="C"><figure class="iseeu highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> x = <span class="number">12</span>;<span class="comment">//定义一个整型变量</span></span><br><span class="line"><span class="type">int</span>* p;<span class="comment">//定义一个int类型的指针变量</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span>* p = &amp;x;<span class="comment">//将x的地址值赋给指针变量</span></span><br></pre></td></tr></table></figure></div>

<p>知道地址后如何访问地址存的变量值?<br>有以下两种方法:</p>
<ol>
<li>“ * “ + 地址</li>
</ol>
<div class="highlight-container" data-rel="C"><figure class="iseeu highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> y = <span class="number">7</span>;</span><br><span class="line"><span class="type">int</span>* q = &amp;y;</span><br><span class="line"></span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;%d&quot;</span>,*q);<span class="comment">//通过地址访问y的值,然后打印y的值</span></span><br></pre></td></tr></table></figure></div>

<ol start="2">
<li>地址 + [  ]</li>
</ol>
<div class="highlight-container" data-rel="C"><figure class="iseeu highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> a[<span class="number">3</span>] = &#123;<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>&#125;;</span><br><span class="line"><span class="type">int</span>* p = a;<span class="comment">//将数组a第一个数的地址值给指针p</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;%d&quot;</span>, p[<span class="number">0</span>])<span class="comment">//通过地址值访问数组a[2]的值</span></span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;%d&quot;</span>, p[<span class="number">2</span>]);<span class="comment">//访问数组a[2]的值</span></span><br></pre></td></tr></table></figure></div>

<ul>
<li>也可以试试第一种方法:</li>
</ul>
<div class="highlight-container" data-rel="C"><figure class="iseeu highlight c"><table><tr><td class="code"><pre><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;%d&quot;</span>, *p);</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;%d&quot;</span>, *(p+<span class="number">2</span>));</span><br></pre></td></tr></table></figure></div>

<ul>
<li>还有这几种方式,也能输出相同的结果:</li>
</ul>
<div class="highlight-container" data-rel="C"><figure class="iseeu highlight c"><table><tr><td class="code"><pre><span class="line">print(<span class="string">&quot;%d&quot;</span>, a[<span class="number">0</span>]);<span class="comment">//知道a的地址(首地址),偏移[0]个位置的值.</span></span><br><span class="line">print(<span class="string">&quot;%d&quot;</span>, a[<span class="number">2</span>]);<span class="comment">//知道a的地址(首地址),偏移[2]个位置的值.</span></span><br><span class="line">print(<span class="string">&quot;%d&quot;</span>, *a);<span class="comment">//知道a的地址(首地址),访问a的值.</span></span><br><span class="line">print(<span class="string">&quot;%d&quot;</span>, *(a+<span class="number">2</span>));<span class="comment">//知道a的地址(首地址),访问(a+2)的值.</span></span><br></pre></td></tr></table></figure></div>

<ul>
<li>同理的<div class="highlight-container" data-rel="C"><figure class="iseeu highlight c"><table><tr><td class="code"><pre><span class="line">print(<span class="string">&quot;%d&quot;</span>,(a+<span class="number">1</span>)[<span class="number">0</span>]);<span class="comment">//知道首地址(a+1),偏移[0]个位置的值.</span></span><br></pre></td></tr></table></figure></div></li>
</ul>
<p>即a[1]的值</p>
<h3 id="结构体"><a href="#结构体" class="headerlink" title="结构体"></a>结构体</h3><p>例如管理学生信息. </p>
<p>若需求存储十个学生的: 姓名 学号 分数.<br>如果没有使用结构体:</p>
<div class="highlight-container" data-rel="C"><figure class="iseeu highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">char</span> name[<span class="number">10</span>];</span><br><span class="line"><span class="type">char</span> score[<span class="number">10</span>];</span><br><span class="line"><span class="type">float</span> sno[<span class="number">10</span>];</span><br></pre></td></tr></table></figure></div>

<p>不能以学生为单位查看,较为凌乱.</p>
<p>试一试结构体定义:</p>
<ul>
<li>定义方式一</li>
</ul>
<div class="highlight-container" data-rel="C"><figure class="iseeu highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">student</span>&#123;</span></span><br><span class="line">    <span class="type">char</span> son[<span class="number">20</span>];</span><br><span class="line">    <span class="type">char</span> name[<span class="number">20</span>];</span><br><span class="line">    <span class="type">float</span> score[<span class="number">3</span>];</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">//若要访问第一个学生的学号:</span></span><br><span class="line">s[<span class="number">0</span>].son;</span><br><span class="line"><span class="comment">//同理,要访问第四个学生的成绩:</span></span><br><span class="line">s[<span class="number">4</span>].score;</span><br></pre></td></tr></table></figure></div>


<ul>
<li>定义方式二(更常用)</li>
</ul>
<blockquote>
<p>typedef 约等于重命名<br>例如: typedef int a, 就是把int重新改名为a.<br>即: int x; 等价于 a x;  </p>
</blockquote>
<div class="highlight-container" data-rel="C"><figure class="iseeu highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">student</span>&#123;</span></span><br><span class="line">    <span class="type">char</span> son[<span class="number">20</span>];</span><br><span class="line">    <span class="type">char</span> name[<span class="number">20</span>];</span><br><span class="line">    <span class="type">float</span> score[<span class="number">3</span>];</span><br><span class="line">&#125; stu;</span><br></pre></td></tr></table></figure></div>
<blockquote>
<p>同理,此处是将 定义一中的 “struct student{…}” 重命名成 “stu”.</p>
</blockquote>
<p>尝试理解以下结构体定义:</p>
<div class="highlight-container" data-rel="C"><figure class="iseeu highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">LNode</span>&#123;</span></span><br><span class="line">    <span class="type">int</span> data;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">LNode</span>* <span class="title">next</span>;</span></span><br><span class="line">&#125; LNode,* Linklist;</span><br><span class="line"></span><br></pre></td></tr></table></figure></div>
<p>逗号相当于分隔符,类似于: int a,b; 是int a; int b;两条语句.</p>
<p>因此可以这样拆分出两条语句:</p>
<blockquote>
<p>typedef struct LNode{…} LNode:<br>将”struct LNode{…}”重命名为 LNode.</p>
</blockquote>
<blockquote>
<p>typedef struct LNode* linklist:<br>将”struct LNode”重命名为 * linklist.</p>
</blockquote>
<p>不过用逗号像这样链接容易造成误解, 大家一般不这样写去为难自己.</p>
<div class="highlight-container" data-rel="C"><figure class="iseeu highlight c"><table><tr><td class="code"><pre><span class="line">Lnode s;<span class="comment">//创造一个LNode类型的空间叫做s.</span></span><br><span class="line"></span><br><span class="line">Linklist p = &amp;s;<span class="comment">//取这个s空间的地址给p空间存放.</span></span><br></pre></td></tr></table></figure></div>

<div class="highlight-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">graph LR;</span><br><span class="line"></span><br><span class="line">A1[data1]</span><br><span class="line">B1[next1]</span><br><span class="line">A2[data2]</span><br><span class="line">B2[next2]</span><br><span class="line"></span><br><span class="line">subgraph LNode1: s</span><br><span class="line">A1---B1</span><br><span class="line">end</span><br><span class="line"></span><br><span class="line">subgraph LNode2</span><br><span class="line">B1--指针--&gt;A2---B2</span><br><span class="line">end</span><br><span class="line"></span><br></pre></td></tr></table></figure></div>

<div class="note-large default"><div class="notel-title"><p>的异同</p>
</div><div class="notel-content"><ul>
<li>#define 是一个预处理器指令，它在编译之前就对代码进行了文本替换;</li>
</ul>
<p>typedef 是一个关键字，它在编译时创建了一个新的类型别名。</p>
<ul>
<li>#define 可以用于定义常量、变量、函数或者代码片段的宏。</li>
</ul>
<p>typedef 可以用于定义结构体、枚举、联合或者指针等复杂的类型的别名。</p>
<ul>
<li>#define 的作用域是从定义处到文件末尾，除非用 #undef 取消。</li>
</ul>
<p>typedef 的作用域是从定义处到所在的代码块末尾。</p>
</div></div>



<h3 id="结构体指针运算符"><a href="#结构体指针运算符" class="headerlink" title="结构体指针运算符"></a>结构体指针运算符</h3><p><code>-&gt;</code> 是一个结构体指针运算符，它用于通过结构体指针访问结构体的成员。<br>它的一般形式是：</p>
<blockquote>
<p><strong>结构体指针-&gt;成员名</strong></p>
</blockquote>
<p>例如，如果 L 是一个指向 SqList 类型的结构体的指针，那么 L-&gt;length 就表示访问该结构体的 length 成员，也就是线性表的当前长度。<br>可以把 -&gt; 理解为先解引用指针，再用 . 访问成员，即：</p>
<p>L-&gt;length 等价于 (*L).length</p>
<h1 id="第一章-绪论"><a href="#第一章-绪论" class="headerlink" title="第一章 绪论"></a>第一章 绪论</h1><blockquote>
<p>不得不说我们老师讲的是真不行,说的话总是有很多完全没必要的句子,得不到她要说的重点</p>
</blockquote>
<h3 id="基本概念和术语"><a href="#基本概念和术语" class="headerlink" title="基本概念和术语"></a>基本概念和术语</h3><ol>
<li><p>数据: 所有能输入到计算机中去能用来描述客观事物的符号</p>
</li>
<li><p>数据元素: 数据的基本单位, 也称为 <strong>结点</strong> 或 <strong>记录</strong></p>
</li>
<li><p>数据项:  有独立含义的 <strong>数据最小单位</strong>, 也称为 <strong>域</strong></p>
</li>
<li><p>数据对象: 是性质相同的数据元素的集合, 是数据的一个子级</p>
</li>
<li><p>数据结构: 是相互之间存在一种或多种特定关系的数据元素的集合</p>
</li>
</ol>
<h2 id="什么是数据结构"><a href="#什么是数据结构" class="headerlink" title="什么是数据结构"></a>什么是数据结构</h2><blockquote>
<p><strong>程序设计 &#x3D; 数据结构 + 算法</strong>  </p>
</blockquote>
<p>就是数据与元素的<strong>关系集合</strong></p>
<hr>
<p>传统上,数据结构分为<strong>逻辑结构</strong>和<strong>存储(物理)结构</strong>。   </p>
<h3 id="逻辑结构"><a href="#逻辑结构" class="headerlink" title="逻辑结构"></a>逻辑结构</h3><p>是数据元素间抽象化的相互关系, 与数据的存储无关, 独立于计算机.    </p>
<p>逻辑结构又分为:  </p>
<ol>
<li>线性结构</li>
</ol>
<p>有且仅有一个开始节点和一个终端节点. 且所有节点最多只有一个直接前驱和一个后继.  </p>
<p>例如: 线性表, 栈, 队列</p>
<ol start="2">
<li>非线性结构</li>
</ol>
<p>一个节点可能有多个直接前驱和直接后继.</p>
<p>例如: 树, 图</p>
<h3 id="存储-物理-结构"><a href="#存储-物理-结构" class="headerlink" title="存储(物理)结构"></a>存储(物理)结构</h3><p>是数据元素及其关系在计算机存储器中的存储方式</p>
<p>物理结构又分为:</p>
<ol>
<li>顺序存储</li>
</ol>
<p>借助元素在存储器中的 <strong>相对位置</strong> 来表示数据元素间的逻辑关系.</p>
<p>例如: 顺序表</p>
<ol start="2">
<li>链式存储</li>
</ol>
<p>借助指示元素存储地址的 <strong>指针</strong> 表示数据元素间的逻辑关系.</p>
<p>例如: 链表</p>
<h2 id="什么是算法"><a href="#什么是算法" class="headerlink" title="什么是算法"></a>什么是算法</h2><p>顾名思义,就是计算的技巧和方式。<br>对于给定的问题,是可以由多个算法来解决。  </p>
<h2 id="算法的基本特征"><a href="#算法的基本特征" class="headerlink" title="算法的基本特征"></a>算法的基本特征</h2><p>算法的基本特征要：<br><strong>输入、输出、有穷性、确定性和可行性</strong>  </p>
<p>算法设计的要求有：<br><strong>正确性、可读性、健壮性、时间效率高和存储量低</strong></p>
<h3 id="时间复杂度"><a href="#时间复杂度" class="headerlink" title="时间复杂度"></a>时间复杂度</h3><p>语句频度: 一条语句的重复执行次数.</p>
<p>算法的执行时间 &#x3D;&#x3D; 该算法中所有语句的频度之和.</p>
<blockquote>
<p>$T(n) &#x3D; O(f(n))$ </p>
</blockquote>
<p>例如:</p>
<div class="highlight-container" data-rel="C"><figure class="iseeu highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span>( i=<span class="number">0</span>; i&lt;n; i++)</span><br><span class="line">    <span class="keyword">for</span>( j=<span class="number">0</span>; j&lt;n; j++)</span><br><span class="line">        c[i][j]=a[i][j]+b[i][j]</span><br></pre></td></tr></table></figure></div>

<p>是for循环的嵌套, 每个for循环的语句频度为n.<br>因此, 整体为n*n</p>
<p>即:   $T(n) &#x3D; O(n^2)$ </p>
<h3 id="空间复杂度"><a href="#空间复杂度" class="headerlink" title="空间复杂度"></a>空间复杂度</h3><p>算法所需存储空间的度量, 记作</p>
<blockquote>
<p>$S(n) &#x3D; O(f(n))$</p>
</blockquote>
<p>通常,求”复杂度”是指时间复杂度</p>
<h1 id="第二章-线性表"><a href="#第二章-线性表" class="headerlink" title="第二章 线性表"></a>第二章 线性表</h1><h2 id="线性表的定义"><a href="#线性表的定义" class="headerlink" title="线性表的定义"></a>线性表的定义</h2><p>类型相同的数据元素构成的有限序列</p>
<p>序列的每一个元素都有唯一的前驱和后继, 除了开头和结尾两个节点 (线性结构的特点)</p>
<h3 id="线性表的顺序存储"><a href="#线性表的顺序存储" class="headerlink" title="线性表的顺序存储"></a>线性表的顺序存储</h3><p>分配一块 <strong>地址连续的</strong> 存储单元依次去存放这些元素, 将其称之为<strong>顺序标</strong></p>
<p>例如数组.</p>
<p><strong>基本操作:</strong></p>
<ul>
<li>定义线性表的存储结构</li>
</ul>
<div class="highlight-container" data-rel="C"><figure class="iseeu highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> MAXSIZE 100 <span class="comment">//线性表的最大长度</span></span></span><br><span class="line"><span class="keyword">typedef</span> <span class="type">int</span> ElemType; <span class="comment">//假设线性表的元素类型为整型</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> &#123;</span></span><br><span class="line">    ElemType data[MAXSIZE]; <span class="comment">//用数组存储元素</span></span><br><span class="line">    <span class="type">int</span> length; <span class="comment">//线性表的当前长度</span></span><br><span class="line">&#125; SqList; <span class="comment">//顺序表类型</span></span><br></pre></td></tr></table></figure></div>

<ul>
<li>**InitList(*L)**: 初始化操作,建立一个空的线性表L.</li>
</ul>
<div class="highlight-container" data-rel="C"><figure class="iseeu highlight c"><table><tr><td class="code"><pre><span class="line">Status <span class="title function_">InitList</span><span class="params">(SqList *L)</span> &#123;</span><br><span class="line">    L-&gt;length = <span class="number">0</span>; <span class="comment">//将线性表的长度设为0</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; MAXSIZE; i++) &#123;</span><br><span class="line">        L-&gt;data[i] = <span class="number">0</span>; <span class="comment">//将所有元素设为0</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>
<p>初始化操作中，把线性表长度设为0是为了表示线性表是空的，没有任何元素。  </p>
<p>但是，这并不意味着线性表的 <strong>存储空间</strong> 是空的，因为我们用数组来存储元素，数组是一种静态的数据结构，它在定义时就分配了固定的内存空间。  </p>
<p>因此，即使线性表长度为0，数组中仍然可能有一些原来的数据或者垃圾数据，这些数据并不属于线性表，但是占用了线性表的存储空间。</p>
<p>所以将长度设定为0后依旧还得将线性表的存储空间，即所有元素设置为0。</p>
<ul>
<li><strong>ListEmpty(L)</strong>: 判断线性表是否为空表,若为空返回true.</li>
</ul>
<div class="highlight-container" data-rel="C"><figure class="iseeu highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">bool</span> <span class="title function_">ListEmpty</span><span class="params">(SqList L)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (L.length == <span class="number">0</span>) &#123; <span class="comment">//如果线性表的长度为0,则为空表</span></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<ul>
<li>**ClearList(*L)**: 清空线性表L.</li>
</ul>
<div class="highlight-container" data-rel="C"><figure class="iseeu highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">ClearList</span><span class="params">(SqList *L)</span> &#123;</span><br><span class="line">    L-&gt;length = <span class="number">0</span>; <span class="comment">//将线性表的长度设为0</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; MAXSIZE; i++) &#123;</span><br><span class="line">        L-&gt;data[i] = <span class="number">0</span>; <span class="comment">//将所有元素设为0</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>与初始化基本类似</p>
<ul>
<li>GetElem(L,i,*e): 将线性表L中的第i个位置元素值返回给e.</li>
</ul>
<div class="highlight-container" data-rel="C"><figure class="iseeu highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">bool</span> <span class="title function_">GetElem</span><span class="params">(SqList L, <span class="type">int</span> i, ElemType *e)</span> &#123;</span><br><span class="line">    <span class="comment">//判断i是否合法,即1&lt;=i&lt;=L.length</span></span><br><span class="line">    <span class="keyword">if</span> (i &lt; <span class="number">1</span> || i &gt; L.length) &#123; </span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>; <span class="comment">//如果不合法,则返回false</span></span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        *e = L.data[i<span class="number">-1</span>]; <span class="comment">//如果合法,则将第i个元素的值赋给e</span></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>; <span class="comment">//并返回true</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<ul>
<li>LocateElem(L,e): 在线性表L中查找和e相等的元素,若成功返回表中序号,否则返回0.</li>
</ul>
<div class="highlight-container" data-rel="C"><figure class="iseeu highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">LocateElem</span><span class="params">(SqList L, ElemType e)</span> &#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; L.length; i++) &#123; <span class="comment">//遍历线性表中的所有元素</span></span><br><span class="line">        <span class="keyword">if</span> (L.data[i] == e) &#123; <span class="comment">//如果找到和e相等的元素</span></span><br><span class="line">            <span class="keyword">return</span> i+<span class="number">1</span>; <span class="comment">//则返回其在表中的序号(从1开始)</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>; <span class="comment">//如果没有找到,则返回0</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<ul>
<li>ListInsert(*L,i,e): 在线性表L中第i个位置插入新元素e.</li>
</ul>
<div class="highlight-container" data-rel="C"><figure class="iseeu highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">bool</span> <span class="title function_">ListInsert</span><span class="params">(SqList *L, <span class="type">int</span> i, ElemType e)</span> &#123;</span><br><span class="line">    <span class="comment">//判断i是否合法,即1&lt;=i&lt;=L.length+1</span></span><br><span class="line">    <span class="keyword">if</span> (i &lt; <span class="number">1</span> || i &gt; L-&gt;length + <span class="number">1</span>) &#123; </span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>; <span class="comment">//如果不合法,则返回false</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//判断线性表是否已满,即L.length==MAXSIZE</span></span><br><span class="line">    <span class="keyword">if</span> (L-&gt;length == MAXSIZE) &#123; </span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>; <span class="comment">//如果已满,则返回false</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//从最后一个元素开始向前遍历到第i个元素,将每个元素后移一位</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> j = L-&gt;length - <span class="number">1</span>; j &gt;= i - <span class="number">1</span>; j--) &#123; </span><br><span class="line">        L-&gt;data[j+<span class="number">1</span>] = L-&gt;data[j];</span><br><span class="line">    &#125;</span><br><span class="line">    L-&gt;data[i<span class="number">-1</span>] = e; <span class="comment">//将新元素e插入到第i个位置上</span></span><br><span class="line">    L-&gt;length++; <span class="comment">//将线性表的长度加1</span></span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>; <span class="comment">//返回true表示插入成功</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<ul>
<li>ListDelete(*L,i,*e): 删除线性表L中第i个位置元素,并用e返回其值.</li>
</ul>
<div class="highlight-container" data-rel="C"><figure class="iseeu highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">bool</span> <span class="title function_">ListDelete</span><span class="params">(SqList *L, <span class="type">int</span> i, ElemType *e)</span> &#123;</span><br><span class="line">    <span class="comment">//判断i是否合法,即1&lt;=i&lt;=L.length</span></span><br><span class="line">    <span class="keyword">if</span> (i &lt; <span class="number">1</span> || i &gt; L-&gt;length) &#123; </span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>; <span class="comment">//如果不合法,则返回false</span></span><br><span class="line">    &#125;</span><br><span class="line">    *e = L-&gt;data[i<span class="number">-1</span>]; <span class="comment">//将第i个元素的值赋给e</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> j = i; j &lt; L-&gt;length; j++) &#123; <span class="comment">//从第i+1个元素开始向后遍历到最后一个元素,将每个元素前移一位</span></span><br><span class="line">        L-&gt;data[j<span class="number">-1</span>] = L-&gt;data[j];</span><br><span class="line">    &#125;</span><br><span class="line">    L-&gt;length--; <span class="comment">//将线性表的长度减1</span></span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>; <span class="comment">//返回true表示删除成功</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<ul>
<li>ListLength(L): 返回线性表L的元素个数.</li>
</ul>
<div class="highlight-container" data-rel="C"><figure class="iseeu highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">ListLength</span><span class="params">(SqList L)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> L.length; <span class="comment">//直接返回线性表的长度属性即可</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<h3 id="线性表的链式存储"><a href="#线性表的链式存储" class="headerlink" title="线性表的链式存储"></a>线性表的链式存储</h3><p>链式存储结构除了存储其本身的信息外,还存储一个指示其直接后继的存储位置信息.  </p>
<ul>
<li><strong>数据域</strong> :存储的元素信息</li>
<li><strong>指针域</strong> :存储直接后驱的位置信息</li>
<li><strong>存储映像</strong>:两部分的统称,也称作节点(Node)</li>
</ul>
<p>结点链接,成为一个链表,即为线性表的链式存储结构.<br>最后一个节点指针为空.</p>
<p><strong>单链表的定义</strong></p>
<div class="highlight-container" data-rel="C"><figure class="iseeu highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">//定义单链表结点类型</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">LNode</span> &#123;</span></span><br><span class="line">    ElemType data; <span class="comment">//数据域</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">LNode</span> *<span class="title">next</span>;</span> <span class="comment">//指针域</span></span><br><span class="line">&#125; LNode, *LinkList; <span class="comment">//LNode为结点类型，LinkList为指向结点的指针类型</span></span><br></pre></td></tr></table></figure></div>

<p><strong>基本操作</strong></p>
<ul>
<li>**InitList(*L)**: 初始化操作，建立一个空的单链表L.</li>
</ul>
<div class="highlight-container" data-rel="C"><figure class="iseeu highlight c"><table><tr><td class="code"><pre><span class="line">Status <span class="title function_">InitList</span><span class="params">(LinkList *L)</span> &#123;</span><br><span class="line">    *L = (LNode *)<span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(LNode)); <span class="comment">//申请一个头结点</span></span><br><span class="line">    <span class="keyword">if</span> (*L == <span class="literal">NULL</span>) &#123; <span class="comment">//如果申请失败，返回错误</span></span><br><span class="line">        <span class="keyword">return</span> ERROR;</span><br><span class="line">    &#125;</span><br><span class="line">    (*L)-&gt;next = <span class="literal">NULL</span>; <span class="comment">//将头结点的指针域置空</span></span><br><span class="line">    <span class="keyword">return</span> OK;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<ul>
<li><strong>ListEmpty(L)</strong>: 判断单链表是否为空表，若为空返回true.</li>
</ul>
<div class="highlight-container" data-rel="C"><figure class="iseeu highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">bool</span> <span class="title function_">ListEmpty</span><span class="params">(LinkList L)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (L-&gt;next == <span class="literal">NULL</span>) &#123; <span class="comment">//如果头结点的指针域为空，说明没有数据元素</span></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<ul>
<li>**ClearList(*L)**: 清空单链表L.</li>
</ul>
<div class="highlight-container" data-rel="C"><figure class="iseeu highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">void</span> <span class="title function_">ClearList</span><span class="params">(LinkList *L)</span> &#123;</span><br><span class="line">    LinkList p, q; <span class="comment">//定义两个临时指针变量</span></span><br><span class="line">    p = (*L)-&gt;next; <span class="comment">//p指向第一个数据结点</span></span><br><span class="line">    <span class="keyword">while</span> (p) &#123; <span class="comment">//循环释放所有数据结点</span></span><br><span class="line">        q = p-&gt;next; <span class="comment">//q保存p的后继结点</span></span><br><span class="line">        <span class="built_in">free</span>(p); <span class="comment">//释放p所指向的结点</span></span><br><span class="line">        p = q; <span class="comment">//p指向下一个结点</span></span><br><span class="line">    &#125;</span><br><span class="line">    (*L)-&gt;next = <span class="literal">NULL</span>; <span class="comment">//将头结点的指针域置空</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<ul>
<li>**GetElem(L,i,*e)**: 将单链表L中的第i个位置元素值返回给e.</li>
</ul>
<div class="highlight-container" data-rel="C"><figure class="iseeu highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">bool</span> <span class="title function_">GetElem</span><span class="params">(LinkList L, <span class="type">int</span> i, ElemType *e)</span> &#123;</span><br><span class="line">    LinkList p; <span class="comment">//定义一个临时指针变量</span></span><br><span class="line">    <span class="type">int</span> j = <span class="number">0</span>; <span class="comment">//定义一个计数器变量</span></span><br><span class="line">    p = L; <span class="comment">//p指向头结点</span></span><br><span class="line">    <span class="keyword">while</span> (p &amp;&amp; j &lt; i) &#123; <span class="comment">//循环遍历到第i个结点或者到达表尾</span></span><br><span class="line">        p = p-&gt;next; <span class="comment">//p指向下一个结点</span></span><br><span class="line">        j++; <span class="comment">//计数器加1</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (!p || j &gt; i) &#123; <span class="comment">//如果没有找到第i个结点或者i不合法，返回false</span></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    *e = p-&gt;data; <span class="comment">//如果找到第i个结点，将其数据域赋给e，并返回true</span></span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<ul>
<li><strong>LocateElem(L,e)</strong>: 在单链表L中查找和e相等的元素，若成功返回表中序号，否则返回0.</li>
</ul>
<div class="highlight-container" data-rel="C"><figure class="iseeu highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">LocateElem</span><span class="params">(LinkList L, ElemType e)</span> &#123;</span><br><span class="line">    LinkList p; <span class="comment">//定义一个临时指针变量</span></span><br><span class="line">    <span class="type">int</span> i = <span class="number">0</span>; <span class="comment">//定义一个计数器变量</span></span><br><span class="line">    p = L-&gt;next; <span class="comment">//p指向第一个数据结点</span></span><br><span class="line">    <span class="keyword">while</span> (p) &#123; <span class="comment">//循环遍历所有数据结点或者找到和e相等的元素为止</span></span><br><span class="line">        i++; <span class="comment">//计数器加1</span></span><br><span class="line">        <span class="keyword">if</span> (p-&gt;data == e) &#123; <span class="comment">//如果找到和e相等的元素，返回其在表中的序号(从1开始)</span></span><br><span class="line">            <span class="keyword">return</span> i;</span><br><span class="line">        &#125;</span><br><span class="line">        p = p-&gt;next; <span class="comment">//p指向下一个结点</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>; <span class="comment">//如果没有找到和e相等的元素，返回0表示失败</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<ul>
<li>**ListInsert(*L,i,e)**: 在单链表L中第i个位置插入新元素e.</li>
</ul>
<div class="highlight-container" data-rel="C"><figure class="iseeu highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">bool</span> <span class="title function_">ListInsert</span><span class="params">(LinkList *L, <span class="type">int</span> i, ElemType e)</span> &#123;</span><br><span class="line">    LinkList p, s; <span class="comment">//定义两个临时指针变量</span></span><br><span class="line">    <span class="type">int</span> j = <span class="number">0</span>; <span class="comment">//定义一个计数器变量</span></span><br><span class="line">    p = *L; <span class="comment">//p指向头结点</span></span><br><span class="line">    <span class="keyword">while</span> (p &amp;&amp; j &lt; i - <span class="number">1</span>) &#123; <span class="comment">//循环遍历到第i-1个结点或者到达表尾</span></span><br><span class="line">        p = p-&gt;next; <span class="comment">//p指向下一个结点</span></span><br><span class="line">        j++; <span class="comment">//计数器加1</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (!p || j &gt; i - <span class="number">1</span>) &#123; <span class="comment">//如果没有找到第i-1个结点或者i不合法，返回false</span></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    s = (LinkList)<span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(LNode)); <span class="comment">//申请一个新的结点</span></span><br><span class="line">    <span class="keyword">if</span> (!s) &#123; <span class="comment">//如果申请失败，返回false</span></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    s-&gt;data = e; <span class="comment">//将新元素e赋给新结点的数据域</span></span><br><span class="line">    s-&gt;next = p-&gt;next; <span class="comment">//将新结点的指针域指向原来第i个结点</span></span><br><span class="line">    p-&gt;next = s; <span class="comment">//将第i-1个结点的指针域指向新结点</span></span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>; <span class="comment">//返回true表示插入成功</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<ul>
<li>**ListDelete(*L,i,*e)**: 删除单链表L中第i个位置元素，并用e返回其值.</li>
</ul>
<div class="highlight-container" data-rel="C"><figure class="iseeu highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">bool</span> <span class="title function_">ListDelete</span><span class="params">(LinkList *L, <span class="type">int</span> i, ElemType *e)</span> &#123;</span><br><span class="line">    LinkList p, q; <span class="comment">//定义两个临时指针变量</span></span><br><span class="line">    <span class="type">int</span> j = <span class="number">0</span>; <span class="comment">//定义一个计数器变量</span></span><br><span class="line">    p = *L; <span class="comment">//p指向头结点</span></span><br><span class="line">    <span class="keyword">while</span> (p-&gt;next &amp;&amp; j &lt; i - <span class="number">1</span>) &#123; <span class="comment">//循环遍历到第i-1个结点或者到达表尾的前一个结点</span></span><br><span class="line">        p = p-&gt;next; <span class="comment">//p指向下一个结点</span></span><br><span class="line">        j++; <span class="comment">//计数器加1</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (!p-&gt;next || j &gt; i - <span class="number">1</span>) &#123; <span class="comment">//如果没有找到第i-1个结点或者i不合法，返回false</span></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    q = p-&gt;next; <span class="comment">//q指向第i个结点</span></span><br><span class="line">    p-&gt;next = q-&gt;next; <span class="comment">//将第i-1个结点的指针域指向第i+1个结点</span></span><br><span class="line">    *e = q-&gt;data; <span class="comment">//将被删除的第i个结点的数据域赋给e</span></span><br><span class="line">    <span class="built_in">free</span>(q); <span class="comment">//释放被删除的第i个结点的空间</span></span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>; <span class="comment">//返回true表示删除成功</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<ul>
<li><strong>ListLength(L)</strong>: 返回单链表L的元素个数.</li>
</ul>
<div class="highlight-container" data-rel="C"><figure class="iseeu highlight c"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> <span class="title function_">ListLength</span><span class="params">(LinkList L)</span> &#123;</span><br><span class="line">    LinkList p; <span class="comment">//定义一个临时指针变量</span></span><br><span class="line">    <span class="type">int</span> len = <span class="number">0</span>; <span class="comment">//定义一个计数器变量，初始为0</span></span><br><span class="line">    p = L-&gt;next; <span class="comment">//p指向第一个数据结点</span></span><br><span class="line">    <span class="keyword">while</span> (p) &#123; <span class="comment">//循环遍历所有数据结点，直到到达表尾为止</span></span><br><span class="line">        len++; <span class="comment">//计数器加1</span></span><br><span class="line">        p = p-&gt;next; <span class="comment">//p指向下一个结点</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> len; <span class="comment">//返回计数器的值，即为单链表的长度</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<ol>
<li>单链表图例 :</li>
</ol>
<div class="highlight-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">graph LR;</span><br><span class="line">    subgraph 头节点</span><br><span class="line">    A[&quot;head| · &quot;]</span><br><span class="line">    end</span><br><span class="line">    B[&quot;a1| ·&quot;]</span><br><span class="line">    subgraph 结点</span><br><span class="line">    C[&quot;a2(数据)| ·(指针)&quot;]</span><br><span class="line">    end</span><br><span class="line">    D[...]</span><br><span class="line">    E[&quot;an|null&quot;]</span><br><span class="line"></span><br><span class="line">    G[·]--头指针--&gt;A--&gt;B--&gt;C--&gt;D--&gt;E</span><br></pre></td></tr></table></figure></div>

<ol start="2">
<li>空链表图例:</li>
</ol>
<div class="highlight-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">graph LR;</span><br><span class="line">    subgraph 头节点</span><br><span class="line">    A[&quot;head| · &quot;]</span><br><span class="line">    end</span><br><span class="line">    E[&quot;an|null&quot;]</span><br><span class="line"></span><br><span class="line">    G[·]--头指针--&gt;A--&gt;E</span><br></pre></td></tr></table></figure></div>

<p>头指针: </p>
<ul>
<li>是链表指向的<strong>第一个节点</strong>的指针 (若链表存在<strong>头节点</strong>,则指向头节点).</li>
<li>头指针具有<strong>标识作用</strong>,因此常用头指针冠以链表的名字.</li>
<li>无论链表是否为空,头指针<strong>均不为空</strong>.</li>
<li>头指针是链表<strong>必要</strong>元素</li>
</ul>
<p>头节点:</p>
<ul>
<li>是为了操作的统一和方便而设立. (统一了在第一元素节点前插入删除操作与其他节点)</li>
<li>头节点 <strong>不一定</strong> 是链表的必要元素.</li>
</ul>
<h1 id="栈"><a href="#栈" class="headerlink" title="栈"></a>栈</h1><h3 id="顺序栈"><a href="#顺序栈" class="headerlink" title="顺序栈"></a>顺序栈</h3><p>只能在表的一端(栈顶)进行插入和删除运算的线性表  </p>
<p>逻辑结构与线性表相同, 仍为一对一的关系.<br>存储结构以顺序栈更常见, 也可用链栈.</p>
<p>栈<strong>只能在栈顶</strong>插入, 删除;只能存取<strong>栈顶</strong>的元素</p>
<p><strong>后进先出</strong></p>
<p>存出结构:</p>
<div class="highlight-container" data-rel="C"><figure class="iseeu highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span>&#123;</span></span><br><span class="line">    SElemType *base;</span><br><span class="line">    SElemType *top;</span><br><span class="line">    <span class="type">int</span> stacksize;</span><br><span class="line">&#125; SqStack;</span><br></pre></td></tr></table></figure></div>

<p>栈空标志: base &#x3D;&#x3D; top<br>栈满标志: S-&gt;top - S-&gt;base &#x3D;&#x3D; S-&gt;stacksize</p>
<p>初始化:</p>
<p>进栈:</p>
<p>取栈顶元素:</p>
<p>栈顶判空:</p>
<p>求栈长度:</p>
]]></content>
      <tags>
        <tag>学习笔记</tag>
      </tags>
  </entry>
  <entry>
    <title>前端Web开发</title>
    <url>/2023/09/17/%E5%89%8D%E7%AB%AFWeb%E5%BC%80%E5%8F%91-HTML5-CSS-JS%E7%AC%94%E8%AE%B0/</url>
    <content><![CDATA[<p>Web包括: HTML5、CSS和JavaScrip<br>笔记来源于: <a class="link"   href="https://www.bilibili.com/video/BV1kM4y127Li?p=1" >黑马Web开发教程 <i class="fa-regular fa-arrow-up-right-from-square fa-sm"></i></a></p>
<h1 id="HTML-定义"><a href="#HTML-定义" class="headerlink" title="HTML 定义"></a>HTML 定义</h1><p>HTML 超文本标记语言 ———— HyperText Markup Language</p>
<p>超文本是链接<br>标记也叫标签,带尖括号的文本</p>
<ol>
<li>标签语法</li>
</ol>
<div class="highlight-container" data-rel="Html"><figure class="iseeu highlight html"><table><tr><td class="code"><pre><span class="line">     <span class="tag">&lt;<span class="name">strong</span>&gt;</span> 需要加粗的文字 <span class="tag">&lt;/<span class="name">strong</span>&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- 开始标签                结束标签 --&gt;</span></span><br></pre></td></tr></table></figure></div>

<p>这种标签成为双标签,会成对出现.</p>
<p>也有只有开始标签,没有结束标签的单标签:<br><code>&lt;br&gt;</code> 换行<br><code>&lt;hr&gt;</code> 水平线</p>
<ol start="2">
<li>标签的关系</li>
</ol>
<ul>
<li><p>父子关系(嵌套关系)</p>
</li>
<li><p>兄弟关系(并列关系)</p>
</li>
</ul>
<h3 id="HTML-基本骨架"><a href="#HTML-基本骨架" class="headerlink" title="HTML 基本骨架"></a>HTML 基本骨架</h3><div class="highlight-container" data-rel="Html"><figure class="iseeu highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">html</span>&gt;</span><span class="comment">&lt;!-- 整个网页 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">head</span>&gt;</span><span class="comment">&lt;!-- 头部,存放给浏览器看的信息,例如CSS --&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">title</span>&gt;</span>网页标题<span class="tag">&lt;/<span class="name">title</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line">    </span><br><span class="line">    <span class="tag">&lt;<span class="name">body</span>&gt;</span><span class="comment">&lt;!-- 主体,存放给用户看的信息,例如图片、文字 --&gt;</span></span><br><span class="line">        网页主体内容</span><br><span class="line">    <span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span>        </span><br></pre></td></tr></table></figure></div>

<p>在VS Code中使用: <strong>!+Tab</strong> 快速生成骨架</p>
<h1 id="HTML-标签"><a href="#HTML-标签" class="headerlink" title="HTML 标签"></a>HTML 标签</h1><h3 id="标题标签"><a href="#标题标签" class="headerlink" title="标题标签"></a>标题标签</h3><p>h1~h6(双标签)</p>
<h3 id="文本格式化标签"><a href="#文本格式化标签" class="headerlink" title="文本格式化标签"></a>文本格式化标签</h3><table>
<thead>
<tr>
<th>格式</th>
<th>常规</th>
<th>缩写</th>
</tr>
</thead>
<tbody><tr>
<td>加粗</td>
<td>strong</td>
<td>b</td>
</tr>
<tr>
<td>倾斜</td>
<td>em</td>
<td>i</td>
</tr>
<tr>
<td>下划线</td>
<td>ins</td>
<td>u</td>
</tr>
<tr>
<td>删除线</td>
<td>del</td>
<td>s</td>
</tr>
</tbody></table>
<h3 id="超链接标签"><a href="#超链接标签" class="headerlink" title="超链接标签"></a>超链接标签</h3><p>超链接是双标签,格式如下</p>
<div class="highlight-container" data-rel="Html"><figure class="iseeu highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">a</span> <span class="attr">href</span> = <span class="string">&quot;超链接&quot;</span>&gt;</span>显示的文本<span class="tag">&lt;/<span class="name">a</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">audio</span> <span class="attr">src</span>=<span class="string">&quot;音频路径&quot;</span> <span class="attr">controls</span> <span class="attr">loop</span> <span class="attr">autoplay</span>&gt;</span><span class="tag">&lt;/<span class="name">audio</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">video</span> <span class="attr">src</span>=<span class="string">&quot;视频路径&quot;</span> <span class="attr">controls</span> &gt;</span><span class="tag">&lt;/<span class="name">video</span>&gt;</span></span><br></pre></td></tr></table></figure></div>

<h3 id="列表"><a href="#列表" class="headerlink" title="列表"></a>列表</h3><p>有 <strong>无序列表、有序列表、定义列表</strong></p>
<ol>
<li>无序列表:</li>
</ol>
<div class="highlight-container" data-rel="Html"><figure class="iseeu highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">ul</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">li</span>&gt;</span>第一项<span class="tag">&lt;/<span class="name">li</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">li</span>&gt;</span>第二项<span class="tag">&lt;/<span class="name">li</span>&gt;</span>   </span><br><span class="line">    <span class="tag">&lt;<span class="name">li</span>&gt;</span>...<span class="tag">&lt;/<span class="name">li</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">ul</span>&gt;</span></span><br></pre></td></tr></table></figure></div>

<ol start="2">
<li>有序列表:</li>
</ol>
<div class="highlight-container" data-rel="Html"><figure class="iseeu highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">ol</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">li</span>&gt;</span>第一步<span class="tag">&lt;/<span class="name">li</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">li</span>&gt;</span>第二步<span class="tag">&lt;/<span class="name">li</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">li</span>&gt;</span>...<span class="tag">&lt;/<span class="name">li</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">ol</span>&gt;</span></span><br></pre></td></tr></table></figure></div>

<ol start="3">
<li>定义列表:</li>
</ol>
<div class="highlight-container" data-rel="Html"><figure class="iseeu highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dl</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">dt</span>&gt;</span>列表标题<span class="tag">&lt;/<span class="name">dt</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">dd</span>&gt;</span>列表描述1<span class="tag">&lt;/<span class="name">dd</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">dd</span>&gt;</span>列表描述2<span class="tag">&lt;/<span class="name">dd</span>&gt;</span></span><br><span class="line">    ...</span><br><span class="line"><span class="tag">&lt;/<span class="name">dl</span>&gt;</span></span><br></pre></td></tr></table></figure></div>
<h3 id="表格"><a href="#表格" class="headerlink" title="表格"></a>表格</h3><table>
<thead>
<tr>
<th>标签名</th>
<th>描述</th>
</tr>
</thead>
<tbody><tr>
<td>table</td>
<td>表格</td>
</tr>
<tr>
<td>tr</td>
<td>行</td>
</tr>
<tr>
<td>th</td>
<td>表头单元格</td>
</tr>
<tr>
<td>td</td>
<td>内容单元格</td>
</tr>
</tbody></table>
<p>使用border属性可以为表格添加边框线<br>跨行合并 属性值rowspan<br>跨列合并 属性值colspan  </p>
<h3 id="表单"><a href="#表单" class="headerlink" title="表单"></a>表单</h3><ol>
<li>input标签</li>
</ol>
<div class="highlight-container" data-rel="Html"><figure class="iseeu highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span> = <span class="string">&quot;...&quot;</span>&gt;</span>  </span><br></pre></td></tr></table></figure></div>


<table>
<thead>
<tr>
<th>type属性值</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>text</td>
<td>文本框,用于单行文本</td>
</tr>
<tr>
<td>password</td>
<td>密码框</td>
</tr>
<tr>
<td>radio</td>
<td>单选框</td>
</tr>
<tr>
<td>checkbox</td>
<td>多选框</td>
</tr>
<tr>
<td>file</td>
<td>上传文件</td>
</tr>
</tbody></table>
<ul>
<li>text和password<br>使用属性值palceholder能添加占位文本提示信息</li>
<li>radio<br>默认单选框没有单选功能<br>使用name属性值设置相同名称能达到单选效果<br>使用checked属性值默认选中(多选也可)  </li>
<li>file<br>添加multiple属性实现文件多选功能</li>
</ul>
<ol start="2">
<li>下拉菜单</li>
</ol>
<div class="highlight-container" data-rel="Html"><figure class="iseeu highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">select</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">option</span>&gt;</span>第一项<span class="tag">&lt;/<span class="name">option</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">option</span>&gt;</span>第二项<span class="tag">&lt;/<span class="name">option</span>&gt;</span></span><br><span class="line">    ...</span><br><span class="line"><span class="tag">&lt;<span class="name">select</span>&gt;</span></span><br></pre></td></tr></table></figure></div>

<p>option中使用selected属性值 默认选中一个option</p>
<ol start="3">
<li>文本域标签</li>
</ol>
<div class="highlight-container" data-rel="Html"><figure class="iseeu highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">textarea</span>&gt;</span>提示文本<span class="tag">&lt;/<span class="name">textarea</span>&gt;</span></span><br></pre></td></tr></table></figure></div>

<p>多行输入时使用文本域.  </p>
<ol start="4">
<li>label标签</li>
</ol>
<p><strong>写法一:</strong><br>label标签包裹内容<br>再设置for属性值,和表单控件的id属性值相同.</p>
<div class="highlight-container" data-rel="Html"><figure class="iseeu highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span> = <span class="string">&quot;radio&quot;</span> <span class="attr">id</span> =<span class="string">&quot;man&quot;</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">label</span> <span class="attr">for</span> = <span class="string">&quot;man&quot;</span>&gt;</span>男<span class="tag">&lt;/<span class="name">label</span>&gt;</span></span><br></pre></td></tr></table></figure></div>

<p><strong>写法二:</strong><br>不使用属性直接包裹文字和表单控件.</p>
<div class="highlight-container" data-rel="Html"><figure class="iseeu highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">label</span>&gt;</span><span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span> = <span class="string">&quot;radio&quot;</span>&gt;</span>女<span class="tag">&lt;/<span class="name">label</span>&gt;</span></span><br></pre></td></tr></table></figure></div>
<p>使用label标签增大表单控制范围</p>
<ol start="5">
<li>按钮</li>
</ol>
<div class="highlight-container" data-rel="Html"><figure class="iseeu highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">button</span> <span class="attr">type</span>=<span class="string">&quot;&quot;</span>&gt;</span>按钮名称<span class="tag">&lt;/<span class="name">button</span>&gt;</span></span><br></pre></td></tr></table></figure></div>

<table>
<thead>
<tr>
<th>type属性值</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>submi</td>
<td>提交</td>
</tr>
<tr>
<td>reset</td>
<td>重置</td>
</tr>
<tr>
<td>button</td>
<td>默认无功能(配合js使用)</td>
</tr>
</tbody></table>
<h3 id="无语义布局标签"><a href="#无语义布局标签" class="headerlink" title="无语义布局标签"></a>无语义布局标签</h3><p>用来布局网页,划分区域,摆放内容</p>
<ul>
<li>div</li>
</ul>
<div class="highlight-container" data-rel="Html"><figure class="iseeu highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span>&gt;</span>div标签,独占一行<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure></div>

<ul>
<li>span</li>
</ul>
<div class="highlight-container" data-rel="Html"><figure class="iseeu highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">span</span>&gt;</span>span标签,不换行<span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br></pre></td></tr></table></figure></div>

<h3 id="字符实体"><a href="#字符实体" class="headerlink" title="字符实体"></a>字符实体</h3><p>能在网页中显示预留字符</p>
<table>
<thead>
<tr>
<th>显示结果</th>
<th>描述</th>
<th>实体名称</th>
</tr>
</thead>
<tbody><tr>
<td></td>
<td>空格</td>
<td><code>&amp;nbsp;</code></td>
</tr>
<tr>
<td>&lt;</td>
<td>小于号</td>
<td><code>&amp;lt;</code></td>
</tr>
<tr>
<td>&gt;</td>
<td>大于号</td>
<td><code>&amp;gt;</code></td>
</tr>
</tbody></table>
<h1 id="CSS-定义"><a href="#CSS-定义" class="headerlink" title="CSS 定义"></a>CSS 定义</h1><p>层叠样式表(Casading Style Sheets),是一种 <strong>样式表</strong> 语言.<br>用来<strong>描述</strong> HTML文档 的呈现方式,用于美化内容.</p>
<p>CSS书写基本格式:</p>
<blockquote>
<p> 选择器 { CSS属性 }</p>
</blockquote>
<p><strong>键值对:</strong> 属性名和属性值成对出现</p>
<h3 id="CSS-的引入方式"><a href="#CSS-的引入方式" class="headerlink" title="CSS 的引入方式"></a>CSS 的引入方式</h3><ul>
<li>内部样式</li>
</ul>
<p>在title标签下方添加 <strong>Style双标签</strong> ,能书写CSS代码. </p>
<ul>
<li>外部样式</li>
</ul>
<p>将CSS代码写在单独的CSS文件中(.css)<br>再在HTML中, 使用 <strong>link标签</strong> 引入</p>
<div class="highlight-container" data-rel="Html"><figure class="iseeu highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">link</span> <span class="attr">rel</span>=<span class="string">&quot;(关系:样式表)stylesheet&quot;</span> <span class="attr">href</span>=<span class="string">&quot;./my.css&quot;</span>&gt;</span></span><br></pre></td></tr></table></figure></div>


<ul>
<li>行内样式</li>
</ul>
<p>需配合 JavaScript 使用, 写在标签的 <strong>style属性</strong> 值里.</p>
<div class="highlight-container" data-rel="Html"><figure class="iseeu highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">style</span>=<span class="string">&quot;color:red; font-size:20px;&quot;</span>&gt;</span>这是div标签<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure></div>

<h1 id="CSS-选择器"><a href="#CSS-选择器" class="headerlink" title="CSS 选择器"></a>CSS 选择器</h1><p>能查找并选择标签, 实现设置样式的目的</p>
<h2 id="基础选择器"><a href="#基础选择器" class="headerlink" title="基础选择器"></a>基础选择器</h2><h3 id="标签选择器"><a href="#标签选择器" class="headerlink" title="标签选择器"></a>标签选择器</h3><p>使用标签名作为选择器, 能选中<strong>同名</strong>标签, 以设置同一种样式.<br>缺点: 由于批量选中,无法差异化设置.</p>
<h3 id="类选择器"><a href="#类选择器" class="headerlink" title="类选择器"></a>类选择器</h3><p>能够差异化选中标签</p>
<ol>
<li>定义类选择器 –&gt; <strong>.类名</strong></li>
<li>使用类选择器 –&gt; 标签添加 <strong>class &#x3D; “类名”</strong></li>
</ol>
<div class="highlight-container" data-rel="Html"><figure class="iseeu highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">style</span>&gt;</span><span class="language-css"></span></span><br><span class="line"><span class="language-css"><span class="comment">/* 定义类选择器 */</span></span></span><br><span class="line"><span class="language-css"><span class="selector-class">.color</span>&#123;</span></span><br><span class="line"><span class="language-css">    <span class="attribute">color</span>:red;</span></span><br><span class="line"><span class="language-css">&#125;</span></span><br><span class="line"><span class="language-css"></span></span><br><span class="line"><span class="language-css"><span class="selector-class">.size</span>&#123;</span></span><br><span class="line"><span class="language-css">    <span class="attribute">font-size</span>: <span class="number">50px</span>;</span></span><br><span class="line"><span class="language-css">&#125;</span></span><br><span class="line"><span class="language-css"></span><span class="tag">&lt;/<span class="name">style</span>&gt;</span>    </span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- 使用类选择器 --&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- 一个类能给多个标签使用 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;color&quot;</span>&gt;</span>这是div标签1<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- 用空格隔开多个类名 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">&quot;color size&quot;</span>&gt;</span>这是div标签2<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure></div>

<h3 id="id选择器"><a href="#id选择器" class="headerlink" title="id选择器"></a>id选择器</h3><p>能够差异化选中标签, 同一个id名在页面只能用一次.<br>一般配合 JavaScript 使用</p>
<ol>
<li>定义id选择器 –&gt; <strong>#id名</strong></li>
<li>使用id选择器 –&gt; 标签添加 <strong>id&#x3D;”id”</strong></li>
</ol>
<div class="highlight-container" data-rel="Html"><figure class="iseeu highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">style</span>&gt;</span><span class="language-css"></span></span><br><span class="line"><span class="language-css">    <span class="comment">/* 定义id选择器 */</span></span></span><br><span class="line"><span class="language-css">    <span class="selector-id">#red</span>&#123;</span></span><br><span class="line"><span class="language-css">        <span class="attribute">color</span>:red;</span></span><br><span class="line"><span class="language-css">    &#125;</span></span><br><span class="line"><span class="language-css"></span><span class="tag">&lt;/<span class="name">style</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- 使用id选择器 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">&quot;red&quot;</span>&gt;</span>这是 div 标签<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure></div>

<h3 id="通配符选择器"><a href="#通配符选择器" class="headerlink" title="通配符选择器"></a>通配符选择器</h3><p>能选中页面所有标签,设置相同样式.</p>
<div class="highlight-container" data-rel="Html"><figure class="iseeu highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">style</span>&gt;</span><span class="language-css"></span></span><br><span class="line"><span class="language-css">* &#123;</span></span><br><span class="line"><span class="language-css">    <span class="attribute">color</span>:red;</span></span><br><span class="line"><span class="language-css">&#125;</span></span><br><span class="line"><span class="language-css"></span><span class="tag">&lt;/<span class="name">style</span>&gt;</span></span><br></pre></td></tr></table></figure></div>

<p>在网页初期使用, 清除网页的默认格式.</p>
<h2 id="复合选择器"><a href="#复合选择器" class="headerlink" title="复合选择器"></a>复合选择器</h2><p>其实都是基础选择器的组合,本质也是选择器.<br>能更精确地选中标签.</p>
<h3 id="后代选择器"><a href="#后代选择器" class="headerlink" title="后代选择器"></a>后代选择器</h3><blockquote>
<p>父选择器 子选择器 {CSS属性}    </p>
</blockquote>
<p>(用空格隔开)</p>
<h3 id="子代选择器"><a href="#子代选择器" class="headerlink" title="子代选择器"></a>子代选择器</h3><blockquote>
<p>父选择器 &gt; 子选择器 {CSS属性}    </p>
</blockquote>
<p>只选中最近的子级</p>
<h3 id="并集选择器"><a href="#并集选择器" class="headerlink" title="并集选择器"></a>并集选择器</h3><p>选中 <strong>多组</strong> 标签设置 <strong>相同</strong> 的样式  </p>
<blockquote>
<p>选择器1,选择器2,…,选择器N { CSS属性 }</p>
</blockquote>
<p>用逗号隔开</p>
<h3 id="交集选择器"><a href="#交集选择器" class="headerlink" title="交集选择器"></a>交集选择器</h3><p>选中存在 <strong>多个特点</strong> 的标签</p>
<blockquote>
<p>选择器1选择器2 { CSS属性 }</p>
</blockquote>
<p>连着写,不隔开.</p>
<h3 id="伪类选择器"><a href="#伪类选择器" class="headerlink" title="伪类选择器"></a>伪类选择器</h3><p>能选中元素的状态来设置样式( 例如要设置鼠标悬停的样式 ).</p>
<blockquote>
<p>设置鼠标选择状态: 选择器:hover {CSS属性}</p>
</blockquote>
<ul>
<li>伪类-超链接的状态</li>
</ul>
<p>超链接有四种状态:</p>
<table>
<thead>
<tr>
<th>选择器</th>
<th>作用</th>
</tr>
</thead>
<tbody><tr>
<td>:link</td>
<td>访问前</td>
</tr>
<tr>
<td>:visited</td>
<td>访问后</td>
</tr>
<tr>
<td>:hover</td>
<td>鼠标悬停</td>
</tr>
<tr>
<td>:active</td>
<td>点击时</td>
</tr>
</tbody></table>
<h2 id="进阶选择器"><a href="#进阶选择器" class="headerlink" title="进阶选择器"></a>进阶选择器</h2><h3 id="结构伪类选择器"><a href="#结构伪类选择器" class="headerlink" title="结构伪类选择器"></a>结构伪类选择器</h3><table>
<thead>
<tr>
<th>选择器</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>E:first-child</td>
<td>查找第一个E元素</td>
</tr>
<tr>
<td>E:last-child</td>
<td>查找最后一个E元素</td>
</tr>
<tr>
<td>E:nth-child(N)</td>
<td>查找第N个E元素(第一个元素N&#x3D;1)</td>
</tr>
</tbody></table>
<p>N支持数学运算.<br>例如: 2N, 选中偶数标签</p>
<h3 id="伪元素选择器"><a href="#伪元素选择器" class="headerlink" title="伪元素选择器"></a>伪元素选择器</h3><p>能创建虚拟元素(伪元素), 一般用来摆放装饰性内容.</p>
<table>
<thead>
<tr>
<th>选择器</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>E::before</td>
<td>在E元素<strong>里面最前面</strong>添加一个伪元素</td>
</tr>
<tr>
<td>E::after</td>
<td>在E元素<strong>里面最后面</strong>添加一个伪元素</td>
</tr>
</tbody></table>
<p>注意:  </p>
<ul>
<li>必须设置<code>content:&quot; &quot;</code>属性, 用来设置伪元素的内容.</li>
<li>默认为行内显示</li>
<li>权重和标签选择器相同</li>
</ul>
<h1 id="CSS-特性"><a href="#CSS-特性" class="headerlink" title="CSS 特性"></a>CSS 特性</h1><p>CSS能化简代码&#x2F;定位问题,并解决问题  </p>
<h3 id="继承性"><a href="#继承性" class="headerlink" title="继承性"></a>继承性</h3><p><strong>子级</strong> 默认继承 <strong>父级</strong> 的 <strong>文字控制</strong> 属性  </p>
<p>例如,我们可以选择 body 标签,设置文字效果.</p>
<h3 id="层叠性"><a href="#层叠性" class="headerlink" title="层叠性"></a>层叠性</h3><ul>
<li>相同的属性会覆盖</li>
<li>不同的属性会叠加</li>
</ul>
<h3 id="优先级"><a href="#优先级" class="headerlink" title="优先级"></a>优先级</h3><p>优先级也称为 <strong>“权重”</strong><br>当一个标签使用了多种选择器时, 会基于不同种类的选择器的匹配规则</p>
<blockquote>
<p>通配符 &lt; 标签 &lt; 类选择 &lt; id选择 &lt; 行内样式 &lt; !important</p>
</blockquote>
<p>总的来说范围大的优先级越低.<br><strong>!important</strong> 写在css大括号内, 能提高权重, 谨慎使用.</p>
<h3 id="标签的显示模式"><a href="#标签的显示模式" class="headerlink" title="标签的显示模式"></a>标签的显示模式</h3><p>布局页面的时候, 根据需求选择合适的标签显示模式摆放内容.</p>
<ul>
<li><strong>块级元素</strong><br>独占一行, 宽度默认与父级元素相等.<br>宽高属性生效.</li>
<li><strong>行内元素</strong><br>一行共存多个, 宽度默认尺寸由内容撑开.<br>宽高属性不生效. </li>
<li><strong>行内块元素</strong><br>一行共存多个, 宽度默认尺寸由内容撑开.<br>宽高属性生效.</li>
</ul>
<h3 id="转换显示模式"><a href="#转换显示模式" class="headerlink" title="转换显示模式"></a>转换显示模式</h3><p>属性名: display<br>属性值: </p>
<table>
<thead>
<tr>
<th>属性值</th>
<th>效果</th>
</tr>
</thead>
<tbody><tr>
<td>block</td>
<td>块级</td>
</tr>
<tr>
<td>inline-block</td>
<td>行内块</td>
</tr>
<tr>
<td>inline</td>
<td>行内</td>
</tr>
</tbody></table>
<h1 id="CSS-属性"><a href="#CSS-属性" class="headerlink" title="CSS 属性"></a>CSS 属性</h1><h3 id="文字的控制属性"><a href="#文字的控制属性" class="headerlink" title="文字的控制属性"></a>文字的控制属性</h3><ul>
<li>字体大小  <code>font-size</code></li>
</ul>
<p>设置文字尺寸,PC端网页常用单位为 <strong>px</strong>.<br>默认大小一般是16px.  </p>
<ul>
<li>字体粗细  <code>font-weight</code></li>
</ul>
<p>属性值一般写数字表示字体粗细.<br>400-normal(正常), 700-bold(加粗)</p>
<ul>
<li>字体倾斜  <code>font-style</code></li>
</ul>
<p>一般用来 <strong>清除</strong> 字体的默认倾斜.<br>normal(不倾斜), italic(倾斜)</p>
<ul>
<li>行高  <code>line-height</code></li>
</ul>
<p>设置多行文本的间距<br>数字px(以px为单位设置行高),<br>数字(以当前的px为基准,倍数设置行高).</p>
<p><strong>垂直居中技巧: 行高属性值等于盒子高度属性值</strong></p>
<ul>
<li>字体样式  <code>font-family</code></li>
</ul>
<p>设置字体样式.<br>属性值填写字体名称即可,多个字体逗号隔开.</p>
<p>font-family属性最后设置一个字体族名.<br>sans-serif是无衬线字体.</p>
<ul>
<li>字体复合属性   <code>font</code></li>
</ul>
<p>节省代码量,能集合字体的其他属性调整.<br>font: 是否倾斜 是否加粗 字号&#x2F;行高 字体 (<strong>必须</strong>按顺序书写)</p>
<ul>
<li>文本缩进  <code>text-indent</code></li>
</ul>
<p>属性值: 数字px,数字em(按比例调整,推荐1em)</p>
<ul>
<li><p>文本对齐 <code>text-align</code></p>
</li>
<li><p>修饰线 <code>text-decoration</code></p>
</li>
</ul>
<table>
<thead>
<tr>
<th>属性值</th>
<th>效果</th>
</tr>
</thead>
<tbody><tr>
<td>none</td>
<td>无</td>
</tr>
<tr>
<td>underline</td>
<td>下划线</td>
</tr>
<tr>
<td>line-through</td>
<td>删除线</td>
</tr>
<tr>
<td>overline</td>
<td>上划线</td>
</tr>
</tbody></table>
<ul>
<li>颜色  <code>color</code></li>
</ul>
<table>
<thead>
<tr>
<th>颜色表示方式</th>
<th>属性值</th>
<th>说明</th>
<th>使用场景</th>
</tr>
</thead>
<tbody><tr>
<td>颜色关键字</td>
<td>颜色的英文单词</td>
<td>red、green、…</td>
<td>学习测试</td>
</tr>
<tr>
<td>rgb表示法</td>
<td>rgb(r,g,b)</td>
<td>rgb三原色, 取值为0~255</td>
<td>了解</td>
</tr>
<tr>
<td>rgba表示法</td>
<td>rgba(r,g,b,a)</td>
<td>a是透明度</td>
<td>开发使用</td>
</tr>
<tr>
<td>十六进制表示法</td>
<td>#RRGGBB</td>
<td>三原色的值并一起,但是十六进制</td>
<td>设计稿开发</td>
</tr>
</tbody></table>
<h3 id="背景图属性"><a href="#背景图属性" class="headerlink" title="背景图属性"></a>背景图属性</h3><p>使用背景图实现装饰效果.<br>属性名: <strong>background-image</strong> (bgi)<br>属性值: <strong>url</strong> (背景图片URL)</p>
<p>常用的几个属性:</p>
<table>
<thead>
<tr>
<th>描述</th>
<th>属性</th>
</tr>
</thead>
<tbody><tr>
<td>背景色</td>
<td>background-color</td>
</tr>
<tr>
<td>背景图</td>
<td>background-image</td>
</tr>
<tr>
<td>背景图平铺方式</td>
<td>background-repeat</td>
</tr>
</tbody></table>
<p>其中, 关于背景图平铺方式的属性有以下几种:</p>
<table>
<thead>
<tr>
<th>属性值</th>
<th>效果</th>
</tr>
</thead>
<tbody><tr>
<td>no-repeat</td>
<td>不平铺</td>
</tr>
<tr>
<td>repeat</td>
<td>平铺 (默认效果)</td>
</tr>
<tr>
<td>repeat-x</td>
<td>水平方向平铺</td>
</tr>
<tr>
<td>reapeat-y</td>
<td>竖直方向平铺</td>
</tr>
</tbody></table>
<p>以及其他几个属性:</p>
<table>
<thead>
<tr>
<th>描述</th>
<th>属性</th>
</tr>
</thead>
<tbody><tr>
<td>背景图位置</td>
<td>background-position</td>
</tr>
<tr>
<td>背景图缩放</td>
<td>background-size</td>
</tr>
<tr>
<td>背景图固定</td>
<td>background-attachment</td>
</tr>
<tr>
<td>背景图复合属性</td>
<td>background</td>
</tr>
</tbody></table>
<h1 id="盒子模型"><a href="#盒子模型" class="headerlink" title="盒子模型"></a>盒子模型</h1><h2 id="盒子模型的组成"><a href="#盒子模型的组成" class="headerlink" title="盒子模型的组成"></a>盒子模型的组成</h2><ul>
<li>内容区域- width &amp; heigth</li>
<li>内边距  - padding</li>
<li>边框线 - boder</li>
<li>外边距 -margin</li>
</ul>
<div class="highlight-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line">graph LR;</span><br><span class="line"></span><br><span class="line">subgraph 外边距-margin</span><br><span class="line">subgraph 内边距-padding</span><br><span class="line">内容区域</span><br><span class="line">end</span><br><span class="line">end</span><br><span class="line"></span><br><span class="line">边框线-boder ---&gt; 内边距-padding</span><br><span class="line"></span><br><span class="line">```  </span><br><span class="line"></span><br><span class="line">#### 关于尺寸的计算</span><br><span class="line"></span><br><span class="line">* **默认情况**</span><br><span class="line"></span><br><span class="line">盒子尺寸 = 内容尺寸 + border尺寸 + 内边距尺寸 + margin尺寸</span><br><span class="line"></span><br><span class="line">* **內减模式(box-sizing:border-box)**</span><br><span class="line"></span><br><span class="line">设置为自动减去 border/padding的尺寸</span><br><span class="line"></span><br><span class="line">盒子尺寸 = 内容尺寸 -  border尺寸 - 内边距尺寸 + margin尺寸</span><br><span class="line"></span><br><span class="line">### 行内元素</span><br><span class="line"></span><br><span class="line">行内元素添加内外边距, 无法改变元素的垂直位置</span><br><span class="line"></span><br><span class="line">解决方法: </span><br><span class="line">添加 line-height 改变垂直位置</span><br><span class="line"></span><br><span class="line">### 边框线</span><br><span class="line"></span><br><span class="line">属性名: border - (设置方位名词可单独控制) </span><br><span class="line">属性值: 边框线粗细 线条样式 颜色(不区分顺序)</span><br><span class="line"></span><br><span class="line">常用线条样式</span><br><span class="line">属性值|线条样式</span><br><span class="line">---|---</span><br><span class="line">solid|实线</span><br><span class="line">dashed|虚线</span><br><span class="line">dotted|点线</span><br><span class="line"></span><br><span class="line">使用属性: border-radius 添加圆角.</span><br><span class="line"></span><br><span class="line">* 正圆  </span><br><span class="line">给正方形盒子设置属性值为 宽高的一半/50%.</span><br><span class="line"></span><br><span class="line">* 胶囊形状  </span><br><span class="line">给长方形盒子设置圆角属性为 盒子高度的一半.</span><br><span class="line"></span><br><span class="line">### 内边距</span><br><span class="line"></span><br><span class="line">属性名:padding / padding-方位名词</span><br><span class="line"></span><br><span class="line">**padding 多值写法**</span><br><span class="line"></span><br><span class="line">padding: 上 右 下 左 (空格隔开,从上方顺时针应用)</span><br><span class="line">padding: 上 左右 下</span><br><span class="line">padding: 上下 左右</span><br><span class="line">padding: 四个方向相同值</span><br><span class="line"></span><br><span class="line">### 外边距</span><br><span class="line"></span><br><span class="line">同padding, 作用拉开两个盒子之间的距离.</span><br><span class="line"></span><br><span class="line">在外边距两边设置auto, 能使版心居中.</span><br><span class="line"></span><br><span class="line">#### 外边距合并现象</span><br><span class="line"></span><br><span class="line">若两兄弟元素垂直排列, 上下的margin会合并, 取最大值.</span><br><span class="line"></span><br><span class="line">#### 外边距塌陷问题</span><br><span class="line"></span><br><span class="line">父子级标签中, 若在子级添加上外边距, 会导致父级一起向下移动.</span><br><span class="line"></span><br><span class="line">解决方法:  </span><br><span class="line">* 取消子级margin, 父级设置padding</span><br><span class="line">* 父级设置overflow: hidden</span><br><span class="line">* 父级设置border-top</span><br><span class="line">### 清除默认样式</span><br><span class="line"></span><br><span class="line">* 清除默认边距  </span><br><span class="line">margin: 0;  </span><br><span class="line">padding: 0;  </span><br><span class="line">box-sizing: border-box;(内减模式)</span><br><span class="line"></span><br><span class="line">* 去掉列表的项目符号  </span><br><span class="line">list-style: none;</span><br><span class="line"></span><br><span class="line">### 元素溢出解决方法</span><br><span class="line"></span><br><span class="line">属性名: overflow</span><br><span class="line"></span><br><span class="line">属性值|效果</span><br><span class="line">---|---</span><br><span class="line">hidden|溢出部分隐藏</span><br><span class="line">scroll|显示滚动条</span><br><span class="line">auto|溢出时才显示滚动条</span><br><span class="line"></span><br><span class="line">### 盒子阴影(拓展)</span><br><span class="line"></span><br><span class="line">属性名: box-shadow  </span><br><span class="line">属性值: X轴偏移 Y轴偏移 模糊半径 扩散半径 颜色 内外阴影</span><br><span class="line"></span><br><span class="line">默认为外阴影, 内阴影需要添加 inset</span><br><span class="line"></span><br><span class="line"># 标准流和浮动 (了解)</span><br><span class="line"></span><br><span class="line">### 标准流</span><br><span class="line"></span><br><span class="line">标准流也称文档流, 是标签在页面中默认的排布规则.</span><br><span class="line"></span><br><span class="line">### 浮动(float)</span><br><span class="line"></span><br><span class="line">浮动能让块元素水平排列, 具备行内块的特点.</span><br><span class="line"></span><br><span class="line">浮动后元素会顶对齐, 并脱离标准流控制, 如果父级元素未设置高度, 脱离后会对网页布局造成影响.</span><br><span class="line"></span><br><span class="line">#### 关于清除浮动带来的影响:</span><br><span class="line"></span><br><span class="line">1. 额外标签法</span><br><span class="line"></span><br><span class="line">在父元素雷人的最后添加一个块级元素, 将其css属性设置为 clear: both</span><br><span class="line"></span><br><span class="line">2. 单伪元素法</span><br><span class="line">```CSS</span><br><span class="line">.clearfix::after&#123;</span><br><span class="line">    content: &quot;&quot;;</span><br><span class="line">    display: block:</span><br><span class="line">    clear: both;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<ol start="3">
<li>双伪元素法</li>
</ol>
<div class="highlight-container" data-rel="Css"><figure class="iseeu highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.clearfix</span><span class="selector-pseudo">::before</span>,</span><br><span class="line"><span class="selector-class">.clearfix</span><span class="selector-pseudo">::after</span>&#123;</span><br><span class="line">    <span class="attribute">content</span>: <span class="string">&quot;&quot;</span>;</span><br><span class="line">    <span class="attribute">display</span>: table;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-class">.clearfix</span><span class="selector-pseudo">::after</span>&#123;</span><br><span class="line">    <span class="attribute">clear</span>: both;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<ol start="4">
<li>overflow 溢出隐藏</li>
</ol>
<p>父元素添加CSS属性 overflow: hidden</p>
<h1 id="flex-布局"><a href="#flex-布局" class="headerlink" title="flex 布局"></a>flex 布局</h1><p>flex 布局也叫弹性布局, 相比浮动, felx更简单灵活.</p>
<p>给父元素设置 <code>dispaly: felx</code> ,子元素可以自动挤压或拉伸.</p>
<ul>
<li>主轴对齐方式:</li>
</ul>
<p>属性名: <code>justify-content</code>  </p>
<table>
<thead>
<tr>
<th>属性值</th>
<th>效果</th>
</tr>
</thead>
<tbody><tr>
<td><strong>center</strong></td>
<td>弹性盒子沿主轴居中排列</td>
</tr>
<tr>
<td><strong>space-between</strong></td>
<td>弹性盒子沿主轴均匀排列, 空白间距均分在盒子<strong>之间</strong>.</td>
</tr>
<tr>
<td><strong>space-around</strong></td>
<td>弹性盒子沿主轴均匀排列, 空白间距均分在盒子<strong>两侧</strong>.</td>
</tr>
<tr>
<td><strong>space-evenly</strong></td>
<td>弹性盒子沿主轴均匀排列, 盒子与容器之间间距相等.</td>
</tr>
<tr>
<td>felx-start</td>
<td>默认值, 弹性盒子从主轴起点依次排起.</td>
</tr>
<tr>
<td>felx-end</td>
<td>弹性盒子从主轴终点依次排起.</td>
</tr>
</tbody></table>
<ul>
<li>侧轴对齐方式</li>
</ul>
<p>属性名:<br><code>align-items</code> :选中当前弹性容器内所有弹性盒子的侧轴, 设置对齐方式<br><code>slign-self</code> :单独控制某个弹性盒子的侧轴</p>
<table>
<thead>
<tr>
<th>属性值</th>
<th>效果</th>
</tr>
</thead>
<tbody><tr>
<td>strench</td>
<td>弹性盒子沿着侧轴线被拉伸至铺满容器(默认)</td>
</tr>
<tr>
<td>center</td>
<td>弹性盒子沿侧轴居中排列</td>
</tr>
</tbody></table>
<ul>
<li>修改主轴方向</li>
</ul>
<p>属性名: <code>flex-direction</code></p>
<table>
<thead>
<tr>
<th>属性值</th>
<th>效果</th>
</tr>
</thead>
<tbody><tr>
<td><strong>column</strong></td>
<td>垂直方向, 从上到下</td>
</tr>
<tr>
<td>row</td>
<td>水平方向, 从左向右(默认)</td>
</tr>
<tr>
<td>row-reverse</td>
<td>水平方向, 从右向左</td>
</tr>
<tr>
<td>column-reverse</td>
<td>垂直方向, 从下向上</td>
</tr>
</tbody></table>
<ul>
<li>弹性伸缩比</li>
</ul>
<p>属性名: <code>flex</code><br>控制弹性盒子的主轴方向尺寸.<br>属性值: 整数数字, 表示占用父级剩余尺寸的份数.</p>
<ul>
<li>弹性盒子换行显示</li>
</ul>
<p>属性名: <code>flex-wrap</code>  </p>
<table>
<thead>
<tr>
<th>属性值</th>
<th>效果</th>
</tr>
</thead>
<tbody><tr>
<td><strong>wrap</strong></td>
<td>换行</td>
</tr>
<tr>
<td>nowrap</td>
<td>不换行(默认)</td>
</tr>
</tbody></table>
<ul>
<li>行对齐方式</li>
</ul>
<p>属性名: <code>align-content</code></p>
<p>属性值</p>
]]></content>
      <tags>
        <tag>学习笔记</tag>
      </tags>
  </entry>
  <entry>
    <title>C++11起-新特性</title>
    <url>/2023/09/17/CPP11%E8%B5%B7-%E6%96%B0%E7%89%B9%E6%80%A7/</url>
    <content><![CDATA[<p>本文是关于C++11起的一些新特性介绍.由我朋友@EMCC所编写,他的b站:<br><a class="link"   href="https://space.bilibili.com/1843315943" >EMC方的b站空间 <i class="fa-regular fa-arrow-up-right-from-square fa-sm"></i></a></p>
<h1 id="列表初始化"><a href="#列表初始化" class="headerlink" title="列表初始化"></a>列表初始化</h1><h2 id="初始化赋值"><a href="#初始化赋值" class="headerlink" title="初始化赋值"></a>初始化赋值</h2><p><code>C++11</code>允许普通变量和数组也使用列表初始化赋值。</p>
<p>旧标准：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> a = <span class="number">15</span>;		<span class="comment">//声明变量a，初始值为15</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">b</span><span class="params">(<span class="number">15</span>)</span></span>;		<span class="comment">//声明变量b，初始值为15</span></span><br><span class="line"><span class="type">int</span> arr[]=&#123; <span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>&#125;;<span class="comment">//声明数组arr，3个元素1、2、3</span></span><br></pre></td></tr></table></figure></div>

<p><code>C++11</code>标准，（统一初始化列表，赋值等号可省略）：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> a = &#123;<span class="number">15</span>&#125;;			<span class="comment">//声明变量a，初始值为15</span></span><br><span class="line"><span class="type">int</span> b&#123;<span class="number">15</span>&#125;;				<span class="comment">//声明变量b，初始值为15</span></span><br><span class="line"><span class="type">int</span> arr[]&#123; <span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>&#125;;	<span class="comment">//声明变量b，初始值为15</span></span><br></pre></td></tr></table></figure></div>

<h2 id="列表初始化防止类型收窄"><a href="#列表初始化防止类型收窄" class="headerlink" title="列表初始化防止类型收窄"></a>列表初始化防止类型收窄</h2><p>类型收窄是指，变量赋值时发生隐式类型转换，因为数据精度或范围不匹配，导致精度丢失或数据丢失。</p>
<p>示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="type">int</span> a = <span class="number">1024</span>;</span><br><span class="line">	<span class="type">char</span> b = a; <span class="comment">//编译不会报错，但数据丢失,类型收窄</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;b = %din&quot;</span>, b);<span class="comment">//输出结果为0</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>使用列表初始化可以防止上面的类型收窄。</p>
<p>示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="type">int</span> a = <span class="number">1024</span>;</span><br><span class="line">    <span class="type">char</span> b&#123; a &#125;;<span class="comment">//编译时会直接报错，提示：C2397 从&quot;int&quot;转换到&quot; char&quot;需要收缩转换</span></span><br><span class="line">	<span class="built_in">printf</span>(<span class="string">&quot;b = %din&quot;</span>, b);</span><br><span class="line">	<span class="built_in">system</span>(<span class="string">&quot;pause&gt;nul&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>注意！部分编译器会运行编译通过，但可能会有警告提示或者没有任何提示，但打印<code>b</code>的值时，仍然为<code>0</code>。</p>
<h1 id="原始字面量"><a href="#原始字面量" class="headerlink" title="原始字面量"></a>原始字面量</h1><p>在 <code>C++11</code>中添加了定义原始字符串的字面量。</p>
<p>语法：</p>
<ul>
<li><code>R “xxx(原始字符串)xxx”</code></li>
<li>其中<code>()</code>两边的字符串是说明文字，类似注释，可以省略。</li>
<li><code>()</code>两边的字符必须一样，否则编译不通过。</li>
</ul>
<p>原始字面量<code>R</code>可以直接表示字符串的实际含义，而不需要额外对字符串做转译或连接等操作。</p>
<p>原始字面量<code>R</code>可以直接赋值给<code>string</code>字符串，内容完全与原始字面量内的内容相同。</p>
<p>示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span><span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span><span class="string">&lt;string&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    string str = <span class="string">&quot;D:\hello\world\test.text&quot;</span>;</span><br><span class="line">    cout &lt;&lt; str &lt;&lt; endl;</span><br><span class="line">    string str1 = <span class="string">&quot;D:\\hello\\world\\test.text&quot;</span>;</span><br><span class="line">    cout &lt;&lt; str1 &lt;&lt; endl;</span><br><span class="line">    string str2 = <span class="string">R&quot;(D:\hello\world\test.text)&quot;</span>;</span><br><span class="line">    cout &lt;&lt; str2 &lt;&lt; endl;</span><br><span class="line"> </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>输出结果：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line">D:helloworld    est.text</span><br><span class="line">D:\hello\world\test.text</span><br><span class="line">D:\hello\world\test.text</span><br></pre></td></tr></table></figure></div>

<p>在<code>C++11</code>之前如果一个字符串分别写到了不同的行里边，需要加连接符，这种方式不仅繁琐，还破坏了表达式的原始含义，如果使用原始字面量就变得简单很多，很强直观，可读性强。</p>
<p>示例：</p>
<ul>
<li><p>非<code>C++11</code>：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span><span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span><span class="string">&lt;string&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    string str = <span class="string">&quot;&lt;html&gt;\</span></span><br><span class="line"><span class="string">        &lt;head&gt;\</span></span><br><span class="line"><span class="string">        &lt;title&gt;\</span></span><br><span class="line"><span class="string">        海贼王\</span></span><br><span class="line"><span class="string">        &lt;/title&gt;\</span></span><br><span class="line"><span class="string">        &lt;/head&gt;\</span></span><br><span class="line"><span class="string">        &lt;body&gt;\</span></span><br><span class="line"><span class="string">        &lt;p&gt;\</span></span><br><span class="line"><span class="string">        我是要成为海贼王的男人!!!\</span></span><br><span class="line"><span class="string">        &lt;/p&gt;\</span></span><br><span class="line"><span class="string">        &lt;/body&gt;\</span></span><br><span class="line"><span class="string">        &lt;/html&gt;&quot;</span>;</span><br><span class="line">    cout &lt;&lt; str &lt;&lt; endl;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>
</li>
<li><p><code>C++11</code></p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span><span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span><span class="string">&lt;string&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    string str = <span class="string">R&quot;(&lt;html&gt;</span></span><br><span class="line"><span class="string">        &lt;head&gt;</span></span><br><span class="line"><span class="string">        &lt;title&gt;</span></span><br><span class="line"><span class="string">        海贼王</span></span><br><span class="line"><span class="string">        &lt;/title&gt;</span></span><br><span class="line"><span class="string">        &lt;/head&gt;</span></span><br><span class="line"><span class="string">        &lt;body&gt;</span></span><br><span class="line"><span class="string">        &lt;p&gt;</span></span><br><span class="line"><span class="string">        我是要成为海贼王的男人!!!</span></span><br><span class="line"><span class="string">        &lt;/p&gt;</span></span><br><span class="line"><span class="string">        &lt;/body&gt;</span></span><br><span class="line"><span class="string">        &lt;/html&gt;)&quot;</span>;</span><br><span class="line">    cout &lt;&lt; str &lt;&lt; endl;</span><br><span class="line"> </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div></li>
</ul>
<h1 id="自动类型推导"><a href="#自动类型推导" class="headerlink" title="自动类型推导"></a>自动类型推导</h1><h2 id="auto自动类型推导"><a href="#auto自动类型推导" class="headerlink" title="auto自动类型推导"></a><code>auto</code>自动类型推导</h2><hr>
<h3 id="使用示例"><a href="#使用示例" class="headerlink" title="使用示例"></a>使用示例</h3><hr>
<p><code>auto</code>的自动类型推导，用于从初始化表达式中推断出变量的数据类型。</p>
<p>从这个意义上讲，<code>auto</code>并非一种类型声明，而是一个类型声明时的占位符，编译器在编译时期会将<code>auto</code>替换为变量实际的类型。</p>
<p>通过<code>auto</code>的自动类型推导，可以大大简化我们的编程工作。</p>
<p>示例（<code>auto</code>配合模板使用）：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">func</span><span class="params">(vector&lt;<span class="type">int</span>&gt; &amp;tmp)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">for</span> (<span class="keyword">auto</span> i= tmp.<span class="built_in">begin</span>(); i != tmp.<span class="built_in">end</span>();++i)</span><br><span class="line">    &#123;</span><br><span class="line">		cout &lt;&lt; *i;</span><br><span class="line">    &#125;</span><br><span class="line">    cout &lt;&lt; endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>上面的代码中无需明确指定<code>i</code>的类型，使用<code>auto</code>后编译器会自动推导<code>i</code>的类型，极大方便了代码编写。</p>
<h3 id="推导规则"><a href="#推导规则" class="headerlink" title="推导规则"></a>推导规则</h3><hr>
<p><code>auto</code>可以和引用、指针一起结合使用，还可以带上<code>const</code>和<code>volatile</code>修饰符。</p>
<p>通过一组例子来理解一下推导规则：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> x = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">auto</span> * a = &amp;x; 			<span class="comment">//a→int *，auto被推导为int</span></span><br><span class="line"><span class="keyword">auto</span> b = x; 			<span class="comment">//b→int，auto被推导为int</span></span><br><span class="line"><span class="keyword">auto</span> c = &amp;x; 			<span class="comment">//c→int &amp;,atuo被推导为int</span></span><br><span class="line"><span class="keyword">auto</span> *d = x; 			<span class="comment">//error，无法推导auto类型，因为指针未被限定为常量</span></span><br><span class="line"><span class="keyword">auto</span> &amp;e = x; 			<span class="comment">//e→const int&amp;,auto被推导为 int</span></span><br><span class="line"><span class="keyword">auto</span> &amp;f = &amp;x; 			<span class="comment">// error, 未定义const int地址指向的内容</span></span><br><span class="line"><span class="type">const</span> <span class="keyword">auto</span> g = x; 		<span class="comment">//g→const int,auto被推导为int</span></span><br><span class="line"><span class="type">const</span> <span class="keyword">auto</span> *h = x; 		<span class="comment">//error，指针类型未定义</span></span><br><span class="line"><span class="type">const</span> <span class="keyword">auto</span> &amp;i = x; 		<span class="comment">//i→ const int，auto被推导为int</span></span><br><span class="line"><span class="type">const</span> <span class="keyword">auto</span> j = &amp;x; 		<span class="comment">//j→const int *,auto被推导为int</span></span><br><span class="line"><span class="type">const</span> <span class="keyword">auto</span> *k = &amp;x; 	<span class="comment">//k→const int *,auto被推导为int</span></span><br><span class="line">   <span class="type">const</span> <span class="keyword">auto</span> &amp;l = &amp;x; 	<span class="comment">//l→const int *，auto被推导为int，这里和未加const限定符的区别是，const限定了指针为常量</span></span><br></pre></td></tr></table></figure></div>

<h3 id="注意事项"><a href="#注意事项" class="headerlink" title="注意事项"></a>注意事项</h3><hr>
<ul>
<li><p>使用<code>auto</code>定义的变量必须初始化。</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="type">int</span> a=<span class="number">10</span>;</span><br><span class="line">    <span class="keyword">auto</span> b ;<span class="comment">//编译报错，auto定义的变量必须初始化。</span></span><br><span class="line">    b = <span class="number">20</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>
</li>
<li><p>低版本编译器中，函数的参数列表不支持<code>auto</code>变量。</p>
<p>示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//VS2013，不支持以下写法，但高版本编译器支持</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">fun</span><span class="params">(<span class="keyword">auto</span> a)</span></span>&#123;<span class="comment">//编译报错</span></span><br><span class="line">    <span class="comment">//...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>
</li>
<li><p><code>auto</code>变量不能作为自定义类型的成员变量。</p>
<p>示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line">  <span class="keyword">struct</span> <span class="title class_">ak</span></span><br><span class="line">  &#123;</span><br><span class="line">  	<span class="type">int</span> a;</span><br><span class="line">  	<span class="keyword">auto</span> b = <span class="number">90</span>;<span class="comment">//编译报错，此处不允许使用auto</span></span><br><span class="line">  &#125;;</span><br><span class="line"></span><br><span class="line">- `<span class="keyword">auto</span>`不能用于定义数组。</span><br><span class="line"></span><br><span class="line">  示例：</span><br><span class="line"></span><br><span class="line">  ```c++</span><br><span class="line">  <span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function">  </span>&#123;</span><br><span class="line">  	<span class="keyword">auto</span> sd[] = &#123;<span class="number">0</span>,<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>&#125;;<span class="comment">//编译报错，auto类型不能出现在顶级数组类型中</span></span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure></div>
</li>
<li><p>模板实例化参数不能是<code>auto</code>。</p>
<p>示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line">vector&lt;<span class="keyword">auto</span>&gt;b = &#123; <span class="number">1</span> &#125;;<span class="comment">//编译报错，此处不允许使用auto</span></span><br></pre></td></tr></table></figure></div>
</li>
<li><p><code>auto</code>只能用于类的静态成员，不能用于类的非静态成员（普通成员），如果我们想推导非静态成员的类型，这个时候就必须使用<code>decltype</code>了。</p>
</li>
</ul>
<h2 id="decltype自动类型推导"><a href="#decltype自动类型推导" class="headerlink" title="decltype自动类型推导"></a><code>decltype</code>自动类型推导</h2><hr>
<h3 id="使用示例-1"><a href="#使用示例-1" class="headerlink" title="使用示例"></a>使用示例</h3><hr>
<p><code>decltype</code>实际上有点像<code>auto</code>的反函数，<code>auto</code>可以让你声明一个变量，而<code>decltype</code>则可以从一个变量或表达式中得到其类型。</p>
<p>因为<code>auto</code>并不适用于所有的自动类型推导场景，在某些特殊情况下<code>auto</code>用起来非常不方便，甚至压根无法使用，所以<code>decltype</code>关键字也被引入到<code>C++11</code>中。</p>
<p>使用示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="type">int</span> i;</span><br><span class="line">	<span class="keyword">decltype</span>(i) j = <span class="number">0</span>;</span><br><span class="line">	cout &lt;&lt; <span class="built_in">typeid</span>(j).<span class="built_in">name</span>() &lt;&lt; endl;</span><br><span class="line">    </span><br><span class="line">	<span class="type">float</span> a;</span><br><span class="line">	<span class="type">double</span> b;</span><br><span class="line">	<span class="keyword">decltype</span>(a + b) c;</span><br><span class="line">	cout &lt;&lt; <span class="built_in">typeid</span>(c).<span class="built_in">name</span>() &lt;&lt;endl;</span><br><span class="line">    </span><br><span class="line">	vector&lt;<span class="type">int</span>&gt; tmp;</span><br><span class="line">	<span class="keyword">decltype</span>(tmp.<span class="built_in">begin</span>()) k;</span><br><span class="line">	<span class="keyword">for</span> (k = tmp.<span class="built_in">begin</span>(); k != tmp.<span class="built_in">end</span>();++k)</span><br><span class="line">    &#123;</span><br><span class="line">		<span class="comment">//......</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>输出结果：</p>
<div class="highlight-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">int</span><br><span class="line">double</span><br><span class="line">class std::_Vector_iterator&lt;class std::_Vector_val&lt;struct std::_Simple_types&lt;int&gt; &gt; &gt;</span><br></pre></td></tr></table></figure></div>

<p>对于匿名类型的变量，可以通过<code>decltype</code>获取类型后再次定义变量。</p>
<p>示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">enum</span> &#123;Ok, Error &#125; flag;<span class="comment">//匿名类型的枚举变量flag，在C++11以前，后续代码中无法再次定义这个枚举变量</span></span><br><span class="line"><span class="keyword">decltype</span>(flag) flag2;<span class="comment">//通过decltype获取flag的类型，再次定义这个匿名枚举类型的变量flag2</span></span><br></pre></td></tr></table></figure></div>

<h3 id="推导规则-1"><a href="#推导规则-1" class="headerlink" title="推导规则"></a>推导规则</h3><hr>
<p>当程使用<code>decltype(exp)</code>获取类型时，编译器将根据以下三条规则得出结果：</p>
<ul>
<li>如果<code>exp</code>是一个不被括号<code>()</code>包围的表达式，或者是一个类成员访问表达式，或者是一个单独的变量，那么<code>decltype(exp)</code>的类型就和<code>exp</code>一致。</li>
<li>如果<code>exp</code>是函数调用，那么<code>decltype(exp)</code>的类型就和函数返回值的类型一致。</li>
<li>如果<code>exp</code> 是一个左值，或者被括号<code>()</code>包围，那么<code>decltype(exp)</code>的类型就是<code>exp</code>的引用；假设<code>exp</code>的类型为<code>T</code>，那么<code>decltype(exp)</code>的类型就是<code>T&amp;</code></li>
</ul>
<p>几个实际的例子：</p>
<ul>
<li><p>示例1：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Student</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="type">static</span> <span class="type">int</span> total;</span><br><span class="line">    string name;</span><br><span class="line">    <span class="type">int</span> age;</span><br><span class="line">    <span class="type">float</span> scores;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="type">int</span> Student::total = <span class="number">0</span>;</span><br><span class="line"><span class="function"><span class="type">int</span>  <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="type">int</span> n = <span class="number">0</span>;</span><br><span class="line">    <span class="type">const</span> <span class="type">int</span> &amp;r = n;</span><br><span class="line">    Student stu;</span><br><span class="line">    <span class="keyword">decltype</span>(n) a = n;  	<span class="comment">//n 为 int 类型，a 被推导为 int 类型</span></span><br><span class="line">    <span class="keyword">decltype</span>(r) b = n;     	<span class="comment">//r 为 const int&amp; 类型, b 被推导为 const int&amp; 类型</span></span><br><span class="line">    <span class="keyword">decltype</span>(Student::total) c = <span class="number">0</span>;  <span class="comment">//total 为类 Student 的一个 int 类型的成员变量，c 被推导为 int 类型</span></span><br><span class="line">    <span class="keyword">decltype</span>(stu.name) url = <span class="string">&quot;http://c.biancheng.net/cplus/&quot;</span>;  <span class="comment">//total 为类 Student 的一个 string 类型的成员变量，url被推导为 string 类型</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>
</li>
<li><p>示例2：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*-------------函数声明--------------*/</span></span><br><span class="line"><span class="function"><span class="type">int</span>&amp; <span class="title">func_int_r</span><span class="params">(<span class="type">int</span>, <span class="type">char</span>)</span></span>;				<span class="comment">//返回值为 int&amp;</span></span><br><span class="line"><span class="function"><span class="type">int</span>&amp;&amp; <span class="title">func_int_rr</span><span class="params">(<span class="type">void</span>)</span></span>;				<span class="comment">//返回值为 int&amp;&amp;</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">func_int</span><span class="params">(<span class="type">double</span>)</span></span>;					<span class="comment">//返回值为 int</span></span><br><span class="line"><span class="function"><span class="type">const</span> <span class="type">int</span>&amp; <span class="title">fun_cint_r</span><span class="params">(<span class="type">int</span>, <span class="type">int</span>, <span class="type">int</span>)</span></span>;	<span class="comment">//返回值为 const int&amp;</span></span><br><span class="line"><span class="function"><span class="type">const</span> <span class="type">int</span>&amp;&amp; <span class="title">func_cint_rr</span><span class="params">(<span class="type">void</span>)</span></span>;			<span class="comment">//返回值为 const int&amp;&amp;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/*-------------decltype类型推导---------*/</span></span><br><span class="line"><span class="type">int</span> n = <span class="number">100</span>;</span><br><span class="line"><span class="keyword">decltype</span>(<span class="built_in">func_int_r</span>(<span class="number">100</span>, <span class="string">&#x27;A&#x27;</span>)) a = n;  	<span class="comment">//a 的类型为 int&amp;</span></span><br><span class="line"><span class="keyword">decltype</span>(<span class="built_in">func_int_rr</span>()) b = <span class="number">0</span>;  		<span class="comment">//b 的类型为 int&amp;&amp;</span></span><br><span class="line"><span class="keyword">decltype</span>(<span class="built_in">func_int</span>(<span class="number">10.5</span>)) c = <span class="number">0</span>;   		<span class="comment">//c 的类型为 int</span></span><br><span class="line"><span class="keyword">decltype</span>(<span class="built_in">fun_cint_r</span>(<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>))  x = n;    	<span class="comment">//x 的类型为 const int &amp;</span></span><br><span class="line"><span class="keyword">decltype</span>(<span class="built_in">func_cint_rr</span>()) y = <span class="number">0</span>;  		<span class="comment">// y 的类型为 const int&amp;&amp;</span></span><br></pre></td></tr></table></figure></div>
</li>
<li><p>示例3：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Base</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="type">int</span> x;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="type">const</span> Base obj;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">//带有括号的表达式</span></span><br><span class="line">    <span class="keyword">decltype</span>(obj.x) a = <span class="number">0</span>;  	<span class="comment">//obj.x 为类的成员访问表达式，符合推导规则一，a 的类型为 int</span></span><br><span class="line">    <span class="keyword">decltype</span>((obj.x)) b = a;  	<span class="comment">//obj.x 带有括号，符合推导规则三，b 的类型为 int&amp;。</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">//加法表达式</span></span><br><span class="line">    <span class="type">int</span> n = <span class="number">0</span>, m = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">decltype</span>(n + m) c = <span class="number">0</span>;  	<span class="comment">//n+m 得到一个右值，符合推导规则一，所以推导结果为 int</span></span><br><span class="line">    <span class="keyword">decltype</span>(n = n + m) d = c;  <span class="comment">//n=n+m 得到一个左值，符号推导规则三，所以推导结果为 int&amp;</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div></li>
</ul>
<h2 id="追踪返回类型"><a href="#追踪返回类型" class="headerlink" title="追踪返回类型"></a>追踪返回类型</h2><hr>
<h3 id="普通函数"><a href="#普通函数" class="headerlink" title="普通函数"></a>普通函数</h3><hr>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">func1</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> a+ b;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">auto</span> <span class="title">func2</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span> -&gt; <span class="type">int</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> a + b;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">auto</span> <span class="title">func3</span><span class="params">(<span class="type">int</span> a, <span class="type">double</span> b)</span> -&gt; <span class="title">decltype</span><span class="params">(a+b)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> a+ b;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">auto</span> <span class="title">func4</span><span class="params">(<span class="keyword">auto</span> a, <span class="keyword">auto</span> b)</span> -&gt; <span class="title">decltype</span><span class="params">(a + b)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">return</span> a + b;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>对于上面3个函数，<code>func1</code>是传统写法，指定返回值类型，返回值始终是<code>int</code>；</p>
<p><code>func2</code>使用<code>auto</code>推导返回值类型，但在参数列表后面指定了<code>auto</code>的类型为<code>int</code>；</p>
<p><code>func3</code>使用<code>auto</code>推导返回值类型，并在参数列表后指定<code>auto</code>的类型为使用<code>decltype</code>推导出的结果。（返回值类型应该仍然为<code>double</code>，因为<code>int+double</code>发生隐式类型转换，类型仍应该为<code>double</code>。）</p>
<p>但对于<code>func4</code>，使用<code>auto</code>推导传入的参数，同时推导返回值类型，并指定<code>auto</code>的类型为使用<code>decltype</code>推导出的结果，这样子写更加智能和方便，无需明确指定返回值类型，避免了重载减少了重复代码。</p>
<p>示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">auto</span> <span class="title">func</span><span class="params">(<span class="keyword">auto</span> a, <span class="keyword">auto</span> b)</span> -&gt; <span class="title">decltype</span><span class="params">(a + b)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">return</span> a + b;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">auto</span> a = <span class="built_in">func</span>(<span class="number">10</span>,<span class="number">90</span>);</span><br><span class="line">	cout &lt;&lt; <span class="built_in">typeid</span>(a).<span class="built_in">name</span>() &lt;&lt; endl;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">auto</span> b = <span class="built_in">func</span>(<span class="number">0.5</span>,<span class="number">10</span>);</span><br><span class="line">	cout &lt;&lt; <span class="built_in">typeid</span>(b).<span class="built_in">name</span>() &lt;&lt; endl;</span><br><span class="line"></span><br><span class="line">	<span class="type">float</span> m= <span class="number">0.123456789</span>;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">auto</span> c = <span class="built_in">func</span>(m, <span class="number">10</span>);</span><br><span class="line">	cout &lt;&lt; <span class="built_in">typeid</span>(c).<span class="built_in">name</span>() &lt;&lt; endl;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">system</span>(<span class="string">&quot;pause&gt;nul&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>输出结果：</p>
<div class="highlight-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">int</span><br><span class="line">double</span><br><span class="line">float</span><br></pre></td></tr></table></figure></div>

<h3 id="模板函数"><a href="#模板函数" class="headerlink" title="模板函数"></a>模板函数</h3><hr>
<p>对于模板函数，再无法准确判定返回值类型的情况下，也可以使用<code>auto</code>和<code>decltype</code>使编写代码更加方便和和智能。</p>
<p>示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">template</span>&lt;<span class="keyword">class</span> T1, <span class="keyword">class</span> T2&gt;</span></span><br><span class="line"><span class="function"><span class="keyword">auto</span> <span class="title">func</span><span class="params">(T1 <span class="type">const</span>&amp; t1, <span class="type">const</span> T2&amp; t2)</span> -&gt; <span class="title">decltype</span><span class="params">(t1* t2)</span><span class="comment">//右值引用，使用const修饰，前后两个位置都可以</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">return</span> t1 * t2;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">auto</span> a= <span class="built_in">func</span>(<span class="number">10</span>,<span class="number">10</span>);</span><br><span class="line">	cout &lt;&lt; <span class="string">&quot;a=&quot;</span> &lt;&lt; a &lt;&lt; endl;</span><br><span class="line">	cout &lt;&lt; <span class="built_in">typeid</span>(a).<span class="built_in">name</span>() &lt;&lt; endl;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">auto</span> b = <span class="built_in">func</span>(<span class="number">0.56</span>, <span class="number">10</span>);</span><br><span class="line">	cout &lt;&lt; <span class="string">&quot;b=&quot;</span> &lt;&lt; b &lt;&lt; endl;</span><br><span class="line">	cout &lt;&lt; <span class="built_in">typeid</span>(b).<span class="built_in">name</span>() &lt;&lt; endl;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">system</span>(<span class="string">&quot;pause&gt;nul&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>这里对于模板函数的传入参数无法准确判断其相乘后的返回值类型，因此使用<code>auto</code>和<code>decltype</code>来自动推导返回值。</p>
<p>在上面的例子中也可以省略参数列表后面的 <code>-&gt; decltype(t1* t2)</code>。</p>
<p>输出结果：</p>
<div class="highlight-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">a=100</span><br><span class="line">int</span><br><span class="line">b=5.6</span><br><span class="line">double</span><br></pre></td></tr></table></figure></div>

<h3 id="其他示例"><a href="#其他示例" class="headerlink" title="其他示例"></a>其他示例</h3><hr>
<p>追踪返回类型声明的函数也会带给大家一些喜出望外的效果：</p>
<p>示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">OuterType</span></span><br><span class="line"> &#123;</span><br><span class="line">     <span class="keyword">struct</span> <span class="title class_">InnerType</span> &#123; <span class="type">int</span> i; &#125;;</span><br><span class="line">     <span class="function">InnerType <span class="title">GetInner</span><span class="params">()</span></span>;</span><br><span class="line">     InnerType it;</span><br><span class="line"> &#125;;</span><br><span class="line"></span><br><span class="line"> <span class="function"><span class="keyword">auto</span> <span class="title">OuterType::GetInner</span><span class="params">()</span>-&gt;InnerType		 <span class="comment">// 可以不写OuterType::InnerType</span></span></span><br><span class="line"><span class="function"> </span>&#123;</span><br><span class="line">     <span class="keyword">return</span> it;</span><br><span class="line"> &#125;</span><br></pre></td></tr></table></figure></div>

<p>简化函数的定义。常见于函数指针中。</p>
<p>示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;type_traits&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"> <span class="built_in">int</span> (*(*<span class="built_in">pf</span>()) ()) ()</span><br><span class="line"> &#123;</span><br><span class="line">     <span class="keyword">return</span> <span class="literal">nullptr</span>;</span><br><span class="line"> &#125;</span><br><span class="line"></span><br><span class="line"> <span class="comment">// 一个返回函数指针的函数（假设为func函数）</span></span><br><span class="line"> <span class="comment">// auto (*)()-&gt;int(*)();</span></span><br><span class="line"> <span class="comment">// 一个返回func函数的指针的函数</span></span><br><span class="line"> <span class="comment">// auto pf1()-&gt;auto (*)() -&gt; int(*)();</span></span><br><span class="line"></span><br><span class="line"> <span class="function"><span class="keyword">auto</span> <span class="title">pf1</span><span class="params">()</span> -&gt; <span class="title">auto</span><span class="params">(*)</span><span class="params">()</span> -&gt; <span class="title">int</span> <span class="params">(*)</span><span class="params">()</span></span></span><br><span class="line"><span class="function"> </span>&#123;</span><br><span class="line">     <span class="keyword">return</span> <span class="literal">nullptr</span>;</span><br><span class="line"> &#125;</span><br><span class="line"></span><br><span class="line"> <span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"> </span>&#123;</span><br><span class="line">     cout &lt;&lt; is_same&lt;<span class="keyword">decltype</span>(pf), <span class="keyword">decltype</span>(pf1)&gt;::value &lt;&lt; endl;  <span class="comment">// 1</span></span><br><span class="line"> &#125;</span><br></pre></td></tr></table></figure></div>

<p>广泛应用在转发函数中。</p>
<p>示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">double</span> <span class="title">foo</span><span class="params">(<span class="type">int</span> a)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> (<span class="type">double</span>)a + <span class="number">0.1</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">foo</span><span class="params">(<span class="type">double</span> b)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> (<span class="type">int</span>)b;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt;</span><br><span class="line"><span class="function"><span class="keyword">auto</span> <span class="title">Forward</span><span class="params">(T t)</span> -&gt; <span class="title">decltype</span><span class="params">(foo(t))</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">foo</span>(t);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    cout &lt;&lt; <span class="built_in">Forward</span>(<span class="number">2</span>) &lt;&lt; endl;    <span class="comment">// 2.1</span></span><br><span class="line">    cout &lt;&lt; <span class="built_in">Forward</span>(<span class="number">0.5</span>) &lt;&lt; endl;  <span class="comment">// 0</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>参考链接：<a class="link"   href="https://blog.csdn.net/qq_45254369/article/details/127452583" > C++11之追踪返回类型_追踪返回值类型_林夕07的博客-CSDN博客 <i class="fa-regular fa-arrow-up-right-from-square fa-sm"></i></a></p>
<h1 id="范围for循环"><a href="#范围for循环" class="headerlink" title="范围for循环"></a>范围<code>for</code>循环</h1><p>传统<code>for</code>循环：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="type">int</span> a[]= &#123; <span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>&#125;;</span><br><span class="line">    <span class="type">int</span> n = <span class="built_in">sizeof</span>(a) / <span class="built_in">sizeof</span>(*a);</span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> i= <span class="number">0</span>; i&lt;n; ++i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="type">int</span> tmp = a[i];</span><br><span class="line">        cout &lt;&lt; tmp&lt;&lt; <span class="string">&quot;, &quot;</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	cout &lt;&lt; endl;</span><br><span class="line">	</span><br><span class="line">	<span class="built_in">system</span>(<span class="string">&quot;pause&quot;</span>);<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>C++11范围<code>for</code>循环：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="type">int</span> a[]= &#123; <span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>&#125;;</span><br><span class="line">    </span><br><span class="line">	<span class="keyword">for</span> (<span class="type">int</span> tmp : a)</span><br><span class="line">    &#123;</span><br><span class="line">        cout &lt;&lt; tmp &lt;&lt; <span class="string">&quot;,&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    cout &lt;&lt;endl;</span><br><span class="line">    </span><br><span class="line">	<span class="built_in">system</span>(<span class="string">&quot;pause&quot;</span>);<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>上面两中方式的输出效果是一样的。</p>
<p>同时，范围<code>for</code>循环可以使用引用接受参数，达到修改的目的：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="type">int</span> a[]= &#123; <span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>&#125;;</span><br><span class="line">    </span><br><span class="line">	<span class="keyword">for</span> (<span class="type">int</span>&amp; tmp : a)</span><br><span class="line">    &#123;</span><br><span class="line">        tmp++;<span class="comment">//数组每个值都加1</span></span><br><span class="line">    &#125;</span><br><span class="line">    cout &lt;&lt;endl;</span><br><span class="line">    </span><br><span class="line">	<span class="built_in">system</span>(<span class="string">&quot;pause&quot;</span>);<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>注意！遍历对象必须是确定范围的，如果将一个数组作为参数传入一个函数，那么在函数体中是无法使用范围<code>for</code>循环遍历的，因为数组传入的是首地址，范围大小是不确定的。</p>
<p>范围<code>for</code>循环适用多种容器，包括<code>STL</code>中的大部分容器，使用范围<code>for</code>循环的代码更加简洁明了，可读性更高，使用起来也比传统<code>for</code>循环简单很多。</p>
<h1 id="静态断言"><a href="#静态断言" class="headerlink" title="静态断言"></a>静态断言</h1><p><code>C++</code>提供了调试工具<code>assert()</code>，这是一个宏，用于在&#x3D;&#x3D;运行阶段&#x3D;&#x3D;对断言进行检查，如果条件为真，执行程序，否则调用<code>abort()</code>使程序中断。</p>
<p><code>C++11</code>新增了关键字<code>static_assert</code>，可用于在&#x3D;&#x3D;编译阶段&#x3D;&#x3D;对断言进行测试。</p>
<p>语法：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">static_assert</span>(bool_constexpr,<span class="string">&quot;message&quot;</span>);</span><br><span class="line"><span class="comment">//bool_constexpr必须是常量表达式条件</span></span><br><span class="line"><span class="comment">//message是发生断言时提示的错误信息，只能是双引号包含的字符串文本</span></span><br></pre></td></tr></table></figure></div>

<ul>
<li>静态断言在编译时进行，断言在运行时进行。</li>
<li>因为在编译时不产生目标代码，所以静态断言基本不影响程序运行性能。</li>
<li>静态断言只能在32位机器上进行。</li>
<li>当<code>bool_constexpr</code>为假时，输出错误信息。</li>
<li>静态断言判断表达式涉及模板时，会等待模板实例化后再做判断。</li>
</ul>
<p>静态断言的好处：</p>
<ul>
<li>更早的报告错误，我们知道构建是早于运行的，更早的错误报告意味着开发成本的降低</li>
<li>减少运行时开销，静态断言是编译期检测的，减少了运行时开销</li>
</ul>
<h1 id="使用nullptr取代NULL"><a href="#使用nullptr取代NULL" class="headerlink" title="使用nullptr取代NULL"></a>使用<code>nullptr</code>取代<code>NULL</code></h1><p>在某些情况下<code>int</code> 类型和<code>NULL</code>会产生二义性，因为实际上<code>NULL</code>等于<code>0</code>，大多数情况下编译器会将之看作整形，<code>NULL</code>只是是一个<code>#define NULL 0</code>的宏定义，可以同时赋值给指针和普通变量。当<code>NULL</code>作为参数给函数传参时，编译器会理解成<code>int</code>类型。</p>
<p>为了避免这种二义性的产生，在给指针置空时，更推荐使用<code>nullptr</code>。</p>
<p><code>nullptr</code>是专门用于给指针置空的关键字，尽管表达式：<code>NULL==nullptr</code>的结果为真，但<code>nullptr</code>只能给指针赋值。</p>
<p>总之，在给指针置空时，推荐使用<code>nullptr</code>而不是<code>NULL</code>。</p>
<h1 id="强类型枚举"><a href="#强类型枚举" class="headerlink" title="强类型枚举"></a>强类型枚举</h1><h2 id="传统枚举的局限性"><a href="#传统枚举的局限性" class="headerlink" title="传统枚举的局限性"></a>传统枚举的局限性</h2><hr>
<ul>
<li><p>传统枚举方式枚举成员的作用域在同一作用域下是共享的，无法同时定义两个含有相同名称的枚举成员。</p>
<p>以下情况会报错：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">enum</span> <span class="title class_">Status1</span> &#123;Ok, Error&#125;;</span><br><span class="line">    <span class="keyword">enum</span> <span class="title class_">Status2</span> &#123;Ok, Error&#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>上述代码编译会报错，因为在<code>main</code>函数作用域下，传统枚举成员的作用域是共享的，会发生重定义错误。</p>
<p>当然可以通过多种方式解决这个问题，但会让代码变得繁琐和冗余，可读性下降。</p>
</li>
<li><p>传统枚举不能指定枚举成员类型，默认指定为<code>int</code>整形，因此在作为参数传参等场景下会发生隐式类型转换，导致精度丢失、数据丢失等问题。</p>
</li>
</ul>
<p>详见：<a class="link"   href="https://www.bilibili.com/video/BV19T411T71n?vd_source=03da7de0e80f9ff60fdeca7b512ba3f5" >C++ 你所不知道的枚举类型（ENUM） <i class="fa-regular fa-arrow-up-right-from-square fa-sm"></i></a></p>
<h2 id="C-11强类型枚举"><a href="#C-11强类型枚举" class="headerlink" title="C++11强类型枚举"></a><code>C++11</code>强类型枚举</h2><hr>
<p>为了解决上述问题，C++11引入了强类型枚举，解决了枚举成员作用域共享和枚举成员类型不能指定的问题。</p>
<p>语法：</p>
<ul>
<li><p>在<code>enum</code>和枚举名称之间加上<code>class</code>或<code>struct</code>修饰，这两种修饰方式无区别。</p>
</li>
<li><p>在枚举名称后加上“<code>:</code>”和数据类型，可以指定该枚举的枚举成员为冒号后的数据类型。</p>
</li>
<li><p>若不指定枚举成员的数据类型，则仍然默认为<code>int</code>类型。</p>
</li>
<li><p>如果不对枚举成员赋初始值，则按传统枚举默认赋值的规则给枚举成员进行赋值。</p>
</li>
<li><p>指定的枚举成员类型其基础类型必须是整形。如：<code>char</code>、<code>long</code>、<code>long long</code>、<code>unsigned int</code>等。但<code>float</code>、<code>double</code>就不可以，因为其基础类型为浮点型。</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">enum class</span> <span class="title class_">Status</span> &#123; Ok=<span class="number">1</span>, Error &#125;;<span class="comment">//默认 int 类型，默认值：Ok=1、Error=2（Ok+1）</span></span><br><span class="line"><span class="keyword">enum struct</span> <span class="title class_">Status2</span> &#123; Ok=<span class="number">1</span>, Error=<span class="number">9</span>, No&#125;;<span class="comment">//默认 int 类型，并赋值O k=1、Error=9，No没有赋值，则No=10（Error+1）</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">enum struct</span> <span class="title class_">Status3</span> : <span class="type">char</span> &#123; Ok, Error=<span class="string">&#x27;a&#x27;</span>, No&#125;;</span><br><span class="line"><span class="comment">//指定 Status3 的枚举成员为 char 型，并给Error赋值为&#x27;a&#x27;，Ok和No没有赋值，则Ok=0（ASCLL码值对应NULL），NO=98（即：a的ASCLL码+1，对应字符为b）</span></span><br></pre></td></tr></table></figure></div>
</li>
<li><p>获取枚举成员必须要在枚举名称之前加上作用域</p>
</li>
<li><p>获取到的枚举成员类型为该枚举类型，而非指定的枚举成员类型，但其记录的值不变，使用时需要将枚举成员进行强转。</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">enum class</span> <span class="title class_">test</span> :<span class="type">char</span></span><br><span class="line">&#123;</span><br><span class="line">    ok=<span class="string">&#x27;o&#x27;</span>, no=<span class="string">&#x27;n&#x27;</span>, yes=<span class="string">&#x27;y&#x27;</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">enum class</span> <span class="title class_">test</span> resut;<span class="comment">//定义枚举变量resut，class可省略，或enum和class同时省略</span></span><br><span class="line"><span class="comment">/*下面两种定义方式也可以*/</span></span><br><span class="line"><span class="keyword">enum</span> <span class="title class_">test</span> resut1;</span><br><span class="line">test resut2;</span><br><span class="line"></span><br><span class="line">resut=test::yes;<span class="comment">//给枚举变量赋初值，只能是枚举中的枚举成员，但枚举成员名前必须加上作用域，因为强类型枚举的枚举成员作用域是不共享的</span></span><br><span class="line"></span><br><span class="line"><span class="comment">//cout&lt;&lt; resut &lt;&lt;endl;//错误，因为resut实际上是 test 枚举类型，需要强转成 char 型才能获取期望的结果</span></span><br><span class="line">cout &lt;&lt; (<span class="type">char</span>)resut &lt;&lt; endl;<span class="comment">//C风格强转</span></span><br><span class="line">cout &lt;&lt; <span class="built_in">static_cast</span>&lt;<span class="type">char</span>&gt;(test::no) &lt;&lt; endl; <span class="comment">//推荐使用C++的类型转换</span></span><br></pre></td></tr></table></figure></div>

<p>输出结果：</p>
<div class="highlight-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">y</span><br><span class="line">n</span><br></pre></td></tr></table></figure></div></li>
</ul>
<p>除了上述提到的一些优点，指定数据类型还可以突破原本<code>int</code>类型的最大长度限制，如：指定枚举成员类型为<code>unsigned long long</code></p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">enum class</span> <span class="title class_">test</span> :<span class="type">unsigned</span> <span class="type">long</span> <span class="type">long</span></span><br><span class="line">&#123;</span><br><span class="line">    ok, no=<span class="number">9999999999999999999</span><span class="comment">//已经远远超过 int 所能表示的最大值</span></span><br><span class="line">&#125;;</span><br><span class="line">cout &lt;&lt; <span class="built_in">static_cast</span>&lt;<span class="type">unsigned</span> <span class="type">long</span> <span class="type">long</span>&gt;(test::no) &lt;&lt; endl;</span><br></pre></td></tr></table></figure></div>

<p>输出结果：</p>
<div class="highlight-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">9999999999999999999</span><br></pre></td></tr></table></figure></div>

<h2 id="前置声明的写法"><a href="#前置声明的写法" class="headerlink" title="前置声明的写法"></a>前置声明的写法</h2><hr>
<p>对于前置声明，以下写法将被接受：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line">enmu <span class="keyword">class</span> <span class="title class_">Clolor</span>:<span class="type">char</span>;            		<span class="comment">//前置声明枚举类</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">Foo</span><span class="params">(Color*p)</span></span>;                     	<span class="comment">//前置声明的使用</span></span><br><span class="line"><span class="comment">//....................</span></span><br><span class="line"><span class="keyword">enum class</span> <span class="title class_">Color</span>:<span class="type">char</span>&#123;RED, GREEN, BLACK, WHITE&#125;;      <span class="comment">//前置声明的定义</span></span><br></pre></td></tr></table></figure></div>

<p>分文件编写：</p>
<p>在头文件中：</p>
<div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">enum class</span> <span class="title class_">LogLevel</span>;<span class="comment">//前置声明，并默认成员类型为int</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Logger</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// ........</span></span><br><span class="line">    </span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">logging</span><span class="params">(<span class="keyword">enum</span> <span class="keyword">class</span> LogLevel level, String message)</span></span>;<span class="comment">//enum class可省略</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br></pre></td></tr></table></figure></div>

<p>在源文件中：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&quot;头文件&quot;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">//枚举的实现</span></span><br><span class="line"><span class="keyword">enum class</span> <span class="title class_">LogLevel</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// ........</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// ........</span></span><br></pre></td></tr></table></figure></div>

<h2 id="参考"><a href="#参考" class="headerlink" title="参考"></a>参考</h2><p><a class="link"   href="https://zhuanlan.zhihu.com/p/164634712" >C++干货系列——“强”枚举有多强 <i class="fa-regular fa-arrow-up-right-from-square fa-sm"></i></a><br><a class="link"   href="https://www.runoob.com/w3cnote/cpp-enums-intro-and-strongly-typed.html" >C++ 之 enum 枚举量声明、定义、使用与强类型枚举详解 <i class="fa-regular fa-arrow-up-right-from-square fa-sm"></i></a><br><a class="link"   href="https://blog.csdn.net/daoming1112/article/details/54932881" >C++11强类型枚举 <i class="fa-regular fa-arrow-up-right-from-square fa-sm"></i></a><br>推荐：<a class="link"   href="https://www.bilibili.com/video/BV1da411K7Kp?vd_source=03da7de0e80f9ff60fdeca7b512ba3f5" >明解 C++ Primer：19.3枚举类型 <i class="fa-regular fa-arrow-up-right-from-square fa-sm"></i></a></p>
<h1 id="constexpr修饰的变量和表达式"><a href="#constexpr修饰的变量和表达式" class="headerlink" title="constexpr修饰的变量和表达式"></a><code>constexpr</code>修饰的变量和表达式</h1><h2 id="概念和使用方法"><a href="#概念和使用方法" class="headerlink" title="概念和使用方法"></a>概念和使用方法</h2><hr>
<p><code>constexpr</code>修饰的变量和表达式称为常量表达式。</p>
<p>常量表达式主要是允许一些计算发生在编译时，即发生在代码编译而不是运行的时候。</p>
<p>这是很大的优化：假如有些事情可以在编译时做，它将只做一次，而不是每次程序运行时都计算。</p>
<p>关键字：<code>constexpr</code></p>
<p>用法：使用<code>constexpr</code>修饰函数、表达式或变量<br>示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">GetNum</span> <span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">return</span> <span class="number">3</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">const</span> <span class="type">int</span> <span class="title">GetNum2</span> <span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">return</span> <span class="number">3</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">constexpr</span> <span class="type">int</span> <span class="title">GetNum3</span> <span class="params">()</span><span class="comment">//常量表达式，发生在编译阶段</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">return</span> <span class="number">3</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span> <span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// enum &#123;el = GetNum() , e2&#125;;	//编译错误	//枚举成员初始化，必须是整型常量</span></span><br><span class="line">    <span class="comment">// enum &#123;el = GetNum2() , e2&#125;; 	//加上const，同样编译错误，因为GetNum2在运行时才调用</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">enum</span> &#123;e1=<span class="built_in">GetNum3</span>() , e2&#125;; <span class="comment">//正确，编译通过，因为 GetNum3() 在编译阶段发生调用</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">constexpr</span> <span class="type">int</span> tmp = <span class="built_in">GetNum3</span>();<span class="comment">//全部发生在编译阶段</span></span><br><span class="line">    <span class="keyword">enum</span> &#123;al = tmp, a2&#125;;<span class="comment">//正确，编译通过</span></span><br><span class="line">    </span><br><span class="line">    </span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<h2 id="constexpr修饰函数的限制"><a href="#constexpr修饰函数的限制" class="headerlink" title="constexpr修饰函数的限制"></a><code>constexpr</code>修饰函数的限制</h2><hr>
<p>只对于<code>C++11</code>标准，有以下限制：</p>
<ul>
<li><p>函数中只能有一个<code>return</code>的语句，不允许有其他除了<code>ruturn</code>以外的语句（有少数特例，允许包括：<code>using</code>指令、静态断言、<code>typedef</code>）</p>
</li>
<li><p>函数必须返回值（不能是无返回值的<code>void</code>函数)</p>
</li>
<li><p>在使用前必须已有定义</p>
</li>
<li><p><code>return</code>返回语句表达式中不能使用&#x3D;&#x3D;非常量&#x3D;&#x3D;表达式的函数、全局数据，且必须是一个常量表达式</p>
</li>
</ul>
<p>在<code>C++11</code>以后的标准中（截至<code>C++20</code>）上述所有限制都不成立，使用起来和普通函数没有什么区别，对于特殊场景编译器会自动选择并优化或提示错误（参考上述示例的使用场景），这里不做过多讲述。</p>
<h2 id="constexpr修饰类的成员函数"><a href="#constexpr修饰类的成员函数" class="headerlink" title="constexpr修饰类的成员函数"></a><code>constexpr</code>修饰类的成员函数</h2><hr>
<p>在<code>C++11</code>以后的标准中（截至<code>C++20</code>）上述所有限制都不成立，使用起来和普通成员函数没有什么区别，对于特殊场景编译器会自动选择并优化，这里不做过多讲述。</p>
<h1 id="用户自定义字面量"><a href="#用户自定义字面量" class="headerlink" title="用户自定义字面量"></a>用户自定义字面量</h1><p>复制自文章：<a class="link"   href="https://zhuanlan.zhihu.com/p/111369693" >C++11新特性2 - 用户定义字面量 <i class="fa-regular fa-arrow-up-right-from-square fa-sm"></i></a>，并稍加增改。</p>
<h2 id="C-中的字面量"><a href="#C-中的字面量" class="headerlink" title="C++中的字面量"></a>C++中的字面量</h2><hr>
<p><code>C++</code> 自带4种字面量：</p>
<ul>
<li>整形 <code>123</code></li>
<li>浮点型 <code>12.3</code></li>
<li>字符 <code>&#39;1&#39;</code></li>
<li>字符串 <code>&quot;123&quot;</code></li>
</ul>
<p>字面量又可添加后缀来表明具体类型：</p>
<ul>
<li>无符号整形(<code>unsigned int</code>): <code>123u</code></li>
<li>长整形(<code>long</code>): <code>123l</code></li>
</ul>
<p>在 <code>C++03</code> 中，我们可以定义一个浮点数height</p>
<div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">double</span> height = <span class="number">3.4</span></span><br></pre></td></tr></table></figure></div>

<p>那么，痛点来了，此处的<code>height</code>的单位是什么呢？米？厘米？又或是英尺？</p>
<p>在面对此类问题时，如果我们能编写如下代码，事情就会简单许多:</p>
<div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line">height = <span class="number">3</span>cm;</span><br><span class="line"></span><br><span class="line"><span class="comment">// ratio = (3 * 10) / 2</span></span><br><span class="line">ratio = <span class="number">3</span>cm / <span class="number">2</span>mm;</span><br></pre></td></tr></table></figure></div>

<h2 id="用户自定义字面量后缀"><a href="#用户自定义字面量后缀" class="headerlink" title="用户自定义字面量后缀"></a>用户自定义字面量后缀</h2><hr>
<div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">long</span> <span class="type">double</span> <span class="keyword">operator</span><span class="string">&quot;&quot;</span> _cm(<span class="type">long</span> <span class="type">double</span> x) </span><br><span class="line">&#123;</span><br><span class="line">  <span class="keyword">return</span> x * <span class="number">10</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">long</span> <span class="type">double</span> <span class="keyword">operator</span><span class="string">&quot;&quot;</span> _m(<span class="type">long</span> <span class="type">double</span> x) </span><br><span class="line">&#123;</span><br><span class="line">  <span class="keyword">return</span> x * <span class="number">1000</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">long</span> <span class="type">double</span> <span class="keyword">operator</span><span class="string">&quot;&quot;</span> _mm(<span class="type">long</span> <span class="type">double</span> x) </span><br><span class="line">&#123;</span><br><span class="line">  <span class="keyword">return</span> x;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// height = 30.0</span></span><br><span class="line"><span class="keyword">auto</span> height = <span class="number">3.0</span>_cm; </span><br><span class="line"></span><br><span class="line"><span class="comment">// length = 1230.0</span></span><br><span class="line"><span class="keyword">auto</span> length = <span class="number">1.23</span>_m;</span><br></pre></td></tr></table></figure></div>

<p>如果使用这种写法，<code>_cm</code>, <code>_m</code>, <code>_mm</code> 等函数将在运行时被调用，如果希望在编译时就调用字面量后缀函数，则需要把函数定义为 <code>constexpr</code>，例如</p>
<div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">constexpr</span> <span class="type">long</span> <span class="type">double</span> <span class="keyword">operator</span><span class="string">&quot;&quot;</span> _cm(<span class="type">long</span> <span class="type">double</span> x) </span><br><span class="line">&#123;</span><br><span class="line">  <span class="keyword">return</span> x * <span class="number">10</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>更进一步，我们甚至可以</p>
<div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 注意，如果这里要定义函数为 constexpr</span></span><br><span class="line"><span class="comment">// 编译时需要使用 c++14 标准(-std=c++14)</span></span><br><span class="line"><span class="type">int</span> <span class="keyword">operator</span><span class="string">&quot;&quot;</span> _bin(<span class="type">const</span> <span class="type">char</span>* s, <span class="type">size_t</span> n) <span class="comment">//只需给第一个参数传参，第二个参数自动推算，测第一个参数的长度，给第二个参数赋值</span></span><br><span class="line">&#123;</span><br><span class="line">  <span class="type">int</span> ret = <span class="number">0</span>;</span><br><span class="line">  <span class="keyword">for</span> (<span class="type">int</span> i = <span class="number">0</span>; i &lt; n; i++)</span><br><span class="line">  &#123;</span><br><span class="line">    ret = (ret &lt;&lt; <span class="number">1</span>) | (s[i] - <span class="string">&#x27;0&#x27;</span>);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> ret;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> num = <span class="string">&quot;110&quot;</span>_bin; <span class="comment">// num = 6</span></span><br></pre></td></tr></table></figure></div>

<h2 id="自定义字面量的限制"><a href="#自定义字面量的限制" class="headerlink" title="自定义字面量的限制"></a><strong>自定义字面量的限制</strong></h2><hr>
<p><code>C++11 </code>只允许字面量后缀函数的参数为以下类型，即整数，浮点以及字符串：</p>
<ul>
<li><code>unsigned long long</code></li>
<li><code>long double</code></li>
<li><code>char const*</code></li>
<li><code>char const*, std::size_t</code></li>
<li><code>wchar_t const*, std::size_t</code></li>
<li><code>char16_t const*, std::size_t</code></li>
<li><code>char32_t const*, std::size_t</code></li>
</ul>
<p>返回值则无类型限制</p>
<p>参数中有<code>size_t</code>时，只需给第一个参数传参，第二个参数<code>size_t</code>自动推算。</p>
<h2 id="使用场景示例"><a href="#使用场景示例" class="headerlink" title="使用场景示例"></a>使用场景示例</h2><div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="keyword">operator</span><span class="string">&quot;&quot;</span>_cm(<span class="type">const</span> <span class="type">char</span>* str)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="function">string <span class="title">num</span><span class="params">(str)</span></span>;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">atoi</span>(num.<span class="built_in">c_str</span>());;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">auto</span> hight = <span class="number">100</span>_cm;</span><br><span class="line">    <span class="type">int</span> weight = <span class="number">200</span>_cm;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;size = &quot;</span> &lt;&lt; hight * weight  &lt;&lt; endl;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>输出结果：</p>
<div class="highlight-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">size = 20000</span><br></pre></td></tr></table></figure></div>

<h1 id="右值引用"><a href="#右值引用" class="headerlink" title="右值引用"></a>右值引用</h1><h2 id="左值和右值的概念"><a href="#左值和右值的概念" class="headerlink" title="左值和右值的概念"></a>左值和右值的概念</h2><p>在 C++ 或者 C 语言中，一个表达式（可以是字面量、变量、对象、函数的返回值等）根据其使用场景不同，分为左值表达式和右值表达式。</p>
<p>左值指既能够出现在等号左边，也能出现在等号右边的变量；右值则是只能出现在等号右边的变量。</p>
<ul>
<li>左值是可寻址的变量，有持久性；</li>
<li>右值一般是不可寻址的常量，或在表达式求值过程中创建的无名临时对象，短暂性的。</li>
</ul>
<p>左值和右值主要的区别之一是左值可以被修改，而右值不能。</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> a; <span class="comment">// a 为左值</span></span><br><span class="line">a = <span class="number">3</span>; <span class="comment">// 3 为右值</span></span><br></pre></td></tr></table></figure></div>

<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">func</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> a;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">mian</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="type">int</span> a = <span class="built_in">func</span>();<span class="comment">//a为右值，func的返回值是右值，无法直接对func的返回值进行修改</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<h2 id="左值引用和右值引用"><a href="#左值引用和右值引用" class="headerlink" title="左值引用和右值引用"></a>左值引用和右值引用</h2><h3 id="左值引用"><a href="#左值引用" class="headerlink" title="左值引用"></a>左值引用</h3><p>详见文件：<a href=".%5CC++%E5%9F%BA%E7%A1%80.md">C++基础.md</a>——引用篇，详解</p>
<h3 id="右值引用-1"><a href="#右值引用-1" class="headerlink" title="右值引用"></a>右值引用</h3><p><code>C++98/03</code>标准中就有引用，使用 <code>&amp;</code> 表示。但此种引用方式有一个缺陷，即正常情况下只能操作<code>C++</code>中的左值，无法对右值添加引用。</p>
<p>示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> num = <span class="number">10</span>;</span><br><span class="line"><span class="type">int</span> &amp;b = num; <span class="comment">//正确</span></span><br><span class="line"><span class="type">int</span> &amp;c = <span class="number">10</span>; <span class="comment">//错误</span></span><br></pre></td></tr></table></figure></div>

<p>如上所示，编译器允许我们为<code>num</code>左值建立一个引用，但不可以为<code>10</code>这个右值建立引用。因此，<code>C++98/03</code>标准中的引用又称为左值引用。</p>
<p>注意，虽然<code>C++98/03</code>标准不支持为右值建立非常量左值引用，但允许使用常量左值引用操作右值。也就是说，常量左值引用既可以操作左值，也可以操作右值，例如：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> num = <span class="number">10</span>;</span><br><span class="line"><span class="type">const</span> <span class="type">int</span> &amp;b = num;</span><br><span class="line"><span class="type">const</span> <span class="type">int</span> &amp;c = <span class="number">10</span>;</span><br></pre></td></tr></table></figure></div>

<p>右值往往是没有名称的，因此要使用它只能借助引用的方式。</p>
<p>这就产生一个问题，实际开发中我们可能需要对右值进行修改（实现移动语义时就需要），显然左值引用的方式是行不通的。</p>
<p><code>C++11</code>标准新引入了另一种引用方式，称为右值引用，用 <code>&amp;&amp;</code>表示。</p>
<p><code>C++</code>标准委员会在选定右值引用符号时，既希望能选用现有<code>C++</code>内部已有的符号，还不能与<code>C++ 98 /03</code> 标准产生冲突，最终选定了 2 个 <code>&amp;</code> 表示右值引用。</p>
<p>需要注意的，和声明左值引用一样，右值引用也必须立即进行初始化操作，且只能使用右值进行初始化，比如：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> num = <span class="number">10</span>;</span><br><span class="line"><span class="type">int</span> &amp;&amp; a = num;  <span class="comment">//错误，左值不能被右值引用</span></span><br><span class="line"><span class="type">int</span> &amp;&amp; a = <span class="number">10</span>;</span><br></pre></td></tr></table></figure></div>

<p>和常量左值引用不同的是，右值引用还可以对右值进行修改。例如：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> &amp;&amp; a = <span class="number">10</span>;</span><br><span class="line">a = <span class="number">100</span>;</span><br><span class="line">cout &lt;&lt; a &lt;&lt; endl;</span><br></pre></td></tr></table></figure></div>

<p>程序输出结果为 <code>100</code>。</p>
<p><code>C++</code>语法上是支持定义常量右值引用的，例如：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">const</span> <span class="type">int</span>&amp;&amp; a = <span class="number">10</span>;</span><br></pre></td></tr></table></figure></div>

<p>但这种定义出来的右值引用并无实际用处。</p>
<p>一方面，右值引用主要用于移动语义和完美转发，其中前者需要有修改右值的权限</p>
<p>其次，常量右值引用的作用就是引用一个不可修改的右值，这项工作完全可以交给常量左值引用完成。</p>
<table>
<caption>
C++左值引用和右值引用</caption>
<tbody>
<tr>
<th colspan="1" rowspan="2">
引用类型</th>
<th colspan="4" rowspan="1">
可以引用的值类型</th>
<th colspan="1" rowspan="2">
使用场景</th>
</tr>
<tr>
<th>
非常量左值</th>
<th>
常量左值</th>
<th>
非常量右值</th>
<th>
常量右值</th>
</tr>
<tr>
<td>
非常量左值引用</td>
<td>
Y</td>
<td>
N</td>
<td>
N</td>
<td>
N</td>
<td>
无</td>
</tr>
<tr>
<td>
常量左值引用</td>
<td>
Y</td>
<td>
Y</td>
<td>
Y</td>
<td>
Y</td>
<td>
常用于类中构建拷贝构造函数</td>
</tr>
<tr>
<td>
非常量右值引用</td>
<td>
N</td>
<td>
N</td>
<td>
Y</td>
<td>
N</td>
<td>
移动语义、完美转发</td>
</tr>
<tr>
<td>
常量右值引用</td>
<td>
N</td>
<td>
N</td>
<td>
Y</td>
<td>
Y</td>
<td>
无实际用途</td>
</tr>
</tbody>
</table>

<p>表中，Y 表示支持，N 表示不支持。</p>
<p><code>C++11</code>标准中对右值做了更细致的划分，分别称为纯右值（Pure value，简称 pvalue）和将亡值（eXpiring value，简称 xvalue ）。</p>
<p>其中纯右值就是<code>C++98/03</code>标准中的右值，而将亡值则指的是和右值引用相关的表达式（比如某函数返回的<code>T &amp;&amp;</code>类型的表达式）。</p>
<h1 id="移动语义"><a href="#移动语义" class="headerlink" title="移动语义"></a>移动语义</h1><h2 id="为什么需要移动语义"><a href="#为什么需要移动语义" class="headerlink" title="为什么需要移动语义"></a>为什么需要移动语义</h2><p>如果一个对象中有堆区资源，需要编写拷贝构造函数和赋值函数，实现深拷贝。</p>
<p>深拷贝把对象中的堆区资源复制了一份，如果源对象（被拷贝的对象）是临时对象，拷贝完就没什么用了，这样会造成没有意义的资源申请和释放操作。</p>
<p>同时临时对象的维护（创建和销毁）对性能有严重影响。</p>
<p>如果能够直接使用源对象拥有的资源，可以节省资源申请和释放的时间。</p>
<p>C++11新增加的移动语义就能够做到这一点。</p>
<p>实现移动语义要增加两个函数：移动构造函数和移动赋值函数。</p>
<p>转移语义可以将资源（堆，系统对象等）从一个对象转移到另一个对象，这样能够减少不必要的临时对象的创建、拷贝以及销毁，能够大幅度提高C++应用程序的性能。</p>
<p>右值引用就是用来支持转移语义的。</p>
<p>转移语义是和拷贝语义相对的，可以类比文件的剪切与拷贝，当我们将文件从一个目录拷贝到另一个目录时，速度比剪切慢很多。</p>
<p>通过转移语义，临时对象中的资源能够转移其它的对象里。</p>
<h2 id="转移构造函数"><a href="#转移构造函数" class="headerlink" title="转移构造函数"></a>转移构造函数</h2><p>移动构造函数是一种特殊的构造函数，用于在对象之间进行资源的移动而不是复制。</p>
<p>移动构造函数通常用于以下情况：</p>
<ul>
<li>当使用右值引用（使用双&amp;&amp;符号）绑定到一个临时对象时，移动构造函数将被调用。</li>
<li>当使用<code>std::move</code>函数将一个对象强制转换为右值引用时，移动构造函数将被调用。</li>
</ul>
<p>移动构造函数的目的是将资源（例如堆分配的内存、文件句柄等）从一个对象转移到另一个对象，而不是复制这些资源。</p>
<p>通过移动而不是复制资源，可以显著提高性能，避免不必要的内存分配和复制操作。</p>
<p>语法：</p>
<p>移动构造函数的语法与普通构造函数的语法非常相似，只是在参数列表前加上了双&amp;&amp;符号来表示右值引用。</p>
<div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="built_in">ClassName</span>(ClassName&amp;&amp; other);  <span class="comment">// 移动构造函数</span></span><br></pre></td></tr></table></figure></div>

<p>在移动构造函数中，参数<code>other</code>是一个右值引用，表示要移动资源的对象。在函数体内，你可以使用移动操作符<code>std::move</code>来将资源从<code>other</code>对象转移给当前对象。</p>
<p>移动构造函数通常会将源对象的成员指针设置为<code>nullptr</code>，以避免在析构函数中重复释放资源。</p>
<p>这是因为资源的所有权已经转移到了目标对象。</p>
<p>移动构造函数通常也可以声明为<code>noexcept</code>，以表明它不会抛出异常。这对于某些情况下的优化和语义保证很重要。</p>
<p>下面是一个完整的示例，展示了移动构造函数的语法和用法：</p>
<div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">MyClass</span> </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="type">int</span>* data;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 默认构造函数</span></span><br><span class="line">    <span class="built_in">MyClass</span>() : <span class="built_in">data</span>(<span class="literal">nullptr</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;Default constructor called.&quot;</span> &lt;&lt; std::endl;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 移动构造函数</span></span><br><span class="line">    <span class="built_in">MyClass</span>(MyClass&amp;&amp; other) <span class="keyword">noexcept</span> : <span class="built_in">data</span>(<span class="literal">nullptr</span>) </span><br><span class="line">    &#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;Move constructor called.&quot;</span> &lt;&lt; std::endl;</span><br><span class="line">        data = other.data;</span><br><span class="line">        other.data = <span class="literal">nullptr</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 析构函数</span></span><br><span class="line">    ~<span class="built_in">MyClass</span>() </span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (data != <span class="literal">nullptr</span>) </span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">delete</span> data;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    MyClass source;</span><br><span class="line">    source.data = <span class="keyword">new</span> <span class="built_in">int</span>(<span class="number">42</span>);</span><br><span class="line"></span><br><span class="line">    <span class="function">MyClass <span class="title">destination</span><span class="params">(std::move(source))</span></span>;</span><br><span class="line"></span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;source.data: &quot;</span> &lt;&lt; source.data &lt;&lt; std::endl;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;destination.data: &quot;</span> &lt;&lt; destination.data &lt;&lt; std::endl;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>在上面的示例中，我们定义了一个<code>MyClass</code>类，包含了一个指针成员变量<code>data</code>。我们使用移动构造函数将资源从<code>source</code>对象移动到<code>destination</code>对象。</p>
<p>在移动构造函数的参数列表中，使用了<code>ClassName&amp;&amp;</code>的形式来表示右值引用。</p>
<p>需要注意，一旦资源被移动到目标对象，源对象的资源将不再有效。</p>
<p>因此，应该小心使用移动构造函数，确保正确处理资源所有权和对象的状态。</p>
<h2 id="转移赋值函数"><a href="#转移赋值函数" class="headerlink" title="转移赋值函数"></a>转移赋值函数</h2><p>和拷贝构造函数类似，有几点需要注意：</p>
<ul>
<li>参数（右值）的符号必须是右值引用符号，即“&amp;&amp;”。</li>
<li>参数（右值）不可以是常量，因为我们需要修改右值。</li>
<li>参数(右值)的资源链接和标记必须修改，否则，右值的析构函数就会释放资源，转移到新对象的资源也就无效了。</li>
</ul>
<p>有了右值引用和转移语义，我们在设计和实现类时，对于需要动态申请大量资源的类，应该设计转移构造函数和转移赋值函数，以提高应用程序的效率。</p>
<h2 id="std-move"><a href="#std-move" class="headerlink" title="std::move"></a><code>std::move</code></h2><p>对于一个左值，会调用拷贝构造函数，但是有些左值是局部变量，生命周期也很短，能不能也移动而不是拷贝呢?</p>
<p>C++11为了解决这个问题，提供了<code>std:move()</code>方法来将左值转义为右值，从而方便使用移动语义。</p>
<p>它其实就是告诉编译器，虽然我是一个左值，但不要对我用拷贝构造函数，用移动构造函数。</p>
<p>左值对象被转移资源后，不会立刻析构，只有在离开自己的作用域的时候才会析构，如果继续使用左值中的资源，可能会发生意想不到的错误。</p>
<p>如果没有提供移动构造&#x2F;赋值函数，只提供了拷贝构造&#x2F;赋值函数，编译器找不到移动构造&#x2F;赋值函数就去寻找拷贝构造&#x2F;赋值函数。</p>
<p>C++11中的所有容器都实现了移动语义，避免对含有资源的对象发生无谓的拷贝。</p>
<p>移动语义对于拥有资源（如内存、文件句柄)的对象有效，如果是基本类型，使用移动语义没有意义。</p>
<p>示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> a;</span><br><span class="line"><span class="type">int</span> &amp;&amp;r1 = a;<span class="comment">//编译失败</span></span><br><span class="line"><span class="type">int</span> &amp;&amp;r2 = std:<span class="built_in">move</span>(a);<span class="comment">//编译通过</span></span><br></pre></td></tr></table></figure></div>

<h1 id="完美转发"><a href="#完美转发" class="headerlink" title="完美转发"></a>完美转发</h1><p>完美转发适用于这样的场景：</p>
<p>需要将一组参数原封不动的传递给另一个函数。</p>
<p>“原封不动”不仅仅是参数的值不变，在C++中，除了参数值之外，还有一下两组属性：</p>
<p>左值&#x2F;右值和 const&#x2F;non-const。</p>
<p>完美转发就是在参数传递过程中，所有这些属性和参数值都不能改变，同时，而不产生额外的开销，就好像转发者不存在一样。</p>
<p>在泛型函数中，这样的需求非常普遍。</p>
<h1 id="类型转换"><a href="#类型转换" class="headerlink" title="类型转换"></a>类型转换</h1><h2 id="const-cast"><a href="#const-cast" class="headerlink" title="const_cast"></a><code>const_cast</code></h2><hr>
<p><code>const_cast</code>是<code>C++</code>中的一个类型转换运算符，用于将<code>const</code>和<code>volatile</code>限定符从指针或引用类型中移除。</p>
<p>它的语法如下：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">const_cast</span>&lt;new_type&gt;(expression)</span><br></pre></td></tr></table></figure></div>

<p>其中，<code>new_type</code>是要转换成的类型，<code>expression</code>是要转换的表达式。</p>
<p>举个例子，假设有一个<code>const int</code>类型的变量，我们想要修改它的值，可以使用<code>const_cast</code>将其转换成<code>int</code>类型，然后进行修改。</p>
<p>示例代码如下：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">const</span> <span class="type">int</span> a = <span class="number">10</span>;</span><br><span class="line"><span class="type">int</span>&amp; b = <span class="built_in">const_cast</span>&lt;<span class="type">int</span>&amp;&gt;(a);</span><br><span class="line">b = <span class="number">20</span>;</span><br><span class="line">std::cout &lt;&lt; a &lt;&lt; std::endl; <span class="comment">// 输出10</span></span><br><span class="line">std::cout &lt;&lt; b &lt;&lt; std::endl; <span class="comment">// 输出20</span></span><br></pre></td></tr></table></figure></div>

<p>需要注意的是，使用<code>const_cast</code>进行类型转换可能会导致未定义行为，因为它可以打破<code>const</code>限定符的保护。</p>
<p>因此，在使用<code>const_cast</code>时需要谨慎，确保不会对原始对象造成损害。</p>
<h2 id="reinterpret-cast"><a href="#reinterpret-cast" class="headerlink" title="reinterpret_cast"></a><code>reinterpret_cast</code></h2><hr>
<p><code>reinterpret_cast</code>是<code>C++</code>中的一个类型转换运算符，用于进行底层的类型转换。这是一个很危险的转换，编译器不会做任何类型安全的检查。</p>
<p>它可以将一个指针或引用转换为任何其他类型的指针或引用，甚至可以将一个指针转换为一个整数类型。</p>
<p>它的语法如下：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">reinterpret_cast</span>&lt;new_type&gt;(expression)</span><br></pre></td></tr></table></figure></div>

<p>其中，<code>new_type</code>是要转换成的类型，<code>expression</code>是要转换的表达式。</p>
<p>举个例子，假设有一个<code>int</code>类型的变量，我们想要将它的值转换成一个指针，可以使用<code>reinterpret_cast</code>。</p>
<p>示例代码如下：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> a = <span class="number">10</span>;</span><br><span class="line"><span class="type">int</span>* p = <span class="built_in">reinterpret_cast</span>&lt;<span class="type">int</span>*&gt;(a);</span><br><span class="line">*p = <span class="number">20</span>;</span><br><span class="line">std::cout &lt;&lt; a &lt;&lt; std::endl; <span class="comment">// 输出20</span></span><br><span class="line">std::cout &lt;&lt; *p &lt;&lt; std::endl; <span class="comment">// 输出20</span></span><br></pre></td></tr></table></figure></div>

<p>需要注意的是，使用<code>reinterpret_cast</code>进行类型转换可能会导致未定义行为，因为它可以将一个指针转换为一个不相关的类型。</p>
<p>因此，在使用<code>reinterpret_cast</code>时需要谨慎，确保转换的类型是合法的。</p>
<h2 id="dynamic-cast"><a href="#dynamic-cast" class="headerlink" title="dynamic_cast"></a><code>dynamic_cast</code></h2><hr>
<p><code>dynamic_cast</code>是<code>C++</code>中的一个类型转换运算符，用于在运行时进行类型转换。</p>
<p>它主要用于将父类指针或引用转换为子类指针或引用（这种转换也称下行转换），或者将<code>void</code>指针转换为其他类型的指针。</p>
<p>它的语法如下：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">dynamic_cast</span>&lt;new_type&gt;(expression)</span><br></pre></td></tr></table></figure></div>

<p>其中，<code>new_type</code>是要转换成的类型，<code>expression</code>是要转换的表达式。</p>
<p>举个例子，假设有一个基类<code>Animal</code>和两个派生类<code>Cat</code>和<code>Dog</code>，我们想要将一个<code>Animal</code>指针转换成<code>Cat</code>指针，可以使用<code>dynamic_cast</code>。</p>
<p>示例代码如下：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Animal</span> </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">makeSound</span><span class="params">()</span> </span>&#123;&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Cat</span> : <span class="keyword">public</span> Animal </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">makeSound</span><span class="params">()</span> <span class="keyword">override</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;Meow!&quot;</span> &lt;&lt; std::endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Dog</span> : <span class="keyword">public</span> Animal </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">makeSound</span><span class="params">()</span> <span class="keyword">override</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;Woof!&quot;</span> &lt;&lt; std::endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Animal* animal = <span class="keyword">new</span> <span class="built_in">Cat</span>();</span><br><span class="line">    Cat* cat = <span class="built_in">dynamic_cast</span>&lt;Cat*&gt;(animal);</span><br><span class="line">    <span class="keyword">if</span> (cat) </span><br><span class="line">    &#123;</span><br><span class="line">        cat-&gt;<span class="built_in">makeSound</span>(); <span class="comment">// 输出Meow!</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">delete</span> animal;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>使用<code>dynamic_cast</code>进行类型转换时，如果转换失败，会返回一个空指针或引用。</p>
<p>因此，在使用<code>dynamic_cast</code>时需要进行判断，确保转换成功。</p>
<h2 id="上行转换和下行转换"><a href="#上行转换和下行转换" class="headerlink" title="上行转换和下行转换"></a>上行转换和下行转换</h2><hr>
<p>在C++中，上行转换（upcasting）和下行转换（downcasting）是与继承和多态相关的概念。</p>
<p>上行转换是指将子类的指针或引用隐式地转换为父类的指针或引用。</p>
<p>这种转换是安全的，因为子类对象包含了父类的成员，因此可以通过父类的接口来访问子类的成员。</p>
<p>例如：</p>
<div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Animal</span> </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">eat</span><span class="params">()</span> </span></span><br><span class="line"><span class="function">     </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Animal is eating.&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Dog</span> : <span class="keyword">public</span> Animal </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">eat</span><span class="params">()</span> <span class="keyword">override</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Dog is eating.&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">bark</span><span class="params">()</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Dog is barking.&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Dog dog;</span><br><span class="line">    Animal* animalPtr = &amp;dog;  <span class="comment">// 上行转换</span></span><br><span class="line">    animalPtr-&gt;<span class="built_in">eat</span>();  <span class="comment">// 调用的是子类的 eat() 函数</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>在上面的示例中，<code>Dog</code> 类是 <code>Animal</code> 类的子类。通过将 <code>Dog</code> 类的对象的指针赋值给 <code>Animal</code> 类的指针，发生了上行转换。</p>
<p>即 <code>animalPtr</code> 指向的是 <code>Dog</code> 类的对象，但只能通过 <code>Animal</code> 类的接口来访问。</p>
<p>下行转换是指将父类的指针或引用显式地转换为子类的指针或引用。这种转换需要使用 <code>dynamic_cast</code> 运算符（下一篇将讲到），并且在运行时进行类型检查，因为并非所有父类的指针或引用都可以转换为子类类型。</p>
<p>例如：</p>
<div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Animal</span> </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">eat</span><span class="params">()</span> </span></span><br><span class="line"><span class="function">     </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Animal is eating.&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Dog</span> : <span class="keyword">public</span> Animal </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">eat</span><span class="params">()</span> <span class="keyword">override</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Dog is eating.&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">bark</span><span class="params">()</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Dog is barking.&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Animal animal;</span><br><span class="line">    Animal* animalPtr = &amp;animal;</span><br><span class="line">    Dog* dogPtr = <span class="built_in">dynamic_cast</span>&lt;Dog*&gt;(animalPtr);  <span class="comment">// 下行转换，这段代码的作用是将父类指针animalPtr转换为子类指针dogPtr</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (dogPtr != <span class="literal">nullptr</span>) </span><br><span class="line">    &#123;</span><br><span class="line">        dogPtr-&gt;<span class="built_in">bark</span>();  <span class="comment">// 通过子类指针访问子类的成员</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> </span><br><span class="line">    &#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Downcasting failed.&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>在上面的示例中，<code>dynamic_cast</code> 运算符将 <code>Animal</code> 类的指针 <code>animalPtr</code> 转换为 <code>Dog</code> 类的指针 <code>dogPtr</code>。</p>
<p>由于 <code>animalPtr</code> 实际指向的是 <code>Animal</code> 类的对象，而不是 <code>Dog</code> 类的对象，所以转换失败，<code>dogPtr</code> 的值为 <code>nullptr</code>。</p>
<p>输出结果：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line">Downcasting failed.</span><br></pre></td></tr></table></figure></div>

<p>因此，建议在进行下行转换之前，先使用 <code>dynamic_cast</code> 运算符进行类型检查，以确保转换的安全性。</p>
<p>如果不使用 <code>dynamic_cast</code> 运算符进行类型检查，虽然可以转换成功，但是当调用子类成员时程序就可能会发生错误。</p>
<p>上行转换是安全的，因为子类对象是父类对象的扩展，可以通过父类接口访问子类的成员。</p>
<p>下行转换需要进行类型检查，因为父类对象的指针或引用并不总是指向子类对象。</p>
<p>如果使用C风格的强制类型转换，将父类指针或引用直接转换为子类指针或引用，而不使用<code>dynamic_cast</code>进行类型检查，可能会导致以下问题：</p>
<ul>
<li><p>未进行类型检查：</p>
<p>C风格的强制类型转换不会进行类型检查，因此无法确保父类指针或引用指向的对象的确是子类对象。如果父类指针或引用并不指向子类对象，而你进行了转换，将会导致不可预测的行为和潜在的错误。</p>
</li>
<li><p>切割对象：</p>
<p>如果你使用强制类型转换将父类指针或引用转换为子类指针或引用，而父类对象中含有子类特有的成员或虚函数，这些子类特有的内容将会被切割掉，导致在转换后无法访问这些成员或调用这些虚函数。</p>
</li>
<li><p>安全性问题：</p>
<p>由于没有进行类型检查，使用强制类型转换可能会破坏C++的类型系统和继承体系的安全性。这可能导致程序在运行时产生未定义行为、内存访问错误或崩溃。</p>
</li>
</ul>
<p>示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Animal</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">eat</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Animal is eating.&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Dog</span> : <span class="keyword">public</span> Animal</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">eat</span><span class="params">()</span> <span class="keyword">override</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Dog is eating.&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">bark</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Dog is barking.&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">    string name = <span class="string">&quot;ggbom&quot;</span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Dog* dogPtr =(Dog*) <span class="keyword">new</span> Animal*; </span><br><span class="line">    cout &lt;&lt;<span class="string">&quot;dog&#x27;s name:&quot;</span> &lt;&lt; dogPtr-&gt;name &lt;&lt; endl;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;dog&#x27;s name length:&quot;</span> &lt;&lt; dogPtr-&gt;name.<span class="built_in">length</span>() &lt;&lt; endl;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>上述这段代码运行的结果是不确定的，有时会报错提示读取写入内存冲突，有时会使程序直接崩溃，有时则能打印出<code>dog&#39;s name:</code> 和<code>dog&#39;s name length:0</code>，但是<code>dogPtr-&gt;name</code>的部分是空白，有时则只打印<code>dog&#39;s name:</code>，然后程序直接结束。</p>
<p>因此，只在C++中使用<code>dynamic_cast</code>进行下行转换，以确保类型安全性和正确性。</p>
<p><code>dynamic_cast</code>会在运行时进行类型检查，只有在父类指针或引用确实指向子类对象时才会返回有效的子类指针或引用。。</p>
<h2 id="static-cast"><a href="#static-cast" class="headerlink" title="static_cast"></a><code>static_cast</code></h2><hr>
<p><code>static_cast</code>是<code>C++</code>中的一个类型转换运算符，用于进行静态类型转换。</p>
<p>用于一些基本类型的转换和有继承关系的父类和子类指针或引用间的转换。</p>
<p><code>static_cast</code>不会产生动态转换的类型安全检查的开销，由程序员来确保转换是安全的。</p>
<p>它的语法如下：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">static_cast</span>&lt;new_type&gt;(expression)</span><br></pre></td></tr></table></figure></div>

<p>其中，<code>new_type</code>是要转换成的类型，<code>expression</code>是要转换的表达式。</p>
<p>举个例子，假设有一个int类型的变量，我们想要将它的值转换成一个<code>double</code>类型，可以使用<code>static_cast</code>。</p>
<p>示例代码如下：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> a = <span class="number">10</span>;</span><br><span class="line"><span class="type">double</span> b = <span class="built_in">static_cast</span>&lt;<span class="type">double</span>&gt;(a);</span><br><span class="line">std::cout &lt;&lt; b &lt;&lt; std::endl; <span class="comment">// 输出10.0</span></span><br></pre></td></tr></table></figure></div>

<p>需要注意的是，使用<code>static_cast</code>进行类型转换时，如果转换不安全，可能会导致未定义行为。</p>
<p>因此，在使用<code>static_cast</code>时需要谨慎，确保转换的类型是安全的。</p>
<h1 id="lambda表达式"><a href="#lambda表达式" class="headerlink" title="lambda表达式"></a>lambda表达式</h1><p>lambda表达式是一种用于创建匿名函数的简洁语法。</p>
<p>lambda表达式可以用作函数对象，并且可以捕获上下文中的变量。</p>
<p>下面是一个使用lambda表达式的示例：</p>
<div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;functional&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="type">int</span> factor = <span class="number">2</span>;</span><br><span class="line">    std::function&lt;<span class="type">int</span>(<span class="type">int</span>)&gt; func = [factor](<span class="type">int</span> x) &#123; <span class="keyword">return</span> factor * x; &#125;;</span><br><span class="line">    std::cout &lt;&lt; <span class="built_in">func</span>(<span class="number">3</span>) &lt;&lt; std::endl;  <span class="comment">// 输出：6</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>在上面的示例中，lambda表达式<code>[factor](int x) &#123; return factor * x; &#125;</code>定义了一个函数对象，它接受一个<code>int</code>参数并返回<code>factor * x</code>的结果。</p>
<p>在lambda表达式中，我们通过方括号<code>[]</code>捕获了外部变量<code>factor</code>，以便在lambda表达式中使用。</p>
<p>lambda表达式的一般形式是：</p>
<ul>
<li><code>[捕获列表](参数列表) -&gt; 返回类型 &#123; 函数体 &#125;</code></li>
<li>其中捕获列表指定需要捕获的外部变量，参数列表指定函数的参数，返回类型指定函数的返回类型，函数体则是函数的实现。</li>
</ul>
<p>lambda表达式还支持各种功能，如可变捕获（允许修改被捕获的变量）、自动类型推导、多行函数体等。</p>
<p>它们非常灵活，并且通常与算法函数（如<code>std::for_each</code>、<code>std::transform</code>等）一起使用，用于对容器中的元素进行处理。</p>
<p>其中<code>参数列表</code>、在函数无参时可以省略，<code>-&gt;</code>、<code>返回值类型</code>也可以省略，省略后将由编译器自动推导。<code>[捕获列表]</code>和<code>&#123;函数体&#125;</code>不可以省略，因此最简单的lambda表达式的形式是这样的：<code>[]&#123;&#125;;</code>，解释：这是一个匿名函数对象，无参、无返回值、函数体内部为空实现，捕获列表为空。</p>
<p>关于捕获列表：</p>
<p>捕获列表用于指定Lambda表达式中要捕获的外部变量。</p>
<p>将lambda表达式理解成函数，如果需要在函数体内部使用到函数体外的变量和表达式，那么就需要在捕获列表中指明需要用到的表达式。</p>
<p>可以使用引用捕获或值捕获，使用引用捕获时当在函数体内部对捕获的变量进行了修改，那么外部变量原来的值也会发生改变，值捕获相当于拷贝了一份，函数体内部的操作不会影响原来的值，同样使用值捕获也不能在函数体内部对捕获的变量进行修改。</p>
<p>下面是捕获列表的几种常见写法：</p>
<ol>
<li><p><code>[&amp;]</code>：使用引用捕获方式，表示捕获外部作用域中的所有变量（按引用方式）。</p>
</li>
<li><p><code>[=]</code>：使用直接捕获方式，表示捕获外部作用域中的所有变量（按值方式）。</p>
</li>
<li><p><code>[x, &amp;y]</code>：捕获外部作用域中的变量<code>x</code>（按值方式）和变量<code>y</code>（按引用方式）。</p>
</li>
<li><p><code>[&amp;z, this]</code>：捕获外部作用域中的变量<code>z</code>（按引用方式）和当前对象指针<code>this</code>（按值方式）。</p>
</li>
<li><p><code>[]</code>：不捕获任何外部变量。</p>
</li>
</ol>
<p>在捕获列表中，<code>&amp;</code>表示按引用捕获，<code>=</code>表示按值捕获。</p>
<p>捕获列表中的变量名可以是当前作用域中的任何变量，包括函数参数、局部变量等。使用逗号将不同的变量进行分隔。</p>
<p>示例代码1：</p>
<div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="type">int</span> x = <span class="number">5</span>;</span><br><span class="line">    <span class="type">int</span> y = <span class="number">10</span>;</span><br><span class="line">    <span class="type">int</span> z = <span class="number">15</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 按引用捕获x和y，按值捕获z</span></span><br><span class="line">    <span class="keyword">auto</span> lambda1 = [&amp;x, &amp;y, z]() </span><br><span class="line">    &#123;</span><br><span class="line">        x++;</span><br><span class="line">        y++;</span><br><span class="line">        <span class="comment">// z++; // 错误，无法修改被值捕获的变量</span></span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;x: &quot;</span> &lt;&lt; x &lt;&lt; std::endl;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;y: &quot;</span> &lt;&lt; y &lt;&lt; std::endl;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;z: &quot;</span> &lt;&lt; z &lt;&lt; std::endl;</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 按值捕获x和y，不捕获其他变量</span></span><br><span class="line">    <span class="keyword">auto</span> lambda2 = [=]() </span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">// x++; // 错误，无法修改被值捕获的变量</span></span><br><span class="line">        <span class="comment">// y++; // 错误，无法修改被值捕获的变量</span></span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;x: &quot;</span> &lt;&lt; x &lt;&lt; std::endl;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;y: &quot;</span> &lt;&lt; y &lt;&lt; std::endl;</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">lambda1</span>(); <span class="comment">// 输出：x: 6, y: 11, z: 15</span></span><br><span class="line">    <span class="built_in">lambda2</span>(); <span class="comment">// 输出：x: 5, y: 10</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>在这个示例中，我们定义了两个Lambda表达式：<code>lambda1</code>和<code>lambda2</code>。<code>lambda1</code>的捕获列表使用了引用捕获和值捕获的组合，它按引用捕获了变量<code>x</code>和<code>y</code>，按值捕获了变量<code>z</code>。<code>lambda2</code>的捕获列表使用了纯值捕获，它按值捕获了变量<code>x</code>和<code>y</code>。你可以尝试运行这段代码，观察输出结果，以加深对捕获列表的理解。</p>
<p>需要注意的是，捕获列表中的变量名必须是在Lambda表达式的外部作用域中可见的。</p>
<p>对于捕获的变量，如果在Lambda表达式内部对其进行修改，使用引用捕获的变量会影响到外部作用域中的原始变量，而使用直接捕获的变量只会修改Lambda表达式内部的副本，不会影响外部作用域中的原始变量。</p>
<p>示例代码2：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="type">int</span> x = <span class="number">5</span>;</span><br><span class="line">    <span class="type">int</span> y = <span class="number">10</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 引用捕获示例</span></span><br><span class="line">    <span class="keyword">auto</span> lambdaRef = [&amp;]() </span><br><span class="line">    &#123;</span><br><span class="line">        x++;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;x: &quot;</span> &lt;&lt; x &lt;&lt; std::endl;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;y: &quot;</span> &lt;&lt; y &lt;&lt; std::endl;</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 直接捕获示例</span></span><br><span class="line">    <span class="keyword">auto</span> lambdaCopy = [=]() </span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">// y++; // 错误，无法修改被值捕获的变量</span></span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;x: &quot;</span> &lt;&lt; x &lt;&lt; std::endl;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;y: &quot;</span> &lt;&lt; y &lt;&lt; std::endl;</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">lambdaRef</span>();  <span class="comment">// 输出：x: 6, y: 10</span></span><br><span class="line">    <span class="built_in">lambdaCopy</span>(); <span class="comment">// 输出：x: 5, y: 10</span></span><br><span class="line"></span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;x: &quot;</span> &lt;&lt; x &lt;&lt; std::endl;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;y: &quot;</span> &lt;&lt; y &lt;&lt; std::endl;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure></div>









<h1 id="std-function"><a href="#std-function" class="headerlink" title="std::function"></a><code>std::function</code></h1><h2 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h2><p>当你在C++中需要将函数作为参数传递或者将其存储在容器中时，<code>std::function</code>和lambda表达式是两个非常有用的工具。</p>
<p>它们都是C++11引入的特性，可以用于创建可调用对象。</p>
<p><code>std::function</code>是一个通用的函数封装器，可以用来存储、复制和调用任何可调用对象（函数指针、函数对象、成员函数指针等）。</p>
<p>使用<code>std::function</code>，你可以将函数包装在一个类型安全的容器中，并将其传递给其他函数或算法进行处理。</p>
<p>下面是一个使用<code>std::function</code>的简单示例：</p>
<div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;functional&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">add</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> a + b;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    std::function&lt;<span class="type">int</span>(<span class="type">int</span>, <span class="type">int</span>)&gt; func = add;</span><br><span class="line">    std::cout &lt;&lt; <span class="built_in">func</span>(<span class="number">3</span>, <span class="number">4</span>) &lt;&lt; std::endl;  <span class="comment">// 输出：7</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>在上面的示例中，<code>std::function&lt;int(int, int)&gt;</code>表示一个可调用对象，它接受两个<code>int</code>参数并返回一个<code>int</code>值。</p>
<p>通过将<code>add</code>函数分配给<code>func</code>，我们可以通过<code>func(3, 4)</code>调用<code>add</code>函数。</p>
<p>在C++中，<code>std::function</code>是一个通用的函数封装器，它可以存储、复制和调用任何可调用对象，例如函数、函数指针、Lambda 表达式、成员函数指针等。<code>std::function</code>提供了一种将函数对象与其参数类型和返回类型解耦的方式，使得函数对象可以像普通函数一样传递和使用。</p>
<p><code>std::function</code>定义在头文件 <code>&lt;functional&gt;</code> 中，它是一个类模板，其基本语法如下：</p>
<div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line">std::function&lt;返回类型(参数类型)&gt; func;</span><br></pre></td></tr></table></figure></div>

<p>其中，<code>返回类型</code>是函数对象的返回类型，而<code>参数类型</code>是函数对象的参数类型。下面来详细讲解<code>std::function</code>的一些特性和用法：</p>
<h2 id="std-function的创建和调用"><a href="#std-function的创建和调用" class="headerlink" title="std::function的创建和调用"></a><code>std::function</code>的创建和调用</h2><p>要创建一个<code>std::function</code>对象，可以通过函数指针、函数对象、Lambda 表达式等来进行初始化，</p>
<p>可以像调用普通函数一样，使用括号运算符来调用<code>std::function</code>对象。</p>
<p>如下所示：</p>
<div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;functional&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 函数对象</span></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Functor</span> </span><br><span class="line">&#123;</span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">operator</span><span class="params">()</span><span class="params">(<span class="type">int</span> x, <span class="type">int</span> y)</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> x + y;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">//普通函数</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">add</span><span class="params">(<span class="type">int</span> x, <span class="type">int</span> y)</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> x + y;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    std::function&lt;<span class="type">int</span>(<span class="type">int</span>, <span class="type">int</span>)&gt; func1 = add;</span><br><span class="line">    std::function&lt;<span class="type">int</span>(<span class="type">int</span>, <span class="type">int</span>)&gt; func2 = <span class="built_in">Functor</span>();</span><br><span class="line">    std::function&lt;<span class="type">int</span>(<span class="type">int</span>, <span class="type">int</span>)&gt; func3 = [](<span class="type">int</span> x, <span class="type">int</span> y) &#123; <span class="keyword">return</span> x + y; &#125;;</span><br><span class="line">    </span><br><span class="line">    std::cout &lt;&lt; <span class="built_in">func1</span>(<span class="number">3</span>, <span class="number">5</span>) &lt;&lt; std::endl; <span class="comment">// Output: 8</span></span><br><span class="line">    std::cout &lt;&lt; <span class="built_in">func2</span>(<span class="number">3</span>, <span class="number">5</span>) &lt;&lt; std::endl; <span class="comment">// Output: 8</span></span><br><span class="line">    std::cout &lt;&lt; <span class="built_in">func3</span>(<span class="number">3</span>, <span class="number">5</span>) &lt;&lt; std::endl; <span class="comment">// Output: 8</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<h2 id="空std-function"><a href="#空std-function" class="headerlink" title="空std::function"></a>空<code>std::function</code></h2><p>如果一个<code>std::function</code>没有与之关联的可调用对象，那么它被称为空的。</p>
<p>调用一个空的<code>std::function</code>会导致<code>std::bad_function_call</code>异常的抛出。</p>
<div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;functional&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    std::function&lt;<span class="type">int</span>(<span class="type">int</span>, <span class="type">int</span>)&gt; func;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">try</span> </span><br><span class="line">    &#123;</span><br><span class="line">        <span class="type">int</span> result = <span class="built_in">func</span>(<span class="number">3</span>, <span class="number">5</span>); <span class="comment">// 调用空的std::function，会抛出std::bad_function_call异常</span></span><br><span class="line">        std::cout &lt;&lt; result &lt;&lt; std::endl;</span><br><span class="line">    &#125; </span><br><span class="line">    <span class="built_in">catch</span> (<span class="type">const</span> std::bad_function_call&amp; ex) </span><br><span class="line">    &#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;Exception caught: &quot;</span> &lt;&lt; ex.<span class="built_in">what</span>() &lt;&lt; std::endl; <span class="comment">// Output: Exception caught: bad_function_call</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<h2 id="std-function与多态"><a href="#std-function与多态" class="headerlink" title="std::function与多态"></a><code>std::function</code>与多态</h2><p>由于<code>std::function</code>可以存储任意可调用对象，因此它可以用来实现多态行为。</p>
<p>这意味着可以将不同的函数对象绑定到一个<code>std::function</code>上，然后通过调用<code>std::function</code>来实现不同的行为。</p>
<div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;functional&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Animal</span> </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">makeSound</span><span class="params">()</span> <span class="type">const</span> </span>= <span class="number">0</span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Dog</span> : <span class="keyword">public</span> Animal</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">makeSound</span><span class="params">()</span> <span class="type">const</span> <span class="keyword">override</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;Woof!&quot;</span> &lt;&lt; std::endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Cat</span> : <span class="keyword">public</span> Animal</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">makeSound</span><span class="params">()</span> <span class="type">const</span> <span class="keyword">override</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;Meow!&quot;</span> &lt;&lt; std::endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    std::function&lt;<span class="type">void</span>(<span class="type">const</span> Animal&amp;)&gt; makeSoundFunc;</span><br><span class="line"></span><br><span class="line">    Dog dog;</span><br><span class="line">    Cat cat;</span><br><span class="line"></span><br><span class="line">    makeSoundFunc = [](<span class="type">const</span> Animal&amp; animal) &#123; animal.<span class="built_in">makeSound</span>(); &#125;;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">makeSoundFunc</span>(dog); <span class="comment">// Output: Woof!</span></span><br><span class="line">    <span class="built_in">makeSoundFunc</span>(cat); <span class="comment">// Output: Meow!</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>在上述例子中，我们使用<code>std::function</code>将不同的动物对象（<code>Dog</code>和<code>Cat</code>）的<code>makeSound()</code>方法封装起来，然后通过<code>makeSoundFunc()</code>调用不同的动物的声音输出。</p>
<p><code>std::function</code>是C++中一个非常有用的工具，特别是在需要实现多态行为或在接口设计中解耦函数的返回类型和参数类型时。</p>
<p>它使得处理函数对象变得更加灵活和通用。</p>
<h2 id="std-function与非静态成员函数"><a href="#std-function与非静态成员函数" class="headerlink" title="std::function与非静态成员函数"></a><code>std::function</code>与非静态成员函数</h2><p>类的非静态成员函数是依赖于实例化对象的，需要先创建并实例化出对象，使用<code>std::function</code>封装类的非静态成员函数有三种方法：使用lambda表达式、使用<code>std::bind</code>、直接封装。</p>
<p>下面将给出使用lambda表达式和直接封装的代码示例。</p>
<p>lambda表达式：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;functional&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">MyClass</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">nonStaticFunction</span><span class="params">(<span class="type">int</span> value)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;Non-static function called with value: &quot;</span> &lt;&lt; value &lt;&lt; std::endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    MyClass obj;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 使用lambda表达式也可以将类的实例和成员函数捕获，创建一个可调用的对象</span></span><br><span class="line">    std::function&lt;<span class="type">void</span>(<span class="type">int</span>)&gt; func = [&amp;obj](<span class="type">int</span> value)&#123;obj.<span class="built_in">nonStaticFunction</span>(value);&#125;;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 现在可以像调用普通函数一样调用封装的成员函数</span></span><br><span class="line">    <span class="built_in">func</span>(<span class="number">100</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>直接封装：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;functional&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">MyClass</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">add</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> a + b;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    MyClass obj;</span><br><span class="line"></span><br><span class="line">    std::function&lt;<span class="type">int</span>(MyClass&amp;, <span class="type">int</span>, <span class="type">int</span>)&gt; func = &amp;MyClass::add;</span><br><span class="line">	<span class="comment">//调用</span></span><br><span class="line">    <span class="built_in">func</span>();</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>使用<code>std::bind</code>将在后续讲解。</p>
<h2 id="std-function的成员函数"><a href="#std-function的成员函数" class="headerlink" title="std::function的成员函数"></a><code>std::function</code>的成员函数</h2><p><code>std::function</code>是一个通用的函数封装器，它提供了一系列成员函数来管理和操作封装的可调用对象。</p>
<p>下面是<code>std::function</code>的一些重要成员函数及其作用：</p>
<ul>
<li><p>构造函数：</p>
<ul>
<li><code>function::function()</code>：默认构造函数，创建一个空的<code>std::function</code>对象。</li>
<li><code>function::function(nullptr_t)</code>：构造一个空的<code>std::function</code>对象。</li>
<li><code>function::function(const function&amp;)</code>：复制构造函数，用于创建一个<code>std::function</code>对象，复制另一个<code>std::function</code>对象的内容。</li>
</ul>
</li>
<li><p>赋值和重置：</p>
<ul>
<li><code>function::operator=</code>：赋值运算符，用于将一个<code>std::function</code>对象赋值给另一个。</li>
<li><code>function::operator=(nullptr_t)</code>：将<code>std::function</code>对象重置为空。</li>
</ul>
</li>
<li><p>调用和运行时类型判断：</p>
<ul>
<li><code>function::operator()</code>：函数调用运算符，用于调用封装的可调用对象。</li>
<li><code>function::target()</code>：返回指向封装的可调用对象的指针，可以用于判断其运行时类型。</li>
<li><code>function::target_type()</code>：返回封装的可调用对象的类型。</li>
</ul>
</li>
<li><p>检查是否为空：</p>
<ul>
<li><code>function::operator bool()</code>：将<code>std::function</code>对象转换为<code>bool</code>值，判断是否为空。</li>
</ul>
</li>
<li><p>交换两个对象的内容：</p>
<ul>
<li><p><code>function::swap()</code>：用于交换两个<code>std::function</code>对象的内容。</p>
<p><code>swap()</code>函数允许在不复制或移动可调用对象的情况下，高效地交换两个<code>std::function</code>对象的内容。这对于需要在运行时更改<code>std::function</code>对象的封装内容非常有用。</p>
<p><code>swap()</code>函数是无异常的 (<code>noexcept</code>)，因此它不会引发异常。这使得在涉及异常安全性的代码中使用<code>swap()</code>函数非常安全。</p>
</li>
</ul>
</li>
</ul>
<p>请注意，<code>std::function</code>的成员函数和操作符的行为会根据封装的可调用对象的类型而有所不同。</p>
<p>例如：</p>
<ul>
<li><p>如果封装的是一个函数指针，那么调用运算符的行为就类似于直接调用该函数。</p>
</li>
<li><p>如果封装的是一个函数对象，那么调用运算符将调用函数对象的<code>operator()</code>成员函数。</p>
</li>
</ul>
<p>这些成员函数使得<code>std::function</code>能够灵活地处理不同类型的可调用对象，并提供了一致的接口来管理和使用这些对象。</p>
<h1 id="std-bind"><a href="#std-bind" class="headerlink" title="std::bind"></a><code>std::bind</code></h1><h2 id="概述-1"><a href="#概述-1" class="headerlink" title="概述"></a>概述</h2><p>在C++中，<code>std::bind</code>是一个非常有用的函数模板，用于创建一个函数对象，可以将参数绑定到该对象，并延迟调用相应的函数。它的主要作用是部分应用函数参数或重新排序函数参数。<code>std::bind</code>位于<code>&lt;functional&gt;</code>头文件中，并在C++11及以上版本的标准中提供支持。</p>
<p><code>std::bind</code>的语法如下：</p>
<div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">template</span>&lt; <span class="keyword">class</span> F, <span class="keyword">class</span>... Args &gt;</span></span><br><span class="line"><span class="function">std::function&lt;<span class="title">F</span><span class="params">(Args...)</span>&gt; <span class="title">bind</span><span class="params">( F&amp;&amp; f, Args&amp;&amp;... args )</span></span>;</span><br></pre></td></tr></table></figure></div>

<p><code>std::bind</code>接受一个可调用对象 <code>f</code> 和若干个参数 <code>args</code>（这些参数可以是常量、变量或者其他函数对象），返回一个新的函数对象，该函数对象可以在稍后调用，实际执行时，将会使用绑定的参数和<code>f</code>原来的参数一起传递。</p>
<h2 id="绑定自由函数"><a href="#绑定自由函数" class="headerlink" title="绑定自由函数"></a>绑定自由函数</h2><div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;functional&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">add</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> a + b;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">auto</span> func = std::<span class="built_in">bind</span>(add, <span class="number">10</span>, <span class="number">20</span>);</span><br><span class="line">    std::cout &lt;&lt; <span class="built_in">func</span>() &lt;&lt; std::endl; <span class="comment">// 输出 30</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>在这个例子中，我们通过<code>std::bind</code>将<code>add</code>函数的前两个参数绑定为<code>10</code>和<code>20</code>。</p>
<p>生成的<code>func</code>函数对象可以直接调用，而无需再传递<code>add</code>函数的前两个参数。</p>
<h2 id="绑定成员函数"><a href="#绑定成员函数" class="headerlink" title="绑定成员函数"></a>绑定成员函数</h2><div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;functional&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Calculator</span> </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">add</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> a + b;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    Calculator calc;</span><br><span class="line">    <span class="keyword">auto</span> func = std::<span class="built_in">bind</span>(&amp;Calculator::add, &amp;calc, <span class="number">10</span>, <span class="number">20</span>);</span><br><span class="line">    std::cout &lt;&lt; <span class="built_in">func</span>() &lt;&lt; std::endl; <span class="comment">// 输出 30</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>在这个例子中，我们使用<code>std::bind</code>将<code>Calculator</code>类的成员函数<code>add</code>绑定到对象<code>calc</code>上，并绑定前两个参数为<code>10</code>和<code>20</code>。</p>
<p>注意：非静态成员函数是依赖于实例化对象的。</p>
<p>生成的<code>func</code>函数对象可以直接调用，并在调用时使用绑定的对象和参数。</p>
<h2 id="占位符"><a href="#占位符" class="headerlink" title="占位符"></a>占位符</h2><p><code>std::bind</code>的占位符是一个特殊的标记，用于表示在调用绑定后的函数对象时，哪些参数需要从调用时的实际参数中获取，而哪些参数是已经绑定好的。</p>
<p>占位符以<code>std::placeholders::_N</code>的形式表示，其中<code>N</code>是一个非负整数，表示参数的位置索引。</p>
<p><code>std::placeholders::_1</code>表示第一个参数，<code>std::placeholders::_2</code>表示第二个参数，以此类推。</p>
<p>在使用<code>std::bind</code>时，可以在绑定的函数对象中使用占位符，然后在调用函数对象时，传递实际的参数来替换这些占位符，以完成参数的绑定。</p>
<div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;functional&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">add</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b, <span class="type">int</span> c)</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> a + b + c;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">auto</span> func = std::<span class="built_in">bind</span>(add, std::placeholders::_2, std::placeholders::_1, std::placeholders::_3);</span><br><span class="line">    std::cout &lt;&lt; <span class="built_in">func</span>(<span class="number">10</span>, <span class="number">20</span>, <span class="number">30</span>) &lt;&lt; std::endl; <span class="comment">// 输出 60  (20 + 10 + 30)</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure></div>

<p>在这个例子中，我们使用<code>std::bind</code>将函数<code>add</code>的参数重新排列，通过占位符的位置调整了参数的顺序。</p>
<p><code>std::placeholders::_2</code>表示实际调用时的第二个参数，<code>std::placeholders::_1</code>表示实际调用时的第一个参数，<code>std::placeholders::_3</code>表示实际调用时的第三个参数。</p>
<p>&#x3D;&#x3D;注意：参数顺序与<code>std::placeholders::_N</code>中N的顺序是一一对应的。假如只有<code>std::placeholders::_2</code>而没有<code>std::placeholders::_1</code>，那么只传入一个参数时将会报错，传入两个参数时第一个参数将会被忽略，以此类推。&#x3D;&#x3D;</p>
<h2 id="绑定到lambda函数"><a href="#绑定到lambda函数" class="headerlink" title="绑定到lambda函数"></a>绑定到lambda函数</h2><div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;functional&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="type">int</span> base = <span class="number">10</span>;</span><br><span class="line">    <span class="keyword">auto</span> func = std::<span class="built_in">bind</span>([](<span class="type">int</span> x, <span class="type">int</span> y) &#123; <span class="keyword">return</span> x + y + base; &#125;, <span class="number">20</span>, std::placeholders::_1);</span><br><span class="line">    std::cout &lt;&lt; <span class="built_in">func</span>(<span class="number">30</span>) &lt;&lt; std::endl; <span class="comment">// 输出 60 (20 + 30 + 10)</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>在这个例子中，我们使用<code>std::bind</code>将一个Lambda函数绑定，并在绑定时指定了其中的一个参数为<code>20</code>，另一个参数使用<code>std::placeholders::_1</code>占位符，表示稍后调用<code>func</code>时传递的参数。同时，Lambda函数内部使用了外部的变量<code>base</code>，这个变量在绑定时被捕获了。</p>
<h2 id="std-bind和std-function结合使用"><a href="#std-bind和std-function结合使用" class="headerlink" title="std::bind和std::function结合使用"></a><code>std::bind</code>和<code>std::function</code>结合使用</h2><p>可以使用<code>std::function</code>来封装类中的非静态成员函数。</p>
<p>要封装类中的非静态成员函数，需要使用<code>std::bind</code>或lambda表达式，将类的实例（对象）绑定到成员函数上。</p>
<p>这样，就可以将成员函数封装成一个可调用的对象，然后再将其传递给<code>std::function</code>。</p>
<p>下面是一个示例代码，演示了如何使用<code>std::function</code>配合<code>std::bind</code>封装类中的非静态成员函数：</p>
<div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;functional&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">MyClass</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">nonStaticFunction</span><span class="params">(<span class="type">int</span> value)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;Non-static function called with value: &quot;</span> &lt;&lt; value &lt;&lt; std::endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    MyClass obj;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 使用std::bind将类的实例和成员函数绑定，创建一个可调用的对象</span></span><br><span class="line">    std::function&lt;<span class="type">void</span>(<span class="type">int</span>)&gt; func1 = std::<span class="built_in">bind</span>(&amp;MyClass::nonStaticFunction, &amp;obj, std::placeholders::_1);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 现在可以像调用普通函数一样调用封装的成员函数</span></span><br><span class="line">    <span class="built_in">func1</span>(<span class="number">42</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>在上面的示例中，<code>func1</code>是<code>std::function</code>类型的对象，封装了<code>MyClass</code>类的<code>nonStaticFunction</code>成员函数。</p>
<p><code>func1</code>使用<code>std::bind</code>将<code>obj</code>对象和成员函数绑定在一起。</p>
<p>请注意，在使用<code>std::bind</code>时，需要传递类实例的指针作为第一个参数，以确保成员函数能够正确访问类的实例。</p>
<p>而在使用lambda表达式时，可以通过捕获列表来捕获类实例，以便在lambda函数体内使用。</p>
<h1 id="继承构造"><a href="#继承构造" class="headerlink" title="继承构造"></a>继承构造</h1><p>在C++11中引入了继承构造特性（Inheriting Constructors），允许派生类继承基类的构造函数（默认构造函数、拷贝构造函数、移动构造函数除外）。</p>
<p>这种特性简化了代码，避免了在派生类中重新实现相同的构造函数。</p>
<p>继承构造特性通过在派生类中使用<code>using</code>声明语句来实现。</p>
<p>通过<code>using</code>声明，派生类可以直接使用基类的构造函数，而不需要重新定义或调用它们。</p>
<p>示例：</p>
<div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Base</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Base</span>(<span class="type">int</span> x) </span><br><span class="line">    &#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;Base constructor called with x = &quot;</span> &lt;&lt; x &lt;&lt; std::endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Derived</span> : <span class="keyword">public</span> Base </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="keyword">using</span> Base::Base;  <span class="comment">// 继承基类的构造函数</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">DoSomething</span><span class="params">()</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;Doing something in the derived class.&quot;</span> &lt;&lt; std::endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="function">Derived <span class="title">derived</span><span class="params">(<span class="number">42</span>)</span></span>;</span><br><span class="line">    derived.<span class="built_in">DoSomething</span>();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>在上面的代码中，<code>Base</code>类有一个接受一个整数参数的构造函数。</p>
<p><code>Derived</code>类通过使用<code>using Base::Base;</code>语句继承了<code>Base</code>的构造函数。</p>
<p>这意味着在创建<code>Derived</code>对象时，可以直接使用<code>Base</code>类的构造函数，并且传递相同的参数。</p>
<p>输出：</p>
<div class="highlight-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">Base constructor called with x = 42</span><br><span class="line">Doing something in the derived class.</span><br></pre></td></tr></table></figure></div>

<p>可以看到，通过继承构造特性，<code>Derived</code>类实例化时会自动调用<code>Base</code>类的构造函数，并传递相应的参数。</p>
<p>继承构造特性还可以用于多重继承的情况，允许派生类继承多个基类的构造函数。</p>
<p>如果基类构造函数有默认参数，派生类也会继承这些默认参数。</p>
<p>继承构造特性简化了代码编写，并提高了代码的可读性和维护性。</p>
<p>注意事项：</p>
<ul>
<li><p>继承构造一般只适用于派生类中没有新增成员变量的情况下，继承构造只能初始化基类中的成员变量，不能初始化派生类中的成员变量，如果派生类新增了成员变量，那么继承构造的意义不大。</p>
</li>
<li><p>使用继承构造时，派生类不能是虚继承基类，否则编译报错。</p>
</li>
<li><p>派生类一旦使用继承构造，那么编译器不会再提供默认构造函数。</p>
</li>
<li><p>派生类只能继承可见的基类构造函数。如果基类的构造函数是私有的或受保护的，派生类无法使用继承构造特性。只有公有的基类构造函数才能被继承。</p>
</li>
<li><p>如果派生类自己定义了与基类构造函数相同的构造函数（包括参数类型和个数），那么继承构造特性将失效。派生类不会继承基类的构造函数，而是使用自己定义的构造函数。</p>
</li>
<li><p>继承构造特性会继承基类构造函数的默认参数。这意味着在派生类中，可以选择省略这些默认参数或者提供新的默认参数值。</p>
</li>
<li><p>如果派生类从多个基类继承构造函数，并且这些构造函数具有相同的签名，那么在派生类中使用继承构造特性时会引发重载解析问题。这时需要使用限定符或显式地指定所需的构造函数。</p>
</li>
<li><p>如果基类使用虚拟继承（virtual inheritance），派生类无法继承其构造函数。在这种情况下，需要在派生类中手动定义构造函数，并在初始化列表中调用基类的构造函数。</p>
</li>
</ul>
<h1 id="委托构造"><a href="#委托构造" class="headerlink" title="委托构造"></a>委托构造</h1><p>C++11引入了委托构造（Delegating Constructors）的特性，它允许一个构造函数在同一个类中调用另一个构造函数来完成对象的初始化。</p>
<p>这样可以避免在类中重复编写相似的初始化代码，提高代码的可读性和维护性。</p>
<p>在使用委托构造时，可以通过在构造函数的初始化列表中使用类似于函数调用的语法来调用其他构造函数。</p>
<p>下面是一个简单的示例代码，演示了如何使用委托构造：</p>
<div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">MyClass</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">MyClass</span>() : <span class="built_in">MyClass</span>(<span class="number">0</span>) </span><br><span class="line">    &#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;Default constructor&quot;</span> &lt;&lt; std::endl;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">MyClass</span>(<span class="type">int</span> x) : <span class="built_in">MyClass</span>(x, <span class="number">0</span>) </span><br><span class="line">    &#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;Constructor with one parameter: &quot;</span> &lt;&lt; x &lt;&lt; std::endl;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">MyClass</span>(<span class="type">int</span> x, <span class="type">int</span> y) : <span class="built_in">x_</span>(x), <span class="built_in">y_</span>(y) </span><br><span class="line">    &#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;Constructor with two parameters: &quot;</span> &lt;&lt; x &lt;&lt; <span class="string">&quot;, &quot;</span> &lt;&lt; y &lt;&lt; std::endl;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">int</span> x_;</span><br><span class="line">    <span class="type">int</span> y_;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    MyClass obj1;               <span class="comment">// 调用默认构造函数</span></span><br><span class="line">    <span class="function">MyClass <span class="title">obj2</span><span class="params">(<span class="number">42</span>)</span></span>;           <span class="comment">// 调用带一个参数的构造函数</span></span><br><span class="line">    <span class="function">MyClass <span class="title">obj3</span><span class="params">(<span class="number">10</span>, <span class="number">20</span>)</span></span>;       <span class="comment">// 调用带两个参数的构造函数</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>在上面的代码中，<code>MyClass</code>类中定义了三个构造函数。</p>
<p>默认构造函数调用了带一个参数的构造函数，带一个参数的构造函数又调用了带两个参数的构造函数。</p>
<p>这种构造函数之间的相互调用就是委托构造。</p>
<p>输出：</p>
<div class="highlight-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">Constructor with two parameters: 0, 0</span><br><span class="line">Default constructor</span><br><span class="line">Constructor with one parameter: 42</span><br><span class="line">Constructor with two parameters: 10, 20</span><br></pre></td></tr></table></figure></div>

<p>可以看到，通过使用委托构造，不需要在每个构造函数中重复编写相同的初始化代码。</p>
<p>相反，每个构造函数只需要关注它自己特定的初始化逻辑，并将共享的初始化工作委托给其他构造函数。</p>
<p>需要注意的是，委托构造函数的调用发生在当前构造函数的初始化列表中。</p>
<p>委托构造函数必须位于当前构造函数初始化列表的最开始位置。</p>
<p>这是因为在初始化列表中的其他成员初始化之前，必须先选择正确的构造函数进行委托。</p>
<p>委托构造使得构造函数的代码更加简洁和易于维护。它是提高代码可读性和减少重复代码的有用工具。</p>
<h1 id="智能指针"><a href="#智能指针" class="headerlink" title="智能指针"></a>智能指针</h1><h2 id="智能指针的概述"><a href="#智能指针的概述" class="headerlink" title="智能指针的概述"></a>智能指针的概述</h2><hr>
<p>C++的指针包括两种：</p>
<ul>
<li>原始指针（raw pointer)</li>
<li>智能指针</li>
</ul>
<p>智能指针是原始指针的封装，其优点是会自动分配内存，不用担心潜在的内存泄露。</p>
<p>并不是所有的指针都可以封装成智能指针，很多时候原始指针要更方便。</p>
<p>C++11中通过引入智能指针的概念，使得C++程序员不需要手动释放内存。</p>
<p>智能指针的种类：</p>
<ul>
<li><p><code>std:unique_ptr</code></p>
</li>
<li><p><code>std::shared_ptr</code></p>
</li>
<li><p><code>std::weak_ptr</code></p>
</li>
<li><p><code>std::auto ptr</code>（已被废弃）</p>
</li>
</ul>
<p>各种指针中，最常用的是裸指针，其次是<code>unique _ptr</code>和<code>shared ptr</code></p>
<p><code>weak_ptr</code>是<code>shared _ptr</code>的一个补充，应用场景较少。</p>
<p>智能指针只解决一部分问题，即独占、共享、所有权、指针的释放、传输</p>
<p>智能指针没有从根本上解决C++内存安全问题，不加以注意依然会造成内存安全问题</p>
<h2 id="unique-ptr（独占指针）"><a href="#unique-ptr（独占指针）" class="headerlink" title="unique_ptr（独占指针）"></a><code>unique_ptr</code>（独占指针）</h2><hr>
<h3 id="概念和用法"><a href="#概念和用法" class="headerlink" title="概念和用法"></a>概念和用法</h3><hr>
<p><code>unique_ptr</code>是一种智能指针，用于管理动态分配的对象。</p>
<p>它提供了一种独占式所有权模式，即&#x3D;&#x3D;一个对象只能由一个<code>unique_ptr</code>拥有&#x3D;&#x3D;。</p>
<p>当<code>unique_ptr</code>被销毁或重置时，它所拥有的对象也会被销毁。</p>
<p>以下是<code>unique_ptr</code>的主要特点和用法：</p>
<ul>
<li><p><strong>头文件引入</strong>：</p>
<p><code>unique_ptr</code>位于 <code>&lt;memory&gt;</code> 头文件中，要使用它，需要包含该头文件。</p>
</li>
<li><p><strong>对象的所有权</strong>：</p>
<p><code>unique_ptr</code>通过将对象包装在一个指针内部，拥有对该对象的独占所有权。这意味着没有其他指针可以同时指向该对象。</p>
</li>
<li><p><strong>动态内存分配</strong>：</p>
<p>通常，<code>unique_ptr</code>用于管理动态分配的对象。可以使用<code>new</code>关键字来创建一个对象，并将其传递给<code>unique_ptr</code>的构造函数。</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">std::unique_ptr&lt;<span class="type">int</span>&gt; <span class="title">ptr</span><span class="params">(<span class="keyword">new</span> <span class="type">int</span>)</span></span>; <span class="comment">// 创建一个动态分配的int对象</span></span><br></pre></td></tr></table></figure></div>
</li>
<li><p><strong>指针语义</strong>：</p>
<p><code>unique_ptr</code>具有与原始指针类似的语义，可以像使用原始指针一样对待它。</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line">  <span class="function">std::unique_ptr&lt;<span class="type">int</span>&gt; <span class="title">ptr</span><span class="params">(<span class="keyword">new</span> <span class="type">int</span>)</span></span>; <span class="comment">// 创建一个动态分配的int对象</span></span><br><span class="line">  *ptr = <span class="number">10</span>;      <span class="comment">// 对动态分配的对象进行赋值</span></span><br><span class="line">  <span class="type">int</span> value = *ptr; <span class="comment">// 从指针获取对象的值</span></span><br><span class="line"></span><br><span class="line">- **移动语义**：</span><br><span class="line"></span><br><span class="line">  由于`unique_ptr`拥有对象的独占所有权，因此它可以被移动，但不能被复制。</span><br><span class="line"></span><br><span class="line">  这意味着可以将`unique_ptr`转移到另一个`unique_ptr`，而原始的`unique_ptr`将失去对对象的所有权。</span><br><span class="line"></span><br><span class="line">  ```c++</span><br><span class="line">  <span class="function">std::unique_ptr&lt;<span class="type">int</span>&gt; <span class="title">ptr1</span><span class="params">(<span class="keyword">new</span> <span class="type">int</span>)</span></span>;</span><br><span class="line">  std::unique_ptr&lt;<span class="type">int</span>&gt; ptr2 = std::<span class="built_in">move</span>(ptr1); <span class="comment">// 转移ptr1的所有权给ptr2</span></span><br><span class="line"></span><br><span class="line">- **释放内存**：</span><br><span class="line"></span><br><span class="line">  当`unique_ptr`超出范围、被重置或被销毁时，它所拥有的对象会自动被销毁。</span><br><span class="line"></span><br><span class="line">  这意味着不需要手动调用`<span class="keyword">delete</span>`来释放内存，从而避免了内存泄漏。</span><br><span class="line"></span><br><span class="line">  ```c++</span><br><span class="line">  <span class="function">std::unique_ptr&lt;<span class="type">int</span>&gt; <span class="title">ptr</span><span class="params">(<span class="keyword">new</span> <span class="type">int</span>)</span></span>;</span><br><span class="line">  <span class="comment">// ...</span></span><br><span class="line">  ptr.<span class="built_in">reset</span>(); <span class="comment">// 重置unique_ptr，对象会被销毁</span></span><br><span class="line">  <span class="comment">// unique_ptr生命周期结束对象也会被自动销毁</span></span><br></pre></td></tr></table></figure></div>
</li>
<li><p><strong>空指针检查</strong>：</p>
<p>可以使用<code>unique_ptr</code>的<code>get</code>函数来检查它是否指向一个有效的对象。</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line">  std::unique_ptr&lt;<span class="type">int</span>&gt; ptr;</span><br><span class="line">  <span class="keyword">if</span> (ptr.<span class="built_in">get</span>() != <span class="literal">nullptr</span>) </span><br><span class="line">  &#123;</span><br><span class="line">      <span class="comment">// ptr指向一个有效的对象</span></span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">- **自定义删除器**：</span><br><span class="line"></span><br><span class="line">   `unique_ptr`还可以使用自定义的删除器函数或函数对象来管理对象的销毁。</span><br><span class="line">   </span><br><span class="line">   ```cpp</span><br><span class="line">   <span class="keyword">struct</span> <span class="title class_">CustomDeleter</span> </span><br><span class="line">   &#123;</span><br><span class="line">       <span class="function"><span class="type">void</span> <span class="title">operator</span><span class="params">()</span><span class="params">(<span class="type">int</span>* ptr)</span> <span class="type">const</span> </span></span><br><span class="line"><span class="function">       </span>&#123;</span><br><span class="line">           <span class="comment">// 自定义的对象销毁操作</span></span><br><span class="line">           <span class="keyword">delete</span> ptr;</span><br><span class="line">       &#125;</span><br><span class="line">   &#125;;</span><br><span class="line">   </span><br><span class="line">   <span class="function">std::unique_ptr&lt;<span class="type">int</span>, CustomDeleter&gt; <span class="title">ptr</span><span class="params">(<span class="keyword">new</span> <span class="type">int</span>, CustomDeleter())</span></span>;</span><br><span class="line">   <span class="comment">//第二个参数即重载()的仿函数，模板参数中删除器类型必须指定，函数参数可以不指定</span></span><br><span class="line">   <span class="comment">//std::unique_ptr&lt;int, CustomDeleter&gt; ptr(new int);//这样也行，效果一样</span></span><br></pre></td></tr></table></figure></div></li>
</ul>
<p><code>unique_ptr</code>提供了一种安全且方便的方式来管理动态分配的对象，可以避免内存泄漏和手动内存管理的麻烦。</p>
<p>但需要注意的是，由于其独占性质，<code>unique_ptr</code>不适用于需要多个指针共享所有权的情况。在这种情况下，应该考虑使用<code>shared_ptr</code>智能指针。</p>
<h3 id="错误使用示例"><a href="#错误使用示例" class="headerlink" title="错误使用示例"></a>错误使用示例</h3><hr>
<p>关于<code>unique_ptr</code>的独占性，以下是一些可能导致错误的示例：</p>
<p><strong>错误示例 1：复制<code>unique_ptr</code></strong></p>
<div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">std::unique_ptr&lt;<span class="type">int</span>&gt; <span class="title">ptr1</span><span class="params">(<span class="keyword">new</span> <span class="type">int</span>)</span></span>;</span><br><span class="line">std::unique_ptr&lt;<span class="type">int</span>&gt; ptr2 = ptr1; <span class="comment">// 错误！不能复制unique_ptr</span></span><br></pre></td></tr></table></figure></div>

<p>由于<code>unique_ptr</code>的独占性，它不能被复制。在上面的示例中，试图将<code>ptr1</code>复制给<code>ptr2</code>将导致编译错误。</p>
<p><strong>错误示例 2：使用原始指针共享所有权</strong></p>
<div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">std::unique_ptr&lt;<span class="type">int</span>&gt; <span class="title">ptr</span><span class="params">(<span class="keyword">new</span> <span class="type">int</span>)</span></span>;</span><br><span class="line"><span class="type">int</span>* rawPtr = ptr.<span class="built_in">get</span>(); <span class="comment">// 获取原始指针</span></span><br><span class="line"><span class="function">std::shared_ptr&lt;<span class="type">int</span>&gt; <span class="title">sharedPtr</span><span class="params">(rawPtr)</span></span>; <span class="comment">// 错误！不能与shared_ptr共享所有权</span></span><br></pre></td></tr></table></figure></div>

<p>由于<code>unique_ptr</code>的独占性，它不能与<code>shared_ptr</code>共享所有权。</p>
<p>在上面的示例中，将<code>unique_ptr</code>的原始指针传递给<code>shared_ptr</code>的构造函数是错误的，会导致多个智能指针同时管理同一个对象，可能引发内存释放问题。</p>
<p><strong>错误示例 3：返回局部的<code>unique_ptr</code></strong></p>
<div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">std::unique_ptr&lt;<span class="type">int</span>&gt; <span class="title">createPtr</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="function">std::unique_ptr&lt;<span class="type">int</span>&gt; <span class="title">ptr</span><span class="params">(<span class="keyword">new</span> <span class="type">int</span>)</span></span>;</span><br><span class="line">    <span class="keyword">return</span> ptr; <span class="comment">// 错误！返回局部unique_ptr的所有权</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">std::unique_ptr&lt;<span class="type">int</span>&gt; ptr = <span class="built_in">createPtr</span>(); <span class="comment">// 错误！悬空指针</span></span><br></pre></td></tr></table></figure></div>

<p>在上面的示例中，<code>createPtr</code>函数试图返回一个局部创建的<code>unique_ptr</code>，这是错误的。当函数返回时，局部对象将被销毁，导致指向无效内存的悬空指针。</p>
<p><strong>错误示例 4：将相同的原始指针传递给多个<code>unique_ptr</code></strong></p>
<div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span>* rawPtr = <span class="keyword">new</span> <span class="type">int</span>;</span><br><span class="line"><span class="function">std::unique_ptr&lt;<span class="type">int</span>&gt; <span class="title">ptr1</span><span class="params">(rawPtr)</span></span>;</span><br><span class="line"><span class="function">std::unique_ptr&lt;<span class="type">int</span>&gt; <span class="title">ptr2</span><span class="params">(rawPtr)</span></span>; <span class="comment">// 错误！多个unique_ptr管理同一个原始指针</span></span><br></pre></td></tr></table></figure></div>

<p>在上面的示例中，试图将相同的原始指针传递给多个<code>unique_ptr</code>是错误的。这将导致多个智能指针同时尝试删除同一个内存块，可能导致重复释放或悬空指针。</p>
<p>需要特别注意<code>unique_ptr</code>的独占性质，并避免以上错误，以确保正确使用和管理动态分配的对象。</p>
<h3 id="成员函数详解"><a href="#成员函数详解" class="headerlink" title="成员函数详解"></a>成员函数详解</h3><hr>
<p><code>unique_ptr</code>类提供了一系列成员函数来管理和操作智能指针，以下是其中一些常用的成员函数及其作用：</p>
<ul>
<li><p>**<code>reset()</code>**：重置<code>unique_ptr</code>，释放当前拥有的对象，并接管新的对象。如果没有参数，则释放当前调用对象拥有的对象，<code>uinque_ptr</code>变为悬空指针。</p>
 <div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">std::unique_ptr&lt;<span class="type">int</span>&gt; <span class="title">ptr</span><span class="params">(<span class="keyword">new</span> <span class="type">int</span>)</span></span>;</span><br><span class="line"><span class="comment">// ...</span></span><br><span class="line">ptr.<span class="built_in">reset</span>(<span class="keyword">new</span> <span class="type">int</span>); <span class="comment">// 重置unique_ptr，释放旧对象并接管新对象</span></span><br><span class="line"><span class="comment">//ptr.reset()// 释放拥有的对象，变为悬空指针</span></span><br></pre></td></tr></table></figure></div>
</li>
<li><p>**<code>release()</code>**：释放<code>unique_ptr</code>对对象的所有权，并返回指向该对象的原始指针，<code>unique_ptr</code>变为悬空指针。</p>
 <div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">std::unique_ptr&lt;<span class="type">int</span>&gt; <span class="title">ptr</span><span class="params">(<span class="keyword">new</span> <span class="type">int</span>)</span></span>;</span><br><span class="line"><span class="type">int</span>* rawPtr = ptr.<span class="built_in">release</span>(); <span class="comment">// 释放unique_ptr对对象的所有权，返回原始指针</span></span><br><span class="line"><span class="comment">// ...</span></span><br><span class="line"><span class="comment">//需要手动释放rawPtr</span></span><br><span class="line"><span class="keyword">delete</span> rawPtr;</span><br></pre></td></tr></table></figure></div>
</li>
<li><p>**<code>get()</code>**：返回指向<code>unique_ptr</code>管理的对象的原始指针。</p>
 <div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">std::unique_ptr&lt;<span class="type">int</span>&gt; <span class="title">ptr</span><span class="params">(<span class="keyword">new</span> <span class="type">int</span>)</span></span>;</span><br><span class="line"><span class="type">int</span>* rawPtr = ptr.<span class="built_in">get</span>(); <span class="comment">// 获取指向对象的原始指针</span></span><br></pre></td></tr></table></figure></div>
</li>
<li><p>**<code>operator*()</code>**：重载解引用操作符，返回<code>unique_ptr</code>管理对象的引用。</p>
 <div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">std::unique_ptr&lt;<span class="type">int</span>&gt; <span class="title">ptr</span><span class="params">(<span class="keyword">new</span> <span class="type">int</span>)</span></span>;</span><br><span class="line">*ptr = <span class="number">42</span>; <span class="comment">// 解引用unique_ptr并赋值</span></span><br></pre></td></tr></table></figure></div>
</li>
<li><p>**<code>operator-&gt;()</code>**：重载箭头操作符，返回指向<code>unique_ptr</code>管理对象的指针。</p>
 <div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">std::unique_ptr&lt;std::string&gt; <span class="title">ptr</span><span class="params">(<span class="keyword">new</span> std::string(<span class="string">&quot;Hello&quot;</span>))</span></span>;</span><br><span class="line">std::cout &lt;&lt; ptr-&gt;<span class="built_in">size</span>() &lt;&lt; std::endl; <span class="comment">// 使用箭头操作符访问对象的成员函数</span></span><br></pre></td></tr></table></figure></div>
</li>
<li><p>**<code>operator bool()</code>**：将<code>unique_ptr</code>转换为布尔值，判断指针是否为空（是否指向对象）。</p>
 <div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line">std::unique_ptr&lt;<span class="type">int</span>&gt; ptr;</span><br><span class="line"><span class="keyword">if</span> (ptr) </span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 指针不为空</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>
</li>
<li><p>**<code>operator=</code>**：重载赋值操作符，用于将一个<code>unique_ptr</code>赋值给另一个。赋值后，左侧的<code>unique_ptr</code>接管右侧的对象，并且右侧的<code>unique_ptr</code>变为悬空指针。</p>
 <div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">std::unique_ptr&lt;<span class="type">int</span>&gt; <span class="title">ptr1</span><span class="params">(<span class="keyword">new</span> <span class="type">int</span>)</span></span>;</span><br><span class="line">std::unique_ptr&lt;<span class="type">int</span>&gt; ptr2;</span><br><span class="line">ptr2 = std::<span class="built_in">move</span>(ptr1); <span class="comment">// 将ptr1的所有权转移给ptr2，ptr1变为悬空指针</span></span><br></pre></td></tr></table></figure></div>
</li>
<li><p>**<code>operator[]</code>**：允许使用下标操作符访问<code>unique_ptr</code>管理的数组对象。</p>
  <div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">std::unique_ptr&lt;<span class="type">int</span>[]&gt; <span class="title">arr</span><span class="params">(<span class="keyword">new</span> <span class="type">int</span>[<span class="number">5</span>])</span></span>;</span><br><span class="line">arr[<span class="number">0</span>] = <span class="number">1</span>; <span class="comment">// 访问数组元素</span></span><br></pre></td></tr></table></figure></div>
</li>
<li><p>**<code>swap()</code>**：交换两个<code>unique_ptr</code>对象所管理的对象。</p>
 <div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">std::unique_ptr&lt;<span class="type">int</span>&gt; <span class="title">ptr1</span><span class="params">(<span class="keyword">new</span> <span class="type">int</span>(<span class="number">1</span>))</span></span>;</span><br><span class="line"><span class="function">std::unique_ptr&lt;<span class="type">int</span>&gt; <span class="title">ptr2</span><span class="params">(<span class="keyword">new</span> <span class="type">int</span>(<span class="number">2</span>))</span></span>;</span><br><span class="line">ptr1.<span class="built_in">swap</span>(ptr2); <span class="comment">// 交换ptr1和ptr2的对象</span></span><br></pre></td></tr></table></figure></div>
</li>
<li><p>**<code>get_deleter()</code>**：获取<code>unique_ptr</code>中自定义的删除器函数或函数对象。</p>
 <div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;memory&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Deleter</span> </span><br><span class="line">&#123;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">operator</span><span class="params">()</span><span class="params">(<span class="type">int</span>* ptr)</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;自定义删除器 &quot;</span>&lt;&lt; std::endl;</span><br><span class="line">        <span class="keyword">delete</span> ptr;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="type">int</span>* rawPtr = <span class="keyword">new</span> <span class="built_in">int</span>(<span class="number">99</span>);</span><br><span class="line">        <span class="function">std::unique_ptr&lt;<span class="type">int</span>, Deleter&gt; <span class="title">uniquePtr</span><span class="params">(rawPtr, Deleter())</span></span>;</span><br><span class="line">        <span class="comment">//std::unique_ptr&lt;int, Deleter&gt; uniquePtr(rawPtr);//第二个函数参数可以省略</span></span><br><span class="line">        uniquePtr.<span class="built_in">get_deleter</span>()(<span class="keyword">new</span> <span class="built_in">int</span>(<span class="number">0</span>));</span><br><span class="line">    &#125;</span><br><span class="line"><span class="comment">//输出：</span></span><br><span class="line">    <span class="comment">//自定义删除器</span></span><br><span class="line">    <span class="comment">//自定义删除器</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div></li>
</ul>
<h3 id="使用make-unique创建和初始化"><a href="#使用make-unique创建和初始化" class="headerlink" title="使用make_unique创建和初始化"></a>使用<code>make_unique</code>创建和初始化</h3><hr>
<p><code>std::make_unique</code>是C++14中引入的一个辅助函数模板，用于创建和初始化<code>std::unique_ptr</code>对象，以更方便地进行动态内存分配和对象构造。</p>
<p>它接受类型<code>T</code>和可变数量的参数<code>Args</code>，并返回一个<code>std::unique_ptr&lt;T&gt;</code>对象。</p>
<p>使用<code>std::make_unique</code>的主要优势是它将动态内存分配和对象构造组合在一起，使代码更简洁、更安全。</p>
<p>&#x3D;&#x3D;它避免了直接使用<code>new</code>运算符来手动创建<code>unique_ptr</code>对象&#x3D;&#x3D;，并确保在发生异常时能够正确地销毁已分配的内存。</p>
<p>以下是使用<code>std::make_unique</code>的示例：</p>
<div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;memory&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">MyClass</span> </span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">MyClass</span>(<span class="type">int</span> a, <span class="type">double</span> b) </span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">// 对象的构造逻辑</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    std::unique_ptr&lt;MyClass&gt; ptr = std::<span class="built_in">make_unique</span>&lt;MyClass&gt;(<span class="number">42</span>, <span class="number">3.14</span>);</span><br><span class="line">    <span class="comment">// 使用make_unique创建unique_ptr，并传递构造参数</span></span><br><span class="line">    <span class="comment">// 在此示例中，将调用MyClass的构造函数</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>在上面的示例中，<code>std::make_unique</code>用于创建一个<code>std::unique_ptr&lt;MyClass&gt;</code>对象，并传递构造参数<code>42</code>和<code>3.14</code>给<code>MyClass</code>的构造函数。</p>
<p>这样，通过<code>make_unique</code>可以在一行代码中完成对象的动态分配和构造。</p>
<p>需要注意的是，<code>std::make_unique</code>在C++14中引入，因此如果使用较旧的C++标准（如C++11或更早），则无法直接使用该函数模板。</p>
<p>在这种情况下，可以通过编写自定义的辅助函数来模拟<code>std::make_unique</code>的行为。</p>
<h2 id="shared-ptr（共享指针）"><a href="#shared-ptr（共享指针）" class="headerlink" title="shared_ptr（共享指针）"></a><code>shared_ptr</code>（共享指针）</h2><hr>
<h3 id="概念和用法-1"><a href="#概念和用法-1" class="headerlink" title="概念和用法"></a>概念和用法</h3><hr>
<p><code>shared_ptr</code>是一种智能指针，用于管理动态分配的对象。</p>
<p>与<code>unique_ptr</code>不同，<code>shared_ptr</code>实现了共享所有权的语义，允许多个智能指针共同管理同一个对象。</p>
<p>它通过使用引用计数来跟踪对象的所有权，确保对象在没有引用时被正确释放。</p>
<p>以下是<code>shared_ptr</code>的主要特点和用法：</p>
<ul>
<li><p><strong>头文件引入</strong>：</p>
<p> <code>shared_ptr</code>位于 <code>&lt;memory&gt;</code> 头文件中，因此要使用它，需要包含该头文件。</p>
</li>
<li><p><strong>对象的共享所有权</strong>：</p>
<p> <code>shared_ptr</code>通过在内部维护一个引用计数，允许多个智能指针共享对同一对象的所有权。</p>
 <div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">std::shared_ptr&lt;<span class="type">int</span>&gt; <span class="title">ptr1</span><span class="params">(<span class="keyword">new</span> <span class="type">int</span>)</span></span>;</span><br><span class="line">std::shared_ptr&lt;<span class="type">int</span>&gt; ptr2 = ptr1; <span class="comment">// 共享ptr1对对象的所有权</span></span><br></pre></td></tr></table></figure></div>
</li>
<li><p><strong>动态内存分配</strong>：</p>
<p> 通常，<code>shared_ptr</code>用于管理动态分配的对象。可以使用<code>new</code>关键字来创建一个对象，并将其传递给<code>shared_ptr</code>的构造函数。</p>
 <div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">std::shared_ptr&lt;<span class="type">int</span>&gt; <span class="title">ptr</span><span class="params">(<span class="keyword">new</span> <span class="type">int</span>)</span></span>; <span class="comment">// 创建一个动态分配的int对象</span></span><br></pre></td></tr></table></figure></div>
</li>
<li><p><strong>引用计数</strong>：</p>
<p> <code>shared_ptr</code>内部维护了一个引用计数，跟踪有多少个<code>shared_ptr</code>实例共享同一个对象。</p>
<p> 每当创建或复制一个<code>shared_ptr</code>时，引用计数会增加。当<code>shared_ptr</code>被销毁或重置时，引用计数会减少。</p>
<p> 如果有多个<code>shard_ptr</code>共享同一个对象，当销毁或释放其中一个<code>shard_ptr</code>时，只是将这个<code>shard_ptr</code>和其所管理的内存空间解除绑定，使这个被销毁的<code>shard_ptr</code>无法再操纵原先的堆区空间（<code>shard_ptr</code>置空），并且将计数器减一，但是其他未被销毁的<code>shard_ptr</code>仍然可以操纵堆区内存。</p>
<p> 当引用计数为零时，对象会被自动销毁。</p>
</li>
<li><p><strong>拷贝和移动语义</strong>：</p>
<p> <code>shared_ptr</code>可以通过拷贝构造函数和拷贝赋值运算符进行复制。</p>
<p> 每个<code>shared_ptr</code>实例都会增加引用计数。同时，它也支持移动语义，允许通过移动构造函数和移动赋值运算符将对象的所有权转移到另一个<code>shared_ptr</code>。</p>
 <div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">std::shared_ptr&lt;<span class="type">int</span>&gt; <span class="title">ptr1</span><span class="params">(<span class="keyword">new</span> <span class="type">int</span>)</span></span>;</span><br><span class="line">std::shared_ptr&lt;<span class="type">int</span>&gt; ptr2 = ptr1;   <span class="comment">// 拷贝，引用计数增加</span></span><br><span class="line">std::shared_ptr&lt;<span class="type">int</span>&gt; ptr3 = std::<span class="built_in">move</span>(ptr1); <span class="comment">// 移动，ptr1的所有权转移到ptr3，ptr1变为空指针</span></span><br></pre></td></tr></table></figure></div>
</li>
<li><p><strong>循环引用问题</strong>：</p>
<p> 由于<code>shared_ptr</code>的引用计数机制，当存在循环引用时，对象可能不会被正确释放，导致内存泄漏。</p>
<p> 为了解决这个问题，C++11引入了<code>weak_ptr</code>作为<code>shared_ptr</code>的补充，用于解决循环引用的管理问题。</p>
<p> 关于循环引用将在下一节讲解。这里可以只做了解。</p>
 <div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">std::shared_ptr&lt;A&gt; <span class="title">ptrA</span><span class="params">(<span class="keyword">new</span> A)</span></span>;</span><br><span class="line"><span class="function">std::shared_ptr&lt;B&gt; <span class="title">ptrB</span><span class="params">(<span class="keyword">new</span> B)</span></span>;</span><br><span class="line">ptrA-&gt;b = ptrB; <span class="comment">// 假设A中有一个成员指向B</span></span><br><span class="line">ptrB-&gt;a = ptrA; <span class="comment">// 假设B中有一个成员指向A</span></span><br><span class="line">				<span class="comment">// 循环引用，可能导致对象无法正确释放</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 使用weak_ptr解决循环引用</span></span><br><span class="line"><span class="function">std::shared_ptr&lt;A&gt; <span class="title">ptrA</span><span class="params">(<span class="keyword">new</span> A)</span></span>;</span><br><span class="line"><span class="function">std::shared_ptr&lt;B&gt; <span class="title">ptrB</span><span class="params">(<span class="keyword">new</span> B)</span></span>;</span><br><span class="line">ptrA-&gt;b = std::<span class="built_in">weak_ptr</span>&lt;B&gt;(ptrB);</span><br><span class="line">ptrB-&gt;a = std::<span class="built_in">weak_ptr</span>&lt;A&gt;(ptrA);</span><br></pre></td></tr></table></figure></div>
</li>
<li><p><strong>自定义删除器</strong>：</p>
<p> <code>shared_ptr</code>也可以使用自定义的删除器函数或函数对象来管理对象的销毁，类似于<code>unique_ptr</code>。</p>
 <div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">CustomDeleter</span> </span><br><span class="line">&#123;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">operator</span><span class="params">()</span><span class="params">(<span class="type">int</span>* ptr)</span> <span class="type">const</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="comment">// 自定义的对象销毁操作</span></span><br><span class="line">        <span class="keyword">delete</span> ptr;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function">std::shared_ptr&lt;<span class="type">int</span>&gt; <span class="title">ptr</span><span class="params">(<span class="keyword">new</span> <span class="type">int</span>, CustomDeleter())</span></span>;</span><br></pre></td></tr></table></figure></div></li>
</ul>
<p><code>shared_ptr</code>提供了一种灵活的方式来管理动态分配的对象，允许多个指针共享对同一对象的所有权。</p>
<p>需要注意循环引用问题，并根据具体的使用场景选择合适的智能指针类型。</p>
<h3 id="错误使用示例-1"><a href="#错误使用示例-1" class="headerlink" title="错误使用示例"></a>错误使用示例</h3><hr>
<p>当涉及到<code>shared_ptr</code>的使用时，以下是一些可能导致错误的示例：</p>
<p><strong>错误示例 1：使用原始指针删除对象</strong></p>
<div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span>* rawPtr = <span class="keyword">new</span> <span class="type">int</span>;</span><br><span class="line"><span class="function">std::shared_ptr&lt;<span class="type">int</span>&gt; <span class="title">ptr</span><span class="params">(rawPtr)</span></span>;</span><br><span class="line"><span class="keyword">delete</span> rawPtr; <span class="comment">// 错误！不应该直接删除由shared_ptr管理的对象</span></span><br></pre></td></tr></table></figure></div>

<p>由于<code>shared_ptr</code>负责管理对象的内存，因此不应该直接使用<code>delete</code>来删除由<code>shared_ptr</code>管理的对象。在上面的示例中，试图通过<code>delete</code>关键字删除原始指针，这将导致<code>shared_ptr</code>尝试删除一个无效的指针，可能引发未定义行为。</p>
<p><strong>错误示例 2：使用<code>shared_ptr</code>管理静态对象或栈上对象</strong></p>
<div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> value = <span class="number">42</span>;</span><br><span class="line"><span class="function">std::shared_ptr&lt;<span class="type">int</span>&gt; <span class="title">ptr</span><span class="params">(&amp;value)</span></span>; <span class="comment">// 错误！不应该使用shared_ptr管理静态或栈上对象</span></span><br></pre></td></tr></table></figure></div>

<p><code>shared_ptr</code>应该用于管理动态分配的对象，而不是静态对象或栈上对象。</p>
<p>在上面的示例中，试图将一个指向静态对象的指针传递给<code>shared_ptr</code>的构造函数是错误的。</p>
<p><strong>错误示例 3：使用裸指针与<code>shared_ptr</code>共享所有权</strong></p>
<div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span>* rawPtr = <span class="keyword">new</span> <span class="type">int</span>;</span><br><span class="line"><span class="function">std::shared_ptr&lt;<span class="type">int</span>&gt; <span class="title">ptr1</span><span class="params">(rawPtr)</span></span>;</span><br><span class="line"><span class="function">std::shared_ptr&lt;<span class="type">int</span>&gt; <span class="title">ptr2</span><span class="params">(rawPtr)</span></span>; <span class="comment">// 错误！不能与shared_ptr共享同一个原始指针</span></span><br></pre></td></tr></table></figure></div>

<p>由于<code>shared_ptr</code>采用共享所有权的模式，它不适用于与其他<code>shared_ptr</code>或<code>weak_ptr</code>共享同一个原始指针。</p>
<p>这段代码是错误的，因为它试图将同一个原始指针 <code>rawPtr</code> 传递给两个独立的 <code>shared_ptr</code> 对象 <code>ptr1</code> 和 <code>ptr2</code>。这会导致引用计数错误，可能导致多次释放同一内存块，从而引发未定义行为。</p>
<p>将同一个原始指针直接传递给多个 <code>shared_ptr</code> 对象是错误的，会导致引用计数错误。</p>
<p>正确的方式是通过拷贝构造函数或拷贝赋值运算符从一个 <code>shared_ptr</code> 获得原始指针的所有权，并确保它们共享同一个对象的引用计数。</p>
<p><strong>错误示例 4：循环引用导致内存泄漏</strong></p>
<div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Node</span> </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    std::shared_ptr&lt;Node&gt; next;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function">std::shared_ptr&lt;Node&gt; <span class="title">node1</span><span class="params">(<span class="keyword">new</span> Node)</span></span>;</span><br><span class="line"><span class="function">std::shared_ptr&lt;Node&gt; <span class="title">node2</span><span class="params">(<span class="keyword">new</span> Node)</span></span>;</span><br><span class="line">node1-&gt;next = node2;</span><br><span class="line">node2-&gt;next = node1; <span class="comment">// 错误！循环引用导致对象无法正确释放</span></span><br></pre></td></tr></table></figure></div>

<p>当存在循环引用时，对象无法被正确释放，导致内存泄漏。</p>
<p>在上面的示例中，<code>node1</code>和<code>node2</code>之间形成了循环引用，它们相互持有对方的<code>shared_ptr</code>，当<code>node1</code>生命周期结束时会进行释放，但是内部有一个<code>shared_ptr</code>的智能指针于是就要释放这个智能指针，这个智能指针指向是<code>node2</code>，于是就要释放<code>node2</code>，但是<code>node2</code>内部的<code>shared_ptr</code>的智能指针又指向<code>node1</code>，于是又要释放<code>node1</code>，如此形成闭环，导致对象无法正确释放。</p>
<h3 id="成员函数详解-1"><a href="#成员函数详解-1" class="headerlink" title="成员函数详解"></a>成员函数详解</h3><hr>
<p><code>shared_ptr</code>类提供了一系列成员函数来管理和操作智能指针，以下是它的常用成员函数及其作用：</p>
<ul>
<li><p>**<code>reset()</code>**：重置<code>shared_ptr</code>，释放当前拥有的对象，并接管新的对象。</p>
 <div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">std::shared_ptr&lt;<span class="type">int</span>&gt; <span class="title">ptr</span><span class="params">(<span class="keyword">new</span> <span class="type">int</span>)</span></span>;</span><br><span class="line"><span class="comment">// ...</span></span><br><span class="line">ptr.<span class="built_in">reset</span>(<span class="keyword">new</span> <span class="type">int</span>); <span class="comment">// 重置shared_ptr，释放旧对象并接管新对象</span></span><br></pre></td></tr></table></figure></div>
</li>
<li><p>**<code>get()</code>**：返回指向<code>shared_ptr</code>管理的对象的原始指针。</p>
 <div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">std::shared_ptr&lt;<span class="type">int</span>&gt; <span class="title">ptr</span><span class="params">(<span class="keyword">new</span> <span class="type">int</span>)</span></span>;</span><br><span class="line"><span class="type">int</span>* rawPtr = ptr.<span class="built_in">get</span>(); <span class="comment">// 获取指向对象的原始指针</span></span><br></pre></td></tr></table></figure></div>
</li>
<li><p>**<code>use_count()</code>**：返回当前<code>shared_ptr</code>对象所管理对象的引用计数，即共享该对象的智能指针的数量。</p>
 <div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">std::shared_ptr&lt;<span class="type">int</span>&gt; <span class="title">ptr1</span><span class="params">(<span class="keyword">new</span> <span class="type">int</span>)</span></span>;</span><br><span class="line">std::shared_ptr&lt;<span class="type">int</span>&gt; ptr2 = ptr1;</span><br><span class="line"><span class="type">int</span> count = ptr1.<span class="built_in">use_count</span>(); <span class="comment">// 获取引用计数，值为2</span></span><br></pre></td></tr></table></figure></div>
</li>
<li><p>**<code>unique()</code>**：检查当前<code>shared_ptr</code>是否是唯一的（引用计数为1），即没有其他智能指针与之共享对象。</p>
 <div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">std::shared_ptr&lt;<span class="type">int</span>&gt; <span class="title">ptr1</span><span class="params">(<span class="keyword">new</span> <span class="type">int</span>)</span></span>;</span><br><span class="line">std::shared_ptr&lt;<span class="type">int</span>&gt; ptr2 = ptr1;</span><br><span class="line"><span class="keyword">if</span> (ptr1.<span class="built_in">unique</span>()) </span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// ptr1是唯一的智能指针</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>
</li>
<li><p>**<code>operator*()</code>**：重载解引用操作符，返回<code>shared_ptr</code>管理对象的引用。</p>
 <div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">std::shared_ptr&lt;<span class="type">int</span>&gt; <span class="title">ptr</span><span class="params">(<span class="keyword">new</span> <span class="type">int</span>)</span></span>;</span><br><span class="line">*ptr = <span class="number">42</span>; <span class="comment">// 解引用shared_ptr并赋值</span></span><br></pre></td></tr></table></figure></div>
</li>
<li><p>**<code>operator-&gt;()</code>**：重载箭头操作符，返回指向<code>shared_ptr</code>管理对象的指针。</p>
 <div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">std::shared_ptr&lt;std::string&gt; <span class="title">ptr</span><span class="params">(<span class="keyword">new</span> std::string(<span class="string">&quot;Hello&quot;</span>))</span></span>;</span><br><span class="line">std::cout &lt;&lt; ptr-&gt;<span class="built_in">size</span>() &lt;&lt; std::endl; <span class="comment">// 使用箭头操作符访问对象的成员函数</span></span><br></pre></td></tr></table></figure></div>
</li>
<li><p>**<code>operator bool()</code>**：将<code>shared_ptr</code>转换为布尔值，判断指针是否为空（是否指向对象）。</p>
 <div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line">std::shared_ptr&lt;<span class="type">int</span>&gt; ptr;</span><br><span class="line"><span class="keyword">if</span> (ptr) </span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 指针不为空</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>
</li>
<li><p>**<code>swap()</code>**：交换两个<code>shared_ptr</code>对象所管理的对象。</p>
 <div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">std::shared_ptr&lt;<span class="type">int</span>&gt; <span class="title">ptr1</span><span class="params">(<span class="keyword">new</span> <span class="type">int</span>(<span class="number">1</span>))</span></span>;</span><br><span class="line"><span class="function">std::shared_ptr&lt;<span class="type">int</span>&gt; <span class="title">ptr2</span><span class="params">(<span class="keyword">new</span> <span class="type">int</span>(<span class="number">2</span>))</span></span>;</span><br><span class="line">ptr1.<span class="built_in">swap</span>(ptr2); <span class="comment">// 交换ptr1和ptr2的对象</span></span><br></pre></td></tr></table></figure></div>
</li>
<li><p>**<code>operator=</code>**：重载赋值操作符，用于将一个<code>shared_ptr</code>赋值给另一个。赋值后，左侧的<code>shared_ptr</code>接管右侧的对象，并且引用计数增加。</p>
 <div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">std::shared_ptr&lt;<span class="type">int</span>&gt; <span class="title">ptr1</span><span class="params">(<span class="keyword">new</span> <span class="type">int</span>)</span></span>;</span><br><span class="line">std::shared_ptr&lt;<span class="type">int</span>&gt; ptr2;</span><br><span class="line">ptr2 = ptr1; <span class="comment">// 将ptr1的对象所有权转移给ptr2，并增加引用计数</span></span><br></pre></td></tr></table></figure></div>
</li>
<li><p>**<code>owner_before()</code>**：比较两个<code>shared_ptr</code>对象的所有权关系，用于确定它们在所有权层级中的位置。</p>
  <div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">std::shared_ptr&lt;<span class="type">int</span>&gt; <span class="title">ptr1</span><span class="params">(<span class="keyword">new</span> <span class="type">int</span>)</span></span>;</span><br><span class="line"><span class="function">std::shared_ptr&lt;<span class="type">int</span>&gt; <span class="title">ptr2</span><span class="params">(<span class="keyword">new</span> <span class="type">int</span>)</span></span>;</span><br><span class="line"><span class="keyword">if</span> (ptr1.<span class="built_in">owner_before</span>(ptr2)) </span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// ptr1在ptr2之前（所有权层级中）</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>
</li>
<li><p><strong><code>reset()</code>（重载版本）</strong>：重置<code>shared_ptr</code>，释放当前拥有的对象，并接管新的对象，并使用自定义删除器。</p>
  <div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">CustomDeleter</span> </span><br><span class="line">&#123;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">operator</span><span class="params">()</span><span class="params">(<span class="type">int</span>* ptr)</span> <span class="type">const</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="comment">// 自定义的对象销毁操作</span></span><br><span class="line">        <span class="keyword">delete</span> ptr;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function">std::shared_ptr&lt;<span class="type">int</span>&gt; <span class="title">ptr</span><span class="params">(<span class="keyword">new</span> <span class="type">int</span>, CustomDeleter())</span></span>;</span><br><span class="line"><span class="comment">// ...</span></span><br><span class="line">ptr.<span class="built_in">reset</span>(<span class="keyword">new</span> <span class="type">int</span>, <span class="built_in">CustomDeleter</span>()); <span class="comment">// 重置shared_ptr，释放旧对象并接管新对象</span></span><br></pre></td></tr></table></figure></div></li>
</ul>
<h3 id="使用make-shared创建和初始化"><a href="#使用make-shared创建和初始化" class="headerlink" title="使用make_shared创建和初始化"></a>使用<code>make_shared</code>创建和初始化</h3><p>**<code>make_shared()</code>**：创建一个包含动态分配对象的<code>shared_ptr</code>，并在分配期间同时构造对象。</p>
<p>使用方式与<code>make_unique</code>几乎一样详细参考上节使用《<code>make_unique</code>创建和初始化》的详解。</p>
<div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line">std::shared_ptr&lt;<span class="type">int</span>&gt; ptr = std::<span class="built_in">make_shared</span>&lt;<span class="type">int</span>&gt;(<span class="number">42</span>);</span><br><span class="line"><span class="comment">// 使用make_shared创建shared_ptr，并传递构造参数</span></span><br></pre></td></tr></table></figure></div>

<h3 id="enable-shared-from-this"><a href="#enable-shared-from-this" class="headerlink" title="enable_shared_from_this"></a><code>enable_shared_from_this</code></h3><p><code>std::enable_shared_from_this</code>是一个模板类，定义在<code>&lt;memory&gt;</code>头文件中，它是为了支持在已由<code>std::shared_ptr</code>管理的对象内部安全地获取指向自身的<code>std::shared_ptr</code>而设计的。</p>
<p>为什么要使用<code>enable_shared_from_this</code>:</p>
<ul>
<li><p>在已由<code>std::shared_ptr</code>管理的对象内部安全地获取指向自身的<code>std::shared_ptr</code>，它允许对象自己管理其共享所有权。</p>
</li>
<li><p>共享所有权：当一个对象需要与其他对象共享所有权时，可以使用<code>std::shared_ptr</code>来管理它们之间的关系。通过使用<code>std::enable_shared_from_this</code>，对象可以方便地获取指向自身的共享指针，并与其他对象共享所有权，从而确保对象在使用期间保持有效，直到没有任何共享指针引用它时才会被销毁。</p>
</li>
<li><p>回调函数：在某些情况下，对象可能需要注册回调函数，并在特定事件发生时调用这些回调函数。</p>
<p>使用<code>std::enable_shared_from_this</code>可以安全地将对象自身作为回调函数的参数传递，以避免对象在回调函数执行期间被意外销毁。</p>
</li>
<li><p>继承关系：当存在基类和派生类的继承关系时，派生类可以继承<code>std::enable_shared_from_this</code>，以便获取指向自身的共享指针。</p>
<p>这对于在派生类中访问基类成员或在派生类之间建立共享关系非常有用。</p>
</li>
</ul>
<p>使用<code>std::enable_shared_from_this</code>需要遵循以下几个步骤：</p>
<ul>
<li><p>创建一个类并公开继承自<code>std::enable_shared_from_this</code>模板类，将该类作为<code>std::shared_ptr</code>所管理的对象。</p>
</li>
<li><p>在类中添加需要使用<code>std::shared_ptr</code>的成员函数，这些函数将内部调用<code>shared_from_this()</code>函数来获取指向自身的<code>std::shared_ptr</code>。</p>
</li>
</ul>
<p>示例代码：</p>
<div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;memory&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">MyClass</span> : <span class="keyword">public</span> std::enable_shared_from_this&lt;MyClass&gt; </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function">std::shared_ptr&lt;MyClass&gt; <span class="title">getSharedPointer</span><span class="params">()</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">shared_from_this</span>();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    std::shared_ptr&lt;MyClass&gt; ptr1 = std::<span class="built_in">make_shared</span>&lt;MyClass&gt;();</span><br><span class="line">    std::shared_ptr&lt;MyClass&gt; ptr2 = ptr1-&gt;<span class="built_in">getSharedPointer</span>();</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// ptr1和ptr2指向同一个对象</span></span><br><span class="line">    <span class="keyword">if</span> (ptr1 == ptr2) </span><br><span class="line">    &#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;ptr1 and ptr2 point to the same object.&quot;</span> &lt;&lt; std::endl;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>在上述示例中，<code>MyClass</code>公开继承自<code>std::enable_shared_from_this&lt;MyClass&gt;</code>，并定义了<code>getSharedPointer()</code>成员函数。</p>
<p>在<code>getSharedPointer()</code>函数内部，我们调用了<code>shared_from_this()</code>函数，该函数将返回一个指向当前对象的<code>std::shared_ptr</code>。</p>
<p>在<code>main()</code>函数中，我们首先创建了一个<code>std::shared_ptr</code>指向<code>MyClass</code>的对象<code>ptr1</code>，然后通过调用<code>ptr1-&gt;getSharedPointer()</code>获取了另一个指向相同对象的<code>std::shared_ptr</code>，即<code>ptr2</code>。由于使用了<code>shared_from_this()</code>，<code>ptr1</code>和<code>ptr2</code>指向的是同一个对象，它们共享对象的所有权。</p>
<p>需要注意的是，<code>shared_from_this()</code>只能在已由<code>std::shared_ptr</code>管理的对象内部调用，否则行为是未定义的。</p>
<p>此外，为了安全使用<code>shared_from_this()</code>，必须确保在调用该函数之前已经创建了有效的<code>std::shared_ptr</code>对象来管理当前对象，否则会导致悬空指针问题。</p>
<h2 id="weak-ptr"><a href="#weak-ptr" class="headerlink" title="weak_ptr"></a><code>weak_ptr</code></h2><h3 id="概念和用法-2"><a href="#概念和用法-2" class="headerlink" title="概念和用法"></a>概念和用法</h3><p><code>weak_ptr</code>是一种智能指针，用于解决循环引用的问题。（关于循环引用参考上一节的《错误使用示例》中的”错误示例4“）</p>
<p>循环引用是指两个或多个对象彼此保持对方的引用，导致它们无法被正常地销毁。</p>
<p><code>weak_ptr</code>允许创建一个非拥有（non-owning）的指针，它可以观察和访问对象，但不会增加其引用计数。</p>
<p>当被观察的对象被销毁时，<code>weak_ptr</code>会自动变为<code>nullptr</code>，从而避免悬挂指针（dangling pointer）的问题。</p>
<p><code>weak_ptr</code>通常与<code>shared_ptr</code>一起使用。</p>
<p><code>weak_ptr</code>可以从<code>shared_ptr</code>创建，但不会增加引用计数。</p>
<p>这样，即使存在循环引用，当所有的<code>shared_ptr</code>都释放了对对象的拥有权后，对象也能被正确地销毁。</p>
<p>下面是一个简单的示例代码，演示了如何使用<code>weak_ptr</code>：</p>
<div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;memory&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">MyClass</span> </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    std::weak_ptr&lt;MyClass&gt; otherPtr;</span><br><span class="line"></span><br><span class="line">    ~<span class="built_in">MyClass</span>() </span><br><span class="line">    &#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;MyClass destroyed&quot;</span> &lt;&lt; std::endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    std::shared_ptr&lt;MyClass&gt; sharedPtr1 = std::<span class="built_in">make_shared</span>&lt;MyClass&gt;();</span><br><span class="line">    std::shared_ptr&lt;MyClass&gt; sharedPtr2 = std::<span class="built_in">make_shared</span>&lt;MyClass&gt;();</span><br><span class="line"></span><br><span class="line">    sharedPtr1-&gt;otherPtr = sharedPtr2;</span><br><span class="line">    sharedPtr2-&gt;otherPtr = sharedPtr1;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 释放sharedPtr1和sharedPtr2的拥有权</span></span><br><span class="line">    sharedPtr1.<span class="built_in">reset</span>();</span><br><span class="line">    sharedPtr2.<span class="built_in">reset</span>();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 在上面的reset操作后，循环引用被解除，两个MyClass对象都被销毁</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>在上面的示例中，我们创建了两个<code>shared_ptr</code>对象<code>sharedPtr1</code>和<code>sharedPtr2</code>，并将它们的<code>otherPtr</code>成员变量设置为彼此。这样就形成了一个循环引用。</p>
<p>然后，我们通过调用<code>reset()</code>方法释放了<code>sharedPtr1</code>和<code>sharedPtr2</code>的拥有权。</p>
<p>由于这是最后一次拥有对这两个对象的引用，释放之后的引用计数变为0，<code>shared_ptr</code>会自动销毁这两个对象。</p>
<p>使用<code>weak_ptr</code>可以避免悬挂指针的问题，因为在对象被销毁后，<code>weak_ptr</code>会自动变为<code>nullptr</code>。</p>
<h3 id="成员函数详解-2"><a href="#成员函数详解-2" class="headerlink" title="成员函数详解"></a>成员函数详解</h3><hr>
<ul>
<li><p>**<code>expired()</code>**：用于检查<code>weak_ptr</code>是否失效，即指向的对象是否已经被销毁。如果<code>weak_ptr</code>指向的对象已经销毁，则返回<code>true</code>；否则返回<code>false</code>。</p>
<p>示例代码：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line">std::shared_ptr&lt;<span class="type">int</span>&gt; sharedPtr = std::<span class="built_in">make_shared</span>&lt;<span class="type">int</span>&gt;(<span class="number">42</span>);</span><br><span class="line">std::weak_ptr&lt;<span class="type">int</span>&gt; weakPtr = sharedPtr;</span><br><span class="line"></span><br><span class="line">sharedPtr.<span class="built_in">reset</span>(); <span class="comment">// 销毁sharedPtr所拥有的对象</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (weakPtr.<span class="built_in">expired</span>()) </span><br><span class="line">&#123;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;weakPtr is expired&quot;</span> &lt;&lt; std::endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>
</li>
<li><p>**<code>lock()</code>**：用于获取一个指向<code>weak_ptr</code>所指向对象的<code>shared_ptr</code>，如果<code>weak_ptr</code>已经失效，则返回一个空的<code>shared_ptr</code>（即<code>nullptr</code>）。</p>
<p>示例代码：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line">std::shared_ptr&lt;<span class="type">int</span>&gt; sharedPtr = std::<span class="built_in">make_shared</span>&lt;<span class="type">int</span>&gt;(<span class="number">42</span>);</span><br><span class="line">std::weak_ptr&lt;<span class="type">int</span>&gt; weakPtr = sharedPtr;</span><br><span class="line"></span><br><span class="line">sharedPtr.<span class="built_in">reset</span>(); <span class="comment">// 销毁sharedPtr所拥有的对象</span></span><br><span class="line"></span><br><span class="line">std::shared_ptr&lt;<span class="type">int</span>&gt; lockedPtr = weakPtr.<span class="built_in">lock</span>();</span><br><span class="line"><span class="keyword">if</span> (lockedPtr) </span><br><span class="line">&#123;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;Value: &quot;</span> &lt;&lt; *lockedPtr &lt;&lt; std::endl;</span><br><span class="line">&#125; </span><br><span class="line"><span class="keyword">else</span> </span><br><span class="line">&#123;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;weakPtr is expired&quot;</span> &lt;&lt; std::endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>
</li>
<li><p>**<code>use_count()</code>**：返回与<code>weak_ptr</code>共享对象的<code>shared_ptr</code>的引用计数。如果<code>weak_ptr</code>已经失效，则返回0。</p>
<p>示例代码：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line">std::shared_ptr&lt;<span class="type">int</span>&gt; sharedPtr = std::<span class="built_in">make_shared</span>&lt;<span class="type">int</span>&gt;(<span class="number">42</span>);</span><br><span class="line">std::weak_ptr&lt;<span class="type">int</span>&gt; weakPtr = sharedPtr;</span><br><span class="line"></span><br><span class="line">std::cout &lt;&lt; <span class="string">&quot;sharedPtr use count: &quot;</span> &lt;&lt; sharedPtr.<span class="built_in">use_count</span>() &lt;&lt; std::endl;</span><br><span class="line">std::cout &lt;&lt; <span class="string">&quot;weakPtr use count: &quot;</span> &lt;&lt; weakPtr.<span class="built_in">use_count</span>() &lt;&lt; std::endl;</span><br><span class="line"></span><br><span class="line">std::shared_ptr&lt;<span class="type">int</span>&gt; lockedPtr = weakPtr.<span class="built_in">lock</span>();</span><br><span class="line"></span><br><span class="line">std::cout &lt;&lt; <span class="string">&quot;sharedPtr use count: &quot;</span> &lt;&lt; sharedPtr.<span class="built_in">use_count</span>() &lt;&lt; std::endl;</span><br><span class="line">std::cout &lt;&lt; <span class="string">&quot;weakPtr use count: &quot;</span> &lt;&lt; weakPtr.<span class="built_in">use_count</span>() &lt;&lt; std::endl;</span><br></pre></td></tr></table></figure></div>
</li>
<li><p>**<code>reset()</code>**：将<code>weak_ptr</code>置为空，不再指向任何对象。</p>
<p>示例代码：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line">std::shared_ptr&lt;<span class="type">int</span>&gt; sharedPtr = std::<span class="built_in">make_shared</span>&lt;<span class="type">int</span>&gt;(<span class="number">42</span>);</span><br><span class="line">std::weak_ptr&lt;<span class="type">int</span>&gt; weakPtr = sharedPtr;</span><br><span class="line"></span><br><span class="line">std::cout &lt;&lt; <span class="string">&quot;weakPtr expired? &quot;</span> &lt;&lt; weakPtr.<span class="built_in">expired</span>() &lt;&lt; std::endl;</span><br><span class="line"></span><br><span class="line">weakPtr.<span class="built_in">reset</span>();</span><br><span class="line"></span><br><span class="line">std::cout &lt;&lt; <span class="string">&quot;weakPtr expired? &quot;</span> &lt;&lt; weakPtr.<span class="built_in">expired</span>() &lt;&lt; std::endl;</span><br></pre></td></tr></table></figure></div>

</li>
<li><p>**<code>owner_before()</code>**：比较两个<code>weak_ptr</code>对象所指向的对象所有权的相对顺序。如果当前<code>weak_ptr</code>对象的所指向对象的所有权在另一个<code>weak_ptr</code>对象之前，则返回<code>true</code>；否则返回<code>false</code>。</p>
<p>该函数用于定义<code>weak_ptr</code>对象的排序关系，例如在关联容器（如<code>std::map</code>、<code>std::set</code>）中使用。</p>
<p>示例代码：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line">std::shared_ptr&lt;<span class="type">int</span>&gt; sharedPtr1 = std::<span class="built_in">make_shared</span>&lt;<span class="type">int</span>&gt;(<span class="number">42</span>);</span><br><span class="line">std::shared_ptr&lt;<span class="type">int</span>&gt; sharedPtr2 = std::<span class="built_in">make_shared</span>&lt;<span class="type">int</span>&gt;(<span class="number">24</span>);</span><br><span class="line"></span><br><span class="line">std::weak_ptr&lt;<span class="type">int</span>&gt; weakPtr1 = sharedPtr1;</span><br><span class="line">std::weak_ptr&lt;<span class="type">int</span>&gt; weakPtr2 = sharedPtr2;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (weakPtr1.<span class="built_in">owner_before</span>(weakPtr2))</span><br><span class="line">&#123;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;weakPtr1&#x27;s object is owned before weakPtr2&#x27;s object&quot;</span> &lt;&lt; std::endl;</span><br><span class="line">&#125; </span><br><span class="line"><span class="keyword">else</span> </span><br><span class="line">&#123;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;weakPtr2&#x27;s object is owned before weakPtr1&#x27;s object&quot;</span> &lt;&lt; std::endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>
</li>
<li><p>**<code>swap()</code>**：交换两个<code>weak_ptr</code>对象的内容。</p>
<p>示例代码：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line">std::shared_ptr&lt;<span class="type">int</span>&gt; sharedPtr1 = std::<span class="built_in">make_shared</span>&lt;<span class="type">int</span>&gt;(<span class="number">42</span>);</span><br><span class="line">std::shared_ptr&lt;<span class="type">int</span>&gt; sharedPtr2 = std::<span class="built_in">make_shared</span>&lt;<span class="type">int</span>&gt;(<span class="number">24</span>);</span><br><span class="line"></span><br><span class="line">std::weak_ptr&lt;<span class="type">int</span>&gt; weakPtr1 = sharedPtr1;</span><br><span class="line">std::weak_ptr&lt;<span class="type">int</span>&gt; weakPtr2 = sharedPtr2;</span><br><span class="line"></span><br><span class="line">std::cout &lt;&lt; <span class="string">&quot;Before swap:&quot;</span> &lt;&lt; std::endl;</span><br><span class="line">std::cout &lt;&lt; <span class="string">&quot;weakPtr1 expired? &quot;</span> &lt;&lt; weakPtr1.<span class="built_in">expired</span>() &lt;&lt; std::endl;</span><br><span class="line">std::cout &lt;&lt; <span class="string">&quot;weakPtr2 expired? &quot;</span> &lt;&lt; weakPtr2.<span class="built_in">expired</span>() &lt;&lt; std::endl;</span><br><span class="line"></span><br><span class="line">weakPtr1.<span class="built_in">swap</span>(weakPtr2);</span><br><span class="line"></span><br><span class="line">std::cout &lt;&lt; <span class="string">&quot;After swap:&quot;</span> &lt;&lt; std::endl;</span><br><span class="line">std::cout &lt;&lt; <span class="string">&quot;weakPtr1 expired? &quot;</span> &lt;&lt; weakPtr1.<span class="built_in">expired</span>() &lt;&lt; std::endl;</span><br><span class="line">std::cout &lt;&lt; <span class="string">&quot;weakPtr2 expired? &quot;</span> &lt;&lt; weakPtr2.<span class="built_in">expired</span>() &lt;&lt; std::endl;</span><br></pre></td></tr></table></figure></div>
</li>
<li><p>**<code>operator=</code>**：允许将一个<code>std::weak_ptr</code>对象赋值给另一个<code>std::weak_ptr</code>对象。</p>
<p>赋值操作符执行的是浅拷贝，将右侧的<code>std::weak_ptr</code>对象的状态复制到左侧的对象。</p>
<p>示例代码：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;memory&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    std::shared_ptr&lt;<span class="type">int</span>&gt; sharedPtr1 = std::<span class="built_in">make_shared</span>&lt;<span class="type">int</span>&gt;(<span class="number">42</span>);</span><br><span class="line">    std::shared_ptr&lt;<span class="type">int</span>&gt; sharedPtr2 = std::<span class="built_in">make_shared</span>&lt;<span class="type">int</span>&gt;(<span class="number">24</span>);</span><br><span class="line"></span><br><span class="line">    std::weak_ptr&lt;<span class="type">int</span>&gt; weakPtr1 = sharedPtr1;</span><br><span class="line">    std::weak_ptr&lt;<span class="type">int</span>&gt; weakPtr2 = sharedPtr2;</span><br><span class="line"></span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;weakPtr1 expired? &quot;</span> &lt;&lt; weakPtr1.<span class="built_in">expired</span>() &lt;&lt; std::endl;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;weakPtr2 expired? &quot;</span> &lt;&lt; weakPtr2.<span class="built_in">expired</span>() &lt;&lt; std::endl;</span><br><span class="line"></span><br><span class="line">    weakPtr1 = weakPtr2;</span><br><span class="line"></span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;After assignment:&quot;</span> &lt;&lt; std::endl;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;weakPtr1 expired? &quot;</span> &lt;&lt; weakPtr1.<span class="built_in">expired</span>() &lt;&lt; std::endl;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;weakPtr2 expired? &quot;</span> &lt;&lt; weakPtr2.<span class="built_in">expired</span>() &lt;&lt; std::endl;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>赋值操作符执行的是浅拷贝，只复制了 <code>std::weak_ptr</code> 对象的内部状态，而没有增加引用计数或改变底层的引用关系。</p>
<p>在示例代码的输出中，<code>weakPtr1</code> 的失效状态（即 <code>expired()</code> 返回的值）与 <code>weakPtr2</code> 相同，因为它们指向相同的对象。</p>
<p>使用<code>operator=</code>将一个 <code>std::weak_ptr</code> 对象赋值给另一个，它们将共享相同的状态信息。</p>
</li>
</ul>
<h2 id="相关类和全局函数"><a href="#相关类和全局函数" class="headerlink" title="相关类和全局函数"></a>相关类和全局函数</h2><hr>
<ul>
<li><p>**<code>operator==</code> 和 <code>operator!=</code>**：用于比较两个<code>unique_ptr</code>或<code>shared_ptr</code>对象是否相等或不相等。</p>
<div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">std::unique_ptr&lt;<span class="type">int</span>&gt; <span class="title">ptr1</span><span class="params">(<span class="keyword">new</span> <span class="type">int</span>(<span class="number">1</span>))</span></span>;</span><br><span class="line"><span class="function">std::unique_ptr&lt;<span class="type">int</span>&gt; <span class="title">ptr2</span><span class="params">(<span class="keyword">new</span> <span class="type">int</span>(<span class="number">2</span>))</span></span>;</span><br><span class="line"><span class="keyword">if</span> (ptr1 == ptr2) </span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// ptr1和ptr2指向相同的对象</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">std::shared_ptr&lt;<span class="type">int</span>&gt; <span class="title">ptr1</span><span class="params">(<span class="keyword">new</span> <span class="type">int</span>(<span class="number">1</span>))</span></span>;</span><br><span class="line"><span class="function">std::shared_ptr&lt;<span class="type">int</span>&gt; <span class="title">ptr2</span><span class="params">(<span class="keyword">new</span> <span class="type">int</span>(<span class="number">2</span>))</span></span>;</span><br><span class="line"><span class="keyword">if</span> (ptr1 == ptr2) </span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// ptr1和ptr2指向相同的对象</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>


</li>
<li><p>**<code>operator&lt;</code>, <code>operator&lt;=</code>, <code>operator&gt;</code>, <code>operator&gt;=</code>**：用于比较两个<code>unique_ptr</code>或<code>shared_ptr</code>对象的大小关系（按照指针地址比较）。</p>
<div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">std::unique_ptr&lt;<span class="type">int</span>&gt; <span class="title">ptr1</span><span class="params">(<span class="keyword">new</span> <span class="type">int</span>(<span class="number">1</span>))</span></span>;</span><br><span class="line"><span class="function">std::unique_ptr&lt;<span class="type">int</span>&gt; <span class="title">ptr2</span><span class="params">(<span class="keyword">new</span> <span class="type">int</span>(<span class="number">2</span>))</span></span>;</span><br><span class="line"><span class="keyword">if</span> (ptr1 &lt; ptr2) </span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// ptr1的指针地址小于ptr2的指针地址</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line">  <span class="function">std::shared_ptr&lt;<span class="type">int</span>&gt; <span class="title">ptr1</span><span class="params">(<span class="keyword">new</span> <span class="type">int</span>(<span class="number">1</span>))</span></span>;</span><br><span class="line">  <span class="function">std::shared_ptr&lt;<span class="type">int</span>&gt; <span class="title">ptr2</span><span class="params">(<span class="keyword">new</span> <span class="type">int</span>(<span class="number">2</span>))</span></span>;</span><br><span class="line">  <span class="keyword">if</span> (ptr1 &lt; ptr2)</span><br><span class="line">  &#123;</span><br><span class="line">     <span class="comment">// ptr1的指针地址小于ptr2的指针地址</span></span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">- **`std::hash`**：计算`shared_ptr`对象的哈希值，用于在哈希容器（如`unordered_map`、`unordered_set`）中使用。</span><br><span class="line"></span><br><span class="line">  ```<span class="function">cpp</span></span><br><span class="line"><span class="function">  std::shared_ptr&lt;<span class="type">int</span>&gt; <span class="title">ptr</span><span class="params">(<span class="keyword">new</span> <span class="type">int</span>)</span></span>;</span><br><span class="line">  std::hash&lt;std::shared_ptr&lt;<span class="type">int</span>&gt;&gt; hasher;</span><br><span class="line">  std::<span class="type">size_t</span> hashValue = <span class="built_in">hasher</span>(ptr); <span class="comment">// 计算shared_ptr的哈希值</span></span><br></pre></td></tr></table></figure></div>
</li>
<li><p>**<code>std::swap</code>**：用于交换两个智能指针的内容。可以在需要重新分配资源所有权的情况下，高效地交换两个智能指针。</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line">cppCopy codestd::shared_ptr&lt;<span class="type">int</span>&gt; ptr1 = std::<span class="built_in">make_shared</span>&lt;<span class="type">int</span>&gt;(<span class="number">42</span>);</span><br><span class="line">std::shared_ptr&lt;<span class="type">int</span>&gt; ptr2 = std::<span class="built_in">make_shared</span>&lt;<span class="type">int</span>&gt;(<span class="number">24</span>);</span><br><span class="line"></span><br><span class="line">std::<span class="built_in">swap</span>(ptr1, ptr2);</span><br></pre></td></tr></table></figure></div>
</li>
<li><p>**<code>std::get_deleter</code>**：用于获取智能指针中的删除器（deleter）的函数模板。可用于访问存储在特定类型智能指针对象中的删除器。</p>
<p>删除器是一种用于在智能指针释放资源时执行自定义清理操作的函数对象即仿函数。</p>
<p><code>std::get_deleter</code>函数接受一个<code>shared_ptr</code>指针对象作为参数，并返回对应的删除器的引用。</p>
<p>它只适用于<code>std::shared_ptr</code>共享指针。对于<code>unique_ptr</code>作为其成员函数。</p>
<p>下面是一个示例，演示如何使用<code>std::get_deleter</code>函数获取智能指针的删除器：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;memory&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Deleter</span> </span><br><span class="line">&#123;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">operator</span><span class="params">()</span><span class="params">(<span class="type">int</span>* ptr)</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;Deleting resource: &quot;</span> &lt;&lt; *ptr &lt;&lt; std::endl;</span><br><span class="line">        <span class="keyword">delete</span> ptr;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="function">std::shared_ptr&lt;<span class="type">int</span>&gt; <span class="title">sharedPtr</span><span class="params">(<span class="keyword">new</span> <span class="type">int</span>(<span class="number">42</span>), Deleter())</span></span>;</span><br><span class="line"></span><br><span class="line">    Deleter* deleter = std::<span class="built_in">get_deleter</span>&lt;Deleter, <span class="type">int</span>&gt;(sharedPtr);</span><br><span class="line">    <span class="comment">// 或者可以使用自动类型推断</span></span><br><span class="line">    <span class="keyword">auto</span> deleter = std::<span class="built_in">get_deleter</span>&lt;Deleter, <span class="type">int</span>&gt;(sharedPtr);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>在这个示例中，首先定义了一个自定义的删除器<code>Deleter</code>，它打印要删除的资源的值，并调用<code>delete</code>来释放内存。</p>
<p>然后，创建了一个<code>std::shared_ptr</code>，并将自定义删除器传递给它们。</p>
<p>接下来，使用<code>std::get_deleter</code>来获取智能指针中的删除器。</p>
<p>最后，可以使用获取到的删除器进行其他操作，例如在需要时手动释放资源。</p>
<p>注意，<code>std::get_deleter</code>的模板参数必须要显式指定删除器类型，智能指针维护的类型可以不显式指定。</p>
<p>示例（这样的代码也是可以的）：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;memory&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Deleter</span> </span><br><span class="line">&#123;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">operator</span><span class="params">()</span><span class="params">(<span class="type">int</span>* ptr)</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;Deleting resource: &quot;</span> &lt;&lt; *ptr &lt;&lt; std::endl;</span><br><span class="line">        <span class="keyword">delete</span> ptr;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="function">std::shared_ptr&lt;<span class="type">int</span>&gt; <span class="title">sharedPtr</span><span class="params">(<span class="keyword">new</span> <span class="type">int</span>(<span class="number">42</span>), Deleter())</span></span>;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">auto</span> deleter = std::<span class="built_in">get_deleter</span>&lt;Deleter&gt;(sharedPtr);<span class="comment">//不指定智能指针维护的类型</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div></li>
</ul>
<p>更多内容请查阅手册或参考这篇文章：<a class="link"   href="https://blog.csdn.net/CHYabc123456hh/article/details/109350925" > c++ memory 头文件详细介绍 <i class="fa-regular fa-arrow-up-right-from-square fa-sm"></i></a></p>
<h1 id="final关键字"><a href="#final关键字" class="headerlink" title="final关键字"></a><code>final</code>关键字</h1><p><code>final</code>是C++11中引入的一个新关键字，<code>final</code>关键字用于指示一个类、函数或虚函数不能被继承、重写或覆盖。</p>
<p>它可以被应用于类的声明、虚函数的声明和函数的声明。</p>
<ul>
<li><p>防止类被继承</p>
<p>使用 <code>final</code> 关键字修饰的类不能被继承。这意味着其他类无法派生自该类。</p>
<p>示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Base</span> <span class="keyword">final</span> </span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Derived</span> : <span class="keyword">public</span> Base <span class="comment">// 错误，无法从 final 类继承</span></span><br><span class="line">&#123; </span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></div>
</li>
<li><p>防止虚函数被重写</p>
<p>使用 <code>final</code> 关键字修饰的虚函数不能在派生类中被重写或覆盖。</p>
<p>这在继承体系中可以用于禁止派生类修改某个特定的虚函数。</p>
<p>示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Base</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">foo</span><span class="params">()</span> <span class="keyword">final</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="comment">// ...</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Derived</span> : <span class="keyword">public</span> Base </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">foo</span><span class="params">()</span> <span class="keyword">override</span>  <span class="comment">// 错误，无法重写 final 虚函数</span></span></span><br><span class="line"><span class="function">    </span>&#123; </span><br><span class="line">        <span class="comment">// ...</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></div>
</li>
<li><p>防止普通函数被重写</p>
<p>使用 <code>final</code> 关键字修饰的普通函数（非虚函数）不能在派生类中被重写。它的作用与 <code>final</code> 虚函数类似，但它适用于非虚函数。</p>
<p>示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Base</span> </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">foo</span><span class="params">()</span> <span class="keyword">final</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="comment">// ...</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Derived</span> : <span class="keyword">public</span> Base </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">foo</span><span class="params">()</span> <span class="comment">// 错误，无法重写 final 函数</span></span></span><br><span class="line"><span class="function">    </span>&#123;  </span><br><span class="line">        <span class="comment">// ...</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></div></li>
</ul>
<p>使用<code>final</code>关键字可以在C++中提供额外的控制权，以防止特定类、函数或虚函数被继承、重写或覆盖。</p>
<p>这可以在某些情况下提高代码的安全性和稳定性，以及确保特定实现不会被修改。</p>
<h1 id="override关键字"><a href="#override关键字" class="headerlink" title="override关键字"></a><code>override</code>关键字</h1><p><code>override</code>是C++11中引入的一个新关键字，用于显式地指定某个成员函数为虚函数的重写版本。</p>
<p>当你在派生类中重写基类的虚函数时，可以在该函数的声明后面加上<code>override</code>关键字，表示该函数是重写基类的虚函数。</p>
<p>使用<code>override</code>关键字有两个好处：</p>
<ul>
<li><p>它能够帮助你避免一些常见的错误。</p>
<p>例如：如果你在派生类中重写基类的虚函数时，将函数名或参数列表写错了或者是函数名写错了等。</p>
</li>
<li><p>它能够提高代码的可读性。当其他人阅读你的代码时，看到<code>override</code>关键字，就能够立即知道这是一个重写基类虚函数的函数。</p>
</li>
</ul>
<p>下面是一个简单的例子，演示如何使用<code>override</code>关键字：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Base</span> </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">foo</span><span class="params">()</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;Base::foo()&quot;</span> &lt;&lt; std::endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Derived</span> : <span class="keyword">public</span> Base </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">foo</span><span class="params">()</span> <span class="keyword">override</span> <span class="comment">//表示这是函数时重写父类虚函数的</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;Derived::foo()&quot;</span> &lt;&lt; std::endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Base* b = <span class="keyword">new</span> <span class="built_in">Derived</span>();</span><br><span class="line">    b-&gt;<span class="built_in">foo</span>(); <span class="comment">// 输出 &quot;Derived::foo()&quot;</span></span><br><span class="line">    <span class="keyword">delete</span> b;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>在上面的例子中，我们在派生类<code>Derived</code>中重写了基类<code>Base</code>的虚函数<code>foo()</code>。在重写该函数时，我们在函数声明后面加上了<code>override</code>关键字，表示这是一个重写基类虚函数的函数。</p>
<h1 id="inline关键字（内联函数）"><a href="#inline关键字（内联函数）" class="headerlink" title="inline关键字（内联函数）"></a><code>inline</code>关键字（内联函数）</h1><p>内联函数避免了宏函数的一些问题。</p>
<p>内联函数是 C++ 中用来提高函数执行效率的一种方法。它通过在编译时将函数的代码副本放置在每个调用该函数的地方来实现这一目的。</p>
<p>C++ 内联函数是通常与类一起使用。如果一个函数是内联的，那么在编译时，编译器会把该函数的代码副本放置在每个调用该函数的地方。</p>
<p>对内联函数进行任何修改，都需要重新编译函数的所有客户端，因为编译器需要重新更换一次所有的代码，否则将会继续使用旧的函数。</p>
<p>如果想把一个函数定义为内联函数，则需要在函数名前面放置关键字<code>inline</code>，在调用函数之前需要对函数进行定义。</p>
<p>&#x3D;&#x3D;编译器会自动根据函数的开销来选择是否忽略 <code>inline</code>限定符。&#x3D;&#x3D;</p>
<p>&#x3D;&#x3D;在类定义中的定义的函数都是内联函数，即使没有使用<code>inline</code>说明符。&#x3D;&#x3D;</p>
<p>&#x3D;&#x3D;内联仅仅只是给编译器一个建议，编译器不一定会接受这种建议&#x3D;&#x3D;，如果你没有将函数声明为内联函数，那么编译器也可能将此函数做内联编译。</p>
<p>一个好的编译器将会内联小的、简单的函数。</p>
<p>内联函数最适用于小函数，例如访问私有数据成员的函数。这些一行或两行代码的“访问器”函数的主要用途是返回有关对象的状态信息。</p>
<p>内联能提高函数的执行效率，那么为什么不把所有的函数都定义成内联函数呢？<br>内联以代码膨胀（拷贝）为代价，仅仅省区了函数调用的开销，从而提高程序的执行效率。（开销指的是参数的压栈、跳转、退栈和返回操作）。</p>
<p>一方面，如果执行函数体内代码的时间比函数调用的开销大得多，那么<code>inline</code>效率收益会很小。<br>另一方面，每一处内联函数的调用都要拷贝代码，使程序的总代码量增大，消耗更多的内存空间。<br>以下情况不宜使用内联：</p>
<ul>
<li>如果函数体内代码比较长，使用内联将导致可执行代码膨胀过大。</li>
<li>如果函数体内出现循环或者其他复杂的控制结构，那么执行函数体内代码的时间将比函数调用的开销大得多。</li>
</ul>
<p>因此，是否设置为内联需要权衡：</p>
<ul>
<li>如果调用开销大于执行开销，则设为内联函数；</li>
<li>否则不设为内联函数。</li>
</ul>
<p>下面是一个简单的内联函数示例，用来返回两个数中的最大值：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">inline</span> <span class="type">int</span> <span class="title">Max</span><span class="params">(<span class="type">int</span> x, <span class="type">int</span> y)</span> <span class="comment">//一个简单的小函数</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> (x &gt; y)? x : y;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;Max (20,10): &quot;</span> &lt;&lt; <span class="built_in">Max</span>(<span class="number">20</span>,<span class="number">10</span>) &lt;&lt; endl;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;Max (0,200): &quot;</span> &lt;&lt; <span class="built_in">Max</span>(<span class="number">0</span>,<span class="number">200</span>) &lt;&lt; endl;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;Max (100,1010): &quot;</span> &lt;&lt; <span class="built_in">Max</span>(<span class="number">100</span>,<span class="number">1010</span>) &lt;&lt; endl;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>当上面的代码被编译和执行时，它会产生下列结果：</p>
<div class="highlight-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">Max (20,10): 20</span><br><span class="line">Max (0,200): 200</span><br><span class="line">Max (100,1010): 1010</span><br></pre></td></tr></table></figure></div>

<p><a class="link"   href="https://learn.microsoft.com/zh-cn/cpp/cpp/inline-functions-cpp?view=msvc-170" >内联函数 (C++) | Microsoft Learn <i class="fa-regular fa-arrow-up-right-from-square fa-sm"></i></a><br> <a class="link"   href="https://bing.com/search?q=C+++%E5%86%85%E8%81%94%E5%87%BD%E6%95%B0" >c++ 内联函数 （讲解的TM真好） - CSDN博客 <i class="fa-regular fa-arrow-up-right-from-square fa-sm"></i></a><br><a class="link"   href="https://www.runoob.com/cplusplus/cpp-inline-functions.html" >C++ 内联函数 | 菜鸟教程 <i class="fa-regular fa-arrow-up-right-from-square fa-sm"></i></a><br><a class="link"   href="https://blog.csdn.net/u011327981/article/details/50601800" >c++ 内联函数 （讲解的TM真好） - CSDN博客 <i class="fa-regular fa-arrow-up-right-from-square fa-sm"></i></a><br><a class="link"   href="https://zhuanlan.zhihu.com/p/37436574" >C++内联函数能否是虚函数？ - 知乎 <i class="fa-regular fa-arrow-up-right-from-square fa-sm"></i></a><br><a class="link"   href="https://www.cnblogs.com/2018shawn/p/10851779.html" > C++内联函数的使用 - 余生以学 - 博客园 <i class="fa-regular fa-arrow-up-right-from-square fa-sm"></i></a></p>
<p>对于适合成为内联函数的函数，&#x3D;&#x3D;编译器会自动添加<code>inline</code>关键字使之成为内联函数，大部分场景程序员无需手动添加&#x3D;&#x3D;，因此内联函数不作重点，基本了解即可，这里不再做过多讲述，详细见上述的图文、视频讲解链接。</p>
<h1 id="noexcept关键字"><a href="#noexcept关键字" class="headerlink" title="noexcept关键字"></a><code>noexcept</code>关键字</h1><p><code>noexcept</code>用于指定函数是不会抛出异常的。这个关键字对于异常处理和代码优化非常有用。</p>
<p>异常接口声明的特性在C++11被移除，在未被移除之前，声明一个不会抛出异常的函数是这样的：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">func</span><span class="params">()</span> <span class="title">throw</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">//...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>该语法在C++11被移除，C++11后声明一个不会抛出异常的函数就要使用<code>noexcept</code>关键字</p>
<p>示例：</p>
<div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 声明一个会抛出异常的函数</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">mightThrow</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">throw</span> <span class="string">&quot;This function might throw an exception.&quot;</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 声明一个不会引发异常的函数</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">noThrow</span><span class="params">()</span> <span class="keyword">noexcept</span> <span class="comment">//noexcept表示函数不会抛出异常</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;This function will not throw any exception.&quot;</span> &lt;&lt; std::endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>如果，在有<code>noexcept</code>修饰的函数中抛出了异常，并且在调用时接收了异常，程序则会出现报错或崩溃。</p>
<h1 id="default关键字"><a href="#default关键字" class="headerlink" title="default关键字"></a><code>default</code>关键字</h1><p>在C++中，<code>default</code>关键字用于类中声明默认函数。</p>
<p>类中的默认函数有以下几种：</p>
<ol>
<li>默认构造函数</li>
<li>默认拷贝构造函数</li>
<li>默认拷贝赋值运算符（重载<code>=</code>运算符，参数是左值引用）</li>
<li>默认移动构造函数</li>
<li>默认移动赋值运算符（重载<code>=</code>运算符，参数是右值引用）</li>
<li>默认析构函数</li>
</ol>
<p>默认函数是编译器自动生成的特殊成员函数，用于执行默认的行为。</p>
<p><code>default</code>关键字可以用于以下几个地方：</p>
<ul>
<li><p>声明默认构造函数</p>
<p>如果在类中没有显式定义构造函数，或已经手动定义了有参构造函数，那么编译器就不会自动生成一个默认构造函数。</p>
<p>这时候在创建一个类对象时如果不想立即初始化，就会无法调用默认构造函数，导致编译报错。</p>
<p>示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MyClass</span> </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">MyClass</span>(<span class="type">int</span> x) </span><br><span class="line">    &#123;</span><br><span class="line">    	a = x;    </span><br><span class="line">    &#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">int</span> a;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Myclass A;<span class="comment">//编译报错，没有默认构造。因为手动定义了有参构造，编译器不再提供默认构造</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>可以使用<code>default</code>关键字来显式声明默认构造函数</p>
<p>示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MyClass</span> </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">MyClass</span>() = <span class="keyword">default</span>; <span class="comment">// 声明默认构造函数</span></span><br><span class="line">    </span><br><span class="line">    <span class="built_in">MyClass</span>(<span class="type">int</span> x) </span><br><span class="line">    &#123;</span><br><span class="line">    	a = x;    </span><br><span class="line">    &#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">int</span> a;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Myclass A;<span class="comment">//编译通过</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>
</li>
<li><p>声明默认拷贝构造函数</p>
<p>如果在类中没有显式定义拷贝构造函数，编译器会自动生成一个默认拷贝构造函数。</p>
<p>可以使用<code>default</code>关键字来显式声明默认拷贝构造函数</p>
<p>例如：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MyClass</span> </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">MyClass</span>(<span class="type">const</span> MyClass&amp; other) = <span class="keyword">default</span>; <span class="comment">// 声明默认拷贝构造函数</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></div>
</li>
<li><p>声明默认拷贝赋值运算符</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MyClass</span> </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    MyClass&amp; <span class="keyword">operator</span>=(<span class="type">const</span> MyClass&amp; other) = <span class="keyword">default</span>; <span class="comment">// 声明默认拷贝赋值运算符</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></div>
</li>
<li><p>声明默认移动构造函数</p>
<p>C++11引入了移动语义，可以通过移动构造函数和移动赋值运算符实现高效的资源管理。</p>
<p>如果在类中没有显式定义移动构造函数和移动赋值运算符，编译器会自动生成默认的移动构造函数和移动赋值运算符。</p>
<p>可以使用<code>default</code>关键字来显式声明默认移动构造函数和移动赋值运算符，</p>
<p>例如：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MyClass</span> </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">MyClass</span>(MyClass&amp;&amp; other) = <span class="keyword">default</span>; <span class="comment">// 声明默认移动构造函数</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></div>
</li>
<li><p>声明默认移动赋值运算符</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MyClass</span> </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    MyClass&amp; <span class="keyword">operator</span>=(MyClass&amp;&amp; other) = <span class="keyword">default</span>; <span class="comment">// 声明默认移动赋值运算符</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></div>
</li>
<li><p>声明默认析构函数</p>
<p>如果在类中没有显式定义析构函数，编译器会自动生成一个默认析构函数。</p>
<p>可以使用<code>default</code>关键字来显式声明默认析构函数。</p>
<p>例如：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MyClass</span> </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    ~<span class="built_in">MyClass</span>() = <span class="keyword">default</span>; <span class="comment">// 声明默认析构函数</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></div></li>
</ul>
<p><code>default</code>也可以在类外使用。</p>
<p>示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MyClass</span> </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">MyClass</span>();</span><br><span class="line">&#125;;</span><br><span class="line">MyClass::<span class="built_in">MyClass</span>() = <span class="keyword">default</span>;</span><br></pre></td></tr></table></figure></div>

<p>通过使用<code>default</code>关键字，可以显式声明使用编译器生成的默认函数，这对于需要手动定义其他函数而又需要默认函数的情况非常有用。</p>
<h1 id="delete禁用函数"><a href="#delete禁用函数" class="headerlink" title="=delete禁用函数"></a><code>=delete</code>禁用函数</h1><p>为了能够让程序员显式的禁用某个函数，C++11标准引入了一个新特性：使用<code>=delete</code>修饰函数。</p>
<p>只需在函数声明后上<code>=delete</code>，就可将该函数禁用。</p>
<p>示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MyClass</span> </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// 禁用默认构造函数</span></span><br><span class="line">    <span class="built_in">MyClass</span>() = <span class="keyword">delete</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 禁用拷贝构造函数</span></span><br><span class="line">    <span class="built_in">MyClass</span>(<span class="type">const</span> MyClass&amp;) = <span class="keyword">delete</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 禁用拷贝赋值运算符</span></span><br><span class="line">    MyClass&amp; <span class="keyword">operator</span>=(<span class="type">const</span> MyClass&amp;) = <span class="keyword">delete</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 禁用析构函数</span></span><br><span class="line">    ~<span class="built_in">MyClass</span>() = <span class="keyword">delete</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 禁用特定的成员函数</span></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">doSomething</span><span class="params">()</span> </span>= <span class="keyword">delete</span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    MyClass obj; <span class="comment">// 编译错误，禁用了默认构造函数</span></span><br><span class="line"></span><br><span class="line">    MyClass obj1;</span><br><span class="line">    MyClass obj2 = obj1; <span class="comment">// 编译错误，禁用了拷贝构造函数</span></span><br><span class="line"></span><br><span class="line">    obj1 = obj2; <span class="comment">// 编译错误，禁用了拷贝赋值运算符</span></span><br><span class="line"></span><br><span class="line">    obj1.<span class="built_in">doSomething</span>(); <span class="comment">// 编译错误，禁用了特定的成员函数</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure></div>

<h1 id="使用using取别名"><a href="#使用using取别名" class="headerlink" title="使用using取别名"></a>使用<code>using</code>取别名</h1><p>在C++11中，使用<code>using</code>关键字可以用来为类型创建别名。</p>
<p>它与<code>typedef</code>有一些区别，下面是它们之间的主要区别：</p>
<ul>
<li><p>语法：</p>
<p><code>using</code>语法相对更加直观和一致。</p>
<p>它使用类似于变量赋值的语法，如<code>using NewName = OldType;</code></p>
<p>而<code>typedef</code>则使用一个稍微有些独特的语法，如<code>typedef OldType NewName;</code></p>
</li>
<li><p>支持模板别名：</p>
<p><code>using</code>关键字支持为模板类型创建别名，</p>
<p>例如：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="keyword">using</span> Vector = std::vector&lt;T&gt;;</span><br></pre></td></tr></table></figure></div>

<p>这里<code>Vector</code>是<code>std::vector</code>的别名，它可以用来创建具有相同类型元素的向量。</p>
</li>
<li><p>嵌套类型别名：<code>using</code>关键字可以在类内部为嵌套类型创建别名，而<code>typedef</code>不能直接在类内部使用，需要借助<code>typedef</code>在类外部进行定义。</p>
</li>
<li><p>模板参数位置：在模板定义时，<code>using</code>关键字可以在任意位置指定模板参数，而<code>typedef</code>只能在定义之前指定模板参数。</p>
</li>
<li><p>右结合性：<code>using</code>关键字的右结合性使得它更容易使用模板元编程和模板别名的复杂技术。</p>
</li>
</ul>
<p><code>using</code>关键字在C++11中提供了更加灵活和直观的方式来创建类型别名，并且它还支持模板别名和嵌套类型别名，这些是<code>typedef</code>所没有的特性。</p>
<p>因此，推荐使用<code>using</code>关键字来创建类型别名。但为了保持与旧代码的兼容性，<code>typedef</code>仍然被广泛使用。</p>
<h1 id="函数模板的默认模板参数"><a href="#函数模板的默认模板参数" class="headerlink" title="函数模板的默认模板参数"></a>函数模板的默认模板参数</h1><p>C++11前类模板是支持默认的模板参数的，却不支持函数模板的默认模板参数。</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//1、普通函数带默认参数，c++98编译通过，c++11编译通过</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">DefParm</span><span class="params">(<span class="type">int</span> m=<span class="number">3</span>)</span></span>&#123;&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//2、类模板是支持默认的模板参数，c++98编译通过，c++11编译通过</span></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> T = <span class="type">int</span>&gt;<span class="keyword">class</span> DefClass;</span><br><span class="line"></span><br><span class="line"><span class="comment">//3、函数模板的默认模板参数，c++98-编译失败，c++11-编译通过</span></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> T= <span class="type">int</span>&gt; <span class="type">void</span> <span class="built_in">DefTempParm</span>()&#123;&#125;</span><br></pre></td></tr></table></figure></div>

<p>  类模板的默认模板参数必须从右往左定义，数模板的默认模板参数则没这个限定：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> T ,<span class="keyword">class</span> <span class="title class_">T2</span>= <span class="type">int</span>&gt;<span class="keyword">class</span> DefClass&#123;&#125;;<span class="comment">//编译正确</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> T= <span class="type">int</span> ,<span class="keyword">class</span> T2&gt;<span class="keyword">class</span> DefClass&#123;&#125;;<span class="comment">//编译错误</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">template</span>&lt;<span class="keyword">class</span> T1 </span>= <span class="type">int</span>, <span class="keyword">class</span> <span class="title class_">T2</span>&gt; <span class="function"><span class="type">void</span> <span class="title">DefFunc1</span><span class="params">(T1 a,T2 b)</span></span>&#123;&#125;;<span class="comment">//函数模板无限制，编译通过</span></span><br></pre></td></tr></table></figure></div>



<h1 id="可变参数模板"><a href="#可变参数模板" class="headerlink" title="可变参数模板"></a>可变参数模板</h1><h2 id="可变参数函数模板"><a href="#可变参数函数模板" class="headerlink" title="可变参数函数模板"></a>可变参数函数模板</h2><p>C++11引入了可变参数模板（Variadic Templates）的特性，它允许在模板中接受任意数量的参数。</p>
<p>这个特性非常有用，因为它可以用于编写更加通用和灵活的代码。</p>
<p>变参数模板使用<code>...</code>语法来表示：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> ...T&gt;<span class="comment">//T叫模板参数包</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">func</span> <span class="params">(T... args)</span> <span class="comment">// args叫函数参数包</span></span></span><br></pre></td></tr></table></figure></div>

<p>示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="comment">//可变参数的模板函数</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> ... T&gt;<span class="comment">//T叫模板参数包</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">func</span> <span class="params">(T... args)</span> <span class="comment">//args叫函数参数包</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span> <span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="built_in">func</span>&lt;<span class="type">int</span>&gt; (<span class="number">10</span>);</span><br><span class="line">	<span class="built_in">func</span>&lt;<span class="type">int</span>, <span class="type">int</span>&gt; (<span class="number">10</span>,<span class="number">20</span>);</span><br><span class="line">    <span class="built_in">func</span>&lt;<span class="type">char</span>, <span class="type">int</span>&gt; (<span class="number">10</span>, <span class="string">&#x27;a&#x27;</span> );</span><br><span class="line">	<span class="built_in">func</span>&lt;<span class="type">char</span>,<span class="type">char</span> *, <span class="type">int</span>&gt; (<span class="string">&#x27;a&#x27;</span>, <span class="string">&quot;abc&quot;</span>,<span class="number">250</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>上面的代码编译是可以通过的。</p>
<p>一个使用可变参数模板的函数模板的例子：</p>
<div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 基本情况：当没有参数时，递归终止</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">print</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    std::cout &lt;&lt; std::endl;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 递归调用：打印第一个参数，然后调用自身打印剩余参数</span></span><br><span class="line"><span class="function"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T, <span class="keyword">typename</span>... Args&gt;</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">print</span><span class="params">(<span class="type">const</span> T&amp; first, <span class="type">const</span> Args&amp;... args)</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    std::cout &lt;&lt; first &lt;&lt; <span class="string">&quot; &quot;</span>;</span><br><span class="line">    <span class="built_in">print</span>(args...);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">print</span>(<span class="number">1</span>, <span class="string">&quot;Hello&quot;</span>, <span class="number">3.14</span>, <span class="string">&#x27;A&#x27;</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>输出：</p>
<div class="highlight-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">1 Hello 3.14 A</span><br></pre></td></tr></table></figure></div>

<p>在上面的代码中，我们定义了一个可变参数模板函数<code>print</code>，它可以接受任意数量的参数并将它们打印到标准输出。</p>
<p>在递归调用中，我们首先打印第一个参数<code>first</code>，然后通过<code>args...</code>将剩余的参数传递给自身进行递归调用。</p>
<p>当我们在<code>main</code>函数中调用<code>print</code>函数时，我们传递了四个参数：</p>
<p>整数1，字符串”Hello”，浮点数3.14和字符’A’。</p>
<p>由于<code>print</code>函数是可变参数模板，它能够接受这四个参数并将它们全部打印出来。</p>
<h2 id="可变参数类模板"><a href="#可变参数类模板" class="headerlink" title="可变参数类模板"></a>可变参数类模板</h2><p>除了函数模板，我们还可以使用可变参数模板来定义类模板。</p>
<p>以下是一个简单的示例：</p>
<div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span>... Args&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Tuple</span> </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Tuple</span>(<span class="type">const</span> Args&amp;... args) : values&#123;args...&#125; &#123;&#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">print</span><span class="params">()</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="built_in">printHelper</span>(values);</span><br><span class="line">        std::cout &lt;&lt; std::endl;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="function"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span></span><br><span class="line"><span class="function">    <span class="type">void</span> <span class="title">printHelper</span><span class="params">(<span class="type">const</span> T&amp; value)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        std::cout &lt;&lt; value &lt;&lt; <span class="string">&quot; &quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T, <span class="keyword">typename</span>... Rest&gt;</span></span><br><span class="line"><span class="function">    <span class="type">void</span> <span class="title">printHelper</span><span class="params">(<span class="type">const</span> T&amp; first, <span class="type">const</span> Rest&amp;... rest)</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        std::cout &lt;&lt; first &lt;&lt; <span class="string">&quot; &quot;</span>;</span><br><span class="line">        <span class="built_in">printHelper</span>(rest...);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    std::tuple&lt;Args...&gt; values;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="function">Tuple&lt;<span class="type">int</span>, std::string, <span class="type">double</span>&gt; <span class="title">t</span><span class="params">(<span class="number">42</span>, <span class="string">&quot;Hello&quot;</span>, <span class="number">3.14</span>)</span></span>;</span><br><span class="line">    t.<span class="built_in">print</span>();</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>在上面的代码中，我们定义了一个名为<code>Tuple</code>的类模板，它使用可变参数模板来接受任意数量的类型参数。</p>
<p>在构造函数中，我们将传递的参数打包成一个<code>std::tuple</code>对象来保存。</p>
<p>类模板中的<code>print</code>函数使用递归的方式打印保存的值。</p>
<p>我们使用类内嵌的辅助函数<code>printHelper</code>来实现递归，类似于前面函数模板的例子。</p>
<p>在<code>main</code>函数中，我们创建了一个<code>Tuple</code>对象，类型参数为<code>int</code>，<code>std::string</code>和<code>double</code>，并传递了相应的值。</p>
<p>然后，我们调用<code>print</code>函数来打印这些值。</p>
<p>输出结果将是：<code>42 Hello 3.14</code>。</p>
<h2 id="获取可变参数模板的参数个数"><a href="#获取可变参数模板的参数个数" class="headerlink" title="获取可变参数模板的参数个数"></a>获取可变参数模板的参数个数</h2><p>使用<code>sizeof...(Args)</code>可以获取可变参数模板的参数个数。</p>
<p><code>sizeof...()</code>是一个C++的运算符，用于计算可变参数模板的参数个数。</p>
<p>它返回一个<code>size_t</code>类型的值，表示参数的数量。</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="comment">//可变参数的模板函数</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> ... T&gt;<span class="comment">//T叫模板参数包</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">func</span> <span class="params">(T... args)</span> <span class="comment">//args叫函数参数包</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="comment">//获取可变参数的个数</span></span><br><span class="line">	cout &lt;&lt;<span class="string">&quot;num = &quot;</span> &lt;&lt;<span class="keyword">sizeof</span>.. . (args) &lt;&lt;endl;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span> <span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="built_in">func</span>&lt;<span class="type">int</span>&gt; (<span class="number">10</span>);</span><br><span class="line">	<span class="built_in">func</span>&lt;<span class="type">int</span>, <span class="type">int</span>&gt; (<span class="number">10</span>,<span class="number">20</span>);</span><br><span class="line">    <span class="built_in">func</span>&lt;<span class="type">char</span>, <span class="type">int</span>&gt; (<span class="number">10</span>, <span class="string">&#x27;a&#x27;</span> );</span><br><span class="line">	<span class="built_in">func</span>&lt;<span class="type">char</span>,<span class="type">char</span> *, <span class="type">int</span>&gt; (<span class="string">&#x27;a&#x27;</span>, <span class="string">&quot;abc&quot;</span>,<span class="number">250</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>输出：</p>
<div class="highlight-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">num = 1</span><br><span class="line">num = 2</span><br><span class="line">num = 2</span><br><span class="line">num = 3</span><br></pre></td></tr></table></figure></div>

<p>需要注意的是，<code>sizeof...(Args)</code>是在编译时计算的，因此它提供了在编译期间获取参数个数的能力。这使得可以在编译时进行相关的优化和决策。</p>
<h2 id="模板特化"><a href="#模板特化" class="headerlink" title="模板特化"></a>模板特化</h2><p>模板特化（Template specialization）是C++中一种用于为特定类型或特定模板参数提供自定义实现的机制。</p>
<p>它允许我们根据特定的类型或参数来定义特定版本的模板函数或模板类，以满足特定的需求或提供特定的行为。</p>
<p>模板特化可以分为两种类型：全特化（Full specialization）和偏特化（Partial specialization）。</p>
<ul>
<li><p>全特化（Full specialization）：全特化是针对特定类型或特定参数的完整定义。在全特化中，我们提供了特定类型或参数的实现，完全替代了原始的模板定义。全特化的语法形式为：</p>
 <div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">MyTemplate</span> &#123;</span><br><span class="line">    <span class="comment">// 通用实现</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;&gt;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">MyTemplate</span>&lt;<span class="type">int</span>&gt; &#123;</span><br><span class="line">    <span class="comment">// int 类型的特化实现</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></div>

<p> 在上述示例中，我们定义了一个通用的模板类<code>MyTemplate</code>，并通过全特化为<code>int</code>类型提供了一个特殊的实现。</p>
</li>
<li><p>偏特化（Partial specialization）：偏特化是针对模板参数的部分定义。它允许我们根据参数的特定属性或限制来提供不同的实现。偏特化的语法形式为：</p>
 <div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T, <span class="keyword">typename</span> U&gt;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">MyTemplate</span> &#123;</span><br><span class="line">    <span class="comment">// 通用实现</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">MyTemplate</span>&lt;T, <span class="type">int</span>&gt; &#123;</span><br><span class="line">    <span class="comment">// T 和 int 类型的偏特化实现</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></div>

<p> 在上述示例中，我们定义了一个通用的模板类<code>MyTemplate</code>，并通过偏特化为<code>T</code>和<code>int</code>类型的组合提供了一个特殊的实现。</p>
</li>
</ul>
<p>模板特化使我们能够为特定的类型或参数提供自定义的行为，以满足特殊的需求或提供更精确的实现。</p>
<p>它在泛型编程中起到了非常重要的作用，并且是C++模板系统的强大功能之一。</p>
<h2 id="函数模板参数包的展开"><a href="#函数模板参数包的展开" class="headerlink" title="函数模板参数包的展开"></a>函数模板参数包的展开</h2><h3 id="递归和模板特化展开"><a href="#递归和模板特化展开" class="headerlink" title="递归和模板特化展开"></a>递归和模板特化展开</h3><p>通过递归函数展开参数包，需要提供一个参数包展开的函数和一个递归终止函数。</p>
<p>示例代码：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="comment">//当最后没有参数时调用这个</span></span><br><span class="line"><span class="comment">//递归终止函数1</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">debug</span> <span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	cout &lt;&lt; <span class="string">&quot;empty&quot;</span> &lt;&lt; endl;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/*也可以这样写，当最后只有一个参数时调用这个函数</span></span><br><span class="line"><span class="comment">//递归终止函数2</span></span><br><span class="line"><span class="comment">template&lt;class T&gt;</span></span><br><span class="line"><span class="comment">void debug (T tmp)</span></span><br><span class="line"><span class="comment">&#123;</span></span><br><span class="line"><span class="comment">	cout &lt;&lt;&quot;tmp = &quot; &lt;&lt;tmp &lt;&lt;endl ;</span></span><br><span class="line"><span class="comment">&#125;</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="comment">//以此类推，可以根据需要指定递归终止函数的调用参数</span></span><br><span class="line"></span><br><span class="line"><span class="comment">//可变参数的模板函数</span></span><br><span class="line"><span class="comment">//参数包展开函数</span></span><br><span class="line"><span class="function"><span class="keyword">template</span>&lt;<span class="keyword">class</span> T1,<span class="keyword">class</span> ... T2&gt;</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">debug</span> <span class="params">(T1 first,T2... last)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	cout &lt;&lt; first &lt;&lt; endl;<span class="comment">//递归调用函数本身</span></span><br><span class="line">	<span class="built_in">debug</span> ( last... );</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span> <span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="built_in">debug</span> ( <span class="number">1</span>, <span class="number">2</span>,<span class="number">3</span>, <span class="number">4</span>) ;</span><br><span class="line">	 <span class="comment">/*	函数递归调用过程:</span></span><br><span class="line"><span class="comment">		debug (1，2，3,4);</span></span><br><span class="line"><span class="comment">		debug (2，3,4);</span></span><br><span class="line"><span class="comment">		debug (3,4) ;</span></span><br><span class="line"><span class="comment">		debug (4);</span></span><br><span class="line"><span class="comment">		debug ();	*/</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<h3 id="递归、逗号和初始化列表展开"><a href="#递归、逗号和初始化列表展开" class="headerlink" title="递归、逗号和初始化列表展开"></a>递归、逗号和初始化列表展开</h3><p>使用场景较少，不推荐使用</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">template</span>&lt;<span class="keyword">class</span> T&gt;</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">print</span> <span class="params">(T tmp)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	cout &lt;&lt; tmp &lt;&lt; endl;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//可变参数的模板函数</span></span><br><span class="line"><span class="function"><span class="keyword">template</span>&lt;<span class="keyword">class</span> ...T&gt;</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">expand</span><span class="params">(T ... args)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="comment">//逗号运算符</span></span><br><span class="line">    <span class="comment">//初始化列表</span></span><br><span class="line">	<span class="type">int</span> a[] = &#123; (<span class="built_in">print</span> (args), <span class="number">0</span>) ...&#125;;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span> <span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="built_in">expand</span> (<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<h3 id="折叠表达式（C-17起）展开"><a href="#折叠表达式（C-17起）展开" class="headerlink" title="折叠表达式（C++17起）展开"></a>折叠表达式（C++17起）展开</h3><div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">template</span>&lt;<span class="keyword">typename</span>... Args&gt;</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">baz</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="comment">// 使用折叠表达式展开参数包</span></span><br><span class="line">	((std::cout &lt;&lt; <span class="built_in">typeid</span>(Args).<span class="built_in">name</span>() &lt;&lt; std::endl), ...);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	baz&lt;<span class="type">int</span>, <span class="type">double</span>, <span class="type">char</span>*, std::vector&lt;std::list&lt;<span class="type">int</span>, std::string&gt;&gt;&gt;();</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>输出（因为屏幕长度显示，最后三行换行了）：</p>
<div class="highlight-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">int</span><br><span class="line">double</span><br><span class="line">char * __ptr64</span><br><span class="line">class std::vector&lt;class std::list&lt;int,class std::basic_string&lt;char,struct std::char_traits&lt;char&gt;,class std::allocator&lt;char&gt; &gt; &gt;,class std::allocator&lt;class std::list&lt;int,class std::basic_string&lt;char,struct std::char_traits&lt;char&gt;,class std::allocator&lt;char&gt; &gt; &gt; &gt; &gt;</span><br></pre></td></tr></table></figure></div>

<h2 id="类模板参数包的展开"><a href="#类模板参数包的展开" class="headerlink" title="类模板参数包的展开"></a>类模板参数包的展开</h2><h3 id="继承方式展开"><a href="#继承方式展开" class="headerlink" title="继承方式展开"></a>继承方式展开</h3><p>继承方式展开可变参数模板类的步骤：</p>
<ul>
<li>可变参数模板声明</li>
<li>递归继承模板类</li>
<li>递归终止条件</li>
</ul>
<p>示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//1.可变参数模板声明</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span>...T&gt;<span class="keyword">class</span> <span class="title class_">Car</span> &#123;&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">//2.递归继承模板类本身</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">Head</span>, <span class="keyword">class</span> ...Tail&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Car</span>&lt;Head, Tail...&gt; :<span class="keyword">public</span> Car&lt;Tail...&gt;</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="built_in">Car</span>()</span><br><span class="line">	&#123;</span><br><span class="line">		std::cout &lt;&lt; <span class="string">&quot;type = &quot;</span> &lt;&lt; <span class="built_in">typeid</span>(Head).<span class="built_in">name</span>() &lt;&lt; std::endl;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">//3.递归终止条件</span></span><br><span class="line"><span class="keyword">template</span>&lt;&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Car</span>&lt;&gt; &#123;&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span> <span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;    </span><br><span class="line">	Car&lt;<span class="type">int</span>, std::string, <span class="type">const</span> <span class="type">char</span>*, <span class="type">bool</span>&gt; byd;</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>输出：</p>
<div class="highlight-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">type = bool</span><br><span class="line">type = char const * __ptr64</span><br><span class="line">type = class std::basic_string&lt;char,struct std::char_traits&lt;char&gt;,class std::allocator&lt;char&gt; &gt;</span><br><span class="line">type = int</span><br></pre></td></tr></table></figure></div>

<h3 id="递归和特例化展开"><a href="#递归和特例化展开" class="headerlink" title="递归和特例化展开"></a>递归和特例化展开</h3><ul>
<li>变长模板声明</li>
<li>变长模板类定义</li>
<li>递归终止</li>
</ul>
<p>示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//1.变长模板声明</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="type">int</span> ...last&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Test</span> &#123;&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">//2.变长模板类定义</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="type">int</span> first, <span class="type">int</span> ...last&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Test</span>&lt;first, last...&gt;</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="type">static</span> <span class="type">const</span> <span class="type">int</span> val = first * Test&lt;last...&gt;::val;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">//3.递归终止</span></span><br><span class="line"><span class="keyword">template</span>&lt;&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Test</span>&lt;&gt;</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="type">static</span> <span class="type">const</span> <span class="type">int</span> val = <span class="number">1</span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span> <span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;    </span><br><span class="line">	std::cout &lt;&lt; Test&lt;<span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>&gt;::val &lt;&lt; std::endl;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<h3 id="折叠表达式（C-17起）展开-1"><a href="#折叠表达式（C-17起）展开-1" class="headerlink" title="折叠表达式（C++17起）展开"></a>折叠表达式（C++17起）展开</h3><div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span>... Args&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">MyTemplate</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="type">void</span> <span class="title">process</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="comment">// 使用折叠表达式展开参数包</span></span><br><span class="line">		((std::cout &lt;&lt; <span class="built_in">typeid</span>(Args).<span class="built_in">name</span>() &lt;&lt; std::endl), ...);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"></span><br><span class="line">	MyTemplate&lt;<span class="type">int</span>, <span class="type">char</span>, std::string, <span class="type">const</span> <span class="type">char</span>*&gt; test;</span><br><span class="line">	test.<span class="built_in">process</span>();</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>输出：</p>
<div class="highlight-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">int</span><br><span class="line">char</span><br><span class="line">class std::basic_string&lt;char,struct std::char_traits&lt;char&gt;,class std::allocator&lt;char&gt; &gt;</span><br><span class="line">char const * __ptr64</span><br></pre></td></tr></table></figure></div>

]]></content>
      <tags>
        <tag>学习笔记</tag>
      </tags>
  </entry>
  <entry>
    <title>C++基础</title>
    <url>/2023/09/17/CPP-%E5%9F%BA%E7%A1%80/</url>
    <content><![CDATA[<p>很用心的笔记.由我朋友@EMCC所编写,他的b站:<br><a class="link"   href="https://space.bilibili.com/1843315943" >EMC方的b站空间 <i class="fa-regular fa-arrow-up-right-from-square fa-sm"></i></a></p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//基础语法</span></span><br><span class="line"><span class="comment">//函数高级</span></span><br><span class="line"><span class="comment">//面向对象</span></span><br><span class="line"><span class="comment">//继承和多态 </span></span><br><span class="line"><span class="comment">//文件操作</span></span><br><span class="line"><span class="comment">//左值右值</span></span><br><span class="line"><span class="comment">//C++类型转换</span></span><br><span class="line"><span class="comment">//预处理</span></span><br><span class="line"><span class="comment">//异常处理</span></span><br><span class="line"><span class="comment">//常用数据结构和常用算法</span></span><br><span class="line"><span class="comment">//模板基础</span></span><br><span class="line"><span class="comment">//STL基础、使用</span></span><br><span class="line">常用标准库</span><br><span class="line">正则表达式、正则库</span><br><span class="line">socket网络编程基础</span><br><span class="line">C++<span class="number">11</span>、<span class="number">14</span>、<span class="number">17</span>、<span class="number">20</span>、<span class="number">23</span>等新特性</span><br><span class="line">    萃取、模板泛型编程进阶、元编程、函数式编程</span><br><span class="line">    并发、多线程和多进程、线程库</span><br><span class="line">    <span class="comment">//智能指针</span></span><br><span class="line">    <span class="comment">//Lambda表达式</span></span><br><span class="line">	<span class="comment">//强类型枚举</span></span><br><span class="line">    <span class="comment">//可变参数模板</span></span><br><span class="line">    等....</span><br><span class="line">    </span><br><span class="line">    </span><br><span class="line">后续方向：</span><br><span class="line">QT</span><br><span class="line">QML+Quick</span><br><span class="line">MFC</span><br><span class="line">音、视频开发</span><br><span class="line">图像处理</span><br><span class="line">游戏引擎（游戏开发）</span><br><span class="line">信号处理（通信）</span><br><span class="line">服务器开发</span><br><span class="line">嵌入式开发</span><br><span class="line">等等</span><br><span class="line">    </span><br><span class="line"></span><br></pre></td></tr></table></figure></div>

<p>[TOC]</p>
<h1 id="引用（左值引用）"><a href="#引用（左值引用）" class="headerlink" title="引用（左值引用）"></a>引用（左值引用）</h1><h2 id="基本概念和语法"><a href="#基本概念和语法" class="headerlink" title="基本概念和语法"></a>基本概念和语法</h2><hr>
<p>&#x3D;&#x3D;本章所有内容都是针对C++98标准的左值引用。&#x3D;&#x3D;</p>
<p><a class="link"   href="https://www.runoob.com/cplusplus/cpp-references.html" >C++中的引用是一种特殊的变量，它是另一个已存在变量的别名，可以通过引用或原始变量来访问同一块内存空间。 <i class="fa-regular fa-arrow-up-right-from-square fa-sm"></i></a></p>
<p>引用的语法是在变量类型后加上<code>&amp;</code>符号，然后给引用一个初始值</p>
<p><strong>示例：</strong></p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> a = <span class="number">10</span>; <span class="comment">// 声明一个普通变量a</span></span><br><span class="line"><span class="type">int</span> &amp;b = a; <span class="comment">// 声明一个引用b，它是a的别名</span></span><br><span class="line"></span><br><span class="line">cout &lt;&lt; <span class="string">&quot;a =&quot;</span> &lt;&lt; a &lt;&lt;endl;</span><br><span class="line">cout &lt;&lt; <span class="string">&quot;b = &quot;</span> &lt;&lt; b &lt;&lt;endl;</span><br><span class="line">b = <span class="number">100</span>; <span class="comment">// a和b是同一块内存区域的两个名称，对b进行操作等价于对a进行操作</span></span><br><span class="line">cout &lt;&lt; <span class="string">&quot;a = &quot;</span> &lt;&lt; a &lt;&lt;endl;</span><br><span class="line">cout &lt;&lt; <span class="string">&quot;b = &quot;</span> &lt;&lt; b &lt;&lt;endl;</span><br></pre></td></tr></table></figure></div>

<p><strong>输出结果：</strong></p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line">a = <span class="number">10</span></span><br><span class="line">b = <span class="number">10</span></span><br><span class="line">a = <span class="number">100</span></span><br><span class="line">b = <span class="number">100</span></span><br></pre></td></tr></table></figure></div>

<p><strong>引用的作用有以下几点：</strong></p>
<ul>
<li>可以简化指针的操作，避免使用<code>*</code>和<code>&amp;</code>符号来取值或取地址。</li>
<li>可以提高函数参数传递的效率，避免复制大量数据。</li>
<li>可以使函数返回多个值，而不仅仅是一个。</li>
<li>可以实现运算符重载等高级功能。</li>
</ul>
<h2 id="注意事项"><a href="#注意事项" class="headerlink" title="注意事项"></a>注意事项</h2><hr>
<p><strong>引用的注意事项有以下几点：</strong></p>
<ul>
<li><p>引用&#x3D;&#x3D;必须&#x3D;&#x3D;在声明时&#x3D;&#x3D;初始化&#x3D;&#x3D;，不能像指针那样先声明后赋值。</p>
</li>
<li><p>引用一旦初始化，就&#x3D;&#x3D;不能改变指向&#x3D;&#x3D;其他变量，否则会修改原始变量的值。</p>
</li>
<li><p>引用&#x3D;&#x3D;不能指向空值&#x3D;&#x3D;或&#x3D;&#x3D;无效地址&#x3D;&#x3D;，否则会导致程序崩溃。</p>
</li>
<li><p>引用&#x3D;&#x3D;不占用额外的内存空间&#x3D;&#x3D;，它只是原始变量的另一个名称。</p>
</li>
<li><p>一个变量可以有多个引用，也就是说，可以定义多个引用指向同一个变量。</p>
<p><strong>示例：</strong></p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> a = <span class="number">10</span>; </span><br><span class="line"><span class="type">int</span> &amp;r1 = a; <span class="comment">// r1是a的引用 </span></span><br><span class="line"><span class="type">int</span> &amp;r2 = a; <span class="comment">// r2也是a的引用 </span></span><br><span class="line">cout &lt;&lt; r1 &lt;&lt; <span class="string">&quot; &quot;</span> &lt;&lt; r2 &lt;&lt; endl; <span class="comment">// 输出10 10</span></span><br></pre></td></tr></table></figure></div>
</li>
<li><p>引用不能有引用，也就是说，不能定义一个引用指向另一个引用。</p>
<p><strong>示例：</strong></p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> a = <span class="number">10</span>; </span><br><span class="line"><span class="type">int</span> &amp;r1 = a; <span class="comment">// r1是a的引用 </span></span><br><span class="line"><span class="type">int</span> &amp;r2 = r1; <span class="comment">// 错误，不能定义一个引用指向另一个引用</span></span><br></pre></td></tr></table></figure></div>

<p>但是，可以定义一个变量和一个引用同时指向另一个引用。</p>
<p><strong>示例：</strong></p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> a = <span class="number">10</span>; </span><br><span class="line"><span class="type">int</span> &amp;r1 = a; <span class="comment">// r1是a的引用 </span></span><br><span class="line"><span class="type">int</span> *p = &amp;r1; <span class="comment">// p是指向r1的指针 </span></span><br><span class="line"><span class="type">int</span> &amp;r2 = *p; <span class="comment">// r2是p所指对象的引用，也就是r1的别名 </span></span><br><span class="line">cout &lt;&lt; *p &lt;&lt; <span class="string">&quot; &quot;</span> &lt;&lt; r2 &lt;&lt; endl; <span class="comment">// 输出10 10</span></span><br></pre></td></tr></table></figure></div></li>
</ul>
<h2 id="引用做函数参数"><a href="#引用做函数参数" class="headerlink" title="引用做函数参数"></a>引用做函数参数</h2><hr>
<p>当引用作为函数参数时，需要注意以下几点：</p>
<ul>
<li>如果参数只需要读取而不修改，可以使用常量引用来提高效率和安全性。</li>
<li>如果参数需要修改，并且希望修改能够反映到实参上，可以使用普通引用来传递。</li>
<li>如果参数是类对象或结构体等复杂类型，并且需要修改其内容或调用其成员函数，可以使用普通引用来传递。</li>
</ul>
<p>引用的本质是一个常指针（指针常量），在编译器内部实现为指向原始变量地址的常量。引用传递相当于指针传递。</p>
<p><strong>示例：</strong></p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 值传递</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">swap1</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span> </span></span><br><span class="line"><span class="function"></span>&#123; </span><br><span class="line">    <span class="type">int</span> temp = a; </span><br><span class="line">    a = b; </span><br><span class="line">    b = temp; </span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 指针传递 </span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">swap2</span><span class="params">(<span class="type">int</span> *a, <span class="type">int</span> *b)</span> </span></span><br><span class="line"><span class="function"></span>&#123; </span><br><span class="line">    <span class="type">int</span> temp = *a; </span><br><span class="line">    *a = *b; </span><br><span class="line">    *b = temp; </span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 引用传递 </span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">swap3</span><span class="params">(<span class="type">int</span> &amp;a, <span class="type">int</span> &amp;b)</span> </span></span><br><span class="line"><span class="function"></span>&#123; </span><br><span class="line">    <span class="type">int</span> temp = a; </span><br><span class="line">    a = b; </span><br><span class="line">    b = temp; </span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123; </span><br><span class="line">	<span class="type">int</span> x = <span class="number">10</span>, y = <span class="number">20</span>;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">swap1</span>(x, y); <span class="comment">// x和y不会交换 </span></span><br><span class="line">	<span class="built_in">swap2</span>(&amp;x, &amp;y); <span class="comment">// x和y会交换 </span></span><br><span class="line">	<span class="built_in">swap3</span>(x, y); <span class="comment">// x和y会交换，a是x的别名，b是y的别名</span></span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>; </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<h2 id="引用做函数返回值"><a href="#引用做函数返回值" class="headerlink" title="引用做函数返回值"></a>引用做函数返回值</h2><hr>
<p><strong>当引用作为函数返回值时，需要注意以下几点：</strong></p>
<ul>
<li>不要返回局部变量的引用，因为局部变量在函数结束后会被销毁，导致返回无效地址。</li>
<li>&#x3D;&#x3D;不建议&#x3D;&#x3D;返回堆区对象的引用。</li>
<li>可以返回静态局部变量或全局变量的引用，因为这些变量在程序结束前都有效。</li>
<li>可以返回类对象或结构体等复杂类型的成员变量或成员函数的引用。</li>
</ul>
<p><strong>返回堆区对象的引用是不安全的，有以下几种可能的风险：</strong></p>
<ul>
<li>如果在函数内部释放了堆区对象的内存，那么返回的引用就是一个空指针，指向一个无效的地址。</li>
<li>如果在函数外部释放了堆区对象的内存，那么可能会导致重复释放或者内存泄漏，因为函数外部不一定知道引用指向的是堆区对象。</li>
<li>如果在函数外部修改了堆区对象的值，那么可能会影响到其他地方使用该对象的逻辑，因为堆区对象是共享的。</li>
</ul>
<p>一般来说，不建议返回堆区对象的引用。如果需要返回动态分配的内存，可以使用智能指针或者容器类来管理内存。</p>
<p><strong>示例：</strong></p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 返回局部变量的引用 </span></span><br><span class="line"><span class="function"><span class="type">int</span>&amp; <span class="title">func1</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123; </span><br><span class="line">    <span class="type">int</span> a = <span class="number">10</span>; <span class="comment">// 局部变量 </span></span><br><span class="line">    <span class="keyword">return</span> a; <span class="comment">// 错误！ </span></span><br><span class="line">&#125; <span class="comment">// 局部变量在函数结束后会被销毁，导致返回无效地址</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 返回堆区对象的引用 </span></span><br><span class="line"><span class="function"><span class="type">int</span>&amp; <span class="title">func2</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123; </span><br><span class="line">    <span class="type">int</span>* p = <span class="keyword">new</span> <span class="built_in">int</span>(<span class="number">20</span>);</span><br><span class="line">    <span class="keyword">return</span> p;</span><br><span class="line">&#125; <span class="comment">// 不建议</span></span><br></pre></td></tr></table></figure></div>

<p>引用做函数返回值时，如果返回的是一个静态变量，那么这个引用就可以作为左值，也就是可以被赋值或修改。这是因为静态变量在函数结束后不会被销毁，它的生命周期和作用域都延长了，所以返回的引用仍然有效。</p>
<p><strong>示例：</strong></p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 返回一个静态变量的引用 </span></span><br><span class="line"><span class="function"><span class="type">int</span>&amp; <span class="title">func</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123; </span><br><span class="line">    <span class="type">static</span> <span class="type">int</span> x = <span class="number">10</span>; <span class="comment">// 静态变量 </span></span><br><span class="line">    <span class="keyword">return</span> x; <span class="comment">// 返回引用 </span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123; </span><br><span class="line">    <span class="type">int</span>&amp; a = <span class="built_in">func</span>(); <span class="comment">// 接收返回的引用 </span></span><br><span class="line">    cout &lt;&lt; a &lt;&lt; endl; <span class="comment">// 输出10 </span></span><br><span class="line">    a = <span class="number">20</span>; <span class="comment">// 修改引用指向的值 </span></span><br><span class="line">    cout &lt;&lt; <span class="built_in">func</span>() &lt;&lt; endl; <span class="comment">// 输出20</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<h2 id="引用的本质"><a href="#引用的本质" class="headerlink" title="引用的本质"></a>引用的本质</h2><hr>
<p>这一节根据黑马程序员的C++教程（详细在B站搜索”C++“，播放量最高的就是）编写，也许并不正确，请读者自行甄辨。</p>
<p>引用的本质是一种被限制的指针，它在内部实现时是一个指针常量，也就是说，它指向的对象不能改变，但对象的值可以改变。</p>
<p><strong>引用和指针有以下区别：</strong></p>
<ul>
<li>引用必须在定义时初始化，并且不能重新赋值，而指针可以在任何时候初始化和赋值。</li>
<li>引用不占用额外的内存空间，而指针占用一个存储地址的空间。</li>
<li>引用使用起来更加简洁和安全，不需要解引用操作符<code>*</code>，也不会出现空指针或野指针的问题。</li>
<li>引用只能有一级，不能有多级引用，而指针可以有多级。</li>
</ul>
<p><strong>示例：</strong></p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 定义一个变量a和一个引用r </span></span><br><span class="line"><span class="type">int</span> a = <span class="number">10</span>; </span><br><span class="line"><span class="type">int</span> &amp;r = a; <span class="comment">// 相当于 int *const r = &amp;a; //常量指针的指向不可以修改，值可以修改</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 通过r修改a的值 </span></span><br><span class="line">r = <span class="number">20</span>; </span><br><span class="line">cout &lt;&lt; a &lt;&lt; endl; <span class="comment">// 输出20</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 定义一个函数swap，使用引用作为参数 </span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">swap</span><span class="params">(<span class="type">int</span> &amp;x, <span class="type">int</span> &amp;y)</span> </span></span><br><span class="line"><span class="function"></span>&#123; </span><br><span class="line">    <span class="type">int</span> temp = x; </span><br><span class="line">    x = y; </span><br><span class="line">    y = temp; </span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 调用swap函数 </span></span><br><span class="line"><span class="type">int</span> m = <span class="number">10</span>, n = <span class="number">20</span>; </span><br><span class="line"><span class="built_in">swap</span>(m, n); </span><br><span class="line">cout &lt;&lt; m &lt;&lt; <span class="string">&quot; &quot;</span> &lt;&lt; n &lt;&lt; endl; <span class="comment">// 输出20 10</span></span><br></pre></td></tr></table></figure></div>

<h2 id="常量引用"><a href="#常量引用" class="headerlink" title="常量引用"></a>常量引用</h2><hr>
<p>常量引用是对<code>const</code>的引用，也就是说，它把它所指向的对象看作是常量，因此不可以通过该引用来修改它所指向的对象的值。</p>
<p><strong>常量引用的作用有以下几点：</strong></p>
<ul>
<li>可以避免不必要的拷贝，提高效率。</li>
<li>可以保护被引用的对象，防止意外修改。</li>
<li>可以接受字面值或表达式作为初始值，而普通引用不行。</li>
</ul>
<p><strong>示例：</strong></p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> &amp;a = <span class="number">42</span>; <span class="comment">// 错误：普通引用必须绑定到一个对象上，而不能绑定到一个字面值或表达式</span></span><br><span class="line"></span><br><span class="line"><span class="type">const</span> <span class="type">int</span> &amp;r = <span class="number">42</span>;<span class="comment">// 正确：const引用可以绑定到字面值或表达式上，const引用可以让编译器为字面值或表达式创建一个临时变量，并将引用绑定到该临时变量上</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 声明一个普通变量i和一个常量变量ci </span></span><br><span class="line"><span class="type">int</span> i = <span class="number">10</span>; </span><br><span class="line"><span class="type">const</span> <span class="type">int</span> ci = <span class="number">20</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 声明一个常量引用ri，绑定到i </span></span><br><span class="line"><span class="type">const</span> <span class="type">int</span> &amp;ri = i;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 声明一个常量引用rci，绑定到ci </span></span><br><span class="line"><span class="type">const</span> <span class="type">int</span> &amp;rci = ci;</span><br><span class="line"></span><br><span class="line">rci = <span class="number">30</span>; <span class="comment">// 错误：不能通过常量引用修改被引用的对象 </span></span><br><span class="line">ri = <span class="number">40</span>; <span class="comment">// 错误：不能通过常量引用修改被引用的对象</span></span><br><span class="line"></span><br><span class="line">i = <span class="number">50</span>; <span class="comment">// 正确：可以直接修改普通变量 </span></span><br><span class="line">cout &lt;&lt; ri &lt;&lt; endl; <span class="comment">// 输出50</span></span><br><span class="line"></span><br><span class="line">cout &lt;&lt; r + ri &lt;&lt; endl; <span class="comment">// 输出92</span></span><br></pre></td></tr></table></figure></div>

<h1 id="函数-高级篇"><a href="#函数-高级篇" class="headerlink" title="函数_高级篇"></a>函数_高级篇</h1><h2 id="函数重载"><a href="#函数重载" class="headerlink" title="函数重载"></a>函数重载</h2><hr>
<h3 id="基本使用"><a href="#基本使用" class="headerlink" title="基本使用"></a>基本使用</h3><hr>
<p>函数重载是指在同一作用域内，可以有多个同名函数，只要它们的参数个数、类型或参数顺序不同即可。这样，当调用这个函数时，编译器会根据传入的实参自动选择合适的函数进行调用。</p>
<p>下面的示例是三个重载的函数。</p>
<p><strong>示例：</strong></p>
<div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">add</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> a + b;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">double</span> <span class="title">add</span><span class="params">(<span class="type">int</span> a, <span class="type">double</span> b)</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> a + b;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">double</span> <span class="title">add</span><span class="params">(<span class="type">double</span> b, <span class="type">int</span> a)</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> a + b;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<h3 id="注意事项-1"><a href="#注意事项-1" class="headerlink" title="注意事项"></a>注意事项</h3><hr>
<ul>
<li><p>函数重载必须在同一作用域内。</p>
</li>
<li><p>函数重载的函数名必须相同，但函数的参数列表必须不同。</p>
</li>
<li><p>函数重载不能只是函数参数名的不同。</p>
</li>
<li><p>参数列表的不同可以包括参数的类型、个数或顺序。</p>
</li>
<li><p>重载的函数可以有不同的返回值类型，即函数的返回值不作为函数重载的条件</p>
</li>
<li><p>不能仅仅通过为普通变量添加或删除<code>const</code>修饰符来重载一个函数</p>
</li>
<li><p>可以对函数的常量性进行重载，即可以同时定义一个常量函数和一个非常量函数。</p>
</li>
<li><p>可以对类成员函数进行重载。（有关类将在之后的内容中提到）</p>
<p><strong>示例：</strong></p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">foo</span><span class="params">(<span class="type">int</span> x)</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;foo(int x): &quot;</span> &lt;&lt; x &lt;&lt; endl;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">foo</span><span class="params">(<span class="type">int</span> x)</span> <span class="comment">// 错误：函数的返回值不作为函数重载的条件</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">3</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">foo</span><span class="params">(<span class="type">const</span> <span class="type">int</span> x)</span> <span class="comment">// 错误：不能仅通过为普通变量参数添加const修饰符来重载函数</span></span></span><br><span class="line"><span class="function"></span>&#123; </span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;foo(const int x): &quot;</span> &lt;&lt; x &lt;&lt; endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p><strong>示例：</strong></p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line">code<span class="comment">// 重载函数示例1：参数个数不同</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">sum</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> a + b;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">sum</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b, <span class="type">int</span> c)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> a + b + c;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 重载函数示例2：参数类型不同</span></span><br><span class="line"><span class="function"><span class="type">double</span> <span class="title">sum</span><span class="params">(<span class="type">double</span> a, <span class="type">double</span> b)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> a + b;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">sum</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> a + b;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 重载函数示例3：常量性重载</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">MyClass</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">  <span class="function"><span class="type">void</span> <span class="title">foo</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;non-const foo()&quot;</span> &lt;&lt; std::endl;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="function"><span class="type">void</span> <span class="title">foo</span><span class="params">()</span> <span class="type">const</span> </span>&#123;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;const foo()&quot;</span> &lt;&lt; std::endl;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 重载类成员函数</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">MyClass2</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">  <span class="function"><span class="type">void</span> <span class="title">foo</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;non-const foo()&quot;</span> &lt;&lt; std::endl;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="function"><span class="type">void</span> <span class="title">foo</span><span class="params">()</span> <span class="type">const</span> </span>&#123;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;const foo()&quot;</span> &lt;&lt; std::endl;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">  <span class="comment">// 调用重载函数示例1</span></span><br><span class="line">  <span class="type">int</span> x = <span class="built_in">sum</span>(<span class="number">1</span>, <span class="number">2</span>);</span><br><span class="line">  <span class="type">int</span> y = <span class="built_in">sum</span>(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>);</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 调用重载函数示例2</span></span><br><span class="line">  <span class="type">double</span> a = <span class="built_in">sum</span>(<span class="number">1.0</span>, <span class="number">2.0</span>);</span><br><span class="line">  <span class="type">int</span> b = <span class="built_in">sum</span>(<span class="number">1</span>, <span class="number">2</span>);</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 调用重载函数示例3</span></span><br><span class="line">  MyClass obj1;</span><br><span class="line">  <span class="type">const</span> MyClass obj2;</span><br><span class="line">  obj1.<span class="built_in">foo</span>(); <span class="comment">// 调用非常量函数</span></span><br><span class="line">  obj2.<span class="built_in">foo</span>(); <span class="comment">// 调用常量函数</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">// 调用重载类成员函数</span></span><br><span class="line">  MyClass2 obj3;</span><br><span class="line">  <span class="type">const</span> MyClass2 obj4;</span><br><span class="line">  obj3.<span class="built_in">foo</span>(); <span class="comment">// 调用非常量函数</span></span><br><span class="line">  obj4.<span class="built_in">foo</span>(); <span class="comment">// 调用常量函数</span></span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>
</li>
<li><p>引用作为函数参数时，可以使用引用的类型和常量性来进行函数重载。</p>
<p>使用引用作为函数参数进行函数重载的<strong>示例：</strong></p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">foo</span><span class="params">(<span class="type">int</span>&amp; x)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 处理一个非常量引用</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">foo</span><span class="params">(<span class="type">const</span> <span class="type">int</span>&amp; x)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 处理一个常量引用</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>
</li>
<li><p>当有多个函数重载时，&#x3D;&#x3D;如果某个函数的参数既可以被传递为引用，也可以被传递为值，那么编译器会优先选择传值调用&#x3D;&#x3D;</p>
<p>因为传递值可以避免引用的问题，并且可以更好地利用编译器的优化。如果需要显式地调用一个特定的函数重载，可以使用类型转换或显式地声明参数类型来调用该函数。</p>
<p><strong>例如：</strong></p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">foo</span><span class="params">(<span class="type">int</span> x)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 处理一个整数</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">foo</span><span class="params">(<span class="type">const</span> <span class="type">int</span>&amp; x)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 处理一个常量引用</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>如果我们调用 <code>foo(42)</code>，那么编译器会选择第一个函数，因为它可以直接将整数值 42 传递给函数参数 <code>x</code>，而不需要创建一个临时的引用对象。这样可以更快地执行，因为传递值比传递引用更高效。</p>
<p>但是，如果我们调用 <code>foo(i)</code>，其中 <code>i</code> 是一个整数变量，则编译器会选择第二个函数，因为它可以使用一个引用来访问 <code>i</code>，并且这个引用可以被声明为常量引用，从而保证 <code>i</code> 的值不会被修改。</p>
<p>如果我们需要显式地调用一个特定的函数重载，可以使用类型转换或显式地声明参数类型来调用该函数。</p>
<p>例如，我们可以使用以下方式来调用第一个函数：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">foo</span>(<span class="built_in">static_cast</span>&lt;<span class="type">const</span> <span class="type">int</span>&amp;&gt;(<span class="number">42</span>));</span><br></pre></td></tr></table></figure></div>

<p>或者我们可以使用以下方式来调用第二个函数：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> i = <span class="number">42</span>;</span><br><span class="line"><span class="built_in">foo</span>(<span class="built_in">static_cast</span>&lt;<span class="type">int</span>&gt;(i));</span><br></pre></td></tr></table></figure></div>

<p>在这两个例子中，我们使用 <code>static_cast</code> 强制将值类型转换为引用类型或将引用类型转换为值类型。这样可以显式地指定需要调用的函数版本。</p>
</li>
<li><p>两个使用引用作为参数的重载函数<strong>示例</strong>：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">foo</span><span class="params">(<span class="type">int</span>&amp; x)</span> </span></span><br><span class="line"><span class="function"></span>&#123;   </span><br><span class="line">    <span class="comment">// 处理一个非常量引用 </span></span><br><span class="line">&#125;;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">foo</span><span class="params">(<span class="type">const</span> <span class="type">int</span>&amp; x)</span> </span></span><br><span class="line"><span class="function"></span>&#123;  </span><br><span class="line">    <span class="comment">// 处理一个常量引用</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></div>

<p>对于这两个重载函数，当传入一个字面常量时，编译器会选择调用 <code>foo(const int&amp; x)</code>，例如：<code>foo(10)</code>，因为字面数值是常量，不能被修改，因此需要传递一个常量引用。</p>
<p>如果传入一个变量，例如： <code>int i = 10; foo(i);</code>，则会选择调用 <code>foo(int&amp; x)</code>，因为该函数接受一个非常量引用，可以修改传入的参数。</p>
<p>如果传入的参数是一个 <code>const</code> 类型的变量，则编译器会选择调用 <code>foo(const int&amp; x)</code>。</p>
</li>
<li><p>不建议在重载函数时使用默认参数，这会非常容易出错，这里不做过多解释</p>
</li>
</ul>
<h2 id="函数的默认参数"><a href="#函数的默认参数" class="headerlink" title="函数的默认参数"></a>函数的默认参数</h2><hr>
<p>函数的默认参数是指在函数定义或声明时，为形参指定一个默认值，这样在调用函数时，如果没有给这个形参赋值（没有对应的实参），那么就使用这个默认值。</p>
<p><strong>示例：</strong></p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//带默认参数的函数</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">func</span><span class="params">(<span class="type">int</span> n, <span class="type">float</span> b = <span class="number">1.2</span>, <span class="type">char</span> c = <span class="string">&#x27;@&#x27;</span>)</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    cout &lt;&lt; n &lt;&lt; <span class="string">&quot;, &quot;</span> &lt;&lt; b &lt;&lt; <span class="string">&quot;, &quot;</span> &lt;&lt; c &lt;&lt; endl;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">func</span>(<span class="number">10</span>); <span class="comment">//相当于func(10, 1.2, &#x27;@&#x27;)</span></span><br><span class="line">	<span class="built_in">func</span>(<span class="number">20</span>, <span class="number">9.8</span>); <span class="comment">//相当于func(20, 9.8, &#x27;@&#x27;)</span></span><br><span class="line">	<span class="built_in">func</span>(<span class="number">30</span>, <span class="number">5.5</span>, <span class="string">&#x27;#&#x27;</span>); <span class="comment">//不使用任何默认值</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p><strong>输出结果：</strong></p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="number">10</span>, <span class="number">1.2</span>, @</span><br><span class="line"><span class="number">20</span>, <span class="number">9.8</span>, @</span><br><span class="line"><span class="number">30</span>, <span class="number">5.5</span> #</span><br></pre></td></tr></table></figure></div>

<p><strong>默认参数有以下几个注意事项：</strong></p>
<ul>
<li><p>不建议在重载函数时使用默认参数，这会非常容易出错，这里不做过多解释。</p>
</li>
<li><p>默认参数必须是常量或常量表达式，在它们前面有一个赋值运算符<code>=</code>。</p>
</li>
<li><p>一旦为某个形参指定了默认值，那么它后面的所有形参都必须有默认值。</p>
<p><strong>示例：</strong></p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 错误：b有了默认参数，那么b之后的形参都必须有默认参数，而c没有默认参数</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">func</span><span class="params">(<span class="type">int</span> n, <span class="type">float</span> b = <span class="number">1.2</span>, <span class="type">char</span> c ,<span class="type">double</span> d=<span class="number">5.2</span>)</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    cout &lt;&lt; n &lt;&lt; <span class="string">&quot;, &quot;</span> &lt;&lt; b &lt;&lt; <span class="string">&quot;, &quot;</span> &lt;&lt; c &lt;&lt; <span class="string">&quot;, &quot;</span> &lt;&lt; d &lt;&lt; endl;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//正确，第一个有默认参数的形参之后的所有形参都有默认参数</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">func</span><span class="params">(<span class="type">int</span> n, <span class="type">float</span> b , <span class="type">char</span> c = <span class="string">&#x27;@&#x27;</span>,<span class="type">double</span> d = <span class="number">5.2</span>)</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    cout &lt;&lt; n &lt;&lt; <span class="string">&quot;, &quot;</span> &lt;&lt; b &lt;&lt; <span class="string">&quot;, &quot;</span> &lt;&lt; c &lt;&lt; <span class="string">&quot;, &quot;</span> &lt;&lt; d &lt;&lt; endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>
</li>
<li><p>默认参数应在函数名称最早出现时分配，通常是在函数原型中指定，默认参数只能指定一次。如果函数有声明就会以声明的为准，定义时指出的默认参数就会无效。</p>
<p>如果函数有声明就会以声明的为准，定义时指出的默认参数就会无效。这是因为编译器在处理函数调用时，需要知道函数的参数类型和数量，以及是否有默认值，如果在声明和定义中都给出了默认参数，那么就会造成冲突和混乱。</p>
<p><strong>错误示例：</strong></p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//错误示例</span></span><br><span class="line"><span class="comment">//声明</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">func</span><span class="params">(<span class="type">int</span> n = <span class="number">10</span>)</span></span>; <span class="comment">//给n指定了默认值10</span></span><br><span class="line"><span class="comment">//定义</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">func</span><span class="params">(<span class="type">int</span> n = <span class="number">20</span>)</span><span class="comment">//又给n指定了默认值20，即使它们是一样的也是不行</span></span></span><br><span class="line"><span class="function"></span>&#123; </span><br><span class="line">    cout &lt;&lt; n &lt;&lt; endl;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//调用</span></span><br><span class="line"><span class="built_in">func</span>(); <span class="comment">//这里应该使用哪个默认值呢？</span></span><br></pre></td></tr></table></figure></div>

<p>这样的代码是不合法的，编译器会报错。正确的做法是只在声明或定义中给出一次默认参数。</p>
<p><strong>正确示例：</strong></p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//正确的示例</span></span><br><span class="line"><span class="comment">//声明</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">func</span><span class="params">(<span class="type">int</span> n = <span class="number">10</span>)</span></span>; <span class="comment">//给n指定了默认值10</span></span><br><span class="line"><span class="comment">//定义</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">func</span><span class="params">(<span class="type">int</span> n)</span> <span class="comment">//不再给n指定默认值</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    cout &lt;&lt; n &lt;&lt; endl;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//调用</span></span><br><span class="line"><span class="built_in">func</span>(); <span class="comment">//使用声明中的默认值10</span></span><br></pre></td></tr></table></figure></div>

<p>或者</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//正确的示例</span></span><br><span class="line"><span class="comment">//声明（没有给出默认参数）</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">func</span><span class="params">(<span class="type">int</span> n)</span></span>;</span><br><span class="line"><span class="comment">//定义（给出了默认参数）</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">func</span><span class="params">(<span class="type">int</span> n = <span class="number">10</span>)</span></span>&#123;</span><br><span class="line">    cout &lt;&lt; n &lt;&lt; endl;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//调用（使用定义中的默认值）</span></span><br><span class="line"><span class="built_in">func</span>();</span><br></pre></td></tr></table></figure></div>
</li>
<li><p>当一个实参在一个函数调用中被遗漏时（因为它有默认值)，它后面的所有实参也必须被省略。不能跳过某个实参而给后面的实参赋值。</p>
<p><strong>示例：</strong></p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//带默认参数的函数</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">func</span><span class="params">(<span class="type">int</span> n, <span class="type">float</span> b = <span class="number">1.2</span>, <span class="type">char</span> c = <span class="string">&#x27;@&#x27;</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    cout &lt;&lt; n &lt;&lt; <span class="string">&quot;, &quot;</span> &lt;&lt; b &lt;&lt; <span class="string">&quot;, &quot;</span> &lt;&lt; c &lt;&lt; endl;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// 正确</span></span><br><span class="line">    <span class="built_in">func</span>(<span class="number">10</span>); <span class="comment">//相当于func(10, 1.2, &#x27;@&#x27;)</span></span><br><span class="line">	<span class="built_in">func</span>(<span class="number">20</span>, <span class="number">9.8</span>); <span class="comment">//相当于func(20, 9.8, &#x27;@&#x27;)</span></span><br><span class="line">	<span class="built_in">func</span>(<span class="number">30</span>, <span class="number">5.5</span>, <span class="string">&#x27;#&#x27;</span>); <span class="comment">//不使用任何默认值</span></span><br><span class="line">    <span class="comment">//错误</span></span><br><span class="line">    <span class="built_in">func</span>(<span class="number">10</span>, , <span class="string">&#x27;#&#x27;</span>); <span class="comment">//错误！不能跳过第二个参数而给第三个参数赋值</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>
</li>
<li><p>默认参数与函数重载可能会产生二义性问题。如果有两个重载函数都可以匹配调用语句，那么编译器会报错。</p>
<p>编译器在处理函数调用时，需要根据实参的类型和数量来确定调用哪个重载函数，如果有多个重载函数都符合条件，那么就会造成歧义。</p>
<p><strong>错误示例：</strong></p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//错误的示例</span></span><br><span class="line"><span class="comment">//声明两个重载函数</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">func</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b = <span class="number">10</span>)</span></span>;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">func</span><span class="params">(<span class="type">int</span> a)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">//定义两个重载函数</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">func</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;func(int a, int b) is called&quot;</span> &lt;&lt; endl;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">func</span><span class="params">(<span class="type">int</span> a)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;func(int a) is called&quot;</span> &lt;&lt; endl;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//调用</span></span><br><span class="line"><span class="built_in">func</span>(<span class="number">10</span>); <span class="comment">//这里应该调用哪个重载函数呢？</span></span><br></pre></td></tr></table></figure></div>

<p>这样的代码是不合法的，编译器会报错。正确的做法是避免使用默认参数和函数重载产生歧义。</p>
<p><strong>正确示例：</strong></p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//正确的示例</span></span><br><span class="line"><span class="comment">//声明两个重载函数</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">func</span><span class="params">(<span class="type">int</span> a)</span></span>;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">func</span><span class="params">(<span class="type">double</span> a)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">//定义两个重载函数</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">func</span><span class="params">(<span class="type">int</span> a)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;func(int a) is called&quot;</span> &lt;&lt; endl;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">func</span><span class="params">(<span class="type">double</span> a)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;func(double a) is called&quot;</span> &lt;&lt; endl;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//调用</span></span><br><span class="line"><span class="built_in">func</span>(<span class="number">10</span>); <span class="comment">//调用func(int a)</span></span><br><span class="line"><span class="built_in">func</span>(<span class="number">10.0</span>); <span class="comment">//调用func(double a)</span></span><br></pre></td></tr></table></figure></div></li>
</ul>
<p>有关类中成员函数的重载和默认参数将在类的相关笔记中说明</p>
<h2 id="函数的占位参数"><a href="#函数的占位参数" class="headerlink" title="函数的占位参数"></a>函数的占位参数</h2><hr>
<ul>
<li><p>C++中函数的形参列表可以有占位参数，用来作占位，调用函数时必须填补该位置。</p>
</li>
<li><p>占位参数只有参数类型声明，而没有参数名声明。</p>
</li>
<li><p>一般情况下，在函数体内部无法使用占位参数。占位参数也可以有默认值。</p>
</li>
</ul>
<p>注意事项有以下几点：</p>
<ul>
<li>占位参数不能只有部分参数类型声明，而没有参数名声明，例如 <code>void func(int a, int)</code> 是合法的，但 <code>void func(int, int b)</code> 是不合法的</li>
<li>占位参数一般用于重载运算符或者实现一些特殊功能，不建议在普通函数中使用</li>
</ul>
<p><strong>示例：</strong></p>
<div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">//函数占位参数，占位参数也可以有默认值</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">func01</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span>)</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;this is func&quot;</span> &lt;&lt; endl;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 有默认参数的占位参数</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">func02</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b, <span class="type">int</span> = <span class="number">20</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	cout &lt;&lt; <span class="string">&quot;a + b = &quot;</span> &lt;&lt; a + b &lt;&lt; endl;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">func01</span>(<span class="number">10</span>,<span class="number">10</span>); <span class="comment">//占位参数必须填补</span></span><br><span class="line">    </span><br><span class="line">	<span class="built_in">func02</span>(<span class="number">10</span>,<span class="number">20</span>); <span class="comment">// 错误调用，占位参数也是参数，必须传参数</span></span><br><span class="line">    </span><br><span class="line">	<span class="built_in">func02</span>(<span class="number">10</span>,<span class="number">20</span>,<span class="number">30</span>); <span class="comment">//正确调用</span></span><br><span class="line">	<span class="built_in">func02</span>(<span class="number">10</span>,<span class="number">20</span>); <span class="comment">//正确调用</span></span><br><span class="line">	<span class="built_in">func02</span>(<span class="number">10</span>, <span class="number">20</span>, <span class="number">30</span>); <span class="comment">//正确调用</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<h1 id="类和对象"><a href="#类和对象" class="headerlink" title="类和对象"></a>类和对象</h1><p>C++中的类是一种用户&#x3D;&#x3D;自定义的数据类型&#x3D;&#x3D;，用于封装数据和函数。类的定义包括数据成员和成员函数，数据成员可以是任何C++数据类型，成员函数可以是普通函数或者操作符重载函数。</p>
<p>&#x3D;&#x3D;类似于<code>struct</code>&#x3D;&#x3D;，当然实际不一样</p>
<p>对象是类的一个实例，它是由类定义的模板创建的具体实体。对象可以访问类中的成员函数和数据成员，可以通过对象来操作类中的数据。</p>
<p>类和对象是面向对象编程的核心概念，它们可以帮助我们更好地组织和管理程序的数据和代码，提高程序的可读性、可维护性和可扩展性。</p>
<p>C++面向对象的三大特征是封装、继承和多态。</p>
<ul>
<li>封装（Encapsulation）：指将数据和操作数据的函数绑定在一起，形成一个类。封装可以隐藏数据和实现细节，防止外部程序直接访问和修改数据，从而提高程序的安全性和可靠性。</li>
<li>继承（Inheritance）：指在一个已有类的基础上，创建一个新类，新类具有原有类的属性和方法。通过继承，可以减少重复代码，提高代码的复用性和可维护性。</li>
<li>多态（Polymorphism）：指同一个函数或方法可以根据不同的对象调用产生不同的行为。多态可以提高程序的灵活性和可扩展性，使程序更易于维护和扩展。</li>
</ul>
<p>这三大特征是C++面向对象编程的核心，也是C++在实现面向对象编程中最重要的手段。</p>
<p><strong>通俗易懂的解释什么是类和对象：</strong></p>
<ul>
<li>当我们看到一只狗时，我们可以想象它是“狗”这个大类（狗这个物种）下的一个“对象”。</li>
<li>狗类有一些属性，如颜色、品种、体型等，也有一些方法，如叫、跑、吃等。</li>
<li>每只狗都具有这些属性和方法，但它们的具体值可能会不同，比如颜色是黑色的、品种是哈士奇的、体型是大型的。</li>
<li>但这些不同的狗都是由狗这个类实例化而来的不同对象。</li>
</ul>
<p>我们可以把类比作一个“图纸”或“模板”，用于描述对象应该具有的属性和方法。而对象则是由这个蓝图创建的具体实例，具有这些属性和方法。就像建筑师会按照图纸来建造房屋一样，我们也可以根据类来创建对象。</p>
<p>另一个比喻是，类就像是一个自定义数据类型，而对象则是这个数据类型的具体实例。就像int是一个数据类型，1、2、3等就是<code>int</code>类型的具体实例一样。</p>
<p><strong>例如：</strong></p>
<ol>
<li>车可以作为对象，属性具有品牌、颜色、速度等，行为有加速、刹车、转弯等，每一辆车都属于车这个大的类。</li>
<li>动物可以作为对象，属性具有种类、生活习性、食物喜好等，行为有奔跑、攀爬、捕猎等，每一种动物都属于动物这个大的类。</li>
<li>商品可以作为对象，属性具有名称、价格、规格等，行为有上架、下架、促销等，每一种商品都属于商品这个大的类。</li>
<li>一个人可以作为对象，属性具有姓名、年龄、身高、体重等，行为有走、跑、跳、吃饭等，每一个人都属于人类这个大的类。</li>
</ol>
<h2 id="类的定义和封装"><a href="#类的定义和封装" class="headerlink" title="类的定义和封装"></a>类的定义和封装</h2><hr>
<p><strong>封装是面向对象编程中的重要概念之一，它的作用和意义主要有以下几个方面：</strong></p>
<ul>
<li>提高安全性：通过封装，我们可以将类的实现细节隐藏起来，只将需要外部访问的接口暴露出来，从而防止外部程序直接访问和修改类的数据，提高程序的安全性和可靠性。</li>
<li>提高可维护性：通过封装，我们可以将类的实现细节和外部接口分离开来，使得在修改类的实现细节时，不会对外部程序产生影响，从而提高程序的可维护性和可扩展性。</li>
<li>简化编程：通过封装，我们可以将复杂的操作封装成一个函数或方法，从而简化编程，减少代码的冗余和复杂度。</li>
<li>提高代码的复用性：通过封装，我们可以将一些通用的功能封装成类或函数，供多个程序复用，从而提高代码的复用性。</li>
</ul>
<p>类似定义<code>struct</code>用户自定义数据类型，定义类的关键字是<code>class</code></p>
<p><strong>基本语法：</strong></p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> 类名</span><br><span class="line">&#123;</span><br><span class="line">访问权限:</span><br><span class="line">	属性;</span><br><span class="line">	行为;<span class="comment">//行为指函数</span></span><br><span class="line">    ......</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></div>

<p><strong>示例：</strong></p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 定义一个最简单的类</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">MyClass</span> </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// 成员函数</span></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">print</span><span class="params">()</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;Hello World!&quot;</span> &lt;&lt; std::endl;</span><br><span class="line">    &#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="comment">// 数据成员</span></span><br><span class="line">    <span class="type">int</span> num;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// 创建一个对象</span></span><br><span class="line">    MyClass obj;</span><br><span class="line">    <span class="comment">// 调用成员函数</span></span><br><span class="line">    obj.<span class="built_in">print</span>();</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p><strong>解释：</strong></p>
<ul>
<li><p>当我们定义一个类时，我们需要定义它的属性和行为。属性是指该类具有的特征或者状态，行为则是指该类可以执行的操作。</p>
</li>
<li><p>在这个例子中，我们定义了一个叫做&#96;&#96;MyClass&#96;的类，它具有以下属性和行为：</p>
<ul>
<li><p>属性：</p>
<p><code>num</code>：一个整数类型的私有数据成员。</p>
</li>
<li><p>行为：</p>
<p><code>print()</code>：一个公有成员函数，可以输出一条信息到控制台。输出信息就是这个类的行为。</p>
</li>
</ul>
</li>
<li><p>在这个类中，<code>num</code>是一个私有数据成员，外部程序无法直接访问和修改。</p>
</li>
<li><p><code>print()</code>是一个公有成员函数，可以在类的外部直接访问和调用。</p>
</li>
<li><p>调用<code>print</code>函数时，它会输出一条消息到控制台，这条消息是<code>Hello World!</code>。</p>
</li>
</ul>
<p><strong>注意事项和解释：</strong></p>
<ul>
<li><p>公有(<code>public</code>)、私有(<code>private</code>)、保护(<code>protected</code>)是访问权限的修饰符，用于控制类中成员的访问权限。</p>
</li>
<li><p>公有成员可以被类的外部程序访问和调用，私有成员只能被类的内部程序访问和调用，保护成员可以被该类的派生类的内部程序访问和调用。</p>
</li>
<li><p>定义类时，访问权限修饰的顺序建议按照<code>public</code>、<code>protected</code>、<code>private</code>的顺序出现，即<code>public</code>在前，<code>protected</code>在中间，<code>private</code>在后面。</p>
<p>按照公有、保护、私有的顺序来定义类的成员是一种被广泛接受的，可以使代码更加清晰易读，便于他人理解和维护。</p>
</li>
<li><p>在定义一个类时，一般把属性写在前面，成员函数写在后面。按照访问权限的不同，将属性和成员函数分别写在<code>public</code>、<code>protected</code>、<code>private</code>部分。</p>
</li>
<li><p>访问权限的顺序并没有硬性规定，但是通常我们会按照公有、保护、私有的顺序来定义类的成员。具体的实现方式可以根据实际情况进行调整。</p>
</li>
</ul>
<p>在实际编写代码时，具体的代码组织方式应该根据实际情况来确定。</p>
<p><strong>注意事项：</strong></p>
<ul>
<li>类的成员函数可以在类内定义，也可以在类外定义。</li>
<li>如果在类内定义，则默认为内联函数。如果在类外定义，则需要使用作用域解析运算符<code>::</code>指明该函数属于哪个类。</li>
</ul>
<p><strong>类内定义成员函数示例：</strong></p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MyClass</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">myFunc</span><span class="params">()</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="comment">// 在类内定义成员函数</span></span><br><span class="line">        <span class="comment">// ...</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></div>

<p><strong>类外定义成员函数示例：</strong></p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MyClass</span> </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">myFunc</span><span class="params">()</span></span>; <span class="comment">// 在类内声明成员函数</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">MyClass::myFunc</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// 在类外定义成员函数</span></span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<ul>
<li>如果一个类的成员函数在类外定义，那么在定义之前必须先声明该成员函数，否则编译器会报错。</li>
<li>通常，可以在类的头文件中声明成员函数，在源文件中定义成员函数，以实现类的封装性和可维护性。</li>
</ul>
<h2 id="class和struct的区别"><a href="#class和struct的区别" class="headerlink" title="class和struct的区别"></a><code>class</code>和<code>struct</code>的区别</h2><hr>
<p>在C++中，&#x3D;&#x3D;<code>class</code>和<code>struct</code>本质上是相同的&#x3D;&#x3D;，都可以定义成员变量和成员函数，并且都支持封装、继承和多态等面向对象编程的特性。但是，它们之间有以下几个区别：</p>
<ul>
<li>默认访问权限：在class中，默认的访问权限是<code>private</code>，而在<code>struct</code>中，默认的访问权限是<code>public</code>。因此，在class中定义的成员变量和成员函数是默认无法在类外部访问的，需要通过<code>public</code>关键字进行公开；而在<code>struct</code>中定义的成员变量和成员函数是默认可以在类外部访问的。</li>
<li>继承方式：在<code>class</code>中，继承方式默认是<code>private</code>，即派生类只能通过<code>public</code>成员函数访问基类的成员；而在<code>struct</code>中，继承方式默认是<code>public</code>，即派生类可以直接访问基类的成员。</li>
<li>使用习惯：在使用习惯上，<code>class</code>通常用于定义复杂的数据类型和对象，而<code>struct</code>通常用于定义轻量级的数据类型和数据结构。</li>
</ul>
<p>需要注意的是，以上区别只是默认情况下的差异，在实际使用中，我们可以通过访问权限和继承方式的调整，使<code>class</code>和<code>struct</code>之间的区别也会变得比较模糊。</p>
<h2 id="构造函数"><a href="#构造函数" class="headerlink" title="构造函数"></a>构造函数</h2><hr>
<p>在C++中，构造函数是一种特殊的成员函数，用于&#x3D;&#x3D;初始化类的对象&#x3D;&#x3D;。</p>
<p><strong>注意事项：</strong></p>
<ul>
<li>构造函数的名称必须与类名相同，且不可有返回值类型。</li>
<li>构造函数可以有多个参数，也可以没有参数，以及可以有默认参数。</li>
<li>构造函数可以重载，即同一个类中可以有多个不同参数的构造函数。</li>
<li>构造函数可以在类的内部或外部进行定义，但必须是公有的。</li>
<li>构造函数可以调用其他函数，如成员函数、静态函数等。</li>
<li>如果没有定义构造函数，编译器会提供一个默认的构造函数。</li>
</ul>
<p>以下是一个简单的<strong>示例</strong>，展示了如何定义一个类及其构造函数：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MyClass</span> </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// 构造函数</span></span><br><span class="line">    <span class="built_in">MyClass</span>() </span><br><span class="line">    &#123;</span><br><span class="line">        cout&lt;&lt;<span class="string">&quot;构造函数调用&quot;</span>&lt;&lt;endl;</span><br><span class="line">    &#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">int</span> x;</span><br><span class="line">    <span class="type">int</span> y;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></div>

<ul>
<li>在上面的示例中，<code>MyClass</code>是一个简单的类，包含两个私有成员变量x和y。</li>
<li>当创建<code>MyClass</code>对象时，编译器会自动调用构造函数，从而输出<code>“构造函数调用”</code></li>
</ul>
<p><strong>说明：</strong></p>
<ul>
<li>构造函数还可以用于执行任何必要的预处理或初始化操作，如打开文件或分配内存等。</li>
<li>构造函数可以重载，重载时必须具有不同的参数列表。</li>
<li>构造函数重载时根据提供的参数列表，编译器将自动选择正确的构造函数进行调用。</li>
<li>构造函数对象创建时自动被调用，用于初始化对象的成员变量。</li>
<li>构造函数作用是确保对象被正确地初始化，使程序在使用对象之前具有可预测的状态。</li>
</ul>
<p><strong>例如</strong>，有一个<code>Person</code>类：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Person</span> </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// 构造函数</span></span><br><span class="line">    <span class="built_in">Person</span>(string name, <span class="type">int</span> age) </span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">this</span>-&gt;name = name;</span><br><span class="line">        <span class="keyword">this</span>-&gt;age = age;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 成员函数</span></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">display</span><span class="params">()</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Name: &quot;</span> &lt;&lt; name &lt;&lt; endl;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Age: &quot;</span> &lt;&lt; age &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    string name;</span><br><span class="line">    <span class="type">int</span> age;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// 创建对象时调用构造函数进行初始化</span></span><br><span class="line">    <span class="function">Person <span class="title">p</span><span class="params">(<span class="string">&quot;Tom&quot;</span>, <span class="number">20</span>)</span></span>;</span><br><span class="line">    p.<span class="built_in">display</span>();</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<ul>
<li>上述例子中，定义了一个<code>Person</code>类，其中定义了一个构造函数，构造函数有两个参数，分别是<code>name</code>和<code>age</code>，用于对对象的数据成员进行初始化。</li>
<li>在<code>main</code>函数中，我们创建了一个<code>Person</code>对象，传入了<code>name</code>和<code>age</code>的值来调用构造函数进行初始化，最后调用<code>display</code>函数输出对象的数据成员。</li>
</ul>
<p><strong>注意事项：</strong></p>
<ul>
<li>如果用户没有定义构造函数，则编译器会自动生成一个默认构造函数。</li>
<li>这个构造函数没有参数，仅仅是对对象的成员变量进行默认的初始化。</li>
<li>如果我们的类需要进行特殊的初始化操作，就需要自己定义构造函数。</li>
<li>构造函数可以用于分配内存，但构造函数本身并不执行内存分配操作。</li>
</ul>
<p><strong>注意事项：</strong></p>
<p>在构造函数中进行内存分配等相关操作，存在一定的风险和限制。不建议在构造函数中进行内存分配等操作。如果确实需要进行这些操作，需要做好异常处理。</p>
<ul>
<li>内存分配操作可能失败。在进行内存分配操作时，如果没有足够的内存可用，分配操作会失败并返回一个空指针。如果构造函数无法处理这种情况，可能会导致程序异常或崩溃。</li>
<li>内存分配操作可能耗费大量时间。在进行内存分配操作时，如果分配的内存较大或较多，可能需要很长时间才能完成。如果构造函数在这段时间内阻塞，可能会导致程序性能下降或超时。</li>
<li>构造函数可能会抛出异常。如果在构造函数中进行内存分配等操作时出现异常，可能会导致对象未能正确创建，从而造成程序错误。</li>
<li>创建一个类对象时，首先会为该对象分配内存空间，然后再调用它的构造函数进行初始化。注意顺序。</li>
</ul>
<p><strong>重载构造函数示例：</strong></p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Person</span> </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// 默认构造函数</span></span><br><span class="line">    <span class="built_in">Person</span>() </span><br><span class="line">    &#123;</span><br><span class="line">        name = <span class="string">&quot;&quot;</span>;</span><br><span class="line">        age = <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 重载构造函数1，传入姓名</span></span><br><span class="line">    <span class="built_in">Person</span>(string name) </span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">this</span>-&gt;name = name;</span><br><span class="line">        <span class="keyword">this</span>-&gt;age = <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 重载构造函数2，传入姓名和年龄</span></span><br><span class="line">    <span class="built_in">Person</span>(string name, <span class="type">int</span> age) </span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">this</span>-&gt;name = name;</span><br><span class="line">        <span class="keyword">this</span>-&gt;age = age;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 成员函数</span></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">display</span><span class="params">()</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Name: &quot;</span> &lt;&lt; name &lt;&lt; endl;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Age: &quot;</span> &lt;&lt; age &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    string name;</span><br><span class="line">    <span class="type">int</span> age;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// 调用不同的构造函数进行初始化</span></span><br><span class="line">    Person p1;</span><br><span class="line">    p1.<span class="built_in">display</span>();</span><br><span class="line">    </span><br><span class="line">    <span class="function">Person <span class="title">p2</span><span class="params">(<span class="string">&quot;Tom&quot;</span>)</span></span>;</span><br><span class="line">    p2.<span class="built_in">display</span>();</span><br><span class="line">    </span><br><span class="line">    <span class="function">Person <span class="title">p3</span><span class="params">(<span class="string">&quot;Jerry&quot;</span>, <span class="number">20</span>)</span></span>;</span><br><span class="line">    p3.<span class="built_in">display</span>();</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p><strong>输出结果：</strong></p>
<div class="highlight-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">Name:</span><br><span class="line">Age: 0</span><br><span class="line">Name: Tom</span><br><span class="line">Age: 0</span><br><span class="line">Name: Jerry</span><br><span class="line">Age: 20</span><br></pre></td></tr></table></figure></div>

<p><strong>带有默认参数的构造函数调用示例：</strong></p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Person</span> </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// 带有默认参数的构造函数</span></span><br><span class="line">    <span class="built_in">Person</span>(string name = <span class="string">&quot;&quot;</span>, <span class="type">int</span> age = <span class="number">0</span>) </span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">this</span>-&gt;name = name;</span><br><span class="line">        <span class="keyword">this</span>-&gt;age = age;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 成员函数</span></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">display</span><span class="params">()</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Name: &quot;</span> &lt;&lt; name &lt;&lt; endl;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Age: &quot;</span> &lt;&lt; age &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    string name;</span><br><span class="line">    <span class="type">int</span> age;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// 调用带有默认参数的构造函数进行初始化</span></span><br><span class="line">    Person p1;</span><br><span class="line">    p1.<span class="built_in">display</span>();</span><br><span class="line">    </span><br><span class="line">    <span class="function">Person <span class="title">p2</span><span class="params">(<span class="string">&quot;Tom&quot;</span>)</span></span>;</span><br><span class="line">    p2.<span class="built_in">display</span>();</span><br><span class="line">    </span><br><span class="line">    <span class="function">Person <span class="title">p3</span><span class="params">(<span class="string">&quot;Jerry&quot;</span>, <span class="number">20</span>)</span></span>;</span><br><span class="line">    p3.<span class="built_in">display</span>();</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p><strong>输出结果：</strong></p>
<div class="highlight-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">Name:</span><br><span class="line">Age: 0</span><br><span class="line">Name: Tom</span><br><span class="line">Age: 0</span><br><span class="line">Name: Jerry</span><br><span class="line">Age: 20</span><br></pre></td></tr></table></figure></div>

<h2 id="析构函数"><a href="#析构函数" class="headerlink" title="析构函数"></a>析构函数</h2><hr>
<p>析构函数是一种特殊类型的成员函数，用于在对象被销毁时进行清理工作。它的作用是在对象生命周期结束时，释放对象所占用的内存资源，包括释放动态分配的内存、关闭文件、释放锁等。</p>
<p><strong>使用和注意事项：</strong></p>
<ul>
<li><p>析构函数的命名必须和类名相同，但需要在名称前添加<code>~</code>。</p>
<p>例如：类名是<code>MyClass</code>，则析构函数的名称是<code>~MyClass</code>。</p>
</li>
<li><p>析构函数不能接有何参数，不能被重载。</p>
</li>
<li><p>当类对象被销毁时，编译器会自动调用相应的析构函数。</p>
</li>
</ul>
<p><strong>示例：</strong></p>
<div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">MyClass</span> </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">MyClass</span>() </span><br><span class="line">    &#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;构造函数被调用&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">    ~<span class="built_in">MyClass</span>() </span><br><span class="line">    &#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;析构函数被调用&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    MyClass obj; <span class="comment">// 创建一个 MyClass 对象</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p><strong>输出：</strong></p>
<div class="highlight-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">构造函数被调用</span><br><span class="line">析构函数被调用</span><br></pre></td></tr></table></figure></div>

<p>在上面的示例中，当创建<code>MyClass</code>对象时，构造函数被调用。当程序结束时，析构函数被自动调用，释放对象所占用的内存资源。</p>
<p><strong>注意事项：</strong></p>
<ul>
<li><p>析构函数可以是虚函数。</p>
<p>如果一个类有虚函数，那么它的析构函数通常也应该是虚函数。这是因为，如果一个类的指针被转换为一个基类指针，并使用基类指针删除指向派生类对象的内存，那么如果析构函数不是虚函数，那么只会调用基类的析构函数，而不会调用派生类的析构函数。这会导致内存泄漏和其他问题。</p>
</li>
<li><p>不要手动调用析构函数。</p>
<p>析构函数应该由编译器自动调用，在对象被删除时自动调用。不要在代码中显式地调用析构函数，这通常会导致不可预测的行为。</p>
</li>
<li><p>析构函数应该处理异常。</p>
<p>析构函数应该能够处理可能发生的异常。如果析构函数抛出异常并且没有被捕获，那么程序会调用 std::terminate()，导致程序异常终止。</p>
</li>
<li><p>在析构函数中不要抛出异常。</p>
<p>虽然析构函数应该能够处理异常，但最好不要在析构函数中抛出异常。因为在抛出异常后，程序通常不会继续执行，导致可能未完成的清理工作无法完成，从而导致资源泄漏等问题。</p>
</li>
<li><p>析构函数中应该释放动态分配的内存。</p>
<p>如果一个类中有动态分配的内存，那么它的析构函数应该释放这些内存。否则，这些内存将会泄漏。</p>
</li>
</ul>
<p>总之，析构函数是一种非常重要的函数，它用于在对象生命周期结束时进行清理工作。正确编写析构函数可以有效地避免内存泄漏和其他问题。</p>
<p>有关虚函数将在之后的内容中提到。</p>
<h2 id="拷贝构造函数"><a href="#拷贝构造函数" class="headerlink" title="拷贝构造函数"></a>拷贝构造函数</h2><hr>
<p>拷贝构造函数是C++中一种特殊的构造函数，它用一个同类的对象来初始化新创建的对象。</p>
<p><strong>拷贝构造函数通常用于以下情况：</strong></p>
<ul>
<li><p>通过使用另一个同类型的对象来初始化新创建的对象。</p>
<p><strong>示例：</strong></p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Point</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">int</span> x, y;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">//普通有参构造函数</span></span><br><span class="line">    <span class="built_in">Point</span>(<span class="type">int</span> x1, <span class="type">int</span> y1) </span><br><span class="line">    &#123; </span><br><span class="line">        x = x1;</span><br><span class="line">        y = y1; </span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 拷贝构造函数</span></span><br><span class="line">    <span class="built_in">Point</span>(<span class="type">const</span> Point &amp;p2) </span><br><span class="line">    &#123;</span><br><span class="line">    	x = p2.x; </span><br><span class="line">   		y = p2.y; </span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">getX</span><span class="params">()</span>            </span></span><br><span class="line"><span class="function">    </span>&#123;  </span><br><span class="line">        <span class="keyword">return</span> x; </span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">getY</span><span class="params">()</span>            </span></span><br><span class="line"><span class="function">    </span>&#123;  </span><br><span class="line">        <span class="keyword">return</span> y; </span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="function">Point <span class="title">p1</span><span class="params">(<span class="number">10</span>, <span class="number">15</span>)</span></span>; <span class="comment">// 这里调用了普通的构造函数</span></span><br><span class="line">    </span><br><span class="line">    Point p2 = p1; <span class="comment">// 这里调用了拷贝构造函数</span></span><br><span class="line">    </span><br><span class="line"> </span><br><span class="line">    <span class="comment">// 访问由构造函数赋值的变量</span></span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;p1.x = &quot;</span> &lt;&lt; p1.<span class="built_in">getX</span>() &lt;&lt; <span class="string">&quot;, p1.y = &quot;</span> &lt;&lt; p1.<span class="built_in">getY</span>();</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;\np2.x = &quot;</span> &lt;&lt; p2.<span class="built_in">getX</span>() &lt;&lt; <span class="string">&quot;, p2.y = &quot;</span> &lt;&lt; p2.<span class="built_in">getY</span>();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p><strong>输出：</strong></p>
<div class="highlight-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">p1.x = 10, p1.y = 15</span><br><span class="line">p2.x = 10, p2.y = 15</span><br></pre></td></tr></table></figure></div>
</li>
<li><p>复制对象把它作为参数传递给函数，即值传递的方式给函数参数传值</p>
<p><strong>示例：</strong></p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Point</span> </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">int</span> x, y;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Point</span>(<span class="type">int</span> x1, <span class="type">int</span> y1) </span><br><span class="line">    &#123;</span><br><span class="line">        x = x1;</span><br><span class="line">        y = y1;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">Point</span>(<span class="type">const</span> Point&amp; p2) </span><br><span class="line">    &#123;</span><br><span class="line">        x = p2.x;</span><br><span class="line">        y = p2.y;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">getX</span><span class="params">()</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> x;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">getY</span><span class="params">()</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> y;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">displayPoint</span><span class="params">(Point p)</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;x = &quot;</span> &lt;&lt; p.<span class="built_in">getX</span>() &lt;&lt; <span class="string">&quot;, y = &quot;</span> &lt;&lt; p.<span class="built_in">getY</span>() &lt;&lt; endl;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="function">Point <span class="title">p1</span><span class="params">(<span class="number">10</span>, <span class="number">15</span>)</span></span>;</span><br><span class="line">    Point p2 = p1;</span><br><span class="line">    <span class="built_in">displayPoint</span>(p1);</span><br><span class="line">    <span class="built_in">displayPoint</span>(p2);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p><strong>输出：</strong></p>
<div class="highlight-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">x = 10, y = 15</span><br><span class="line">x = 10, y = 15</span><br></pre></td></tr></table></figure></div>

<p><strong>解释：</strong></p>
<p>使用值传递的方式传递对象参数，这意味着函数将接收一个新的对象副本，而不是原始对象本身。这个新对象副本将在函数内部使用，而原始对象本身则不会受到任何影响。</p>
<p>在 <code>main</code> 函数中，我们创建了两个 Point 对象 <code>p1</code> 和 <code>p2</code>，并将 <code>p1</code> 复制到 <code>p2</code> 中。然后我们调用 <code>displayPoint</code> 函数两次，将 <code>p1</code> 和 <code>p2</code> 作为参数传递给该函数。在函数内部，我们使用了值传递的方式传递对象参数，因此 <code>displayPoint</code> 函数接收到了 <code>p1</code> 和 <code>p2</code> 的副本，并显示它们的 x 和 y 坐标。</p>
</li>
<li><p>复制对象，值传递方式从函数返回这个对象。</p>
<p><strong>示例：</strong></p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Point</span> </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">int</span> x, y;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Point</span>(<span class="type">int</span> x1, <span class="type">int</span> y1) </span><br><span class="line">    &#123;</span><br><span class="line">        x = x1; </span><br><span class="line">        y = y1; </span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">Point</span>(<span class="type">const</span> Point &amp;p) </span><br><span class="line">    &#123;</span><br><span class="line">        x = p.x; </span><br><span class="line">        y = p.y; </span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">getX</span><span class="params">()</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> x; </span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">getY</span><span class="params">()</span> </span></span><br><span class="line"><span class="function">    </span>&#123; </span><br><span class="line">        <span class="keyword">return</span> y; </span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 返回 Point 对象，使用值传递方式</span></span><br><span class="line"><span class="function">Point <span class="title">getPoint</span><span class="params">(<span class="type">int</span> x, <span class="type">int</span> y)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="function">Point <span class="title">p</span><span class="params">(x, y)</span></span>;</span><br><span class="line">    <span class="keyword">return</span> p;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="function">Point <span class="title">p1</span><span class="params">(<span class="number">10</span>, <span class="number">15</span>)</span></span>;</span><br><span class="line">    <span class="function">Point <span class="title">p2</span><span class="params">(p1)</span></span>; <span class="comment">// 使用拷贝构造函数复制对象</span></span><br><span class="line">    Point p3 = <span class="built_in">getPoint</span>(<span class="number">20</span>, <span class="number">25</span>);</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;p1: x = &quot;</span> &lt;&lt; p1.<span class="built_in">getX</span>() &lt;&lt; <span class="string">&quot;, y = &quot;</span> &lt;&lt; p1.<span class="built_in">getY</span>() &lt;&lt; endl;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;p2: x = &quot;</span> &lt;&lt; p2.<span class="built_in">getX</span>() &lt;&lt; <span class="string">&quot;, y = &quot;</span> &lt;&lt; p2.<span class="built_in">getY</span>() &lt;&lt; endl;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;p3: x = &quot;</span> &lt;&lt; p3.<span class="built_in">getX</span>() &lt;&lt; <span class="string">&quot;, y = &quot;</span> &lt;&lt; p3.<span class="built_in">getY</span>() &lt;&lt; endl;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p><strong>输出：</strong></p>
<div class="highlight-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">p1: x = 10, y = 15</span><br><span class="line">p2: x = 10, y = 15</span><br><span class="line">p3: x = 20, y = 25</span><br></pre></td></tr></table></figure></div>

<p><strong>解释：</strong></p>
<p>在 <code>main</code> 函数中，我们创建了三个 Point 对象 <code>p1</code>、<code>p2</code> 和 <code>p3</code>。我们使用 <code>p1</code> 来初始化 <code>p2</code>，这里使用了拷贝构造函数。我们还调用了函数 <code>getPoint</code>，该函数使用值传递方式返回一个 Point 对象。在 <code>main</code> 函数中，我们将该返回值赋值给 <code>p3</code>，并将它们的 x 和 y 坐标正确地复制到了它们的副本中，同时使用值传递方式从函数返回对象也没有出现问题。</p>
</li>
</ul>
<h2 id="构造函数定义规则"><a href="#构造函数定义规则" class="headerlink" title="构造函数定义规则"></a>构造函数定义规则</h2><hr>
<ul>
<li><p>默认情况下C++编译器会自动给一个类添加至少3个函数</p>
<ul>
<li>默认构造函数，无参，函数体为空</li>
<li>默认析构函数，无参，函数体为空</li>
<li>默认拷贝构造函数，对属性进行值拷贝</li>
</ul>
</li>
<li><p>构造函数的定义规则：</p>
<ul>
<li>如果用户定义有参构造函数，C++不再提供默认无参构造函数，但是会提供默认拷贝构造函数</li>
<li>如果用户定义拷贝构造函数，C++不在提供其他任何构造函数</li>
</ul>
</li>
<li><p><strong>示例1：</strong></p>
<p>当用户定义有参构造函数时，C++不再提供默认无参构造函数，但是会提供默认拷贝构造函数。</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Person</span> </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Person</span>(<span class="type">int</span> age) </span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">this</span>-&gt;age = age;</span><br><span class="line">    &#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">int</span> age;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Person p;<span class="comment">// 编译错误，因为没有默认无参构造函数</span></span><br><span class="line">    </span><br><span class="line">    <span class="function">Person <span class="title">p2</span><span class="params">(<span class="number">18</span>)</span></span>;<span class="comment">// 正确</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>
</li>
<li><p><strong>示例2：</strong></p>
<p>当用户定义拷贝构造函数时，C++不再提供其他任何构造函数。</p>
<div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Person</span> </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Person</span>(<span class="type">const</span> Person&amp; other) </span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">this</span>-&gt;age = other.age;</span><br><span class="line">    &#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">int</span> age;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Person p;<span class="comment">// 编译错误，因为没有默认无参构造函数</span></span><br><span class="line">    </span><br><span class="line">    <span class="function">Person <span class="title">p2</span><span class="params">(p)</span></span>;<span class="comment">// 编译错误，因为没有默认拷贝构造函数</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p><strong>示例3：</strong></p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">MyClass</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="type">int</span> num;</span><br><span class="line">    <span class="type">char</span> ch;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    MyClass obj1; <span class="comment">// 会调用默认构造函数</span></span><br><span class="line">    obj1.num = <span class="number">10</span>;</span><br><span class="line">    obj1.ch = <span class="string">&#x27;a&#x27;</span>;</span><br><span class="line">    MyClass obj2 = obj1; <span class="comment">// 会调用默认拷贝构造函数</span></span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;obj1: &quot;</span> &lt;&lt; obj1.num &lt;&lt; <span class="string">&quot; &quot;</span> &lt;&lt; obj1.ch &lt;&lt; endl;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;obj2: &quot;</span> &lt;&lt; obj2.num &lt;&lt; <span class="string">&quot; &quot;</span> &lt;&lt; obj2.ch &lt;&lt; endl;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure></div>

<p><strong>输出结果：</strong></p>
<div class="highlight-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">obj1: 10 a</span><br><span class="line">obj2: 10 a</span><br></pre></td></tr></table></figure></div></li>
</ul>
<h2 id="深拷贝与浅拷贝"><a href="#深拷贝与浅拷贝" class="headerlink" title="深拷贝与浅拷贝"></a>深拷贝与浅拷贝</h2><hr>
<ul>
<li><p><strong>浅拷贝</strong></p>
<ul>
<li>编译器自动提供的拷贝构造函数属于浅拷贝，浅拷贝会将原对象中成员的值逐字节的复制给新对象中对应成员的值。</li>
<li>当类中含有指针成员时，浅拷贝也会逐字节的将原对象的指针成员所指向的地址复制给新对象中对应的指针成员，使得新对象中的对应的指针成员也指向这块地址，使得两个对象中对应的指针成员共用一个地址。</li>
<li>因此，当修改其中一个对象的指针成员时，另一个对象对应的成指针员也会发生改变。</li>
<li>当手动为指针成员开辟存储空间时，浅拷贝会使得两个对象中对应的指针成员指向同一个地址。</li>
<li>手动开辟的内存空间应该手动释放，当使用过浅拷贝的两个对象的生命周期结束时，容易导致对应指针成员指向的地址空间重复释放，从而使得程序崩溃报错。</li>
</ul>
<p>总结：</p>
<ul>
<li><p>对象拷贝后，两个对象的某些指针成员共享同一块内存，当其中一个对象的值改变时，另一个对象的值也会跟着改变，这会导致程序出现难以预料的错误。</p>
</li>
<li><p>当对象被释放时，由于两个对象成员共享同一块内存，可能会导致重复释放内存的问题，从而导致程序崩溃。</p>
</li>
<li><p>当对象中存在动态内存分配的指针时，由于两个对象共享同一块内存，可能会导致指针指向的内存被重复释放的问题，从而导致程序崩溃。</p>
</li>
<li><p>因此，在设计类时，如果类的成员中存在指针或动态内存分配的情况，应该尽量避免使用浅拷贝，而应该使用深拷贝来避免上述问题。</p>
</li>
</ul>
<p><strong>示例：</strong></p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">preson</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="type">int</span> age;</span><br><span class="line">	<span class="type">int</span>* height;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">preson</span>(<span class="type">int</span> m_age,<span class="type">int</span> m_height)<span class="comment">//preson的有参构造函数</span></span><br><span class="line">	&#123;</span><br><span class="line">		age = m_age;</span><br><span class="line">		height = <span class="keyword">new</span> <span class="built_in">int</span>(m_height);<span class="comment">//堆区开辟的内存应该手动释放</span></span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;preson的有参构造函数调用&quot;</span> &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line">	~<span class="built_in">preson</span>()</span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;preson的析构函数调用&quot;</span> &lt;&lt; endl;</span><br><span class="line">		<span class="comment">//浅拷贝会导致同一块内存重复释放，导致程序异常</span></span><br><span class="line">		<span class="keyword">if</span> (height != <span class="literal">NULL</span>)</span><br><span class="line">		&#123;</span><br><span class="line">			<span class="keyword">delete</span> height;</span><br><span class="line">			height = <span class="literal">NULL</span>;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">test01</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="function">preson <span class="title">p1</span><span class="params">(<span class="number">18</span>, <span class="number">160</span>)</span></span>;</span><br><span class="line">	cout &lt;&lt; <span class="string">&quot;p1年龄的地址：&quot;</span> &lt;&lt; (<span class="type">int</span>)&amp;p1.age </span><br><span class="line">        &lt;&lt; <span class="string">&quot;  p1的年龄：&quot;</span> &lt;&lt; p1.age </span><br><span class="line">        &lt;&lt; <span class="string">&quot;   p1身高的地址：&quot;</span> &lt;&lt; (<span class="type">int</span>)p1.height </span><br><span class="line">        &lt;&lt; <span class="string">&quot;  p1的身高：&quot;</span> &lt;&lt; *p1.height &lt;&lt; endl;</span><br><span class="line">    </span><br><span class="line">	preson p2 = p1;<span class="comment">//编译器自动提供拷贝构造函数，浅拷贝</span></span><br><span class="line">    </span><br><span class="line">	cout &lt;&lt; <span class="string">&quot;p2年龄的地址：&quot;</span> &lt;&lt; (<span class="type">int</span>)&amp;p2.age </span><br><span class="line">        &lt;&lt; <span class="string">&quot;  p2的年龄：&quot;</span> &lt;&lt; p2.age </span><br><span class="line">        &lt;&lt; <span class="string">&quot;   p2身高的地址：&quot;</span> &lt;&lt; (<span class="type">int</span>)p2.height </span><br><span class="line">        &lt;&lt; <span class="string">&quot;  p2的身高：&quot;</span> &lt;&lt; *p2.height </span><br><span class="line">        &lt;&lt; endl;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="built_in">test01</span>();</span><br><span class="line">	<span class="built_in">system</span>(<span class="string">&quot;pause&quot;</span>);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>
</li>
<li><p><strong>深拷贝</strong></p>
<p>深拷贝是指在对象复制时，将一个对象的值复制到另一个新的对象中，这两个对象有不同的地址，它们各自占用不同的内存空间，这种复制方式可以避免浅拷贝的问题，同时也能保证原对象和新对象是独立的，互相不会影响。</p>
<p>深拷贝通常需要手动实现，主要是在对象中存在指针或动态内存分配的情况下，需要手动复制指针指向的内存空间，而不是简单的复制指针本身。</p>
<p>在实现深拷贝时，可以通过以下几种方式：</p>
<ol>
<li>手动实现复制构造函数和赋值运算符重载函数，其中包括创建新的内存空间并复制原对象的值，释放原有内存空间等操作。</li>
<li>使用C++11中提供的智能指针（如<code>std::shared_ptr</code>、<code>std::unique_ptr</code>等），这些指针会自动管理内存，可以避免手动释放内存的问题。</li>
<li>使用标准库中的容器类（如<code>std::vector</code>、<code>std::list</code>等），这些容器类会自动管理内存，可以避免手动释放内存的问题。</li>
</ol>
<p><strong>示例：</strong></p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">person</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="type">int</span> age;</span><br><span class="line">	<span class="type">int</span>* height;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">person</span>(<span class="type">int</span> m_age,<span class="type">int</span> m_height)<span class="comment">//person的有参构造函数</span></span><br><span class="line">	&#123;</span><br><span class="line">		age = m_age;</span><br><span class="line">		height = <span class="keyword">new</span> <span class="built_in">int</span>(m_height);<span class="comment">//堆区开辟的内存应该手动释放</span></span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;person的有参构造函数调用&quot;</span> &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="built_in">person</span>(<span class="type">const</span> person &amp;p)<span class="comment">//person的拷贝构造，手动实现深拷贝</span></span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;拷贝构造函数的调用&quot;</span> &lt;&lt; endl;</span><br><span class="line">		age = p.age;</span><br><span class="line">		<span class="comment">//height = p.height;编译器自动提供的拷贝构造实现方式</span></span><br><span class="line">		height = <span class="keyword">new</span> <span class="built_in">int</span>(*p.height);<span class="comment">//深拷贝</span></span><br><span class="line">	&#125;</span><br><span class="line">	~<span class="built_in">person</span>()</span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;person的析构函数调用&quot;</span> &lt;&lt; endl;</span><br><span class="line">		<span class="comment">//浅拷贝会导致同一块内存重复释放，导致程序异常</span></span><br><span class="line">		<span class="keyword">if</span> (height != <span class="literal">NULL</span>)</span><br><span class="line">		&#123;</span><br><span class="line">			<span class="keyword">delete</span> height;</span><br><span class="line">			height = <span class="literal">NULL</span>;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">test01</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="function">person <span class="title">p1</span><span class="params">(<span class="number">18</span>, <span class="number">160</span>)</span></span>;</span><br><span class="line">	cout &lt;&lt; <span class="string">&quot;p1年龄的地址：&quot;</span> &lt;&lt; (<span class="type">int</span>)&amp;p1.age </span><br><span class="line">        &lt;&lt; <span class="string">&quot;  p1的年龄：&quot;</span> &lt;&lt; p1.age </span><br><span class="line">        &lt;&lt; <span class="string">&quot;   p1身高的地址：&quot;</span> &lt;&lt; (<span class="type">int</span>)p1.height </span><br><span class="line">        &lt;&lt; <span class="string">&quot;  p1的身高：&quot;</span> &lt;&lt; *p1.height &lt;&lt; endl;</span><br><span class="line">    </span><br><span class="line">	person p2 = p1;<span class="comment">//编译器自动提供拷贝构造函数，浅拷贝</span></span><br><span class="line">    </span><br><span class="line">	cout &lt;&lt; <span class="string">&quot;p2年龄的地址：&quot;</span> &lt;&lt; (<span class="type">int</span>)&amp;p2.age </span><br><span class="line">        &lt;&lt; <span class="string">&quot;  p2的年龄：&quot;</span> &lt;&lt; p2.age &lt;</span><br><span class="line">        &lt; <span class="string">&quot;   p2身高的地址：&quot;</span> &lt;&lt; (<span class="type">int</span>)p2.height </span><br><span class="line">        &lt;&lt; <span class="string">&quot;  p2的身高：&quot;</span> &lt;&lt; *p2.height &lt;&lt; endl;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="built_in">test01</span>();</span><br><span class="line">	<span class="built_in">system</span>(<span class="string">&quot;pause&quot;</span>);</span><br><span class="line">	<span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div></li>
</ul>
<h2 id="初始化列表"><a href="#初始化列表" class="headerlink" title="初始化列表"></a>初始化列表</h2><hr>
<p>C++中的初始化列表是用来初始化类成员变量的一种常用方式。</p>
<p>它的语法形式是在构造函数的参数列表后面使用冒号分隔符，然后列出成员变量和它们的初始值，<strong>如下所示：</strong></p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MyClass</span> </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">MyClass</span>(<span class="type">int</span> a, <span class="type">int</span> b) : <span class="built_in">var1</span>(a), <span class="built_in">var2</span>(b) </span><br><span class="line">    &#123;</span><br><span class="line">    &#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">int</span> var1;</span><br><span class="line">    <span class="type">int</span> var2;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></div>

<p>在上面的代码中，构造函数的参数列表是 <code>(int a, int b)</code>，冒号后面的初始化列表是 <code>: var1(a), var2(b)</code>。它的作用是将构造函数的参数 <code>a</code> 和 <code>b</code> 分别赋值给成员变量 <code>var1</code> 和 <code>var2</code>。</p>
<ul>
<li>使用初始化列表的好处是可以提高代码的执行效率，因为它避免了在构造函数内部进行赋值操作的额外开销。</li>
<li>初始化列表还可以用来初始化常量成员变量和引用类型成员变量，因为这些成员变量只能在初始化列表中被赋值初始化。</li>
<li>需要注意的是，初始化列表中的成员变量顺序应该和类定义中的顺序一致，否则会导致编译错误。</li>
<li>此外，如果某个成员变量没有在初始化列表中被赋值，它将被默认初始化，这通常是不可预知的，因此建议在初始化列表中为所有成员变量赋初始值。</li>
</ul>
<h2 id="静态成员"><a href="#静态成员" class="headerlink" title="静态成员"></a>静态成员</h2><hr>
<ul>
<li><p>静态成员变量：</p>
<p>静态成员变量：在类中定义的静态变量，它属于整个类而不是某个对象，所有对象共享同一份数据。它可以通过类名或对象名访问，但必须在类外初始化。</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Test</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="type">static</span> <span class="type">int</span> s_var; <span class="comment">// 声明一个静态成员变量 </span></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">show</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; s_var &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">static</span> <span class="type">int</span> d;<span class="comment">//私有静态成员变量</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> Test::s_var = <span class="number">30</span>; <span class="comment">// 必须再在类外初始化</span></span><br><span class="line"><span class="type">int</span> Test::d = <span class="number">40</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Test t1;</span><br><span class="line">    cout &lt;&lt; t1.s_var &lt;&lt; endl;<span class="comment">//通过对象访问</span></span><br><span class="line">    cout &lt;&lt; Test::s_var &lt;&lt; endl;<span class="comment">//通过类名作用域访问</span></span><br><span class="line">    <span class="comment">//cout &lt;&lt; Test::d &lt;&lt; endl;//私有静态成员变量类外无法访问</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;	</span><br></pre></td></tr></table></figure></div>
</li>
<li><p>静态成员函数</p>
<ul>
<li>静态成员函数不依赖于任何类对象，因此可以在没有类对象的情况下被调用。</li>
<li>静态成员函数只能访问静态成员变量和静态成员函数。因为非静态成员变量和函数是依赖于类对象的，因此在没有类对象的情况下无法访问。</li>
<li>静态成员函数可以直接通过类名来调用，也可以通过对象来调用。</li>
<li>静态成员函数也有访问权限，<code>private</code>作用域下的无法访问</li>
</ul>
<p><strong>示例：</strong></p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MyClass</span> </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">int</span> myVar;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="type">static</span> <span class="type">int</span> myStaticVar;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">MyClass</span>(<span class="type">int</span> var) </span><br><span class="line">    &#123;</span><br><span class="line">        myVar = var;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">myFunction</span><span class="params">()</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;myVar: &quot;</span> &lt;&lt; myVar &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">static</span> <span class="type">void</span> <span class="title">myStaticFunction</span><span class="params">()</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;myStaticVar: &quot;</span> &lt;&lt; myStaticVar &lt;&lt; endl;</span><br><span class="line">        <span class="comment">// cout &lt;&lt; &quot;myVar: &quot; &lt;&lt; myVar &lt;&lt; endl;// 错误：无法访问非静态成员变量myVar</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> MyClass::myStaticVar = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="function">MyClass <span class="title">obj1</span><span class="params">(<span class="number">10</span>)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// obj1.myStaticFunction();// 错误：不能通过对象调用静态成员函数</span></span><br><span class="line"></span><br><span class="line">    MyClass::<span class="built_in">myStaticFunction</span>();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div></li>
</ul>
<h2 id="this指针"><a href="#this指针" class="headerlink" title="this指针"></a><code>this</code>指针</h2><hr>
<ul>
<li><p>在C++中，this指针是一个特殊的指针，它指向当前对象的地址。this指针在成员函数内部使用，它可以访问当前对象的成员变量和成员函数。</p>
<p>使用this指针可以避免成员变量和局部变量的名字冲突，并且可以方便地在成员函数中访问对象的成员变量。</p>
<p><strong>使用示例：</strong></p>
<p>在成员函数内部，可以使用this指针来访问对象的成员变量和成员函数。</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">MyClass</span> </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">setValue</span><span class="params">(<span class="type">int</span> value)</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>-&gt;value = value; <span class="comment">// 使用this指针访问成员变量</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">getValue</span><span class="params">()</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>-&gt;value; <span class="comment">// 使用this指针访问成员变量</span></span><br><span class="line">    &#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">int</span> value;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    MyClass obj;</span><br><span class="line">    obj.<span class="built_in">setValue</span>(<span class="number">10</span>);</span><br><span class="line">    cout &lt;&lt; obj.<span class="built_in">getValue</span>() &lt;&lt; endl;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>在上面的示例中，我们创建了一个<code>MyClass</code>对象<code>obj</code>，并调用了<code>setValue</code>函数来设置<code>obj</code>的<code>value</code>成员变量的值为<code>10</code>，然后调用<code>getValue</code>函数来获取<code>obj</code>的v<code>alue</code>成员变量的值并输出。</p>
<p><strong>错误示例：</strong></p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">MyClass</span> </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">setValue</span><span class="params">(<span class="type">int</span> value)</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        value = value; <span class="comment">// 错误的使用方式，不使用this指针</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">getValue</span><span class="params">()</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> value; <span class="comment">// 错误的使用方式，不使用this指针</span></span><br><span class="line">    &#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">int</span> value;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    MyClass obj;</span><br><span class="line">    obj.<span class="built_in">setValue</span>(<span class="number">10</span>);</span><br><span class="line">    cout &lt;&lt; obj.<span class="built_in">getValue</span>() &lt;&lt; endl;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>在上面的错误示例中，我们在<code>setValue</code>和g<code>etValue</code>函数中没有使用this指针来访问成员变量，而是直接使用了函数参数<code>value</code>和成员变量<code>value</code>。这样会导致函数参数<code>value</code>和成员变量<code>value</code>混淆，无法正确访问对象的成员变量。</p>
</li>
</ul>
<h2 id="const修饰的成员函数和对象"><a href="#const修饰的成员函数和对象" class="headerlink" title="const修饰的成员函数和对象"></a><code>const</code>修饰的成员函数和对象</h2><hr>
<p>&#x3D;&#x3D;<code>this</code>指针的本质：&#x3D;&#x3D;<code>className * const this</code>，<code>this</code>指针的指向不可以修改</p>
<ul>
<li><code>const</code>修饰的成员函数：<ul>
<li>函数函数名后面加上<code>const</code>，称这个成员函数为常函数</li>
<li>常成员函数内不可以修改成员属性</li>
<li>成员属性声明时加上<code>mutable</code>关键字后在常成员函数中可以修改</li>
</ul>
</li>
</ul>
<p><strong>示例：</strong></p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MyClass</span> </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">setValue</span><span class="params">(<span class="type">int</span> value)</span> </span></span><br><span class="line"><span class="function">    </span>&#123; </span><br><span class="line">        m_value = value; </span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">getValue</span><span class="params">()</span> <span class="type">const</span> </span></span><br><span class="line"><span class="function">    </span>&#123; </span><br><span class="line">        <span class="keyword">return</span> m_value; </span><br><span class="line">    &#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="keyword">mutable</span> <span class="type">int</span> m_value; <span class="comment">// mutable 修饰的变量</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="type">const</span> MyClass obj; <span class="comment">// 声明一个 const 对象</span></span><br><span class="line">    <span class="type">int</span> value = obj.<span class="built_in">getValue</span>(); <span class="comment">// 可以调用 const 成员函数</span></span><br><span class="line">    <span class="comment">// obj.setValue(10); // 错误：不能调用非 const 成员函数</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>相当与将<code>className * const this</code>改成<code>const className * const this</code>，这样<code>this</code>指针的指向和<code>this</code>指针指向的值都无法修改了</p>
<ul>
<li><p><strong><code>const</code>修饰的对象：</strong></p>
<ul>
<li>声明对象前加上<code>const</code>关键字，则该对象定义为常对象</li>
<li>常对象只能调用常成员函数</li>
</ul>
<p>示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MyClass</span> </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">setValue</span><span class="params">(<span class="type">int</span> value)</span> </span></span><br><span class="line"><span class="function">    </span>&#123; </span><br><span class="line">        m_value = value; </span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">getValue</span><span class="params">()</span> <span class="type">const</span> </span></span><br><span class="line"><span class="function">    </span>&#123; </span><br><span class="line">        <span class="keyword">return</span> m_value; </span><br><span class="line">    &#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">int</span> m_value;</span><br><span class="line">    <span class="keyword">mutable</span> <span class="type">int</span> n_value; <span class="comment">// mutable 修饰的变量</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="type">const</span> MyClass obj; <span class="comment">// 声明一个常对象</span></span><br><span class="line">    <span class="type">int</span> value = obj.<span class="built_in">getValue</span>(); <span class="comment">// 可以调用常成员函数</span></span><br><span class="line">    <span class="comment">// obj.setValue(10); // 错误：常对象不能调用非常成员函数</span></span><br><span class="line">    obj.n_value = <span class="number">100</span>;<span class="comment">//常对象可以修改 mutable 修饰的变量</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div></li>
</ul>
<h2 id="成员函数重载"><a href="#成员函数重载" class="headerlink" title="成员函数重载"></a>成员函数重载</h2><hr>
<h3 id="普通重载"><a href="#普通重载" class="headerlink" title="普通重载"></a>普通重载</h3><p>当一个类中有多个成员函数名称相同但参数列表不同的函数时，就会发生类成员函数的重载。</p>
<p>C++中，类成员函数重载的规则和普通函数重载的规则是一样的，必须满足函数名称相同但参数列表不同的条件。</p>
<p>下面是一个类成员函数重载的<strong>示例</strong>：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line">cppCopy codeclass MyClass </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">foo</span><span class="params">(<span class="type">int</span> x)</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;foo(int x) called with &quot;</span> &lt;&lt; x &lt;&lt; std::endl;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">foo</span><span class="params">(<span class="type">double</span> x)</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;foo(double x) called with &quot;</span> &lt;&lt; x &lt;&lt; std::endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    MyClass obj;</span><br><span class="line">    obj.<span class="built_in">foo</span>(<span class="number">1</span>);      <span class="comment">// 调用foo(int x)</span></span><br><span class="line">    obj.<span class="built_in">foo</span>(<span class="number">1.234</span>);  <span class="comment">// 调用foo(double x)</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<h3 id="常量性重载"><a href="#常量性重载" class="headerlink" title="常量性重载"></a>常量性重载</h3><hr>
<p>常量性重载是指一个成员函数有常量和非常量两种版本，常量版本的成员函数在其函数名后面加上 <code>const</code> 关键字，用于处理常量对象。</p>
<p>常量性重载函数可以让<code>const</code>对象调用特定的函数，并且不能修改对象的状态。</p>
<p>下面是一个常量性重载的<strong>示例</strong>：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line">cppCopy codeclass MyClass </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">foo</span><span class="params">()</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;foo() called&quot;</span> &lt;&lt; std::endl;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">foo</span><span class="params">()</span> <span class="type">const</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;const foo() called&quot;</span> &lt;&lt; std::endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    MyClass obj1;</span><br><span class="line">    obj1.<span class="built_in">foo</span>();       <span class="comment">// 调用 foo()</span></span><br><span class="line">    <span class="type">const</span> MyClass obj2;</span><br><span class="line">    obj2.<span class="built_in">foo</span>();       <span class="comment">// 调用 const foo()</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>注意，常量性重载的成员函数需要保证其不会修改对象的状态，所以在常量成员函数中只能调用其他常量成员函数或常量数据成员，不能调用非常量成员函数或修改数据成员。</p>
<p>常量性重载函数中，常量类对象可以调用常量版本的成员函数，而非常量类对象则只能调用非常量版本的成员函数。</p>
<p>举个例子，假设我们有一个名为 <code>MyClass</code> 的类，它有两个版本的成员函数 <code>foo()</code>：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line">cppCopy codeclass MyClass </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">foo</span><span class="params">()</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;foo() called&quot;</span> &lt;&lt; std::endl;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">foo</span><span class="params">()</span> <span class="type">const</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;const foo() called&quot;</span> &lt;&lt; std::endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></div>

<p>现在假设我们有一个常量类对象 <code>const MyClass obj1;</code> 和一个非常量类对象 <code>MyClass obj2;</code>，它们都可以调用 <code>foo()</code> 成员函数：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line">cppCopy codeobj1.<span class="built_in">foo</span>(); <span class="comment">// 调用 const foo()</span></span><br><span class="line">obj2.<span class="built_in">foo</span>(); <span class="comment">// 调用 foo()</span></span><br></pre></td></tr></table></figure></div>

<p>在普通函数的函数名后面加上 <code>const</code> 关键字是没有意义的。因为 <code>const</code> 关键字用于修饰类的成员函数，它表示该成员函数不会修改类的成员变量。而普通函数没有成员变量，所以 <code>const</code> 关键字对它们没有意义。</p>
<p>在普通函数中，如果要实现类似于常量性重载的效果，可以使用函数重载。例如，可以编写一个非常量版本和一个常量版本的同名函数，分别用于处理非常量对象和常量对象。</p>
<p><strong>例如：</strong></p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">cppCopy codevoid <span class="title">foo</span><span class="params">(<span class="type">int</span> x)</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;foo(int x) called with &quot;</span> &lt;&lt; x &lt;&lt; std::endl;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">foo</span><span class="params">(<span class="type">const</span> <span class="type">int</span> x)</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;const foo(const int x) called with &quot;</span> &lt;&lt; x &lt;&lt; std::endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>这样，可以让非常量对象调用非常量版本的函数，常量对象调用常量版本的函数。</p>
<p>在普通函数的函数名后面加上 <code>const</code> 是语法上不正确的，编译器在编译时会报错。</p>
<p>这是因为 <code>const</code> 关键字只能用于类的成员函数中，表示该成员函数不会修改类的成员变量。普通函数没有成员变量，所以在普通函数名后面加上 <code>const</code> 关键字是没有意义的，也是非法的。</p>
<p>以下是一个示例，试图在普通函数中添加 <code>const</code> 关键字：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">cppCopy codevoid <span class="title">foo</span><span class="params">(<span class="type">const</span> <span class="type">int</span> x)</span> <span class="type">const</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>编译器会报错，因为它不知道如何处理这个语句，它既不是一个成员函数，也不是一个正确的普通函数。</p>
<h2 id="友元"><a href="#友元" class="headerlink" title="友元"></a>友元</h2><hr>
<ul>
<li>友元是C++中的一种特殊关系，它允许一个类的非成员函数或另一个类的成员函数访问该类的私有成员变量和函数。</li>
</ul>
<p><strong>语法：</strong></p>
<ul>
<li>友元函数的声明：在类的内部，使用关键字<code>friend</code>声明一个函数为友元函数，如下所示：</li>
</ul>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MyClass</span> </span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">//friend void friendFunc();//也可以在这里声明</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">friend</span> <span class="type">void</span> <span class="title">friendFunc</span><span class="params">()</span></span>;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">int</span> x;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></div>

<ul>
<li>友元类的声明：在类的内部，使用关键字<code>friend</code>声明一个类为友元类，如下所示：</li>
</ul>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MyClass</span> </span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">//friend class FriendClass;//也可以在这里声明</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="keyword">friend</span> <span class="keyword">class</span> <span class="title class_">FriendClass</span>;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">int</span> x;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></div>

<h3 id="全局函数做友元"><a href="#全局函数做友元" class="headerlink" title="全局函数做友元"></a>全局函数做友元</h3><hr>
<p><strong>示例：</strong></p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MyClass</span> </span><br><span class="line">&#123;</span><br><span class="line">    <span class="function"><span class="keyword">friend</span> <span class="type">void</span> <span class="title">setNum</span><span class="params">(MyClass&amp; obj, <span class="type">int</span> n)</span></span>;<span class="comment">//将全局函数 setNum 声明为友元</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">int</span> num;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">MyClass</span>() : <span class="built_in">num</span>(<span class="number">0</span>)</span><br><span class="line">    &#123;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">setNum</span><span class="params">(MyClass&amp; obj, <span class="type">int</span> n)</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    obj.num = n;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    MyClass obj;</span><br><span class="line">    <span class="built_in">setNum</span>(obj, <span class="number">10</span>);</span><br><span class="line">    cout &lt;&lt; obj.num &lt;&lt; endl;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>在上面的代码中，我们定义了一个名为<code>MyClass</code>的类，其中包含一个私有变量<code>num</code>。</p>
<p>然后我们声明了一个全局函数<code>setNum</code>作为<code>MyClass</code>的友元函数，这样<code>setNum</code>就可以访问<code>MyClass</code>的私有变量<code>num</code>了。</p>
<p>在主函数中我们创建了一个<code>MyClass</code>对象obj，然后调用<code>setNum</code>函数并将<code>obj</code>和<code>10</code>作为参数传递进去，最后输出<code>obj</code>的<code>num</code>值，即得到了10。</p>
<h3 id="类做友元"><a href="#类做友元" class="headerlink" title="类做友元"></a>类做友元</h3><hr>
<p><strong>示例：</strong>其中类<code>B</code>被声明为类A的友元，可以访问类<code>A</code>的私有成员变量<code>x</code></p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">A</span> </span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">friend</span> <span class="keyword">class</span> <span class="title class_">B</span>; <span class="comment">// 类B是类A的友元</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">A</span>(<span class="type">int</span> value) : <span class="built_in">x</span>(value) </span><br><span class="line">    &#123;</span><br><span class="line">    &#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">int</span> x;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">B</span> </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">printX</span><span class="params">(A &amp; a)</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;x is &quot;</span> &lt;&lt; a.x &lt;&lt; std::endl; <span class="comment">// 可以访问类A的私有成员变量x</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="function">A <span class="title">a</span><span class="params">(<span class="number">10</span>)</span></span>;</span><br><span class="line">    B b;</span><br><span class="line">    b.<span class="built_in">printX</span>(a); <span class="comment">// 输出：x is 10</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<h3 id="成员函数做友元"><a href="#成员函数做友元" class="headerlink" title="成员函数做友元"></a>成员函数做友元</h3><hr>
<p><strong>示例：：</strong>其中类<code>A</code>的成员函数<code>funcA()</code>是类<code>B</code>的友元函数，可以访问类<code>B</code>的私有成员变量</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">B</span>; <span class="comment">// 前置声明</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">A</span> </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">funcA</span><span class="params">(B&amp; objB)</span></span>; <span class="comment">// 声明 void funcA 函数，用于访问 B 中的私有成员</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">B</span> </span><br><span class="line">&#123;</span><br><span class="line">    <span class="function"><span class="keyword">friend</span> <span class="type">void</span> <span class="title">A::funcA</span><span class="params">(B&amp; objB)</span></span>; <span class="comment">// 声明A的成员函数为友元函数</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">B</span>(<span class="type">int</span> x): <span class="built_in">x</span>(x) </span><br><span class="line">    &#123;</span><br><span class="line">    &#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">int</span> x;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">A::funcA</span><span class="params">(B&amp; objB)</span> <span class="comment">//类外定义void funcA 函数</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;B的私有成员变量x的值为：&quot;</span> &lt;&lt; objB.x &lt;&lt; endl;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="function">B <span class="title">objB</span><span class="params">(<span class="number">10</span>)</span></span>;</span><br><span class="line">    A objA;</span><br><span class="line">    objA.<span class="built_in">funcA</span>(objB); <span class="comment">// 调用A的成员函数，输出B的私有成员变量x的值</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p><strong>输出：</strong></p>
<div class="highlight-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">B的私有成员变量x的值为：10</span><br></pre></td></tr></table></figure></div>

<h1 id="运算符重载"><a href="#运算符重载" class="headerlink" title="运算符重载"></a>运算符重载</h1><p>概念：对已有运算符进行定义，赋予其另一种功能，以适应不同数据类型</p>
<h2 id="四则运算符重载"><a href="#四则运算符重载" class="headerlink" title="四则运算符重载"></a>四则运算符重载</h2><hr>
<p><code>operator+</code></p>
<p><code>operator-</code></p>
<p><code>operator*</code></p>
<p><code>operator/</code></p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Vector</span> </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="type">float</span> x, y, z;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 构造函数</span></span><br><span class="line">    <span class="built_in">Vector</span>(<span class="type">float</span> x = <span class="number">0</span>, <span class="type">float</span> y = <span class="number">0</span>, <span class="type">float</span> z = <span class="number">0</span>) </span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">this</span>-&gt;x = x;</span><br><span class="line">        <span class="keyword">this</span>-&gt;y = y;</span><br><span class="line">        <span class="keyword">this</span>-&gt;z = z;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 重载&quot;+&quot;运算符</span></span><br><span class="line">    Vector <span class="keyword">operator</span>+(<span class="type">const</span> Vector&amp; v) <span class="type">const</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">Vector</span>(x + v.x, y + v.y, z + v.z);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 重载&quot;-&quot;运算符</span></span><br><span class="line">    Vector <span class="keyword">operator</span>-(<span class="type">const</span> Vector&amp; v) <span class="type">const</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">Vector</span>(x - v.x, y - v.y, z - v.z);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 重载&quot;&lt;&lt;&quot;运算符</span></span><br><span class="line">    <span class="keyword">friend</span> ostream&amp; <span class="keyword">operator</span>&lt;&lt;(ostream&amp; os, <span class="type">const</span> Vector&amp; v) </span><br><span class="line">    &#123;</span><br><span class="line">        os &lt;&lt; <span class="string">&quot;(&quot;</span> &lt;&lt; v.x &lt;&lt; <span class="string">&quot;, &quot;</span> &lt;&lt; v.y &lt;&lt; <span class="string">&quot;, &quot;</span> &lt;&lt; v.z &lt;&lt; <span class="string">&quot;)&quot;</span>;</span><br><span class="line">        <span class="keyword">return</span> os;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">//全局 重载&quot;*&quot;运算符</span></span><br><span class="line">Vector <span class="keyword">operator</span>*(<span class="type">const</span> Vector&amp; v1, <span class="type">const</span> Vector&amp; v2)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">Vector</span>(v1.x * v2.x, v1.y * v2.y, v1.z * v2.z);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//全局 重载&quot;/&quot;运算符</span></span><br><span class="line">Vector <span class="keyword">operator</span>/(<span class="type">const</span> Vector&amp; v1, <span class="type">const</span> Vector&amp; v2)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 检查除数是否为0</span></span><br><span class="line">    <span class="keyword">if</span> (scalar == <span class="number">0</span>) </span><br><span class="line">    &#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;Error: Division by zero!&quot;</span> &lt;&lt; endl;</span><br><span class="line">        <span class="keyword">return</span> v;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">Vector</span>(v1.x / v2.x, v1.y / v2.y, v1.z / v2.z);    <span class="comment">// 执行除法运算</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="function">Vector <span class="title">v1</span><span class="params">(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>)</span></span>;</span><br><span class="line">    <span class="function">Vector <span class="title">v2</span><span class="params">(<span class="number">4</span>, <span class="number">5</span>, <span class="number">6</span>)</span></span>;</span><br><span class="line">    Vector v3 = v1 + v2;</span><br><span class="line">    Vector v4 = v1 - v2;</span><br><span class="line">    Vector v5 = v1 * v2;</span><br><span class="line">    Vector v6 = v1 / v2;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;v1 = &quot;</span> &lt;&lt; v1 &lt;&lt; endl;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;v2 = &quot;</span> &lt;&lt; v2 &lt;&lt; endl;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;v1 + v2 = &quot;</span> &lt;&lt; v3 &lt;&lt; endl;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;v1 - v2 = &quot;</span> &lt;&lt; v4 &lt;&lt; endl;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;v1 * v2 = &quot;</span> &lt;&lt; v5 &lt;&lt; endl;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;v1 / v2 = &quot;</span> &lt;&lt; v6 &lt;&lt; endl;  </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<h2 id="左移（"><a href="#左移（" class="headerlink" title="左移（&lt;&lt;）运算符重载"></a>左移（<code>&lt;&lt;</code>）运算符重载</h2><hr>
<p>左移运算符重载不可以定义在类中作为类的成员函数，只能定义为全局函数，如果定义在类中，需要加上<code>friend</code>关键字，但默认是全局函数</p>
<p><code>cout</code>是一个<code>ostream</code>对象，重载时需要指定参数类型，并返回这个流对象，使得<code>&lt;&lt;</code>可以链式调用。</p>
<p><strong>代码示例：</strong></p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Vector</span> </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="type">float</span> x, y, z;</span><br><span class="line">	<span class="keyword">friend</span> ostream&amp; <span class="keyword">operator</span>&lt;&lt;(ostream&amp; os, <span class="type">const</span> Vector&amp; v) </span><br><span class="line">	&#123;</span><br><span class="line">		os &lt;&lt; <span class="string">&quot;(&quot;</span> &lt;&lt; v.x &lt;&lt; <span class="string">&quot;, &quot;</span> &lt;&lt; v.y &lt;&lt; <span class="string">&quot;, &quot;</span> &lt;&lt; v.z &lt;&lt; <span class="string">&quot;)&quot;</span>;</span><br><span class="line">    	<span class="keyword">return</span> os;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>或者</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Vector</span> </span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">friend</span> ostream&amp; <span class="keyword">operator</span>&lt;&lt;(ostream&amp; os, <span class="type">const</span> Vector&amp; v) </span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="type">float</span> x, y, z;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">ostream&amp; <span class="keyword">operator</span>&lt;&lt;(ostream&amp; os, <span class="type">const</span> Vector&amp; v) </span><br><span class="line">&#123;</span><br><span class="line">	os &lt;&lt; <span class="string">&quot;(&quot;</span> &lt;&lt; v.x &lt;&lt; <span class="string">&quot;, &quot;</span> &lt;&lt; v.y &lt;&lt; <span class="string">&quot;, &quot;</span> &lt;&lt; v.z &lt;&lt; <span class="string">&quot;)&quot;</span>;</span><br><span class="line">    <span class="keyword">return</span> os;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<h2 id="自增（-）运算符重载"><a href="#自增（-）运算符重载" class="headerlink" title="自增（++）运算符重载"></a>自增（<code>++</code>）运算符重载</h2><hr>
<p>前置++运算符重载，要返回引用，同样对于前置<code>--</code>运算符也一样</p>
<p><strong>重载前置自增运算符（++）：</strong></p>
<div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Counter</span> </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">int</span> count;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Counter</span>(<span class="type">int</span> initialCount = <span class="number">0</span>) : <span class="built_in">count</span>(initialCount) &#123;&#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 重载前置自增运算符</span></span><br><span class="line">    Counter&amp; <span class="keyword">operator</span>++() </span><br><span class="line">    &#123;</span><br><span class="line">        count++;</span><br><span class="line">        <span class="keyword">return</span> *<span class="keyword">this</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">getCount</span><span class="params">()</span> <span class="type">const</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> count;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="function">Counter <span class="title">c</span><span class="params">(<span class="number">5</span>)</span></span>;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;Initial count: &quot;</span> &lt;&lt; c.<span class="built_in">getCount</span>() &lt;&lt; endl;</span><br><span class="line"></span><br><span class="line">    ++c;  <span class="comment">// 使用前置自增运算符</span></span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;Count after increment: &quot;</span> &lt;&lt; c.<span class="built_in">getCount</span>() &lt;&lt; endl;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>后置++运算符重载，要返回值，同样对于前置<code>--</code>运算符也一样</p>
<p><strong>重载后置自增运算符（++）：</strong></p>
<div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Counter</span> </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">int</span> count;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Counter</span>(<span class="type">int</span> initialCount = <span class="number">0</span>) : <span class="built_in">count</span>(initialCount) &#123;&#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 重载后置自增运算符</span></span><br><span class="line">    Counter <span class="keyword">operator</span>++(<span class="type">int</span>) </span><br><span class="line">    &#123;</span><br><span class="line">        <span class="function">Counter <span class="title">temp</span><span class="params">(count)</span></span>;  <span class="comment">// 创建临时对象保存当前值</span></span><br><span class="line">        count++;</span><br><span class="line">        <span class="keyword">return</span> temp;  <span class="comment">// 返回保存的临时对象</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">getCount</span><span class="params">()</span> <span class="type">const</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> count;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="function">Counter <span class="title">c</span><span class="params">(<span class="number">5</span>)</span></span>;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;Initial count: &quot;</span> &lt;&lt; c.<span class="built_in">getCount</span>() &lt;&lt; endl;</span><br><span class="line"></span><br><span class="line">    Counter d = c++;  <span class="comment">// 使用后置自增运算符</span></span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;Count after increment: &quot;</span> &lt;&lt; c.<span class="built_in">getCount</span>() &lt;&lt; endl;</span><br><span class="line">    cout &lt;&lt; <span class="string">&quot;Value before increment: &quot;</span> &lt;&lt; d.<span class="built_in">getCount</span>() &lt;&lt; endl;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p><strong>重载前置自增运算符时的注意事项：</strong></p>
<ul>
<li>前置自增运算符的重载函数返回引用，以便允许连续的自增操作。</li>
<li>在重载前置自增运算符时，首先对对象进行自增操作，然后返回自增后的对象。</li>
</ul>
<p><strong>重载后置自增运算符时的注意事项：</strong></p>
<ul>
<li>后置自增运算符的重载函数需要使用一个额外的整型参数（<code>int</code>）来区分后置自增运算符重载函数和前置自增运算符重载函数。</li>
<li>在重载后置自增运算符时，首先创建一个临时对象来保存自增前的对象值，然后对对象进行自增操作，最后返回保存的临时对象。</li>
</ul>
<p>在使用重载后置自增运算符时，需要注意后置自增运算符返回的是自增之前的值，而不是自增之后的值。</p>
<p>因此，在示例代码中，将后置自增运算符的返回值存储在一个新的<code>Counter对</code>象中（变量<code>d</code>），以便打印出自增之前的值。</p>
<h2 id="赋值（-）运算符重载（深拷贝）"><a href="#赋值（-）运算符重载（深拷贝）" class="headerlink" title="赋值（=）运算符重载（深拷贝）"></a>赋值（<code>=</code>）运算符重载（深拷贝）</h2><hr>
<p>当重载赋值运算符<code>=</code>时，要确保进行的是深拷贝，以防止内存重复释放。</p>
<p><strong>代码示例：</strong></p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">MyClass</span> </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="type">int</span>* data; <span class="comment">// 指针成员，动态分配的数据</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">MyClass</span>(<span class="type">int</span> value) </span><br><span class="line">    &#123;</span><br><span class="line">        data = <span class="keyword">new</span> <span class="type">int</span>;</span><br><span class="line">        *data = value;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 拷贝构造函数</span></span><br><span class="line">    <span class="built_in">MyClass</span>(<span class="type">const</span> MyClass&amp; other)</span><br><span class="line">    &#123;</span><br><span class="line">        data = <span class="keyword">new</span> <span class="type">int</span>;</span><br><span class="line">        *data = *(other.data);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 赋值运算符重载</span></span><br><span class="line">    MyClass&amp; <span class="keyword">operator</span>=(<span class="type">const</span> MyClass&amp; other) </span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">// 检查自我赋值</span></span><br><span class="line">        <span class="keyword">if</span> (<span class="keyword">this</span> == &amp;other)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">return</span> *<span class="keyword">this</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 执行深拷贝</span></span><br><span class="line">        <span class="keyword">delete</span> data; <span class="comment">// 释放原有内存</span></span><br><span class="line">        data = <span class="keyword">new</span> <span class="type">int</span>;</span><br><span class="line">        *data = *(other.data);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> *<span class="keyword">this</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    ~<span class="built_in">MyClass</span>() </span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">delete</span> data;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="type">int</span> <span class="title">getData</span><span class="params">()</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> *data;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="function">MyClass <span class="title">obj1</span><span class="params">(<span class="number">5</span>)</span></span>;</span><br><span class="line">    <span class="function">MyClass <span class="title">obj2</span><span class="params">(<span class="number">10</span>)</span></span>;</span><br><span class="line"></span><br><span class="line">    obj2 = obj1; <span class="comment">// 使用赋值运算符重载进行赋值</span></span><br><span class="line"></span><br><span class="line">    std::cout &lt;&lt; obj1.<span class="built_in">getData</span>() &lt;&lt; std::endl; <span class="comment">// 输出：5</span></span><br><span class="line">    std::cout &lt;&lt; obj2.<span class="built_in">getData</span>() &lt;&lt; std::endl; <span class="comment">// 输出：5</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>在这个示例中，<code>MyClass</code>类包含一个动态分配的整型数据<code>data</code>。</p>
<p>在拷贝构造函数中，我们为<code>data</code>创建新的内存，并将其值设置为被复制对象的值。</p>
<p>在赋值运算符重载中，首先检查是否是自我赋值，然后执行深拷贝。先删除原有的<code>data</code>内存，并为其分配新的内存，然后将其值设置为被赋值对象的值。</p>
<p>通过这样的实现，可以确保在赋值过程中进行深拷贝，从而避免内存重复释放的问题。</p>
<h2 id="其他运算符重载的说明"><a href="#其他运算符重载的说明" class="headerlink" title="其他运算符重载的说明"></a>其他运算符重载的说明</h2><hr>
<p>以下是C++中常见的运算符及其简单说明和是否能重载：</p>
<table>
<thead>
<tr>
<th>运算符</th>
<th>说明</th>
<th>是否能重载（只标识不能重载的）</th>
</tr>
</thead>
<tbody><tr>
<td><code>+</code></td>
<td>加法运算符，用于执行两个操作数的相加操作。</td>
<td></td>
</tr>
<tr>
<td><code>-</code></td>
<td>减法运算符，用于执行两个操作数的相减操作。</td>
<td></td>
</tr>
<tr>
<td><code>*</code></td>
<td>乘法运算符，用于执行两个操作数的相乘操作。</td>
<td></td>
</tr>
<tr>
<td><code>/</code></td>
<td>除法运算符，用于执行两个操作数的相除操作。</td>
<td></td>
</tr>
<tr>
<td><code>%</code></td>
<td>取模运算符，返回两个操作数相除后的余数。</td>
<td></td>
</tr>
<tr>
<td><code>++</code></td>
<td>自增运算符，用于将操作数的值增加1。</td>
<td></td>
</tr>
<tr>
<td><code>--</code></td>
<td>自减运算符，用于将操作数的值减少1。</td>
<td></td>
</tr>
<tr>
<td><code>=</code></td>
<td>赋值运算符，用于将右侧操作数的值赋给左侧操作数。</td>
<td></td>
</tr>
<tr>
<td><code>+=</code></td>
<td>加法赋值运算符，将右侧操作数的值加到左侧操作数上。</td>
<td></td>
</tr>
<tr>
<td><code>-=</code></td>
<td>减法赋值运算符，将右侧操作数的值从左侧操作数中减去。</td>
<td></td>
</tr>
<tr>
<td><code>*=</code></td>
<td>乘法赋值运算符，将右侧操作数的值乘到左侧操作数上。</td>
<td></td>
</tr>
<tr>
<td><code>/=</code></td>
<td>除法赋值运算符，将左侧操作数除以右侧操作数的值。</td>
<td></td>
</tr>
<tr>
<td><code>%=</code></td>
<td>取模赋值运算符，将左侧操作数取模右侧操作数的值。</td>
<td></td>
</tr>
<tr>
<td><code>==</code></td>
<td>相等运算符，用于比较两个操作数是否相等。</td>
<td></td>
</tr>
<tr>
<td><code>!=</code></td>
<td>不等运算符，用于比较两个操作数是否不相等。</td>
<td></td>
</tr>
<tr>
<td><code>&gt;</code></td>
<td>大于运算符，用于比较左侧操作数是否大于右侧操作数。</td>
<td></td>
</tr>
<tr>
<td><code>&lt;</code></td>
<td>小于运算符，用于比较左侧操作数是否小于右侧操作数。</td>
<td></td>
</tr>
<tr>
<td><code>&gt;=</code></td>
<td>大于等于运算符，用于比较左侧操作数是否大于或等于右侧操作数。</td>
<td></td>
</tr>
<tr>
<td><code>&lt;=</code></td>
<td>小于等于运算符，用于比较左侧操作数是否小于或等于右侧操作数。</td>
<td></td>
</tr>
<tr>
<td><code>&amp;&amp;</code></td>
<td>逻辑与运算符，用于执行逻辑与操作。</td>
<td></td>
</tr>
<tr>
<td>&#96;</td>
<td></td>
<td>&#96;</td>
</tr>
<tr>
<td><code>!</code></td>
<td>逻辑非运算符，用于执行逻辑非操作。</td>
<td></td>
</tr>
<tr>
<td><code>&amp;</code></td>
<td>按位与运算符，对操作数的每个位执行逻辑与操作。</td>
<td></td>
</tr>
<tr>
<td>&#96;</td>
<td>&#96;</td>
<td>按位或运算符，对操作数的每个位执行逻辑或操作。</td>
</tr>
<tr>
<td><code>^</code></td>
<td>按位异或运算符，对操作数的每个位执行逻辑异或操作。</td>
<td></td>
</tr>
<tr>
<td><code>~</code></td>
<td>按位取反运算符，对操作数的每个位取反。</td>
<td></td>
</tr>
<tr>
<td><code>&lt;&lt;</code></td>
<td>左移运算符，将操作数的位向左移动指定的位数。</td>
<td></td>
</tr>
<tr>
<td><code>&gt;&gt;</code></td>
<td>右移运算符，将操作数的位向右移动指定的位数。</td>
<td></td>
</tr>
<tr>
<td><code>&amp;=</code></td>
<td>按位与赋值运算符，将右侧操作数的值与左侧操作数进行按位与操作。</td>
<td></td>
</tr>
<tr>
<td>&#96;</td>
<td>&#x3D;&#96;</td>
<td>按位或赋值运算符，将右侧操作数的值与左侧操作数进行按位或操作。</td>
</tr>
<tr>
<td><code>^=</code></td>
<td>按位异或赋值运算符，将右侧操作数的值与左侧操作数进行按位异或操作。</td>
<td></td>
</tr>
<tr>
<td><code>&lt;&lt;=</code></td>
<td>左移赋值运算符，将左侧操作数的值向左移动指定的位数。</td>
<td></td>
</tr>
<tr>
<td><code>&gt;&gt;=</code></td>
<td>右移赋值运算符，将左侧操作数的值向右移动指定的位数。</td>
<td></td>
</tr>
<tr>
<td><code>sizeof</code></td>
<td>大小运算符，用于返回一个对象或数据类型的大小（以字节为单位）。</td>
<td>否</td>
</tr>
<tr>
<td><code>?:</code></td>
<td>条件运算符（三目运算符），用于根据条件选择不同的值。</td>
<td>否</td>
</tr>
<tr>
<td><code>.</code></td>
<td>成员访问运算符，用于访问结构体或类的成员。</td>
<td>否</td>
</tr>
<tr>
<td><code>-&gt;</code></td>
<td>成员指针访问运算符，用于通过指针访问结构体或类的成员。</td>
<td></td>
</tr>
<tr>
<td><code>[]</code></td>
<td>下标运算符，用于访问数组或容器中的元素。</td>
<td></td>
</tr>
<tr>
<td><code>()</code></td>
<td>函数调用运算符，用于调用函数。</td>
<td></td>
</tr>
<tr>
<td><code>new</code></td>
<td>动态分配运算符，用于在堆上分配内存空间。</td>
<td></td>
</tr>
<tr>
<td><code>delete</code></td>
<td>动态释放运算符，用于释放动态分配的内存空间。</td>
<td></td>
</tr>
<tr>
<td><code>new[]</code></td>
<td>数组动态内存分配运算符，用于在堆上分配数组内存</td>
<td></td>
</tr>
<tr>
<td><code>delete[]</code></td>
<td>数组动态内存释放运算符，用于释放先前分配的数组内存</td>
<td></td>
</tr>
<tr>
<td><code>.*</code></td>
<td>成员指针运算符，用于通过指针访问类的成员函数或成员变量。</td>
<td>否</td>
</tr>
<tr>
<td><code>-&gt;*</code></td>
<td>成员指针运算符，用于通过指针访问类的成员函数或成员变量。</td>
<td></td>
</tr>
<tr>
<td><code>::</code></td>
<td>作用域解析运算符，用于访问命名空间、类、结构体或枚举的成员。</td>
<td>否</td>
</tr>
<tr>
<td><code>,</code></td>
<td>逗号运算符，用于在表达式中分隔多个表达式，并按顺序执行</td>
<td></td>
</tr>
</tbody></table>
<ul>
<li><p>上述列表列出了绝大部分常用的运算符，但不是所有运算符的完整列表。还有其他一些运算符它们使用相对较少，这里不列出。</p>
</li>
<li><p>对于其余一些简单的运算符如：比较运算符、逻辑运算符等，请以上面四篇运算符重载的示例作为参考。</p>
</li>
<li><p>剩下的一些功能较为复杂的运算符如：<code>new</code>、<code>delete</code>、<code>,</code>、<code>-&gt;*</code>等的重载属于进阶内容，视情况而定，这里不给出示例代码。</p>
</li>
</ul>
<h1 id="继承"><a href="#继承" class="headerlink" title="继承"></a>继承</h1><h2 id="继承方式"><a href="#继承方式" class="headerlink" title="继承方式"></a>继承方式</h2><hr>
<ul>
<li><p>继承方式分为三种：公共继承（public）、保护继承（protected）、私有继承（private）</p>
</li>
<li><p>所有继承方式均不可访问父类中的私有成员</p>
</li>
<li><p>公共继承父类的子类，其父类的成员的访问属性不变。即：公共继承，父类中的公共成员在子类中仍然是公共成员，父类中的保护成员在子类中仍然是保护成员</p>
</li>
<li><p>私有继承的子类中，除了父类中的私有成员，其余所有的成员在子类中都变为私有成员。</p>
</li>
</ul>
<h2 id="继承中的对象模型"><a href="#继承中的对象模型" class="headerlink" title="继承中的对象模型"></a>继承中的对象模型</h2><hr>
<p>父类中私有成员也是被子类继承下去了，只是由编译器给隐藏后访问不到。</p>
<p>在C++中，子类会继承父类的所有非静态成员，包括私有成员。但是，对于私有成员，编译器会隐藏它们，使其在子类中不可直接访问。这是通过访问权限控制来实现的。</p>
<p>子类继承了父类的私有成员，但无法直接访问这些私有成员。私有成员在子类中仍然存在，并占据相应的内存空间，但无法通过子类对象或指针直接访问。</p>
<p>子类可以通过继承的公有和保护成员来间接访问父类的私有成员。子类可以通过调用父类的公有和保护成员函数来操作父类的私有成员。这样，子类可以间接地使用继承下来的私有成员。</p>
<p>继承使子类拥有了父类的所有非静态成员，但私有成员被编译器隐藏起来，无法直接访问。子类通过父类的公有和保护成员来间接操作和使用继承的私有成员，从而达到了私有成员在子类中不可访问的效果。</p>
<p>私有成员的存在对于子类是有影响的。子类对象会继承父类的私有成员，这些私有成员在子类对象中占据内存空间。如果子类定义了与父类私有成员名称相同的成员，那么子类的成员是不会覆盖父类的私有成员的，父类私有成员仍然存在，仍然占据内存空间。</p>
<h2 id="继承中的构造和析构顺序"><a href="#继承中的构造和析构顺序" class="headerlink" title="继承中的构造和析构顺序"></a>继承中的构造和析构顺序</h2><hr>
<p>继承中先调用父类构造函数，再调用子类构造函数，析构顺序与构造相反。</p>
<p>先创建父类对象，再创建子类对象。</p>
<h2 id="同名成员处理"><a href="#同名成员处理" class="headerlink" title="同名成员处理"></a>同名成员处理</h2><hr>
<p>访问子类同名成员直接访问即可</p>
<p>访问父类同名成员需要加作用域</p>
<p>如果子类中出现和父类同名的成员函数，子类的同名成员会隐藏掉父类中所有同名成员函数</p>
<p>如果想访问到父类中被隐藏的同名成员函数，需要加作用域</p>
<p>静态成员和非静态成员出现同名，处理方式一致</p>
<ul>
<li><p>访问子类同名成员直接访问即可</p>
</li>
<li><p>访问父类同名成员需要加作用域</p>
</li>
</ul>
<p>静态成员变量：所有对象都共享同一份数据，编译阶段分配内存，类内声明，类外初始化</p>
<p>静态成员函数：只能访问静态成员变量</p>
<h2 id="多继承"><a href="#多继承" class="headerlink" title="多继承"></a>多继承</h2><hr>
<p>语法：</p>
<p> <code>class子类 : 继承方式 父类1, 继承方式 父类2 ...</code></p>
<p>多继承可能会引发父类中有同名成员出现，需要加作用域区分</p>
<p>语法示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">A</span></span><br><span class="line">&#123;&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">B</span></span><br><span class="line">&#123;&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">C</span></span><br><span class="line">&#123;&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">D</span> : <span class="keyword">public</span> A, <span class="keyword">protected</span> C, <span class="keyword">private</span> B </span><br><span class="line">&#123;&#125;;</span><br></pre></td></tr></table></figure></div>



<h2 id="菱形继承和虚继承"><a href="#菱形继承和虚继承" class="headerlink" title="菱形继承和虚继承"></a>菱形继承和虚继承</h2><hr>
<p>菱形继承带来的主要问题是子类继承两份相同的数据，导致资源浪费以及形同继承内容无意义</p>
<p><img  
                     lazyload
                     src="/images/loading.svg"
                     data-src="data:image/png;base64,"
                     
                ></p>
<p>为了解决多继承时的命名冲突和冗余数据问题，C++ 提出了虚继承，使得在派生类中只保留一份间接基类的成员。</p>
<p>在继承之前加上关键字<code>virtual</code>变为虚继承。</p>
<p><img  
                     lazyload
                     src="/images/loading.svg"
                     data-src="data:image/png;base64,"
                     
                ></p>
<p>语法示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">A</span></span><br><span class="line">&#123;&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">B</span> : <span class="keyword">virtual</span> <span class="keyword">public</span> A	<span class="comment">//B虚继承A</span></span><br><span class="line">&#123;&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">C</span> : <span class="keyword">virtual</span> <span class="keyword">public</span> A	<span class="comment">//C虚继承A</span></span><br><span class="line">&#123;&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">D</span> : <span class="keyword">public</span> A, <span class="keyword">public</span> B</span><br><span class="line">&#123;&#125;;</span><br></pre></td></tr></table></figure></div>

<p>这样子，<code>D</code>类中就只有一份<code>A</code>类中的数据了。</p>
<p>视频讲解：<a class="link"   href="https://www.bilibili.com/video/BV1et411b73Z?p=134&vd_source=03da7de0e80f9ff60fdeca7b512ba3f5" >【黑马程序员匠心之作|C++教程从0到1入门编程,学习编程不再难】 <i class="fa-regular fa-arrow-up-right-from-square fa-sm"></i></a></p>
<h1 id="多态"><a href="#多态" class="headerlink" title="多态"></a>多态</h1><h2 id="多态的概念"><a href="#多态的概念" class="headerlink" title="多态的概念"></a>多态的概念</h2><hr>
<p>多态是C++面向对象三大特性之一。它意味着调用成员函数时，会根据调用函数的对象的类型来执行不同的函数。</p>
<p>在C++中，允许通过基类型的指针或引用去访问派生对象中的函数，并允许需要执行的函数在运行时进行延迟绑定 (Late binding)，这称之为多态。</p>
<p>多态的目的是为了&#x3D;&#x3D;接口重用&#x3D;&#x3D;。也即，不论传递过来的究竟是类的哪个对象，函数都能够通过同一个接口调用到适应各自对象的实现方法。这样可以提高代码的可扩展性和可维护性。</p>
<p>当类之间存在层次结构，并且类之间是通过继承关联时，就会用到多态。</p>
<p>C++ 多态意味着调用成员函数时，会根据调用函数的对象的类型来执行不同的函数。</p>
<p>多态分为两类</p>
<ul>
<li><p>静态多态：函数重载和运算符重载属于静态多态，复用函数名</p>
</li>
<li><p>动态多态：派生类和虚函数实现运行时多态</p>
</li>
</ul>
<p>静态多态和动态多态区别：</p>
<ul>
<li><p>静态多态的函数地址早绑定–编译阶段确定函数地址</p>
</li>
<li><p>动态多态的函数地址晚绑定–运行阶段确定函数地址</p>
</li>
</ul>
<h2 id="虚函数"><a href="#虚函数" class="headerlink" title="虚函数"></a>虚函数</h2><hr>
<p>虚函数是 C++ 中的一个重要概念，它允许基类指针能够在运行时动态地绑定到派生类对象的成员函数上。</p>
<p>简单来说，虚函数是一个在基类中声明的函数，在派生类中可以被重写。在运行时，如果使用基类指针调用虚函数，那么实际上会调用指向派生类对象的函数。</p>
<p>使用虚函数可以实现多态性，使得程序能够根据实际的对象类型来调用相应的成员函数，而不是在编译时就确定调用的函数。</p>
<p>在 C++ 中，将函数声明为虚函数的方法是在函数声明前加上关键字<code>virtual</code>。</p>
<p>派生类和虚函数实现运行时多态属于动态多态，动态多态的满足条件 ：</p>
<ul>
<li>有继承关系</li>
<li>子类重写父类的虚函数</li>
</ul>
<p>示例代码：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">animal</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">speak01</span><span class="params">()</span><span class="comment">//虚函数</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;动物在叫&quot;</span> &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="type">void</span> <span class="title">speak02</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;动物在叫&quot;</span> &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">dog</span> :<span class="keyword">public</span> animal</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="type">void</span> <span class="title">speak01</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;汪汪汪&quot;</span> &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="type">void</span> <span class="title">speak02</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;汪汪汪&quot;</span> &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">cat</span> : <span class="keyword">public</span> animal</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="type">void</span> <span class="title">speak01</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;喵喵喵&quot;</span> &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="type">void</span> <span class="title">speak02</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;喵喵喵&quot;</span> &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">//执行说话的函数</span></span><br><span class="line"><span class="comment">//地址早绑定在编译阶段确定函数地址</span></span><br><span class="line"><span class="comment">//如果想执行让猫喵喵喵，那么这个函数地址就不能提前绑定，需要在运行阶段进行绑定，地址晚绑定</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">speak</span><span class="params">(animal&amp; animal)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	animal.<span class="built_in">speak01</span>();</span><br><span class="line">	animal.<span class="built_in">speak02</span>();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">test01</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	cat cat01;</span><br><span class="line">	dog dog01;</span><br><span class="line">	<span class="built_in">speak</span>(cat01);</span><br><span class="line">	<span class="built_in">speak</span>(dog01);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="built_in">test01</span>();</span><br><span class="line"></span><br><span class="line">	<span class="built_in">system</span>(<span class="string">&quot;pause&gt;nul&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<h2 id="虚函数原理剖析"><a href="#虚函数原理剖析" class="headerlink" title="虚函数原理剖析"></a>虚函数原理剖析</h2><hr>
<p>普通函数（非静态成员函数），不属于类的对象上，下面这个类的大小为1（空类大小为1）：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">animal</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">speak</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;动物在说话&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    cout&lt;&lt; <span class="built_in">sizeof</span>(animal) &lt;&lt; endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>输出：</p>
<div class="highlight-container" data-rel="Shell"><figure class="iseeu highlight shell"><table><tr><td class="code"><pre><span class="line">1</span><br></pre></td></tr></table></figure></div>

<p>当在非静态成员函数前加上<code>virtual</code>关键字后，原来的普通函数变成一个虚函数，这时候类的大小变为4。</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">animal</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">   <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">speak</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        cout &lt;&lt; <span class="string">&quot;动物在说话&quot;</span> &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    cout&lt;&lt; <span class="built_in">sizeof</span>(animal) &lt;&lt; endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>输出：</p>
<div class="highlight-container" data-rel="Shell"><figure class="iseeu highlight shell"><table><tr><td class="code"><pre><span class="line">4</span><br></pre></td></tr></table></figure></div>

<p>实际上，加上<code>virtual</code>关键字后，普通函数变成一个虚函数，虚函数实际上是一个指针，这个指针称为虚函数指针或虚函数表指针。因而类中多了一个指针类型的成员，大小发生了变化。</p>
<p>这个指针指向一个虚函数表，表的内部记录着这个类作用域下的对应的函数的地址。当子类继承后，子类成员中同样也有了一个虚函数指针，也指向一个虚函数表，如果没有重写这个虚函数，因为继承关系表中的内容与父类是完全一样的，表中的函数地址仍然是父类中对应的函数地址。当子类重写了这个虚函数，子类中的虚函数表内部会将原先父类中对应的函数地址替换成子类中重写的函数的函数地址。</p>
<p>当父类指针或引用指向子类对象时就发生了多态，当调用虚函数时，就发生了动态的多态，由于是子类对象调用，则从子对象的虚函数表中寻找子类重写后的函数。</p>
<h2 id="纯虚函数和抽象类"><a href="#纯虚函数和抽象类" class="headerlink" title="纯虚函数和抽象类"></a>纯虚函数和抽象类</h2><hr>
<p>在多态中，通常父类中虚函数的实现是毫无意义的，主要都是调用子类重写的内容</p>
<p>因此可以将虚函数改为纯虚函数</p>
<p>纯虚函数语法：<code>virtual返回值类型 函数名(参数列表) = 0 ;</code></p>
<p>当类中有了纯虚函数，这个类也称为抽象类。</p>
<p>抽象类特点:</p>
<ul>
<li><p>无法实例化对象</p>
</li>
<li><p>抽象类的子类必须要重写父类中的纯虚函数，否则也属于抽象类</p>
</li>
</ul>
<h2 id="虚析构和纯虚析构"><a href="#虚析构和纯虚析构" class="headerlink" title="虚析构和纯虚析构"></a>虚析构和纯虚析构</h2><hr>
<p>多态使用时，如果子类中有属性开辟到堆区，那么父类指针在释放时无法调用到子类的析构代码</p>
<p>解决方式:将父类中的析构函数改为虚析构或者纯虚析构</p>
<p>虚析构和纯虚析构共性：</p>
<ul>
<li><p>可以解决父类指针释放子类对象</p>
</li>
<li><p>都需要有具体的函数实现</p>
</li>
</ul>
<p>虚析构和纯虚析构区别：</p>
<ul>
<li><p>如果是纯虚析构，该类属于抽象类，无法实例化对象。</p>
</li>
<li><p>纯虚析构需要有函数定义。</p>
</li>
</ul>
<p>示例（父类指针指向子类对象，会导致子类无法析构）：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">animal</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="built_in">animal</span>()</span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;animal的构造函数&quot;</span> &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	~<span class="built_in">animal</span>()</span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;animal的析构函数&quot;</span> &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">speak01</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">cat</span> : <span class="keyword">public</span> animal</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="built_in">cat</span>(string name_)</span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;cat的构造函数&quot;</span> &lt;&lt; endl;</span><br><span class="line">		name = <span class="keyword">new</span> <span class="built_in">string</span>(name_);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	~<span class="built_in">cat</span>()</span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;cat的析构函数&quot;</span> &lt;&lt; endl;</span><br><span class="line"></span><br><span class="line">		<span class="keyword">if</span> (name != <span class="literal">nullptr</span>)</span><br><span class="line">		&#123;</span><br><span class="line">			cout &lt;&lt; <span class="string">&quot;cat成员的内存释放&quot;</span> &lt;&lt; endl;</span><br><span class="line">			<span class="keyword">delete</span> name;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="type">void</span> <span class="title">speak01</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;喵喵喵&quot;</span> &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line">	string* name = <span class="literal">nullptr</span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">test01</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	animal* ani = <span class="keyword">new</span> <span class="built_in">cat</span>(<span class="string">&quot;tom&quot;</span>);</span><br><span class="line">	ani-&gt;<span class="built_in">speak01</span>();</span><br><span class="line">	<span class="keyword">delete</span> ani;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="built_in">test01</span>();</span><br><span class="line"></span><br><span class="line">	<span class="built_in">system</span>(<span class="string">&quot;pause&gt;nul&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>运行结果：</p>
<div class="highlight-container" data-rel="Shell"><figure class="iseeu highlight shell"><table><tr><td class="code"><pre><span class="line">animal的构造函数</span><br><span class="line">cat的构造函数</span><br><span class="line">喵喵喵</span><br><span class="line">animal的析构函数</span><br></pre></td></tr></table></figure></div>

<p>解决方法：将父类对的析构函数改成虚函数（虚析构）：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">animal</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="built_in">animal</span>()</span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;animal的构造函数&quot;</span> &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">virtual</span> ~<span class="built_in">animal</span>()</span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;animal的析构函数&quot;</span> &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">speak01</span><span class="params">()</span> </span>= <span class="number">0</span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">cat</span> : <span class="keyword">public</span> animal</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="built_in">cat</span>(string name_)</span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;cat的构造函数&quot;</span> &lt;&lt; endl;</span><br><span class="line">		name = <span class="keyword">new</span> <span class="built_in">string</span>(name_);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	~<span class="built_in">cat</span>()</span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;cat的析构函数&quot;</span> &lt;&lt; endl;</span><br><span class="line"></span><br><span class="line">		<span class="keyword">if</span> (name != <span class="literal">nullptr</span>)</span><br><span class="line">		&#123;</span><br><span class="line">			cout &lt;&lt; <span class="string">&quot;cat成员的内存释放&quot;</span> &lt;&lt; endl;</span><br><span class="line">			<span class="keyword">delete</span> name;</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="type">void</span> <span class="title">speak01</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;喵喵喵&quot;</span> &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line">	string* name = <span class="literal">nullptr</span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">test01</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	animal* ani = <span class="keyword">new</span> <span class="built_in">cat</span>(<span class="string">&quot;tom&quot;</span>);</span><br><span class="line">	ani-&gt;<span class="built_in">speak01</span>();</span><br><span class="line">	<span class="keyword">delete</span> ani;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="built_in">test01</span>();</span><br><span class="line"></span><br><span class="line">	<span class="built_in">system</span>(<span class="string">&quot;pause&gt;nul&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>运行结果：</p>
<div class="highlight-container" data-rel="Shell"><figure class="iseeu highlight shell"><table><tr><td class="code"><pre><span class="line">animal的构造函数</span><br><span class="line">cat的构造函数</span><br><span class="line">喵喵喵</span><br><span class="line">cat的析构函数</span><br><span class="line">cat成员的内存释放</span><br><span class="line">animal的析构函数</span><br></pre></td></tr></table></figure></div>

<p>如果父类的析构函数是纯虚函数（纯虚析构），则必须要有函数体的实现（定义），需要在类外定义，否则运行报错（链接阶段找不到定义）。</p>
<h2 id="override关键字"><a href="#override关键字" class="headerlink" title="override关键字"></a><code>override</code>关键字</h2><hr>
<p><code>override</code>是C++11中引入的一个新关键字，用于显式地指定某个成员函数为虚函数的重写版本。当你在派生类中重写基类的虚函数时，可以在该函数的声明后面加上<code>override</code>关键字，表示该函数是重写基类的虚函数。</p>
<p>使用<code>override</code>关键字有两个好处：</p>
<ul>
<li><p>它能够帮助你避免一些常见的错误。例如，如果你在派生类中重写基类的虚函数时，不小心将函数名或参数列表写错了，编译器会报错，提示你这不是一个有效的重写。</p>
</li>
<li><p>它能够提高代码的可读性。当其他人阅读你的代码时，看到<code>override</code>关键字，就能够立即知道这是一个重写基类虚函数的函数。</p>
</li>
</ul>
<p>下面是一个简单的例子，演示如何使用<code>override</code>关键字：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Base</span> </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">foo</span><span class="params">()</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;Base::foo()&quot;</span> &lt;&lt; std::endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Derived</span> : <span class="keyword">public</span> Base </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="type">void</span> <span class="title">foo</span><span class="params">()</span> <span class="keyword">override</span> </span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;Derived::foo()&quot;</span> &lt;&lt; std::endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Base* b = <span class="keyword">new</span> <span class="built_in">Derived</span>();</span><br><span class="line">    b-&gt;<span class="built_in">foo</span>(); <span class="comment">// 输出 &quot;Derived::foo()&quot;</span></span><br><span class="line">    <span class="keyword">delete</span> b;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>在上面的例子中，我们在派生类<code>Derived</code>中重写了基类<code>Base</code>的虚函数<code>foo()</code>。在重写该函数时，我们在函数声明后面加上了<code>override</code>关键字，表示这是一个重写基类虚函数的函数。</p>
<h1 id="异常处理"><a href="#异常处理" class="headerlink" title="异常处理"></a>异常处理</h1><p>传统异常处理：</p>
<p>通过返回不同的值来体现或处理异常</p>
<p>缺点：</p>
<ul>
<li><p>返回值意义不明确</p>
</li>
<li><p>返回值返回一个消息</p>
</li>
<li><p>返回值可以忽略</p>
</li>
</ul>
<h2 id="基本语法"><a href="#基本语法" class="headerlink" title="基本语法"></a>基本语法</h2><hr>
<p>示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">auto</span> <span class="title">fun</span><span class="params">(<span class="type">int</span> a,<span class="type">int</span> b)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">if</span> (b == <span class="number">0</span>)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="comment">//throw &quot;被除数为0&quot;;</span></span><br><span class="line">		<span class="keyword">throw</span> <span class="number">0</span>;<span class="comment">//throw语句抛出异常,抛出异常为int类型 0</span></span><br><span class="line">		<span class="comment">//抛出异常后，直接执行catch里的代码，这里的后续代码不会执行</span></span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">return</span> (a / b);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">test</span><span class="params">(<span class="type">int</span> a,<span class="type">int</span> b)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">try</span><span class="comment">//将可能出现异常的语句放在try语句中</span></span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; a &lt;&lt; <span class="string">&quot;除以&quot;</span> &lt;&lt; b &lt;&lt; <span class="string">&quot;等于&quot;</span> &lt;&lt; <span class="built_in">fun</span>(a, b) &lt;&lt; endl;;</span><br><span class="line">	&#125;</span><br><span class="line">    <span class="comment">//一个try语句可以与多个catch相匹配，但至少需要有一个catch,catch用于接收异常，可以指定接收的类型</span></span><br><span class="line">	<span class="built_in">catch</span> (<span class="type">const</span> <span class="type">char</span>* a)<span class="comment">//接收到一个const char* 类型的异常</span></span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; a&lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="built_in">catch</span> (<span class="type">int</span> a)<span class="comment">//接收到int 类型的异常</span></span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">switch</span> (a)<span class="comment">//使用switch语句判断异常并处理</span></span><br><span class="line">		&#123;</span><br><span class="line">		<span class="keyword">case</span> <span class="number">0</span>:</span><br><span class="line">			cout &lt;&lt; <span class="string">&quot;被除数等于0&quot;</span> &lt;&lt; endl;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">		<span class="keyword">case</span> <span class="number">1</span>:</span><br><span class="line">			<span class="comment">//......</span></span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        <span class="comment">//case .....</span></span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">    <span class="built_in">catch</span>(...)<span class="comment">//接收除了上面int和const char*以外的其他所有类型异常，如果只有这个就表示接收所有类型异常</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">//...</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="built_in">test</span>(<span class="number">10</span>, <span class="number">0</span>);</span><br><span class="line">	<span class="built_in">system</span>(<span class="string">&quot;pause&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p><code>throw</code>还可以抛出自定义数据类型，从而可以调用抛出对象的成员函数进一步处理。</p>
<p><code>try</code>中的语句如果抛出异常，则必须进行处理，即：必须有对应的<code>catch</code>代码段进行接收。</p>
<p><code>try</code>中的语句如果没有抛出异常，则后续对应的所有<code>catch</code>代码段都不会执行。</p>
<h2 id="异常（接口）声明"><a href="#异常（接口）声明" class="headerlink" title="异常（接口）声明"></a>异常（接口）声明</h2><hr>
<p>该语法特性已在C++11后弃用，VS编译器中可以使用，无语法错误，但实际没有任何效果，同时也不建议使用该语法。这里不做介绍。</p>
<h2 id="noexcept关键字"><a href="#noexcept关键字" class="headerlink" title="noexcept关键字"></a><code>noexcept</code>关键字</h2><hr>
<p><code>noexcept</code>用于指定函数是不会抛出异常的。这个关键字对于异常处理和代码优化非常有用。</p>
<p>异常接口声明的语法在C++11被移除，在未被移除之前，声明一个不会抛出异常的函数是这样的：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">func</span><span class="params">()</span> <span class="title">throw</span><span class="params">()</span><span class="comment">//表示这个函数不会抛出异常</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">//...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>该语法在C++11被移除，C++11后声明一个不会抛出异常的函数就要使用<code>noexcept</code>关键字</p>
<p>示例：</p>
<div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 声明一个会抛出异常的函数</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">mightThrow</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">throw</span> <span class="string">&quot;This function might throw an exception.&quot;</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 声明一个不会引发异常的函数</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">noThrow</span><span class="params">()</span> <span class="keyword">noexcept</span> <span class="comment">//noexcept表示函数不会抛出异常</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;This function will not throw any exception.&quot;</span> &lt;&lt; std::endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>如果在有<code>noexcept</code>修饰的函数中抛出了异常，并且在调用时接收了异常程序则会出现断点报错。</p>
<h2 id="异常对象的生命周期和栈解旋"><a href="#异常对象的生命周期和栈解旋" class="headerlink" title="异常对象的生命周期和栈解旋"></a>异常对象的生命周期和栈解旋</h2><hr>
<p>异常被抛出后，从进入<code>try</code>块起，到异常被抛掷前，这期间在栈上构造的所有对象，都会被自动析构。</p>
<p>析构的顺序与构造的顺序相反，这一过程称为栈的解旋（栈解旋）</p>
<p>示例1（过程中会创建3个对象）：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">myclass</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="built_in">myclass</span>()</span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;myclass的默认构造&quot;</span> &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="built_in">myclass</span>(<span class="type">const</span> myclass&amp; other)</span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;myclass的拷贝构造&quot;</span> &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line">	~<span class="built_in">myclass</span>()</span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;myclass的析构函数&quot;</span> &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">func</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	myclass one;	<span class="comment">//对象一	默认构造</span></span><br><span class="line">	<span class="keyword">throw</span> one;		<span class="comment">//对象二	拷贝构造</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">test01</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">try</span></span><br><span class="line">	&#123;</span><br><span class="line">		<span class="built_in">func</span>();</span><br><span class="line">	&#125;</span><br><span class="line">    <span class="comment">//对象一	在catch接收之前析构</span></span><br><span class="line">	<span class="built_in">catch</span> (myclass erro)<span class="comment">//对象三	由对象二拷贝构造</span></span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;接收到myclass类型异常&quot;</span> &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line">    <span class="comment">//对象二和对象三在catch段处理完异常后就析构</span></span><br><span class="line">    	cout &lt;&lt; <span class="string">&quot;接收异常后&quot;</span> &lt;&lt; endl;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="built_in">test01</span>();</span><br><span class="line">	<span class="built_in">system</span>(<span class="string">&quot;pause&gt;nul&quot;</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br></pre></td></tr></table></figure></div>

<p>运行结果：</p>
<div class="highlight-container" data-rel="Shell"><figure class="iseeu highlight shell"><table><tr><td class="code"><pre><span class="line">myclass的默认构造</span><br><span class="line">myclass的拷贝构造</span><br><span class="line">myclass的拷贝构造</span><br><span class="line">myclass的析构函数</span><br><span class="line">接收到myclass类型异常</span><br><span class="line">myclass的析构函数</span><br><span class="line">myclass的析构函数</span><br><span class="line">接收异常后</span><br></pre></td></tr></table></figure></div>

<p>示例2（过程中会创建两个对象）：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">myclass</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="built_in">myclass</span>()</span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;myclass的默认构造&quot;</span> &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="built_in">myclass</span>(<span class="type">const</span> myclass&amp; other)</span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;myclass的拷贝构造&quot;</span> &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line">	~<span class="built_in">myclass</span>()</span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;myclass的析构函数&quot;</span> &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">func</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">throw</span> <span class="built_in">myclass</span>();<span class="comment">//抛出匿名对象，避免拷贝，默认构造</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">test01</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">try</span></span><br><span class="line">	&#123;</span><br><span class="line">		<span class="built_in">func</span>();</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="built_in">catch</span> (myclass erro)<span class="comment">//拷贝构造，由匿名对象拷贝</span></span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;接收到myclass类型异常&quot;</span> &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line">    <span class="comment">//匿名对象和erro析构</span></span><br><span class="line">	cout &lt;&lt; <span class="string">&quot;接收异常后&quot;</span> &lt;&lt; endl;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="built_in">test01</span>();</span><br><span class="line">	<span class="built_in">system</span>(<span class="string">&quot;pause&gt;nul&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>运行结果：</p>
<div class="highlight-container" data-rel="Shell"><figure class="iseeu highlight shell"><table><tr><td class="code"><pre><span class="line">myclass的默认构造</span><br><span class="line">myclass的拷贝构造</span><br><span class="line">接收到myclass类型异常</span><br><span class="line">myclass的析构函数</span><br><span class="line">myclass的析构函数</span><br><span class="line">接收异常后</span><br></pre></td></tr></table></figure></div>

<p>示例3（过程中会创建一个对象）：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">myclass</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="built_in">myclass</span>()</span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;myclass的默认构造&quot;</span> &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="built_in">myclass</span>(<span class="type">const</span> myclass&amp; other)</span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;myclass的拷贝构造&quot;</span> &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line">	~<span class="built_in">myclass</span>()</span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;myclass的析构函数&quot;</span> &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">func</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">throw</span> <span class="built_in">myclass</span>();<span class="comment">//抛出匿名对象</span></span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">test01</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">try</span></span><br><span class="line">	&#123;</span><br><span class="line">		<span class="built_in">func</span>();</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="built_in">catch</span> (myclass&amp; erro)<span class="comment">//引用接收throw抛出的匿名对象</span></span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;接收到myclass类型异常&quot;</span> &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line">	cout &lt;&lt; <span class="string">&quot;接收异常后&quot;</span> &lt;&lt; endl;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="built_in">test01</span>();</span><br><span class="line">	<span class="built_in">system</span>(<span class="string">&quot;pause&gt;nul&quot;</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br></pre></td></tr></table></figure></div>

<p>运行结果：</p>
<div class="highlight-container" data-rel="Shell"><figure class="iseeu highlight shell"><table><tr><td class="code"><pre><span class="line">myclass的默认构造</span><br><span class="line">接收到myclass类型异常</span><br><span class="line">myclass的析构函数</span><br><span class="line">接收异常后</span><br></pre></td></tr></table></figure></div>

<p>总结：</p>
<p><code>throw</code>抛出的异常对象在<code>catch</code>处理完后才会被释放，使用<code>throw</code>抛出匿名对象可以在整个异常处理过程中只产生一个对象，避免拷贝构造。</p>
<p>抛出的堆区开辟的对象需要在<code>catch</code>中手动释放。</p>
<h2 id="异常中的多态"><a href="#异常中的多态" class="headerlink" title="异常中的多态"></a>异常中的多态</h2><hr>
<p>在<code>catch</code>中使用父类的引用接受子类对象，就会发生多态。当父类中有虚函数，并且子类重写后，那么<code>catch</code>中调用父类成员函数时就会调用子类中重写的函数。</p>
<p>代码示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">father</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line"></span><br><span class="line">	<span class="function"><span class="keyword">virtual</span> <span class="type">void</span> <span class="title">error</span><span class="params">()</span></span>&#123;&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">son01</span> : <span class="keyword">public</span> father</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="type">void</span> <span class="title">error</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;son01异常&quot;</span> &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">son02</span> : <span class="keyword">public</span> father</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="type">void</span> <span class="title">error</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;son02异常&quot;</span> &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">func01</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">if</span> (a == <span class="number">0</span>)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">throw</span> <span class="built_in">son01</span>();</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">if</span> (b == <span class="number">0</span>)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">throw</span> <span class="built_in">son02</span>();</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">test01</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">try</span></span><br><span class="line">	&#123;</span><br><span class="line">		<span class="built_in">func01</span>(<span class="number">0</span>, <span class="number">10</span>);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="built_in">catch</span> (father&amp; fa)</span><br><span class="line">	&#123;</span><br><span class="line">		fa.<span class="built_in">error</span>();</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">try</span></span><br><span class="line">	&#123;</span><br><span class="line">		<span class="built_in">func01</span>(<span class="number">10</span>, <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">	&#125;</span><br><span class="line">	<span class="built_in">catch</span>(father&amp; fa)</span><br><span class="line">	&#123;</span><br><span class="line">		fa.<span class="built_in">error</span>();</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="built_in">test01</span>();</span><br><span class="line">	<span class="built_in">system</span>(<span class="string">&quot;pause&gt;nul&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>运行结果：</p>
<div class="highlight-container" data-rel="Shell"><figure class="iseeu highlight shell"><table><tr><td class="code"><pre><span class="line">son01异常</span><br><span class="line">son02异常</span><br></pre></td></tr></table></figure></div>

<h2 id="标准异常类std-exception"><a href="#标准异常类std-exception" class="headerlink" title="标准异常类std::exception"></a>标准异常类<code>std::exception</code></h2><hr>
<p>提供一致的接口，以通过<code>throw</code>表达式处理错误。</p>
<p>标准库所生成的所有异常继承自 <code>std::exception</code></p>
<p>继承关系（C++11前）：</p>
<p><img  
                     lazyload
                     src="/images/loading.svg"
                     data-src="data:image/png;base64,"
                     
                ></p>
<p>部分异常类描述：</p>
<table>
<thead>
<tr>
<th>异常类</th>
<th>描述</th>
</tr>
</thead>
<tbody><tr>
<td><strong><code>std::exception</code></strong></td>
<td><strong>该异常是所有标准 C++ 异常的父类。</strong></td>
</tr>
<tr>
<td><code>std::bad_alloc</code></td>
<td>用<code>new</code>动态分配空间失败</td>
</tr>
<tr>
<td><code>std::bad_cast</code></td>
<td>执行<code>dynamic_cast</code>失败</td>
</tr>
<tr>
<td><code>std::bad_typeid</code></td>
<td>对某个空指针<code>p</code>执行<code>typeid(*p) </code></td>
</tr>
<tr>
<td><code>std::bad_exception</code></td>
<td>当某个函数<code>fun()</code>因在执行过程中抛出了异常声明所不允许的异常而调用<code>unexpected()</code>函数时，若<code>unexpected()</code>函数又一次抛出了<code>fun()</code>的异常声明所不允许的异常，且<code>fun()</code>的异常声明列表中有<code>bad_exception</code>,则会有一个<code>bad_exception</code>异常在<code>fun()</code>的调用点被抛出</td>
</tr>
<tr>
<td><strong><code>std::logic_error</code></strong></td>
<td><strong>理论上可以通过读取代码来检测到的异常。</strong></td>
</tr>
<tr>
<td><code>std::domain_error</code></td>
<td>执行一段程序所需要的先决条件不满足</td>
</tr>
<tr>
<td><code>std::invalid_argument</code></td>
<td>表示向函数传入无效参数</td>
</tr>
<tr>
<td><code>std::length_error</code></td>
<td>尝试创建一个长度超过最大允许值的对象</td>
</tr>
<tr>
<td><code>std::out_of_range</code></td>
<td>表示一个参数值不在允许的范围之内</td>
</tr>
<tr>
<td><code>std::future_error</code></td>
<td>(C++11起) 它为处理异步执行和共享状态（<code>std::future</code>、<code>std::promise</code>等）的线程库中的函数在失败时抛出。同 <code>std::system_error</code> ，此异常携带与<code>std::error_code</code>兼容的错误码。</td>
</tr>
<tr>
<td><code>std::bad_optional_access</code></td>
<td>(C++17起) 定义 <code>std::optional::value</code>在访问不含值的 <code>optional</code> 对象时所抛出的异常对象类型。</td>
</tr>
<tr>
<td><strong><code>std::runtime_error</code></strong></td>
<td><strong>理论上不可以通过读取代码来检测到的异常</strong></td>
</tr>
<tr>
<td><code>std::overflow_error</code></td>
<td>算术运算时向上溢出</td>
</tr>
<tr>
<td><code>std::range error</code></td>
<td>内部计算时发生作用域的错误</td>
</tr>
<tr>
<td><code>std::underflow_error</code></td>
<td>算术运算时向下溢出</td>
</tr>
<tr>
<td><code>std::regex_error</code></td>
<td>(C++11起) 定义抛出的异常对象类型，以报告正则表达式库中的错误。</td>
</tr>
<tr>
<td><strong><code>std::system_error</code></strong></td>
<td>(C++11起) <code>std::system_error</code> 是多种库函数（通常是与 OS 设施交接的函数，例如 <code>std::thread</code>的构造函数）在拥有关联于该异常的 <code>std::error_code</code>时抛出的异常类型，同时可能报告该<code>std::error_code </code>。</td>
</tr>
<tr>
<td><code>std::ios_base::failure</code></td>
<td>用来表示C++的输入输出流执行过程中发生的错误。（C++17起，继承自<code>std::system_error</code>）</td>
</tr>
<tr>
<td><code>std::filesystem::filesystem_error</code></td>
<td>(C++17起)类 <code>std::filesystem::filesystem_error</code> 定义文件系统库中函数的抛出版重载所抛出的异常对象。</td>
</tr>
</tbody></table>
<p>加粗的表示下方是其子类。</p>
<p>说明：还有相当多的异常类这里未全部列出，大都是C++11及C++11后的新标准定义的，这里不做再做说明，详细请查阅手册有关：标准库标头<code>&lt;exception&gt;</code>部分，上述表格已经能满足绝大部分使用场景。</p>
<h2 id="标准异常类的使用示例"><a href="#标准异常类的使用示例" class="headerlink" title="标准异常类的使用示例"></a>标准异常类的使用示例</h2><hr>
<p>一些C++语法或某些功能在使用时也许会发生错误，这些语法或功能在其内部实现时，也许就会将这些错误作为异常抛出，</p>
<p>这些异常都属于标准异常<code>std::exception</code>或其子类，都可以用<code>std::exception</code>的引用来接收，从而处理异常。</p>
<p>详细见视频（讲解的很好）：<a class="link"   href="https://www.bilibili.com/video/BV16T41167Zr?vd_source=03da7de0e80f9ff60fdeca7b512ba3f5" >【C++教程 第十三章（2）异常Exception深入解剖】 <i class="fa-regular fa-arrow-up-right-from-square fa-sm"></i></a></p>
<p>示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;exception&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;vector&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">try</span> </span><br><span class="line">    &#123;</span><br><span class="line">        <span class="function">vector&lt;<span class="type">int</span>&gt; <span class="title">a</span><span class="params">(<span class="number">9999999999999999999</span>)</span></span>;<span class="comment">//指定vector容器一个很大很大的空间，在vector内部代码中这将会作为一个错误异常抛出</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">catch</span> (exception&amp; erro)<span class="comment">//抛出的类都是exception类或其子类，使用父类引用接收（多态）</span></span><br><span class="line">    &#123;</span><br><span class="line">        cerr &lt;&lt; erro.<span class="built_in">what</span>() &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">system</span>(<span class="string">&quot;pause&gt;nul&quot;</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>输出结果：</p>
<div class="highlight-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">vector too long</span><br></pre></td></tr></table></figure></div>

<h2 id="自定义异常类继承标准异常类"><a href="#自定义异常类继承标准异常类" class="headerlink" title="自定义异常类继承标准异常类"></a>自定义异常类继承标准异常类</h2><hr>
<p>可以通过继承和重载 <code>exception</code>类或其他标准异常类来定义新的异常。</p>
<p>下面的实例演示了如何使用<code>std::exception</code>类来实现自己的异常：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;exception&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;string&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">MyException</span> : <span class="keyword">public</span> exception </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">MyException</span>(<span class="type">const</span> string&amp; message) : <span class="built_in">message_</span>(message) &#123;&#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">virtual</span> <span class="type">const</span> <span class="type">char</span>* <span class="title">what</span><span class="params">()</span> <span class="type">const</span> <span class="comment">/*noexcept*/</span> <span class="keyword">override</span><span class="comment">//noexcept属于异常（接口）声明，C++11后无任何效果，可忽略</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> message_.<span class="built_in">c_str</span>();</span><br><span class="line">    &#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    std::string message_;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">try</span> </span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="built_in">MyException</span>(<span class="string">&quot;!!!&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">catch</span> (exception&amp; erro)</span><br><span class="line">    &#123;</span><br><span class="line">        cerr &lt;&lt; erro.<span class="built_in">what</span>() &lt;&lt; <span class="string">&#x27;\n&#x27;</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">system</span>(<span class="string">&quot;pause&gt;nul&quot;</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>在这里，<code>what()</code> 是父异常类<code>std::exception</code>提供的一个公共方法，是一个虚函数，它已被所有子异常类重写，同样在自定义异常类中也需要重写，这也体现了C++多态的特性。</p>
<p>输出结果：</p>
<div class="highlight-container" data-rel="Shell"><figure class="iseeu highlight shell"><table><tr><td class="code"><pre><span class="line">!!!</span><br></pre></td></tr></table></figure></div>

<h1 id="模板基础"><a href="#模板基础" class="headerlink" title="模板基础"></a>模板基础</h1><h2 id="模板和泛型编程的概念"><a href="#模板和泛型编程的概念" class="headerlink" title="模板和泛型编程的概念"></a>模板和泛型编程的概念</h2><hr>
<p>泛型编程是一种编程模式，它核心的思想是增加代码的通用性和复用性，使代码能够适用于多种不同的数据类型。</p>
<p>传统的程序设计中，通常会写出多个函数或类来处理不同类型的数据。例如，在C语言中，为了实现针对不同类型的数组排序算法，需要为每一种数据类型编写相应的排序函数，这样会导致代码的冗余和重复。而泛型编程则可以使用单一的函数或类来实现通用的数据操作，避免了这种冗余和重复。</p>
<p>通过使用泛型编程，我们能够实现针对各种类型的数据，使用相同的代码进行处理。这种方式可以带来更高的代码复用性和可维护性，并可以避免出现多个函数或类的代码冗余的情况。</p>
<p>C++语言中的模板就是泛型编程的一种主要实现方式。使用C++模板，我们可以很方便地编写通用的函数或类来处理任意数据类型，极大地提高了代码的复用性、可维护性和可读性。</p>
<p>模板是C++中的一种高级编程特性，简单来说，模板就是一种让程序员编写具有参数类型或参数化表达式的函数和类的机制。利用模板，程序员可以充分利用C++的类型安全和可重用性。</p>
<p>C++中的模板可以分为函数模板和类模板两种。函数模板是将函数定义为泛型的，允许在不同数据类型下共享算法的代码。类模板则是将类定义为泛型的，允许在不同数据类型下共享类的代码实现。</p>
<p>模板的主要用途可以总结为以下几点：</p>
<ol>
<li><p>泛型编程：可以使用模板实现通用算法，可以很方便地处理不同类型的数据。</p>
</li>
<li><p>可重用性：可以通过实例化不同类型的模板，实现数值计算、数据结构等算法的一般化处理，从而大大提高代码的可重用性。</p>
</li>
<li><p>类型安全：使用模板的好处在于可以发现编译器在编译期间能够发现类型错误，从而提高程序的错误检测和安全性。</p>
</li>
</ol>
<p>总之，模板是一种非常重要的C++编程特性，它可以使程序员高效地实现通用算法和数据结构，提高程序的可读性、可重用性和安全性。</p>
<h2 id="函数模板"><a href="#函数模板" class="headerlink" title="函数模板"></a>函数模板</h2><hr>
<h3 id="语法规则"><a href="#语法规则" class="headerlink" title="语法规则"></a>语法规则</h3><hr>
<p>函数模板是一种用于生成通用代码的功能。模板参数允许将函数的参数和返回类型作为模板的参数进行定义，这样可以将同样的代码用于不同的数据类型，从而减少代码冗余和提高代码的重用性。</p>
<p>函数模板的语法形式如下：</p>
<p>语法：“<code>template关键字</code>” + “<code>&lt;typename T&gt;</code>” 或 “<code>&lt;class T&gt;</code>” ，其中<code>T</code>是可自定义的名称，一般用大写字母，对于函数模板来说，<code>typename </code>和<code>class</code>没有区别。</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> T&gt;<span class="comment">//声明接下来将要写一个函数模板</span></span><br><span class="line"><span class="comment">//接下来的一个函数，将作为函数模板</span></span><br><span class="line"><span class="comment">//示例：</span></span><br><span class="line"><span class="function">T <span class="title">max</span><span class="params">(T x, T y)</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  <span class="keyword">return</span> (x &gt; y) ? x : y;<span class="comment">//返回较大者</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>对于上面的例子，使用函数模板时，可以在调用函数时将数据类型作为实参传递给模板参数：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> a = <span class="number">5</span>, b = <span class="number">10</span>;</span><br><span class="line"><span class="type">double</span> c = <span class="number">3.14</span>, d = <span class="number">2.718</span>;</span><br><span class="line"><span class="type">int</span> max_int = <span class="built_in">max</span>&lt;<span class="type">int</span>&gt;(a, b);</span><br><span class="line"><span class="type">double</span> max_double = <span class="built_in">max</span>&lt;<span class="type">double</span>&gt;(c, d);</span><br></pre></td></tr></table></figure></div>

<p>在调用<code>max</code>函数时，使用了尖括号<code>&lt;&gt;</code>将数据类型作为实参传递给模板参数<code>T</code>，从而实例化了一个特定类型的函数。</p>
<p>在第一次调用<code>max</code>时，<code>T</code>被指定为<code>int</code>类型，在第二次调用<code>max</code>时，<code>T</code>被指定为<code>double</code>类型。这样就可以在不同的数据类型之间进行比较操作，从而提高代码的重用性和可读性。</p>
<p>同样的，也可以不指定<code>T</code>的类型，不指定类型时，将由编译器自动根据传入参数推导<code>T</code>的类型。</p>
<p>示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> a = <span class="number">5</span>, b = <span class="number">10</span>;</span><br><span class="line"><span class="type">double</span> c = <span class="number">3.14</span>, d = <span class="number">2.718</span>;</span><br><span class="line"><span class="type">int</span> max_int = <span class="built_in">max</span>(a, b);</span><br><span class="line"><span class="type">double</span> max_double = <span class="built_in">max</span>(c, d);</span><br></pre></td></tr></table></figure></div>

<p>在以上示例中，我们没有显式地指定<code>T</code>类型，而是将不同类型的变量<code>a</code>和<code>b</code>、<code>c</code>和<code>d</code>传递给了<code>max</code>函数。编译器可以推导出<code>a</code>和<code>b</code>的类型是<code>int</code>，而<code>c</code>和<code>d</code>的类型是<code>double</code>，因此编译器会自动实例化一个<code>max</code>函数模板，用于比较<code>int</code>和<code>double</code>类型的数据并返回较大值。</p>
<p>&#x3D;&#x3D;注意事项：&#x3D;&#x3D;<code>T</code>的类型必须一致，否则会报错。</p>
<p>如下所示：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">auto</span> result = <span class="built_in">max</span>(<span class="number">1</span>, <span class="number">2.5</span>);</span><br></pre></td></tr></table></figure></div>

<p>由于<code>1</code>和<code>2.5</code>的类型不同，编译器进行类型推导时，<code>T</code>的类型不一致，因此会报错。</p>
<p>建议在参数类型明确时，显示指定<code>T</code>的类型。</p>
<h3 id="普通函数和函数模板的调用规则"><a href="#普通函数和函数模板的调用规则" class="headerlink" title="普通函数和函数模板的调用规则"></a>普通函数和函数模板的调用规则</h3><hr>
<p>函数模板和普通函数可以发生重载，函数模板间也可以发生重载，当普通函数和模板函数发生重载时，对于传入的参数如果都满足，在没有显示指定模板参数类型的情况下会优先调用普通函数。</p>
<p>示例：</p>
<div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">foo</span><span class="params">(<span class="type">int</span> x)</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;foo(int) called with x = &quot;</span> &lt;&lt; x &lt;&lt; std::endl;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">foo</span><span class="params">(T x)</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;foo(T) called with x = &quot;</span> &lt;&lt; x &lt;&lt; std::endl;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">foo</span>(<span class="number">123</span>); <span class="comment">// 调用普通函数</span></span><br><span class="line">    <span class="built_in">foo</span>(<span class="number">3.14</span>); <span class="comment">// 调用函数模板</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>在此示例中，我们定义了一个名为<code>foo</code>的普通函数和一个同名的函数模板。当我们在<code>main</code>函数中调用<code>foo</code>函数时，编译器会优先选择普通函数而不是函数模板，因为普通函数的匹配级别更高。</p>
<p>通过显示指定模板参数类型或指定模板参数类型为空，可以强制调用函数模板。</p>
<p>示例：</p>
<div class="highlight-container" data-rel="Cpp"><figure class="iseeu highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> x = <span class="number">123</span>;</span><br><span class="line"><span class="built_in">foo</span>(x); <span class="comment">// 调用普通函数</span></span><br><span class="line"><span class="built_in">foo</span>&lt;<span class="type">int</span>&gt;(x); <span class="comment">// 调用函数模板</span></span><br><span class="line">foo&lt;&gt;(x); <span class="comment">// 调用函数模板</span></span><br></pre></td></tr></table></figure></div>

<p>如果函数模板可以对参数产生更好的匹配，则优先调用函数模板。</p>
<p>示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">foo</span><span class="params">(<span class="type">int</span> x)</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;foo(int) called with x = &quot;</span> &lt;&lt; x &lt;&lt; std::endl;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">foo</span><span class="params">(T x)</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;foo(T) called with x = &quot;</span> &lt;&lt; x &lt;&lt; std::endl;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="type">char</span> c = <span class="string">&#x27;c&#x27;</span>;</span><br><span class="line">    <span class="built_in">foo</span>(c); <span class="comment">// 调用函数模板</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>上述例子中调用普通函数时，需要进行隐式类型转换，而调用函数模板时不需要，则编译器优先调用函数模板。</p>
<p>&#x3D;&#x3D;！&#x3D;&#x3D;在实际开发中，不建议普通函数和函数模板重载，容易引起二义性。&#x3D;&#x3D;！&#x3D;&#x3D;</p>
<h3 id="普通函数和函数模板的区别"><a href="#普通函数和函数模板的区别" class="headerlink" title="普通函数和函数模板的区别"></a>普通函数和函数模板的区别</h3><hr>
<p>普通函数可以自动进行隐式类型转换，而函数模板在不显示指定模板参数类型的情况下，不能进行隐式转换。只有在显示指定模板参数类型的情况下才能自动进行隐式类型转换。</p>
<p>示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">add01</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> a + b;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span></span><br><span class="line"><span class="function">T <span class="title">add02</span><span class="params">(T a, T b)</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> a + b;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>当我们调用普通函数<code>max</code>时，如果传入的参数不是<code>int</code>类型，系统会通过隐式类型转换将其转换为<code>int</code>类型：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> a = <span class="number">3</span>;</span><br><span class="line"><span class="type">char</span> b = <span class="string">&#x27;c&#x27;</span>;</span><br><span class="line"><span class="type">int</span> c = <span class="built_in">add01</span>(a, b); <span class="comment">// 系统会自动将 char 类型的 b ，转换成对应的 int 类型的 ASCII码 </span></span><br></pre></td></tr></table></figure></div>

<p>而对于函数模板<code>add02</code>，如果传入的参数不是同一类型，在不显示指定模板参数类型时，会报错：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> a = <span class="number">3</span>;</span><br><span class="line"><span class="type">char</span> b = <span class="string">&#x27;c&#x27;</span>;</span><br><span class="line"><span class="comment">//int c = add02(a, b); // 编译器报错</span></span><br></pre></td></tr></table></figure></div>

<p>当显示指定模板参数类型时，编译器则会自动进行隐式类型转换：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">int</span> a = <span class="number">3</span>;</span><br><span class="line"><span class="type">char</span> b = <span class="string">&#x27;c&#x27;</span>;</span><br><span class="line"><span class="type">int</span> c = <span class="built_in">add02</span>&lt;<span class="type">int</span>&gt;(a, b); <span class="comment">// 会进行隐式类型转换，将 b 转成对应的 ASCII码</span></span><br></pre></td></tr></table></figure></div>



<p>&#x3D;&#x3D;补充说明：&#x3D;&#x3D;调用函数模板时，不显示指定模板类型的调用方式，称之为自动类型推导，所以调用函数模板由两种方式：自动类型推导和显示指定模板参数类型</p>
<h2 id="类模板"><a href="#类模板" class="headerlink" title="类模板"></a>类模板</h2><hr>
<h3 id="语法规则-1"><a href="#语法规则-1" class="headerlink" title="语法规则"></a>语法规则</h3><hr>
<p>类模板允许用户创建一种通用的类，使得类中的某些数据成员或成员函数的参数、返回值可以取任意类型。</p>
<p>语法：“<code>template关键字</code>” + “<code>&lt;class T1,class T2,...&gt;</code>” ，其中<code>T</code>是可自定义的名称，一般用大写字母，”<code>...</code>“表示可以有多个自定义的模板参数。</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">T1</span>, <span class="keyword">class</span> <span class="title class_">T2</span>,...&gt;   </span><br><span class="line"><span class="keyword">class</span> <span class="title class_">class_name</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">//成员变量、函数</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></div>

<p>类模板示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">T1</span>, <span class="keyword">class</span> <span class="title class_">T2</span>&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Person</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="built_in">Person</span>(T1 name, T2 age)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">this</span>-&gt;_name = name;</span><br><span class="line">		<span class="keyword">this</span>-&gt;_age = age;</span><br><span class="line">	&#125;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	T1 _name;</span><br><span class="line">	T2 _age;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">test01</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="function">Person&lt;string, <span class="type">int</span>&gt; <span class="title">p1</span><span class="params">(<span class="string">&quot;张三&quot;</span>, <span class="number">20</span>)</span></span>;<span class="comment">//指定成员的类型为string和int</span></span><br><span class="line">	cout &lt;&lt; <span class="string">&quot;姓名：&quot;</span> &lt;&lt; p1._name &lt;&lt; <span class="string">&quot;  年龄：&quot;</span> &lt;&lt; p1._age &lt;&lt; endl;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="built_in">test01</span>();</span><br><span class="line">	<span class="built_in">system</span>(<span class="string">&quot;pause&gt;nul&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>输出结果：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line">姓名：张三  年龄：<span class="number">20</span></span><br></pre></td></tr></table></figure></div>

<h3 id="类模板和函数模板的区别"><a href="#类模板和函数模板的区别" class="headerlink" title="类模板和函数模板的区别"></a>类模板和函数模板的区别</h3><hr>
<ul>
<li><p>类模板没有自动类型推导的使用方式</p>
<p>示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">T1</span>, <span class="keyword">class</span> <span class="title class_">T2</span>&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Person</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="built_in">Person</span>(T1 name, T2 age)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">this</span>-&gt;_name = name;</span><br><span class="line">		<span class="keyword">this</span>-&gt;_age = age;</span><br><span class="line">	&#125;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	T1 _name;</span><br><span class="line">	T2 _age;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">test01</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="function">Person&lt;string, <span class="type">int</span>&gt; <span class="title">p1</span><span class="params">(<span class="string">&quot;张三&quot;</span>, <span class="number">20</span>)</span></span>;<span class="comment">//指定模板参数类型为string和int</span></span><br><span class="line">    <span class="comment">//Person p1(&quot;张三&quot;, 20);//错误，类模板无法使用自动类型推导</span></span><br><span class="line">	cout &lt;&lt; <span class="string">&quot;姓名：&quot;</span> &lt;&lt; p1._name &lt;&lt; <span class="string">&quot;  年龄：&quot;</span> &lt;&lt; p1._age &lt;&lt; endl;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="built_in">test01</span>();</span><br><span class="line">	<span class="built_in">system</span>(<span class="string">&quot;pause&gt;nul&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>
</li>
<li><p>类模板在模板参数列表中可以有默认参数</p>
<p>示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">T1</span>, <span class="keyword">class</span> <span class="title class_">T2</span> = <span class="type">int</span>&gt;<span class="comment">//指定T2有默认的类型为int</span></span><br><span class="line"><span class="keyword">class</span> Person</span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="built_in">Person</span>(T1 name, T2 age)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">this</span>-&gt;_name = name;</span><br><span class="line">		<span class="keyword">this</span>-&gt;_age = age;</span><br><span class="line">	&#125;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	T1 _name;</span><br><span class="line">	T2 _age;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">test01</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="function">Person&lt;string&gt; <span class="title">p1</span><span class="params">(<span class="string">&quot;张三&quot;</span>, <span class="number">20</span>)</span></span>;<span class="comment">//指定第一个模板参数类型为string，第二个可以不指定，因为模板参数中第二个有默认参数int</span></span><br><span class="line">	cout &lt;&lt; <span class="string">&quot;姓名：&quot;</span> &lt;&lt; p1._name &lt;&lt; <span class="string">&quot;  年龄：&quot;</span> &lt;&lt; p1._age &lt;&lt; endl;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="built_in">test01</span>();</span><br><span class="line">	<span class="built_in">system</span>(<span class="string">&quot;pause&gt;nul&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div></li>
</ul>
<h3 id="类模板中成员函数的创建时机"><a href="#类模板中成员函数的创建时机" class="headerlink" title="类模板中成员函数的创建时机"></a>类模板中成员函数的创建时机</h3><hr>
<p>类模板中成员函数和普通类中成员函数创建时机是有区别的：</p>
<ul>
<li>普通类中的成员函数—开始就可以创建</li>
<li>类模板中的成员函数在调用时才创建</li>
</ul>
<p>示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MyClass1</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="type">void</span> <span class="title">fun1</span><span class="params">()</span> </span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;MyClass1的fun1的调用&quot;</span> &lt;&lt; endl;</span><br><span class="line">	&#125;;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">MyClass2</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="type">void</span> <span class="title">fun2</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;MyClass2的fun2的调用&quot;</span> &lt;&lt; endl;</span><br><span class="line">	&#125;;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">T1</span>&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Person</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    T1 obj;</span><br><span class="line">    </span><br><span class="line">	<span class="function"><span class="type">void</span> <span class="title">fun1</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		obj.<span class="built_in">fun1</span>();<span class="comment">//编译不会报错，但调用时如果T1不是MyClass1类型就会报错</span></span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="function"><span class="type">void</span> <span class="title">fun2</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		obj.<span class="built_in">fun2</span>();<span class="comment">//编译不会报错，但调用时如果T1不是MyClass2类型就会报错</span></span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">//上述两个注释说明，类模板的成员函数是在调用时才创建的</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">test01</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="comment">//Person&lt;int&gt; p1;//错误</span></span><br><span class="line">	<span class="comment">//p1.fun1();</span></span><br><span class="line">    </span><br><span class="line">    Person&lt;MyClass1&gt; p1;</span><br><span class="line">	p1.<span class="built_in">fun1</span>();<span class="comment">//显示：MyClass1的fun1的调用</span></span><br><span class="line">	<span class="comment">//p1.fun2();错误，MyClass1没有fun2()成员</span></span><br><span class="line">    </span><br><span class="line">    Person&lt;MyClass2&gt; p2;</span><br><span class="line">	p2.<span class="built_in">fun2</span>();<span class="comment">//显示：MyClass2的fun2的调用</span></span><br><span class="line">	<span class="comment">//p2.fun1();错误，MyClass2没有fun1()成员</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">test01</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>输出结果：</p>
<div class="highlight-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">MyClass1的fun1的调用</span><br><span class="line">MyClass2的fun2的调用</span><br></pre></td></tr></table></figure></div>

<h3 id="类模板对象做函数参数"><a href="#类模板对象做函数参数" class="headerlink" title="类模板对象做函数参数"></a>类模板对象做函数参数</h3><hr>
<p>类模板实例化出来的对象像函数传参有三种传入方式：</p>
<ul>
<li><p>指定传入的类型：直接显示对象的数据类型</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">T1</span>,<span class="keyword">class</span> <span class="title class_">T2</span>&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Person</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="built_in">Person</span>(T1 name, T2 age) :<span class="built_in">name</span>(name), <span class="built_in">age</span>(age)</span><br><span class="line">	&#123;&#125;</span><br><span class="line">	<span class="function"><span class="type">void</span> <span class="title">show</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;姓名：&quot;</span> &lt;&lt; <span class="keyword">this</span>-&gt;name &lt;&lt; <span class="string">&quot;  年龄：&quot;</span> &lt;&lt; <span class="keyword">this</span>-&gt;age &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line">	T1 name;</span><br><span class="line">	T2 age;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">//指定传入类型</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">printpPerson1</span><span class="params">(Person&lt;string, <span class="type">int</span>&gt;&amp; p)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	p.<span class="built_in">show</span>();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">test01</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	Person&lt;string, <span class="type">int</span>&gt;<span class="built_in">p1</span>(<span class="string">&quot;张三&quot;</span>, <span class="number">20</span>);</span><br><span class="line">    <span class="built_in">printpPerson1</span>(p1);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="built_in">test01</span>();</span><br><span class="line">	<span class="built_in">system</span>(<span class="string">&quot;pause&gt;nul&quot;</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br></pre></td></tr></table></figure></div>


</li>
<li><p>参数模板化：将对象中的参数变为模板进行传递</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">T1</span>,<span class="keyword">class</span> <span class="title class_">T2</span>&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Person</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="built_in">Person</span>(T1 name, T2 age) :<span class="built_in">name</span>(name), <span class="built_in">age</span>(age)</span><br><span class="line">	&#123;&#125;</span><br><span class="line">	<span class="function"><span class="type">void</span> <span class="title">show</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;姓名：&quot;</span> &lt;&lt; <span class="keyword">this</span>-&gt;name &lt;&lt; <span class="string">&quot;  年龄：&quot;</span> &lt;&lt; <span class="keyword">this</span>-&gt;age &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line">	T1 name;</span><br><span class="line">	T2 age;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">//参数模板化</span></span><br><span class="line"><span class="function"><span class="keyword">template</span>&lt;<span class="keyword">class</span> T1, <span class="keyword">class</span> T2&gt;</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">printPerson2</span><span class="params">(Person&lt;T1, T2&gt;&amp; p)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	p.<span class="built_in">show</span>();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">test01</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	Person&lt;string, <span class="type">int</span>&gt;<span class="built_in">p1</span>(<span class="string">&quot;张三&quot;</span>, <span class="number">20</span>);</span><br><span class="line">    <span class="built_in">printpPerson2</span>(p1);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="built_in">test01</span>();</span><br><span class="line">	<span class="built_in">system</span>(<span class="string">&quot;pause&gt;nul&quot;</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure></div>


</li>
<li><p>整个类模板化：将这个对象类型模板化进行传递</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">T1</span>,<span class="keyword">class</span> <span class="title class_">T2</span>&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Person</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="built_in">Person</span>(T1 name, T2 age) :<span class="built_in">name</span>(name), <span class="built_in">age</span>(age)</span><br><span class="line">	&#123;&#125;</span><br><span class="line">	<span class="function"><span class="type">void</span> <span class="title">show</span><span class="params">()</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;姓名：&quot;</span> &lt;&lt; <span class="keyword">this</span>-&gt;name &lt;&lt; <span class="string">&quot;  年龄：&quot;</span> &lt;&lt; <span class="keyword">this</span>-&gt;age &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line">	T1 name;</span><br><span class="line">	T2 age;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">//整个类模板化</span></span><br><span class="line"><span class="function"><span class="keyword">template</span>&lt;<span class="keyword">class</span> T&gt;</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">printPerson3</span><span class="params">(T&amp; p)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	p.<span class="built_in">show</span>();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">test01</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	Person&lt;string, <span class="type">int</span>&gt;<span class="built_in">p1</span>(<span class="string">&quot;张三&quot;</span>, <span class="number">20</span>);</span><br><span class="line">    <span class="built_in">printpPerson3</span>(p1);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="built_in">test01</span>();</span><br><span class="line">	<span class="built_in">system</span>(<span class="string">&quot;pause&gt;nul&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div></li>
</ul>
<h3 id="类模板与继承"><a href="#类模板与继承" class="headerlink" title="类模板与继承"></a>类模板与继承</h3><hr>
<ul>
<li><p>当子类继承的父类是一个类模板时，子类在声明的时候，要指定出父类中“<code>T</code>”的类型，如果不指定。编译器无法给子类分配内存</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">father</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	T m;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">//class son :public father //错误，必须要知道父类中的T类型，才能继承给子类</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">son</span> :<span class="keyword">public</span> father&lt;<span class="type">int</span>&gt;</span><br><span class="line">&#123;</span><br><span class="line">    </span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></div>
</li>
<li><p>如果想灵活指定出父类中“<code>T</code>”的类型，子类也需变为类模板</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">father</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	T m;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">T1</span>,<span class="keyword">class</span> <span class="title class_">T2</span>&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">son</span> :<span class="keyword">public</span> father&lt;T2&gt;<span class="comment">//T1指定成子类自己的类型，T2指定成父类的类型</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	T1 obj;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">test01</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	son&lt;<span class="type">int</span>, string&gt; s1;</span><br><span class="line">	s1.m = <span class="string">&quot;hello&quot;</span>;</span><br><span class="line">	s1.obj = <span class="number">10</span>;</span><br><span class="line"></span><br><span class="line">	cout &lt;&lt; s1.m &lt;&lt; endl;</span><br><span class="line">	cout &lt;&lt; s1.obj &lt;&lt; endl;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="built_in">test01</span>();</span><br><span class="line">	<span class="built_in">system</span>(<span class="string">&quot;pause&gt;nul&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div></li>
</ul>
<h3 id="类模板成员函数的类外实现"><a href="#类模板成员函数的类外实现" class="headerlink" title="类模板成员函数的类外实现"></a>类模板成员函数的类外实现</h3><hr>
<p>代码示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">T1</span>,<span class="keyword">class</span> <span class="title class_">T2</span>&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Person</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="built_in">Person</span>(T1 name, T2 age) :<span class="built_in">name</span>(name), <span class="built_in">age</span>(age);</span><br><span class="line"></span><br><span class="line">	<span class="function"><span class="type">void</span> <span class="title">show</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line">	T1 name;</span><br><span class="line">	T2 age;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">T1</span>, <span class="keyword">class</span> <span class="title class_">T2</span>&gt;</span><br><span class="line">Person&lt;T1, T2&gt;::<span class="built_in">Person</span>(T1 name, T2 age)</span><br><span class="line">&#123;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">T1</span>, <span class="keyword">class</span> <span class="title class_">T2</span>&gt;</span><br><span class="line"><span class="type">void</span> Person&lt;T1, T2&gt;::<span class="built_in">show</span>()</span><br><span class="line">&#123;</span><br><span class="line">	cout &lt;&lt; <span class="string">&quot;姓名：&quot;</span> &lt;&lt; <span class="keyword">this</span>-&gt;name &lt;&lt; <span class="string">&quot;  年龄：&quot;</span> &lt;&lt; <span class="keyword">this</span>-&gt;age &lt;&lt; endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<h3 id="类模板的分文件编写"><a href="#类模板的分文件编写" class="headerlink" title="类模板的分文件编写"></a>类模板的分文件编写</h3><hr>
<p>普通类的分文件编写是在<code>.h</code>中写声明，<code>.cpp</code>中写定义，对于类模板如果采用这种写法则会报错。类模板中成员函数创建时机是在调用阶段，如果采用分普通的分文件方式编写，即在<code>.h</code>文件中写声明，在<code>.cpp</code>文件中写定义，会导致<code>.cpp</code>中的定义链接不到。</p>
<p>报错原因：</p>
<ul>
<li>采用普通分文件编写时，首先包含了<code>.h</code>头文件，编译器知道了类模板的存在，但是类模板中的成员函数在调用时才会创建，所以此时编译器不会去“寻找”成员函数的定义，之后去调用时，因为没有包含<code>.cpp</code>文件<code>.h</code>文件中也没有包含<code>.cpp</code>文件，因此编译器无法链接到类模板中的成员函数。</li>
</ul>
<p>解决方案：</p>
<ul>
<li>直接包含<code>.cpp</code>源文件。示例（假设有一个Person的类模板，在<code>.h</code>中声明，在<code>.cpp</code>中定义）：把<code>#include &quot;Person.h&quot;</code>变为<code>#include &quot;Person.cpp&quot;</code>。</li>
<li>将声明<code>.h</code>文件和实现<code>.cpp</code>文件写到同一个文件中，并更改后缀名为<code>.hpp</code>。<code>hpp</code>是约定俗成的名称，并不是强制的。</li>
<li>采用传统分文件方式编写，在<code>.h</code>文件最后包含上<code>.cpp</code>文件，同时使用<code>#pragma once</code>防止头文件重复包含。在主函数文件中包含<code>.h</code>文件即可。</li>
</ul>
<h3 id="类模板与友元"><a href="#类模板与友元" class="headerlink" title="类模板与友元"></a>类模板与友元</h3><hr>
<p>在类模板中编写友元函数或友元类时，需要注意以下几点：</p>
<ul>
<li>友元函数或友元类的声明位置：友元函数或友元类的声明应该放在类模板内部，并在需要访问私有或保护成员的地方进行声明。通常，在类模板的定义之前声明友元函数或友元类，以便让编译器知道这些函数或类是模板类的友元。</li>
<li>模板参数的传递：如果友元函数或友元类也是一个模板，并且它的模板参数与包含它的类模板的模板参数不同，需要显式传递模板参数。这可以通过将类模板的模板参数列表添加到友元函数或友元类的声明中来实现。</li>
<li>定义位置的选择：友元函数或友元类的定义可以放在类模板的内部或外部。如果将定义放在类模板的内部，需要注意它们是每个实例化的模板类的独立定义，而不是共享的。如果将定义放在类模板的外部，通常需要在定义之前使用<code>template</code>关键字以指明它是一个类模板的友元。</li>
</ul>
<p>友元函数：</p>
<ul>
<li><p>类内声明和定义：直接在类内声明友元并定义即可</p>
<p>代码示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">T1</span>, <span class="keyword">class</span> <span class="title class_">T2</span>&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Person</span></span><br><span class="line">&#123;</span><br><span class="line">	<span class="function"><span class="keyword">friend</span> <span class="type">void</span> <span class="title">show</span><span class="params">(Person&lt;T1, T2&gt;p)</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		std::cout &lt;&lt; <span class="string">&quot;姓名：&quot;</span> &lt;&lt; p.name &lt;&lt; <span class="string">&quot;  年龄：&quot;</span> &lt;&lt; p.age &lt;&lt; std::endl;</span><br><span class="line">	&#125;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="built_in">Person</span>(T1 name, T2 age)</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="keyword">this</span>-&gt;name = name;</span><br><span class="line">		<span class="keyword">this</span>-&gt;age = age;</span><br><span class="line">	&#125;;</span><br><span class="line">	T1 name;</span><br><span class="line">	T2 age;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Person&lt;std::string, <span class="type">int</span>&gt;<span class="built_in">p1</span>(<span class="string">&quot;张三&quot;</span>,<span class="number">20</span>);</span><br><span class="line">	<span class="built_in">show</span>(p1);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>输出结果：</p>
<div class="highlight-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">姓名：张三  年龄：20</span><br></pre></td></tr></table></figure></div>
</li>
<li><p>类内声明类外定义：需要提前让编译器知道友元函数的存在</p>
<p>代码示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">T1</span>, <span class="keyword">class</span> <span class="title class_">T2</span>&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">Person</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">//友元函数的声明</span></span><br><span class="line">    <span class="function"><span class="keyword">template</span>&lt;<span class="keyword">class</span> t3, <span class="keyword">class</span> t4&gt;</span></span><br><span class="line"><span class="function">    <span class="keyword">friend</span> <span class="type">void</span> <span class="title">show</span><span class="params">(Person&lt;t3, t4&gt;p)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Person</span>(T1 name, T2 age)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">this</span>-&gt;name = name;</span><br><span class="line">        <span class="keyword">this</span>-&gt;age = age;</span><br><span class="line">    &#125;;</span><br><span class="line">    T1 name;</span><br><span class="line">    T2 age;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">//友元函数的定义</span></span><br><span class="line"><span class="function"><span class="keyword">template</span>&lt;<span class="keyword">class</span> t3, <span class="keyword">class</span> t4&gt;</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">show</span><span class="params">(Person&lt;t3, t4&gt;p)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;姓名：&quot;</span> &lt;&lt; p.name &lt;&lt; <span class="string">&quot;  年龄：&quot;</span> &lt;&lt; p.age &lt;&lt; std::endl;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Person&lt;std::string, <span class="type">int</span>&gt;<span class="built_in">p1</span>(<span class="string">&quot;张三&quot;</span>,<span class="number">20</span>);</span><br><span class="line">	<span class="built_in">show</span>(p1);  </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div></li>
</ul>
<p>友元类：</p>
<p>代码示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">TemplateClass</span> </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">TemplateClass</span>(T data)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">this</span>-&gt;m_date = date;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 友元类的声明</span></span><br><span class="line">    <span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">U</span>&gt;</span><br><span class="line">    <span class="keyword">friend</span> <span class="keyword">class</span> <span class="title class_">FriendClass</span>;</span><br><span class="line">    </span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    T m_data;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 友元类的定义</span></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">FriendClass</span> &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">FriendClass</span>(<span class="type">const</span> TemplateClass&lt;T&gt;&amp; obj)</span><br><span class="line">    &#123;</span><br><span class="line">        std::cout &lt;&lt; <span class="string">&quot;Friend Class: &quot;</span> &lt;&lt; obj.m_data &lt;&lt; std::endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></div>

<p>友元函数或友元类的声明应该放在类模板内部，并在需要访问私有或保护成员的地方进行声明。通常，在类模板的定义之前声明友元函数或友元类，以便让编译器知道这些函数或类是模板类的友元。</p>
<h3 id="关于typename和class"><a href="#关于typename和class" class="headerlink" title="关于typename和class"></a>关于<code>typename</code>和<code>class</code></h3><hr>
<p>在C++模板中，<code>typename</code>和<code>class</code>关键字用于声明模板参数，它们在大多数情况下是可以互换使用的，没有本质的区别。</p>
<p>然而，它们在某些情况下具有一些细微的区别：</p>
<ul>
<li><p>模板参数的默认关键字：当定义模板参数时，<code>class</code>可以使用默认关键字<code>class</code>或<code>typename</code>。</p>
<p>例如：<code>template &lt;class T&gt;</code> 或 <code>template &lt;typename T&gt;</code>。这两者是等效的，可以互换使用。</p>
</li>
<li><p>嵌套依赖名称：在模板中使用嵌套依赖名称时，必须使用<code>typename</code>关键字。</p>
<p>例如，当在类模板的成员函数中使用依赖于模板参数的类型时，需要使用<code>typename</code>关键字来指示该名称是一个类型。例如：<code>typename T::value_type</code>。</p>
</li>
<li><p>模板参数作为模板参数：当模板参数本身是一个模板时，必须使用<code>class</code>关键字。</p>
<p>例如，当模板参数是一个模板类时，使用<code>class</code>关键字来声明它，如：<code>template &lt;class Container&gt;</code>。</p>
</li>
</ul>
<p><code>typename</code>和<code>class</code>在大多数情况下可以互换使用，没有本质的区别。在某些特定的情况下，使用<code>typename</code>关键字是必要的，例如处理嵌套依赖名称时。</p>
<p>一般来说，根据个人偏好或项目风格指南选择使用<code>typename</code>还是<code>class</code>。</p>
<p>代码示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="keyword">class</span> <span class="title class_">MyContainer</span> </span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="keyword">typedef</span> T value_type;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> Container&gt;</span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">printValueType</span><span class="params">(Container container)</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// 错误示例：尝试使用未指定类型的依赖名称</span></span><br><span class="line">    <span class="comment">// 此处必须使用 typename 关键字</span></span><br><span class="line">    <span class="comment">// std::cout &lt;&lt; Container::value_type &lt;&lt; std::endl;  // 编译错误</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 正确示例：使用 typename 关键字指示依赖名称是类型</span></span><br><span class="line">    <span class="keyword">typename</span> Container::value_type value;</span><br><span class="line">    std::cout &lt;&lt; <span class="built_in">typeid</span>(value).<span class="built_in">name</span>() &lt;&lt; std::endl;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    MyContainer&lt;<span class="type">int</span>&gt; container;</span><br><span class="line">    <span class="built_in">printValueType</span>(container);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>在上述示例中，我们定义了一个模板类 <code>MyContainer</code>，其中包含一个内部类型别名 <code>value_type</code>。</p>
<p>然后，我们定义了一个模板函数 <code>printValueType</code>，该函数接受一个模板参数 <code>Container</code>，并尝试访问 <code>Container</code> 类型的 <code>value_type</code>。</p>
<p>在错误示例中，我们未使用 <code>typename</code> 关键字，编译器会报错，因为它无法确定 <code>Container::value_type</code> 是一个类型还是一个成员变量。</p>
<p>而在正确示例中，我们使用 <code>typename</code> 关键字来明确告诉编译器 <code>Container::value_type</code> 是一个类型。</p>
<p>总结：</p>
<ul>
<li>在涉及到嵌套依赖名称时，必须使用 <code>typename</code> 关键字来指示该名称是一个类型。</li>
<li>在模板参数本身是一个模板时，必须使用 <code>class</code> 关键字来声明它，例如 <code>template &lt;class Container&gt;</code>。</li>
</ul>
<h1 id="STL部分"><a href="#STL部分" class="headerlink" title="STL部分"></a>STL部分</h1><h2 id="说明"><a href="#说明" class="headerlink" title="&#x3D;&#x3D;说明&#x3D;&#x3D;"></a>&#x3D;&#x3D;说明&#x3D;&#x3D;</h2><p>本章的编写根据C++98标准，对于新标准的内容只是略有涉及，部章的部分内容在新的标准中也许是不正确的。</p>
<p>本章的大部分内容只编写了常见、常用的内容，对于不常用、不常见的用法或新标准中新增的、更改的内容详细请自行参阅手册。</p>
<p>截止到C++20标准，<code>STL</code>（标准模板库）提供了以下容器：</p>
<ol>
<li><code>std::array</code>: 固定大小的数组容器。</li>
<li><code>std::vector</code>: 动态数组，可根据需要自动调整大小。</li>
<li><code>std::deque</code>: 双端队列，允许在两端高效地添加和删除元素。</li>
<li><code>std::list</code>: 双向链表。</li>
<li><code>std::forward_list</code>: 单向链表。</li>
<li><code>std::set</code>: 按升序排序的唯一元素集合。</li>
<li><code>std::multiset</code>: 按升序排序的允许重复元素的集合。</li>
<li><code>std::map</code>: 键-值对的有序映射，按键的升序排序。</li>
<li><code>std::multimap</code>: 键-值对的有序映射，允许重复的键。</li>
<li><code>std::unordered_set</code>: 哈希集合，不保证元素顺序。</li>
<li><code>std::unordered_multiset</code>: 哈希集合，允许重复元素，不保证元素顺序。</li>
<li><code>std::unordered_map</code>: 键-值对的哈希映射，不保证元素顺序。</li>
<li><code>std::unordered_multimap</code>: 键-值对的哈希映射，允许重复的键，不保证元素顺序。</li>
<li><code>std::stack</code>: 栈，后进先出（LIFO）的数据结构适配器。</li>
<li><code>std::queue</code>: 队列，先进先出（FIFO）的数据结构适配器。</li>
<li><code>std::priority_queue</code>: 优先队列，根据优先级进行排序的数据结构适配器。</li>
</ol>
<p>这些容器提供了不同的功能和性能特点，可以根据具体的需求选择合适的容器。</p>
<p>以下是C++11标准引入的新容器：</p>
<ol>
<li><code>std::array</code>: 固定大小的数组容器。</li>
<li><code>std::unordered_set</code>: 哈希集合，不保证元素顺序。</li>
<li><code>std::unordered_multiset</code>: 哈希集合，允许重复元素，不保证元素顺序。</li>
<li><code>std::unordered_map</code>: 键-值对的哈希映射，不保证元素顺序。</li>
<li><code>std::unordered_multimap</code>: 键-值对的哈希映射，允许重复的键，不保证元素顺序。</li>
</ol>
<p>以下是C++17标准引入的新容器：</p>
<ol>
<li><code>std::optional</code>: 可选值容器，可以表示一个可能存在或不存在的值。</li>
<li><code>std::variant</code>: 可变类型容器，可以容纳多个可能的类型中的一个值。</li>
<li><code>std::any</code>: 任意类型容器，可以容纳任意类型的值。</li>
<li><code>std::string_view</code>: 字符串视图，用于非拥有权的字符串操作。</li>
</ol>
<p>从C++20开始，没有引入新的容器类型。</p>
<h2 id="string容器"><a href="#string容器" class="headerlink" title="string容器"></a><code>string</code>容器</h2><hr>
<h3 id="构造函数-1"><a href="#构造函数-1" class="headerlink" title="构造函数"></a>构造函数</h3><hr>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">string</span>();							<span class="comment">//创建一个空的字符串例如: string str;</span></span><br><span class="line"><span class="built_in">string</span>(<span class="type">const</span> <span class="type">char</span>* s);				<span class="comment">//使用C风格字符串s初始化</span></span><br><span class="line"><span class="built_in">string</span>(<span class="type">const</span> string&amp; str);			<span class="comment">//使用一个string对象初始化另一个string对象</span></span><br><span class="line"><span class="built_in">string</span>(<span class="type">int</span> n，<span class="type">char</span> c);              <span class="comment">//使用n个字符c初始化</span></span><br></pre></td></tr></table></figure></div>

<h3 id="赋值"><a href="#赋值" class="headerlink" title="赋值"></a>赋值</h3><hr>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line">string&amp; <span class="keyword">operator</span>=(<span class="type">const</span> <span class="type">char</span>* s);			<span class="comment">//char*类型字符串赋值给当前的字符串</span></span><br><span class="line">string&amp; <span class="keyword">operator</span>=(<span class="type">const</span> string &amp;s);			<span class="comment">//把字符串s赋给当前的字符串</span></span><br><span class="line">string&amp; <span class="keyword">operator</span>=(<span class="type">char</span> c);					<span class="comment">//字符赋值给当前的字符串</span></span><br><span class="line"><span class="function">string&amp; <span class="title">assign</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *s)</span></span>;				<span class="comment">//把字符串s赋给当前的字符串</span></span><br><span class="line"><span class="function">string&amp; <span class="title">assign</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *s, <span class="type">int</span> n)</span></span>;		<span class="comment">//把字符串s的前n个字符赋给当前的字符串</span></span><br><span class="line"><span class="function">string&amp; <span class="title">assign</span><span class="params">(<span class="type">const</span> string &amp;s)</span> </span>;			<span class="comment">//把字符串s赋给当前字符串</span></span><br><span class="line"><span class="function">string&amp; <span class="title">assign</span><span class="params">(<span class="type">int</span> n，<span class="type">char</span> c)</span></span>;              <span class="comment">//用n个字符c赋给当前字符串</span></span><br></pre></td></tr></table></figure></div>

<h3 id="拼接"><a href="#拼接" class="headerlink" title="拼接"></a>拼接</h3><hr>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line">string&amp; <span class="keyword">operator</span>+=( <span class="type">const</span> <span class="type">char</span>* str);               <span class="comment">//重载+=操作符</span></span><br><span class="line">string&amp; <span class="keyword">operator</span>+=( <span class="type">const</span> <span class="type">char</span> c);                  <span class="comment">//重载+=操作符</span></span><br><span class="line">string&amp; <span class="keyword">operator</span>+=( <span class="type">const</span> string&amp; str);             <span class="comment">//重载+=操作符</span></span><br><span class="line"><span class="function">string&amp; <span class="title">append</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *s )</span> </span>;                    <span class="comment">//把字符串s连接到当前字符串结尾</span></span><br><span class="line"><span class="function">string&amp; <span class="title">append</span><span class="params">( <span class="type">const</span> <span class="type">char</span> *s, <span class="type">int</span> n)</span></span>;              <span class="comment">//把字符串s的前n个字符连接到当前字符串结尾</span></span><br><span class="line"><span class="function">string&amp; <span class="title">append</span><span class="params">(<span class="type">const</span> string &amp;s )</span> </span>;                  <span class="comment">//同operator+=(const string&amp; str)</span></span><br><span class="line"><span class="function">string&amp; <span class="title">append</span><span class="params">(<span class="type">const</span> string &amp;s，<span class="type">int</span> pos,<span class="type">int</span> n)</span></span>;     <span class="comment">//字符串s中从pos开始的n个字符连接到字符串结尾</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">push_back</span> <span class="params">(<span class="type">char</span> c)</span></span>;						<span class="comment">//它将字符 c 附加到字符串的末尾，将其长度增加一</span></span><br></pre></td></tr></table></figure></div>

<h3 id="查找、替换"><a href="#查找、替换" class="headerlink" title="查找、替换"></a>查找、替换</h3><hr>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">find</span><span class="params">(<span class="type">const</span> string&amp; str, <span class="type">int</span> pos = <span class="number">0</span>)</span> <span class="type">const</span></span>;         <span class="comment">//查找str第一次出现位置,从pos开始查找</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">find</span><span class="params">(<span class="type">const</span> <span class="type">char</span>* s, <span class="type">int</span> pos - e)</span> <span class="type">const</span></span>;             <span class="comment">//查找s第一次出现位置,从pos开始查找</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">find</span><span class="params">(<span class="type">const</span> <span class="type">char</span>* s , <span class="type">int</span> pos， <span class="type">int</span> n)</span> <span class="type">const</span></span>;        <span class="comment">//从pos位置查找s的前n个字符第一次位置</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">find</span><span class="params">(<span class="type">const</span> <span class="type">char</span> c, <span class="type">int</span> pos = e)</span> <span class="type">const</span></span>;              <span class="comment">//查找字符c第一次出现位置</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">rfind</span><span class="params">(<span class="type">const</span> string&amp; str, <span class="type">int</span> pos = npos)</span> <span class="type">const</span></span>;     <span class="comment">//查找str最后一次位置,从pos开始查找</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">rfind</span><span class="params">(<span class="type">const</span> <span class="type">char</span>* s, <span class="type">int</span> pos = npos)</span> <span class="type">const</span></span>;         <span class="comment">//查找s最后一次出现位置,从pos开始查找</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">rfind</span><span class="params">(<span class="type">const</span> <span class="type">char</span>* s, <span class="type">int</span> pos， <span class="type">int</span> n)</span> <span class="type">const</span></span>;        <span class="comment">//从pos查找s的前n个字符最后一次位置</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">rfind</span><span class="params">(<span class="type">const</span> <span class="type">char</span> c, <span class="type">int</span> pos = e)</span> <span class="type">const</span></span>;             <span class="comment">//查找字符c最后一次出现位置</span></span><br><span class="line"></span><br><span class="line"><span class="function">string&amp; <span class="title">replace</span> <span class="params">(<span class="type">size_t</span> pos, <span class="type">size_t</span> len, <span class="type">const</span> string&amp; str)</span></span>; 					<span class="comment">//替换从pos开始len个字符为字符串str  </span></span><br><span class="line"><span class="function">string&amp; <span class="title">replace</span> <span class="params">(<span class="type">size_t</span> pos, <span class="type">size_t</span> len, <span class="type">const</span> <span class="type">char</span>* s)</span></span>;   						<span class="comment">//替换从pos开始的len个字符为C风格字符串s</span></span><br><span class="line"><span class="function">string&amp; <span class="title">replace</span><span class="params">(<span class="type">size_t</span> pos, <span class="type">size_t</span> len, <span class="type">const</span> <span class="type">char</span>* s, <span class="type">size_t</span> n)</span></span>;  				<span class="comment">//用s的前n个字符替换从pos开始长度为len的子串</span></span><br><span class="line"><span class="function">string&amp; <span class="title">replace</span> <span class="params">(<span class="type">size_t</span> pos, <span class="type">size_t</span> len, <span class="type">size_t</span> n, <span class="type">char</span> c)</span></span>; 					<span class="comment">//用n个c字符替换从pos开始长度为len的子串</span></span><br><span class="line"><span class="function">string&amp; <span class="title">replace</span> <span class="params">(<span class="type">size_t</span> pos, <span class="type">size_t</span> len, <span class="type">const</span> string&amp; str, <span class="type">size_t</span> subpos, <span class="type">size_t</span> sublen)</span></span>;</span><br><span class="line"><span class="comment">//用str字符串[subpos,sublen)内的字串替换[pos,len)内的字符</span></span><br><span class="line"></span><br><span class="line"><span class="function">string&amp; <span class="title">replace</span> <span class="params">(const_iterator i1, const_iterator i2, <span class="type">const</span> string&amp; str)</span></span>;		<span class="comment">//用str替换迭代器起始位置和结束位置的字符 </span></span><br><span class="line"><span class="function">string&amp; <span class="title">replace</span> <span class="params">(const_iterator i1, const_iterator i2, <span class="type">const</span> <span class="type">char</span>* s)</span></span>; 			<span class="comment">//用C风格字符串s替换迭代器起始位置和结束位置的字符 </span></span><br><span class="line"><span class="function">string&amp; <span class="title">replace</span> <span class="params">(const_iterator i1, const_iterator i2, <span class="type">const</span> <span class="type">char</span>* s, <span class="type">size_t</span> n)</span></span>; <span class="comment">//用s的前n个字符替换指定迭代器位置(从i1到i2)的字符串</span></span><br><span class="line"><span class="function">string&amp; <span class="title">replace</span> <span class="params">(const_iterator i1, const_iterator i2, <span class="type">size_t</span> n, <span class="type">char</span> c)</span></span>; 		<span class="comment">//n个c字符替换从指定迭代器位置(从i1到i2)的字符串</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">swap</span> <span class="params">(string&amp; str)</span></span>;	<span class="comment">//交换两个字符串的内容</span></span><br></pre></td></tr></table></figure></div>

<h3 id="比较"><a href="#比较" class="headerlink" title="比较"></a>比较</h3><hr>
<p>比较方式：字符串比较是按字符的ASCII码进行对比</p>
<p>相等：返回0</p>
<p>前者大于后者：返回1</p>
<p>前者小于后者：返回-1</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">compare</span><span class="params">(<span class="type">const</span> string &amp;s)</span> <span class="type">const</span></span>;		<span class="comment">//与C++风格字符串s比较</span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">compare</span><span class="params">(<span class="type">const</span> <span class="type">char</span> *s )</span> <span class="type">const</span></span>;		<span class="comment">//与C风格字符串s比较</span></span><br></pre></td></tr></table></figure></div>

<h3 id="获取单个字符"><a href="#获取单个字符" class="headerlink" title="获取单个字符"></a>获取单个字符</h3><hr>
<p><code>C++11</code>声明：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="type">const</span> <span class="type">char</span>&amp; <span class="keyword">operator</span>[] (<span class="type">size_t</span> pos) <span class="type">const</span>;	<span class="comment">//通过[]类似数组下标方式取字符</span></span><br><span class="line"><span class="function"><span class="type">const</span> <span class="type">char</span>&amp; <span class="title">at</span> <span class="params">(<span class="type">size_t</span> pos)</span> <span class="type">const</span></span>;			<span class="comment">//通过at方法获取第pos个字符</span></span><br><span class="line"><span class="function"><span class="type">const</span> <span class="type">char</span>&amp; <span class="title">back</span><span class="params">()</span> <span class="type">const</span></span>;					<span class="comment">//返回对字符串最后一个字符的引用</span></span><br><span class="line"><span class="function"><span class="type">const</span> <span class="type">char</span>&amp; <span class="title">front</span><span class="params">()</span> <span class="type">const</span></span>;					<span class="comment">//返回对字符串第一个字符的引用</span></span><br></pre></td></tr></table></figure></div>

<h3 id="插入和删除"><a href="#插入和删除" class="headerlink" title="插入和删除"></a>插入和删除</h3><hr>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">string&amp; <span class="title">insert</span><span class="params">(<span class="type">int</span> pos, <span class="type">const</span> <span class="type">char</span>* s)</span></span>;				<span class="comment">//插入字符串</span></span><br><span class="line"><span class="function">string&amp; <span class="title">insert</span><span class="params">(<span class="type">int</span> pos, <span class="type">const</span> string&amp; str)</span></span>;			<span class="comment">//插入字符串</span></span><br><span class="line"><span class="function">string&amp; <span class="title">insert</span><span class="params">(<span class="type">int</span> pos, <span class="type">int</span> n,<span class="type">char</span> c)</span></span>;				<span class="comment">//在指定位置插入n个字符c	</span></span><br></pre></td></tr></table></figure></div>

<p><code>C++11</code>声明：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">string&amp; <span class="title">erase</span> <span class="params">(<span class="type">size_t</span> pos = <span class="number">0</span>, <span class="type">size_t</span> len = npos)</span></span>;	<span class="comment">//删除从pos开始的len个字符</span></span><br><span class="line"><span class="function">string&amp; <span class="title">erase</span><span class="params">(<span class="type">const</span> <span class="type">size_t</span> Off = <span class="number">0</span>)</span></span>;				<span class="comment">//删除下标从Off开始直到字符串结尾的元素</span></span><br><span class="line"><span class="function">iterator <span class="title">erase</span> <span class="params">(iterator position)</span></span>;					<span class="comment">//删除迭代器指向的元素，返回值是指向删除元素的下一个元素的迭代器</span></span><br><span class="line"><span class="function">iterator <span class="title">erase</span> <span class="params">(iterator first, iterator last)</span></span>;		<span class="comment">//删除迭代器指定范围[first, last)内的字符串，返回值是指向被删除子串的下一个元素的迭代器</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">clear</span><span class="params">()</span> <span class="keyword">noexcept</span></span>;								<span class="comment">//擦除字符串的内容，使之变成一个空字符串</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">pop_back</span><span class="params">()</span></span>;									<span class="comment">//擦除字符串的最后一个字符，从而有效地将其长度减少一个</span></span><br></pre></td></tr></table></figure></div>

<h3 id="获取子串"><a href="#获取子串" class="headerlink" title="获取子串"></a>获取子串</h3><hr>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">string <span class="title">substr</span> <span class="params">(<span class="type">size_t</span> pos = <span class="number">0</span>, <span class="type">size_t</span> len = npos)</span> <span class="type">const</span></span>;	<span class="comment">//返回由pos开始的n个字符组成的字符串</span></span><br></pre></td></tr></table></figure></div>

<h3 id="长度和容量"><a href="#长度和容量" class="headerlink" title="长度和容量"></a>长度和容量</h3><hr>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">size_t</span> <span class="title">size</span><span class="params">()</span> <span class="type">const</span> <span class="keyword">noexcept</span></span>;		<span class="comment">//以字节为单位返回字符串的长度，英文返回值等于字符数量，中文翻倍</span></span><br><span class="line"><span class="function"><span class="type">size_t</span> <span class="title">length</span><span class="params">()</span> <span class="type">const</span> <span class="keyword">noexcept</span></span>;		<span class="comment">//和size()作用相同，没有区别</span></span><br><span class="line"><span class="function"><span class="type">size_t</span> <span class="title">max_size</span><span class="params">()</span> <span class="type">const</span> <span class="keyword">noexcept</span></span>;	<span class="comment">//返回字符串可以达到的最大长度</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">resize</span> <span class="params">(<span class="type">size_t</span> n, <span class="type">char</span> c)</span></span>;		<span class="comment">//将字符串的大小调整为n个字符的长度,可选：如果n大于现有长度，用字符c填充到新的空间</span></span><br><span class="line"><span class="function"><span class="type">size_t</span> <span class="title">capacity</span><span class="params">()</span> <span class="type">const</span> <span class="keyword">noexcept</span></span>;	<span class="comment">//返回当前为字符串分配的存储空间的大小，以字节表示</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">reserve</span> <span class="params">(<span class="type">size_t</span> n = <span class="number">0</span>)</span></span>;		<span class="comment">//改变容量，预留n个字节的空间，减少拷贝、复制等操作时的运行时间</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">shrink_to_fit</span><span class="params">()</span></span>;				<span class="comment">//使字符串减小其分配容量以适应字符内容的大小</span></span><br></pre></td></tr></table></figure></div>

<h3 id="迭代器"><a href="#迭代器" class="headerlink" title="迭代器"></a>迭代器</h3><hr>
<p>待整理：</p>
<p><a class="link"   href="https://www.w3schools.cn/cpp_standard_library/string.html" >C++ 标准库 -  (w3schools.cn) <i class="fa-regular fa-arrow-up-right-from-square fa-sm"></i></a></p>
<h3 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h3><hr>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">const</span> <span class="type">char</span>* <span class="title">c_str</span><span class="params">()</span> <span class="type">const</span> <span class="keyword">noexcept</span></span>;							<span class="comment">//用于获得等效的 C风格字符串</span></span><br><span class="line"><span class="function"><span class="type">const</span> <span class="type">char</span>* <span class="title">data</span><span class="params">()</span> <span class="type">const</span> <span class="keyword">noexcept</span></span>;							<span class="comment">//C++11后与c_str等效，没有区别</span></span><br><span class="line"><span class="function"><span class="type">size_t</span> <span class="title">copy</span> <span class="params">(<span class="type">char</span>* s, <span class="type">size_t</span> len, <span class="type">size_t</span> pos = <span class="number">0</span>)</span> <span class="type">const</span></span>;	  <span class="comment">//在string对象中从第pos个位置开始，取出len个字符存放到char数组s中</span></span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">empty</span><span class="params">()</span> <span class="type">const</span> <span class="keyword">noexcept</span></span>;								<span class="comment">//返回字符串是否为空（即其长度是否为 0）</span></span><br></pre></td></tr></table></figure></div>

<h2 id="vector容器"><a href="#vector容器" class="headerlink" title="vector容器"></a><code>vector</code>容器</h2><hr>
<h3 id="构造函数-2"><a href="#构造函数-2" class="headerlink" title="构造函数"></a>构造函数</h3><hr>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line">vector&lt;T&gt; v;						<span class="comment">//采用模板实现类实现，默认构造函数</span></span><br><span class="line"><span class="built_in">vector</span>(v.<span class="built_in">begin</span>() , v.<span class="built_in">end</span>() ) ;		 <span class="comment">//将v[begin(), end())区间中的元素拷贝给本身</span></span><br><span class="line"><span class="built_in">vector</span>(n,elem);					    <span class="comment">//构造函数将n个elem拷贝给本身</span></span><br><span class="line"><span class="built_in">vector</span>( <span class="type">const</span> vector &amp;vek ) ;		<span class="comment">//拷贝构造函数。</span></span><br></pre></td></tr></table></figure></div>

<h3 id="赋值-1"><a href="#赋值-1" class="headerlink" title="赋值"></a>赋值</h3><hr>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line">vector&amp; <span class="keyword">operator</span>=( <span class="type">const</span> vector &amp;vec);<span class="comment">//重载等号操作符</span></span><br><span class="line"><span class="built_in">assign</span>( beg, end) ;<span class="comment">//将[beg, end)区间中的数据拷贝赋值给本身。</span></span><br><span class="line"><span class="built_in">assign</span>(n,elem);<span class="comment">//将n个elem拷贝赋值给本身。</span></span><br></pre></td></tr></table></figure></div>

<h3 id="容量和大小"><a href="#容量和大小" class="headerlink" title="容量和大小"></a>容量和大小</h3><hr>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">empty</span>( ) ;<span class="comment">//判断容器是否为空</span></span><br><span class="line"><span class="built_in">capacity</span>();<span class="comment">//容器的容量</span></span><br><span class="line"><span class="built_in">size</span>();<span class="comment">//返回容器中元素的个数</span></span><br><span class="line"><span class="built_in">resize</span>(<span class="type">int</span> num ) ;<span class="comment">//重新指定容器的长度为num，若容器变长，则以默认值填充新位置。</span></span><br><span class="line">				<span class="comment">//如果容器变短，则末尾超出容器长度的元素被删除。</span></span><br><span class="line"><span class="built_in">resize</span>(<span class="type">int</span> num，elem);<span class="comment">//重新指定容器的长度为num，若容器变长，则以elem值填充新位置。</span></span><br><span class="line">					<span class="comment">//如果容器变短，则末尾超出容器长度的元素被删除</span></span><br></pre></td></tr></table></figure></div>

<h3 id="插入和删除-1"><a href="#插入和删除-1" class="headerlink" title="插入和删除"></a>插入和删除</h3><hr>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">push_back</span>(ele);<span class="comment">//尾部插入元素ele</span></span><br><span class="line"><span class="built_in">pop_back</span>();<span class="comment">//删除最后一个元素</span></span><br><span class="line"><span class="built_in">insert</span>(const_iterator pos, ele) ;<span class="comment">//迭代器指向位置pos插入元素ele</span></span><br><span class="line"><span class="built_in">insert</span>(const_iterator pos, <span class="type">int</span> count,ele);<span class="comment">//迭代器指向位置pos插入count个元素ele</span></span><br><span class="line"><span class="built_in">erase</span>( const_iterator pos ) ;<span class="comment">//删除迭代器指向的元素</span></span><br><span class="line"><span class="built_in">erase</span>(const_iterator start，const_iterator end);<span class="comment">//删除迭代器从start到end之间的元素</span></span><br><span class="line"><span class="built_in">clear</span>() ;<span class="comment">//删除容器中所有元素</span></span><br></pre></td></tr></table></figure></div>

<h3 id="存、取"><a href="#存、取" class="headerlink" title="存、取"></a>存、取</h3><hr>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">at</span>(<span class="type">int</span> idx ) ;<span class="comment">//返回索引idx所指的数据</span></span><br><span class="line"><span class="keyword">operator</span>[];<span class="comment">//返回索引idx所指的数据</span></span><br><span class="line"><span class="built_in">front</span>( );<span class="comment">//返回容器中第一个数据元素</span></span><br><span class="line"><span class="built_in">back</span>();<span class="comment">//返回容器中最后一个数据元素</span></span><br></pre></td></tr></table></figure></div>

<h3 id="交换"><a href="#交换" class="headerlink" title="交换"></a>交换</h3><hr>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">swap</span>(vec);<span class="comment">//将vec与本身的元素互换</span></span><br></pre></td></tr></table></figure></div>

<h3 id="预留空间"><a href="#预留空间" class="headerlink" title="预留空间"></a>预留空间</h3><hr>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">reserve</span>(<span class="type">int</span> len);<span class="comment">//容器预留len个元素长度，预留位置不初始化，元素不可访问。</span></span><br></pre></td></tr></table></figure></div>

<h2 id="deque容器"><a href="#deque容器" class="headerlink" title="deque容器"></a><code>deque</code>容器</h2><hr>
<p><code>deque</code>容器是双端数组，可以对头和尾进行插入和删除。</p>
<p>与<code>vector</code>的区别：</p>
<ul>
<li><code>vector</code>对于头部的插入删除效率低，数据量越大，效率越低</li>
<li><code>deque</code>相对而言，对头部的插入删除速度回比<code>vector</code>快</li>
<li><code>vector</code>访问元素时的速度会比<code>deque</code>快,这和两者内部实现有关</li>
</ul>
<h3 id="构造函数-3"><a href="#构造函数-3" class="headerlink" title="构造函数"></a>构造函数</h3><hr>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line">deque&lt;T&gt; deqT;				<span class="comment">//默认构造形式</span></span><br><span class="line"><span class="built_in">deque</span>(beg, end) ;			<span class="comment">//构造函数将[beg, end)区间中的元素拷贝给本身。</span></span><br><span class="line"><span class="built_in">deque</span>(n,elem);				<span class="comment">//构造函数将n个elem拷贝给本身。</span></span><br><span class="line"><span class="built_in">deque</span>(<span class="type">const</span> deque &amp;deq); 	<span class="comment">//拷贝构造函数</span></span><br></pre></td></tr></table></figure></div>

<h3 id="赋值-2"><a href="#赋值-2" class="headerlink" title="赋值"></a>赋值</h3><hr>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line">deque&amp; <span class="keyword">operator</span>=(<span class="type">const</span> deque &amp;deq);			<span class="comment">//重载等号操作符</span></span><br><span class="line"><span class="built_in">assign</span>(beg, end) ;							<span class="comment">//将[beg, end)区间中的数据拷贝赋值给本身。</span></span><br><span class="line"><span class="built_in">assign</span>(n,elem ) ;							<span class="comment">//将n个elem拷贝赋值给本身。</span></span><br></pre></td></tr></table></figure></div>

<h3 id="容器大小"><a href="#容器大小" class="headerlink" title="容器大小"></a>容器大小</h3><hr>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line">deque.<span class="built_in">empty</span>();				<span class="comment">//判断容器是否为空</span></span><br><span class="line">deque.<span class="built_in">size</span>();				<span class="comment">//返回容器中元素的个数</span></span><br><span class="line">deque.<span class="built_in">resize</span>(num);			<span class="comment">//重新指定容器的长度为num,若容器变长，则以默认值填充新位置。</span></span><br><span class="line">							<span class="comment">//如果容器变短，则末尾超出容器长度的元素被删除。</span></span><br><span class="line">deque.<span class="built_in">resize</span>(num，elem) ;	<span class="comment">//重新指定容器的长度为num,若容器变长，则以elem值填充新位置。</span></span><br><span class="line">							<span class="comment">//如果容器变短，则末尾超出容器长度的元素被删除。</span></span><br></pre></td></tr></table></figure></div>

<h3 id="插入和删除-2"><a href="#插入和删除-2" class="headerlink" title="插入和删除"></a>插入和删除</h3><hr>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">push_back</span>(elem) ;			<span class="comment">//在容器尾部添加一个数据</span></span><br><span class="line"><span class="built_in">push_front</span>(elem) ;			<span class="comment">//在容器头部插入一个数据</span></span><br><span class="line"><span class="built_in">pop_back</span>() ;				<span class="comment">//删除容器最后一个数据</span></span><br><span class="line"><span class="built_in">pop_front</span>();				<span class="comment">//删除容器第一个数据</span></span><br><span class="line"><span class="built_in">insert</span>(pos,elem);			<span class="comment">//在pos位置插入一个elem元素的拷贝，返回新数据的位置。</span></span><br><span class="line"><span class="built_in">insert</span>(pos,n,elem);			<span class="comment">//在pos位置插入n个elem数据，无返回值。</span></span><br><span class="line"><span class="built_in">insert</span>( pos,beg,end);		<span class="comment">//在pos位置插入[beg,end)区间的数据，无返回值。</span></span><br><span class="line"><span class="built_in">clear</span>();					<span class="comment">//清空容器的所有数据</span></span><br><span class="line"><span class="built_in">erase</span>(beg,end);				<span class="comment">//删除[beg,end)区间的数据，返回下一个数据的位置。</span></span><br><span class="line"><span class="built_in">erase</span>(pos) ;				<span class="comment">//删除pos位置的数据，返回下一个数据的位置。</span></span><br></pre></td></tr></table></figure></div>

<h3 id="存、取-1"><a href="#存、取-1" class="headerlink" title="存、取"></a>存、取</h3><hr>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">at</span>(<span class="type">int</span> idx);		<span class="comment">//返回索引idx所指的数据</span></span><br><span class="line"><span class="keyword">operator</span>[];			<span class="comment">//返回索引idx所指的数据</span></span><br><span class="line"><span class="built_in">front</span>();			<span class="comment">//返回容器中第一个数据元素</span></span><br><span class="line"><span class="built_in">back</span>();				<span class="comment">//返回容器中最后一个数据元素</span></span><br></pre></td></tr></table></figure></div>

<h3 id="排序"><a href="#排序" class="headerlink" title="排序"></a>排序</h3><hr>
<p>示例用法，之后会详细讲：</p>
<p>默认从大到小排序：</p>
<p>需要包含头文件<code>#include &lt;algorithm&gt;</code></p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sort</span>(iterator beg, iterator end);	<span class="comment">//对beg和end区间内元素进行排序</span></span><br></pre></td></tr></table></figure></div>

<h2 id="stack-容器"><a href="#stack-容器" class="headerlink" title="stack 容器"></a><code>stack</code> 容器</h2><hr>
<h3 id="基本概念"><a href="#基本概念" class="headerlink" title="基本概念"></a>基本概念</h3><hr>
<p><code>stack</code>是一种先进后出(First In Last Out,FILO)，后进先出（LIFO）的容器，它的数据结构是栈，只有一个出口，只有栈顶元素才可以被访问，因此<code>stack</code>容器不允许被遍历。</p>
<p>向栈中插入数据叫入栈或压栈，删除栈中数据叫出栈或弹栈。</p>
<p><img  
                     lazyload
                     src="/images/loading.svg"
                     data-src="https://gd-hbimg.huaban.com/8c1c874271d1e5c3649251f076bb2978aeefc8ce42c2-ZHcmVA"
                     
                ></p>
<h3 id="构造函数-4"><a href="#构造函数-4" class="headerlink" title="构造函数"></a>构造函数</h3><hr>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line">stack&lt;T&gt; stk;					<span class="comment">//stack采用模板类实现， stack对象的默认构造形式</span></span><br><span class="line"><span class="built_in">stack</span>(<span class="type">const</span> stack &amp;stk);		<span class="comment">//拷贝构造函数</span></span><br></pre></td></tr></table></figure></div>

<h3 id="赋值-3"><a href="#赋值-3" class="headerlink" title="赋值"></a>赋值</h3><hr>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line">stack&amp; <span class="keyword">operator</span>=( <span class="type">const</span> stack &amp;stk );<span class="comment">//重载等号操作符</span></span><br></pre></td></tr></table></figure></div>

<h3 id="存、取-2"><a href="#存、取-2" class="headerlink" title="存、取"></a>存、取</h3><hr>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">push</span>(elem) ;	<span class="comment">//向栈顶添加元素</span></span><br><span class="line"><span class="built_in">pop</span>();			<span class="comment">//从栈顶移除第一个元素</span></span><br><span class="line"><span class="built_in">top</span>( );			<span class="comment">//返回栈顶元素</span></span><br></pre></td></tr></table></figure></div>

<h3 id="容器大小-1"><a href="#容器大小-1" class="headerlink" title="容器大小"></a>容器大小</h3><hr>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">empty</span>();<span class="comment">//判断堆栈是否为空</span></span><br><span class="line"><span class="built_in">size</span>();<span class="comment">//返回栈的大小</span></span><br></pre></td></tr></table></figure></div>

<h2 id="queue容器"><a href="#queue容器" class="headerlink" title="queue容器"></a><code>queue</code>容器</h2><hr>
<h3 id="基本概念-1"><a href="#基本概念-1" class="headerlink" title="基本概念"></a>基本概念</h3><hr>
<p><code>queue</code>是一种先进先出(First ln First Out,FIFO)的容器，其数据结构是队列，它有两个出口。</p>
<p><img  
                     lazyload
                     src="/images/loading.svg"
                     data-src="https://gd-hbimg.huaban.com/d02191ef7703f72efc7df591fb8587d700e2862ea867-pVoRZp"
                     
                ></p>
<ul>
<li><p>队列容器只允许从一端新增元素，从另一端移除元素。</p>
</li>
<li><p>队列中只有队头和队尾才可以被外界使用，因此队列不允许有遍历行为。</p>
</li>
</ul>
<p>队列中进数据称为：入队</p>
<p>队列中出数据称为：出队</p>
<h3 id="构造函数-5"><a href="#构造函数-5" class="headerlink" title="构造函数"></a>构造函数</h3><hr>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line">queue&lt;T&gt; que;					<span class="comment">//queue采用模板类实现，queue对象的默认构造形式</span></span><br><span class="line"><span class="built_in">queue</span>(<span class="type">const</span> queue &amp;que);		<span class="comment">//拷贝构造函数</span></span><br></pre></td></tr></table></figure></div>

<h3 id="赋值-4"><a href="#赋值-4" class="headerlink" title="赋值"></a>赋值</h3><hr>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line">queue&amp; <span class="keyword">operator</span>=( <span class="type">const</span> queue &amp;que);	<span class="comment">//重载等号操作符</span></span><br></pre></td></tr></table></figure></div>

<h3 id="存、取-3"><a href="#存、取-3" class="headerlink" title="存、取"></a>存、取</h3><hr>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">push</span>(elem) ;	<span class="comment">//往队尾添加元素</span></span><br><span class="line"><span class="built_in">pop</span>();			<span class="comment">//从队头移除第一个元素</span></span><br><span class="line"><span class="built_in">back</span>();			<span class="comment">//返回最后一个元素</span></span><br><span class="line"><span class="built_in">front</span>();		<span class="comment">//返回第一个元素</span></span><br></pre></td></tr></table></figure></div>

<h3 id="容器大小-2"><a href="#容器大小-2" class="headerlink" title="容器大小"></a>容器大小</h3><hr>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">empty</span>();	<span class="comment">//判断堆栈是否为空</span></span><br><span class="line"><span class="built_in">size</span>();		<span class="comment">//返回栈的大小</span></span><br></pre></td></tr></table></figure></div>

<h2 id="list容器"><a href="#list容器" class="headerlink" title="list容器"></a><code>list</code>容器</h2><hr>
<h3 id="基本概念-2"><a href="#基本概念-2" class="headerlink" title="基本概念"></a>基本概念</h3><hr>
<p><code>list</code>容器是一种物理存储单元上非连续的存储结构，数据元素的逻辑顺序是通过链表中的指针链接实现的。其数据结构是双向循环链表。</p>
<p>链表的组成：链表由—系列结点组成。</p>
<p>单链表结点的组成：一个是存储数据元素的数据域，另一个是存储下一个结点地址的指针域。</p>
<p>双向循环链表：其结点指针域有两个指针，分别记录上一个结点和下一个结点的地址。头结点指针域的上一个结点为尾结点，尾结点的下一个指针域为头结点，以此形成循环。</p>
<p><img  
                     lazyload
                     src="/images/loading.svg"
                     data-src="https://gd-hbimg.huaban.com/b6b4b891d33f1aa861bdd6ad528a5544c6622faca1d5-NTIgD6_fw1200webp"
                     
                ></p>
<p>由于链表的存储方式并不是连续的内存空间，因此<code>list</code>容器中的迭代器只支持前移和后移，属于双向迭代器。</p>
<p><code>list</code>的优点：</p>
<ul>
<li>采用动态存储分配，不会造成内存浪费和溢出。</li>
<li>链表执行插入和删除操作十分方便，修改指针即可，不需要移动大量元素。</li>
</ul>
<p>list的缺点：</p>
<ul>
<li>链表灵活，但是空间(指针域)和时间(遍历）额外耗费较大</li>
<li><code>list</code>有一个重要的性质，插入操作和删除（非迭代器指向的元素）操作都不会造成原有<code>list</code>迭代器的失效，这在vector是不成立的。</li>
</ul>
<h3 id="构造函数-6"><a href="#构造函数-6" class="headerlink" title="构造函数"></a>构造函数</h3><hr>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line">list&lt;T&gt; lst;				<span class="comment">//list采用采用模板类实现,对象的默认构造形式:</span></span><br><span class="line"><span class="built_in">list</span>(beg,end) ;				<span class="comment">//构造函数将[beg, end)区间中的元素拷贝给本身。</span></span><br><span class="line"><span class="built_in">list</span>(n,elem);				<span class="comment">//构造函数将n个elem拷贝给本身。</span></span><br><span class="line"><span class="built_in">list</span>(<span class="type">const</span> list &amp;lst);		<span class="comment">//拷贝构造函数。</span></span><br></pre></td></tr></table></figure></div>

<h3 id="赋值和交换"><a href="#赋值和交换" class="headerlink" title="赋值和交换"></a>赋值和交换</h3><hr>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">assign</span>(beg, end) ;					<span class="comment">//将[beg, end)区间中的数据拷贝赋值给本身。</span></span><br><span class="line"><span class="built_in">assign</span>(n,elem);						<span class="comment">//将n个elem拷贝赋值给本身。</span></span><br><span class="line">list&amp; <span class="keyword">operator</span>=(<span class="type">const</span> list &amp;lst);	 <span class="comment">//重载等号操作符</span></span><br><span class="line"><span class="built_in">swap</span>(lst);						   <span class="comment">//将lst与本身的元素互换。</span></span><br></pre></td></tr></table></figure></div>

<h3 id="容器大小-3"><a href="#容器大小-3" class="headerlink" title="容器大小"></a>容器大小</h3><hr>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">size</span>();		<span class="comment">//返回容器中元素的个数</span></span><br><span class="line"><span class="built_in">empty</span>();	<span class="comment">//判断容器是否为空</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">resize</span>(num) ;		  <span class="comment">//重新指定容器的长度为num，若容器变长，则以默认值填充新位置。</span></span><br><span class="line">					<span class="comment">//如果容器变短，则末尾超出容器长度的元素被删除。</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">resize</span>(num,elem);	  <span class="comment">//重新指定容器的长度为num，若容器变长，则以elem值填充新位置。</span></span><br><span class="line">					<span class="comment">//如果容器变短，则末尾超出容器长度的元素被删除。</span></span><br></pre></td></tr></table></figure></div>

<h3 id="插入和删除-3"><a href="#插入和删除-3" class="headerlink" title="插入和删除"></a>插入和删除</h3><hr>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">push_back</span>(elem);		<span class="comment">//在容器尾部加入一个元素</span></span><br><span class="line"><span class="built_in">pop_back</span>();				<span class="comment">//删除容器中最后一个元素</span></span><br><span class="line"><span class="built_in">push_front</span>(elem);		<span class="comment">//在容器开头插入一个元素</span></span><br><span class="line"><span class="built_in">pop_front</span>();			<span class="comment">//从容器开头移除第一个元素</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">insert</span>(pos,elem);		<span class="comment">//在pos位置插elem元素的拷贝，返回新数据的位置。</span></span><br><span class="line"><span class="built_in">insert</span>(pos,n,elem);		<span class="comment">//在pos位置插入n个elem数据，无返回值。</span></span><br><span class="line"><span class="built_in">insert</span>(pos,beg,end);	<span class="comment">//在pos位置插入[beg,end)区间的数据，无返回值。</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">clear</span>();			<span class="comment">//移除容器的所有数据</span></span><br><span class="line"><span class="built_in">erase</span>(beg,end);		<span class="comment">//删除[beg,end)区间的数据，返回下一个数据的位置。</span></span><br><span class="line"><span class="built_in">erase</span>(pos);			<span class="comment">//删除pos位置的数据，返回下一个数据的位置。</span></span><br><span class="line"><span class="built_in">remove</span>(elem);		<span class="comment">//删除容器中所有与elem值匹配的元素。</span></span><br></pre></td></tr></table></figure></div>

<h3 id="存、取-4"><a href="#存、取-4" class="headerlink" title="存、取"></a>存、取</h3><hr>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">front</span>();	<span class="comment">//返回第一个元素。</span></span><br><span class="line"><span class="built_in">back</span>( );	<span class="comment">//返回最后一个元素。</span></span><br></pre></td></tr></table></figure></div>

<h3 id="反转和排序"><a href="#反转和排序" class="headerlink" title="反转和排序"></a>反转和排序</h3><hr>
<p>将容器中的元素反转，以及将容器中的数据进行排序。</p>
<p>&#x3D;&#x3D;所有不支持随机访问迭代器的容器，不可以用标准算法&#x3D;&#x3D;。</p>
<p>不支持随机访问迭代器的容器，内部会提供对应一些算法。</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">reverse</span>(;	<span class="comment">//反转链表</span></span><br><span class="line"><span class="built_in">sort</span>();		<span class="comment">//链表排序，该函数是成员函数</span></span><br></pre></td></tr></table></figure></div>

<h2 id="pair对组"><a href="#pair对组" class="headerlink" title="pair对组"></a><code>pair</code>对组</h2><hr>
<p>对组指：成对出现的数据。利用对组可以使函数返回两个数据，对组嵌套对组时，可使函数返回多个数据。</p>
<h3 id="创建"><a href="#创建" class="headerlink" title="创建"></a>创建</h3><hr>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">pair&lt;type，type&gt; <span class="title">p</span> <span class="params">(value1, value2)</span></span>;</span><br><span class="line">pair&lt;type, type&gt; p = <span class="built_in">make_pair</span>(value1, value2);</span><br></pre></td></tr></table></figure></div>

<h3 id="数据获取"><a href="#数据获取" class="headerlink" title="数据获取"></a>数据获取</h3><hr>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line">p.first; 	<span class="comment">//获取pair对象p的第一个数据</span></span><br><span class="line">p.second; 	<span class="comment">//获取pair对象p的第二个数据  </span></span><br></pre></td></tr></table></figure></div>

<h2 id="set容器和multiset容器"><a href="#set容器和multiset容器" class="headerlink" title="set容器和multiset容器"></a><code>set</code>容器和<code>multiset</code>容器</h2><hr>
<h3 id="基本概念-3"><a href="#基本概念-3" class="headerlink" title="基本概念"></a>基本概念</h3><hr>
<p>所有元素都会在插入时自动被排序。</p>
<p><code>set</code>和<code>multiset</code>属于关联式容器，底层结构是用二叉树实现。</p>
<p><code>set</code>和<code>multiset</code>区别：</p>
<ul>
<li><code>set</code>不允许容器中有重复的元素 </li>
<li><code>multiset</code>允许容器中有重复的元素</li>
</ul>
<h3 id="构造函数-7"><a href="#构造函数-7" class="headerlink" title="构造函数"></a>构造函数</h3><hr>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line">set&lt;T&gt; st;				<span class="comment">//默认构造函数:</span></span><br><span class="line"><span class="built_in">set</span>(<span class="type">const</span> set &amp;st) ;	<span class="comment">//拷贝构造函数</span></span><br></pre></td></tr></table></figure></div>

<h3 id="赋值-5"><a href="#赋值-5" class="headerlink" title="赋值"></a>赋值</h3><hr>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line">set&amp; <span class="keyword">operator</span>=(<span class="type">const</span> set &amp;st);<span class="comment">//重载等号操作符</span></span><br></pre></td></tr></table></figure></div>

<h3 id="插入和删除-4"><a href="#插入和删除-4" class="headerlink" title="插入和删除"></a>插入和删除</h3><hr>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">insert</span>(elem) ;		<span class="comment">//在容器中插入元素。</span></span><br><span class="line"><span class="built_in">clear</span>();			<span class="comment">//清除所有元素</span></span><br><span class="line"><span class="built_in">erase</span>(pos);			<span class="comment">//删除pos迭代器所指的元素，返回下一个元素的迭代器。</span></span><br><span class="line"><span class="built_in">erase</span>(beg，end);		<span class="comment">//删除区间[beg,end)的所有元素，返回下一个元素的迭代器。</span></span><br><span class="line"><span class="built_in">erase</span>(elem);		<span class="comment">//删除容器中值为elem的元素。</span></span><br></pre></td></tr></table></figure></div>

<p>对于<code>set</code>容器，因为不允许插入相同数据，因此插入函数返回值是一个<code>pair&lt;iterator,bool&gt;</code>的数据类型，可用于判断是否插入成功。</p>
<p>示例代码：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line">set&lt;<span class="type">int</span>&gt; s;</span><br><span class="line">pair&lt;set&lt;<span class="type">int</span>&gt; : :iterator，<span class="type">bool</span>&gt;ret = s.<span class="built_in">insert</span>(<span class="number">10</span>);<span class="keyword">if</span> (ret.second) </span><br><span class="line">&#123;</span><br><span class="line">	cout &lt;&lt;<span class="string">&quot;插入成功!&quot;</span>&lt;&lt;endl;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">else</span> </span><br><span class="line">&#123;</span><br><span class="line">	cout &lt;&lt;<span class="string">&quot;插入失败!”&lt;&lt;endl;</span></span><br><span class="line"><span class="string">&#125;</span></span><br></pre></td></tr></table></figure></div>

<h3 id="容器大小-4"><a href="#容器大小-4" class="headerlink" title="容器大小"></a>容器大小</h3><hr>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">size</span>( ) ;<span class="comment">//返回容器中元素的数目</span></span><br><span class="line"><span class="built_in">empty</span>();<span class="comment">//判断容器是否为空</span></span><br></pre></td></tr></table></figure></div>

<h3 id="交换-1"><a href="#交换-1" class="headerlink" title="交换"></a>交换</h3><hr>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">swap</span>(st);<span class="comment">//交换两个集合容器</span></span><br></pre></td></tr></table></figure></div>

<h3 id="查找"><a href="#查找" class="headerlink" title="查找"></a>查找</h3><hr>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">find</span>(key) ;		<span class="comment">//查找key是否存在,若存在，返回该键的元素的迭代器;若不存在，返回set.end(;</span></span><br><span class="line"><span class="built_in">count</span>(key);		<span class="comment">//统计key的元素个数</span></span><br></pre></td></tr></table></figure></div>

<h3 id="自定义排序规则"><a href="#自定义排序规则" class="headerlink" title="自定义排序规则"></a>自定义排序规则</h3><hr>
<ul>
<li><p>方式1：</p>
<p>定义一个类，重载<code>operator()</code>运算符，定义比较函数，将类名作为<code>set</code>容器的第二个类型。同时，这也是传统写法。</p>
<p>示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MyCompare</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="type">bool</span> <span class="title">operator</span><span class="params">()</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span> <span class="type">const</span><span class="comment">//新C++标准必须添加const，旧标准可不添加</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="keyword">return</span> a &gt; b;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">test</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	set&lt;<span class="type">int</span>, MyCompare&gt; S1;</span><br><span class="line">	S1.<span class="built_in">insert</span>(<span class="number">98</span>);</span><br><span class="line">	S1.<span class="built_in">insert</span>(<span class="number">52</span>);</span><br><span class="line">	S1.<span class="built_in">insert</span>(<span class="number">25</span>);</span><br><span class="line">	S1.<span class="built_in">insert</span>(<span class="number">74</span>);</span><br><span class="line">	S1.<span class="built_in">insert</span>(<span class="number">82</span>);</span><br><span class="line">	S1.<span class="built_in">insert</span>(<span class="number">78</span>);</span><br><span class="line">	<span class="keyword">for</span> (<span class="keyword">auto</span>&amp; i : S1) </span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; i &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>
</li>
<li><p>方式2：</p>
<p>定义一个<code>lambda</code>表达式，并使用<code>decltype</code>获取类型，或者配合<code>std::function</code>及<code>reinterpret_cast</code>使用，作为<code>set</code>容器的第二个类型。</p>
<p>示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">test</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">auto</span> f = [](<span class="type">int</span> a, <span class="type">int</span> b) -&gt;<span class="type">bool</span> &#123;<span class="keyword">return</span> a &gt; b; &#125;;</span><br><span class="line">	<span class="function">set&lt;<span class="type">int</span>, <span class="title">decltype</span><span class="params">(f)</span>&gt; <span class="title">S2</span><span class="params">(f)</span></span>;</span><br><span class="line">	S2.<span class="built_in">insert</span>(<span class="number">98</span>);</span><br><span class="line">	S2.<span class="built_in">insert</span>(<span class="number">52</span>);</span><br><span class="line">	S2.<span class="built_in">insert</span>(<span class="number">25</span>);</span><br><span class="line">	S2.<span class="built_in">insert</span>(<span class="number">74</span>);</span><br><span class="line">	S2.<span class="built_in">insert</span>(<span class="number">82</span>);</span><br><span class="line">	S2.<span class="built_in">insert</span>(<span class="number">78</span>);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">for</span> (<span class="keyword">auto</span>&amp; i : S2) </span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; i &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>
</li>
<li><p>方式3：</p>
<p>可以直接定义一个函数，通过转换将函数指针转换为一个类指针，再解引用，作为<code>set</code>容器的第二个类型。</p>
<p>示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">bool</span> <span class="title">myCompare</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> a &gt; b;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">test</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">auto</span> f = myCompare;</span><br><span class="line">    <span class="keyword">using</span> FuncPtr = <span class="built_in">bool</span>(*)(<span class="type">int</span>, <span class="type">int</span>);</span><br><span class="line">    set&lt;<span class="type">int</span>, std::function&lt;<span class="type">bool</span>(<span class="type">int</span>, <span class="type">int</span>)&gt;&gt; <span class="built_in">S</span>(<span class="built_in">reinterpret_cast</span>&lt;FuncPtr&amp;&gt;(f));<span class="comment">//需要包含头文件#include &lt;functional&gt;</span></span><br><span class="line">    S.<span class="built_in">insert</span>(<span class="number">98</span>); </span><br><span class="line">    S.<span class="built_in">insert</span>(<span class="number">52</span>);</span><br><span class="line">    S.<span class="built_in">insert</span>(<span class="number">25</span>);</span><br><span class="line">    S.<span class="built_in">insert</span>(<span class="number">74</span>);</span><br><span class="line">    S.<span class="built_in">insert</span>(<span class="number">82</span>);</span><br><span class="line">    S.<span class="built_in">insert</span>(<span class="number">78</span>);</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">auto</span>&amp; i : S) </span><br><span class="line">    &#123;</span><br><span class="line">        cout &lt;&lt; i &lt;&lt; endl;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>这段代码定义了一个函数<code>myCompare</code>，它接受两个<code>int</code>类型的参数<code>a</code>和<code>b</code>，并返回一个<code>bool</code>类型的值。函数的作用是比较<code>a</code>和<code>b</code>的大小，如果<code>a</code>大于<code>b</code>，则返回<code>true</code>，否则返回<code>false</code>。</p>
<p>接下来，代码定义了一个变量<code>f</code>，它的类型是<code>myCompare</code>函数的指针。然后，使用<code>using</code>关键字定义了一个别名<code>FuncPtr</code>，它表示一个指向<code>bool</code>类型函数的指针，该函数接受两个<code>int</code>类型的参数。</p>
<p>最后，代码定义了一个<code>set</code>容器<code>S</code>，它的元素类型是<code>int</code>，比较函数类型是<code>std::function&lt;bool(int, int)&gt;</code>。</p>
<p>在构造<code>set</code>对象时，使用<code>reinterpret_cast</code>将<code>f</code>的类型转换为<code>FuncPtr&amp;</code>类型，并将其作为比较函数传递给<code>set</code>对象。这样，<code>set</code>容器就可以使用<code>myCompare</code>函数来比较元素的大小了。</p>
</li>
</ul>
<p>这三段代码的输出结果是一样的，都是按降序输出<code>set</code>容器中的元素。</p>
<p>输出结果：</p>
<div class="highlight-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">98</span><br><span class="line">82</span><br><span class="line">78</span><br><span class="line">74</span><br><span class="line">52</span><br><span class="line">25</span><br></pre></td></tr></table></figure></div>

<h2 id="map容器和multimap容器"><a href="#map容器和multimap容器" class="headerlink" title="map容器和multimap容器"></a><code>map</code>容器和<code>multimap</code>容器</h2><hr>
<p><code>map</code>中所有元素都是<code>pair</code></p>
<p><code>pair</code>中第一个元素为<code>key</code>(键值)，起到索引作用，第二个元素为<code>value</code>(实值)</p>
<p>所有元素都会根据元素的键值自动排</p>
<p><code>map/multimap</code>属于关联式容器，底层结构是用二叉树实现。</p>
<p>可以根据<code>key</code>值快速找到<code>value</code>值</p>
<p><code>map</code>不允许容器中有重复<code>key</code>值元素</p>
<p><code>multimap</code>允许容器中有重复<code>key</code>值元素</p>
<h3 id="构造和赋值函数"><a href="#构造和赋值函数" class="headerlink" title="构造和赋值函数"></a>构造和赋值函数</h3><hr>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line">map&lt;T1, T2&gt; mp;			<span class="comment">//map默认构造函数:</span></span><br><span class="line"><span class="built_in">map</span>(<span class="type">const</span> map &amp;mp);		<span class="comment">//拷贝构造函数</span></span><br><span class="line"></span><br><span class="line">map&amp; <span class="keyword">operator</span>=(<span class="type">const</span> map &amp;mp);<span class="comment">//重载等号操作符</span></span><br></pre></td></tr></table></figure></div>

<h3 id="大小和交换"><a href="#大小和交换" class="headerlink" title="大小和交换"></a>大小和交换</h3><hr>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">size</span>();		<span class="comment">//返回容器中元素的数目</span></span><br><span class="line"><span class="built_in">empty</span>();	<span class="comment">//判断容器是否为空</span></span><br><span class="line"><span class="built_in">swap</span>(st);	<span class="comment">//交换两个集合容器</span></span><br></pre></td></tr></table></figure></div>

<h3 id="插入和删除-5"><a href="#插入和删除-5" class="headerlink" title="插入和删除"></a>插入和删除</h3><hr>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">insert</span>(elem) ;		<span class="comment">//在容器中插入元素。</span></span><br><span class="line"><span class="built_in">clear</span>();			<span class="comment">//清除所有元素</span></span><br><span class="line"><span class="built_in">erase</span>(pos);			<span class="comment">//删除pos迭代器所指的元素，返回下一个元素的迭代器。</span></span><br><span class="line"><span class="built_in">erase</span>(beg,end) ;	<span class="comment">//删除区间[beg,end)的所有元素，返回下一个元素的迭代器。</span></span><br><span class="line"><span class="built_in">erase</span>(key);		<span class="comment">//删除容器中值为key的元素。</span></span><br></pre></td></tr></table></figure></div>

<p>插入方式示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line">map&lt;<span class="type">int</span>,<span class="type">int</span>&gt; m;</span><br><span class="line"><span class="comment">//插入</span></span><br><span class="line"><span class="comment">//第一种</span></span><br><span class="line">m.<span class="built_in">insert</span>(<span class="built_in">pair</span>&lt;<span class="type">int</span>,<span class="type">int</span>&gt;(<span class="number">1</span>, <span class="number">10</span>));</span><br><span class="line"><span class="comment">//第二种</span></span><br><span class="line">m.<span class="built_in">insert</span>(<span class="built_in">make_pair</span>(<span class="number">2</span>, <span class="number">20</span>));</span><br><span class="line"><span class="comment">//第三种</span></span><br><span class="line">m.<span class="built_in">insert</span>(map&lt;<span class="type">int</span>，<span class="type">int</span>&gt;::<span class="built_in">value_type</span>(<span class="number">3</span>, <span class="number">30</span>));</span><br><span class="line"><span class="comment">//第四种</span></span><br><span class="line">m[<span class="number">4</span>] = <span class="number">40</span>;</span><br></pre></td></tr></table></figure></div>

<h3 id="查找和删除"><a href="#查找和删除" class="headerlink" title="查找和删除"></a>查找和删除</h3><hr>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">find</span>(key); 		<span class="comment">//查找key是否存在,若存在，返回该键的元素的迭代器;若不存在，返回set.end();</span></span><br><span class="line"><span class="built_in">count</span>(key);		<span class="comment">//统计key的元素个数</span></span><br></pre></td></tr></table></figure></div>

<h3 id="自定义排序"><a href="#自定义排序" class="headerlink" title="自定义排序"></a>自定义排序</h3><hr>
<p>同<code>set</code>容器，定义一个仿函数或<code>lambda</code>和函数通过转换后，作为模板参数列表的第三个参数。</p>
<h2 id="函数对象"><a href="#函数对象" class="headerlink" title="函数对象"></a>函数对象</h2><hr>
<h3 id="基本概念-4"><a href="#基本概念-4" class="headerlink" title="基本概念"></a>基本概念</h3><hr>
<ul>
<li><p>重载函数调用操作符的类，其对象常称为函数对象。</p>
</li>
<li><p>函数对象使用重载的()时，行为类似函数调用，也叫仿函数。</p>
</li>
<li><p>函数对象(仿函数)的本质是一个类，不是一个函数。</p>
</li>
</ul>
<h3 id="函数对象的使用"><a href="#函数对象的使用" class="headerlink" title="函数对象的使用"></a>函数对象的使用</h3><hr>
<p>函数对象在使用时，可以像普通函数那样调用，可以有参数，可以有返回值，函数对象超出普通函数的概念。</p>
<p>函数对象可以有自己的状态（类成员），这叫做”闭包“，也叫“上下文关联的函数”。</p>
<p>函数对象可以作为参数传递。</p>
<h2 id="谓词（predicate）的概念"><a href="#谓词（predicate）的概念" class="headerlink" title="谓词（predicate）的概念"></a>谓词（predicate）的概念</h2><hr>
<p>在编程中，谓词（Predicate）是指用于判断某个条件是否为真的函数或表达式。</p>
<p>谓词通常返回一个布尔值（True或False），表示某种断言的真假。</p>
<p>在各种编程语言和环境中，谓词都扮演着重要的角色，用于条件判断、过滤、排序、搜索等各种操作。</p>
<p>谓词的概念在逻辑学中也很常见，用于描述命题的真值。</p>
<p>在编程中，谓词是对逻辑思维的一种抽象，让我们能够将复杂的条件逻辑转换为可执行的代码。</p>
<p>在C++中，谓词可以是以下几种形式：</p>
<ul>
<li><p>函数谓词：一个函数，接受一个或多个参数，并返回布尔值。</p>
<p>通过调用该函数并传入相应参数，可以判断特定条件是否为真。</p>
<p>例如，使用一个函数谓词来判断一个数是否是偶数：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 函数谓词：判断一个数是否是偶数</span></span><br><span class="line"><span class="function"><span class="type">bool</span> <span class="title">is_even</span><span class="params">(<span class="type">int</span> number)</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> number % <span class="number">2</span> == <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// 使用谓词函数判断一个数是否为偶数</span></span><br><span class="line">    <span class="type">bool</span> result = <span class="built_in">is_even</span>(<span class="number">10</span>);</span><br><span class="line">    std::cout &lt;&lt; std::boolalpha &lt;&lt; result &lt;&lt; std::endl;  <span class="comment">// 输出: true</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>在这段代码中，定义了一个函数谓词 <code>is_even</code> 来判断一个数是否是偶数。</p>
<p>然后，在 <code>main</code> 函数中使用该谓词来判断整数 <code>10</code> 是否为偶数，并将结果打印出来。</p>
<p>并通过 <code>std::boolalpha</code> 操纵符来让输出以布尔值形式显示。</p>
<p>在C++中，谓词通常是用于判断条件是否为真的函数或函数对象（仿函数）。</p>
<p>谓词的主要特点是它返回<code>bool</code>类型的结果，用于条件判断。</p>
<ul>
<li><p>返回<code>bool</code>类型的仿函数也称为谓词。在C++中，谓词通常是一种函数对象（仿函数），它可以是普通函数、Lambda表达式或重载了函数调用运算符<code>operator()</code>的类对象。</p>
</li>
<li><p>如果<code>operator()</code>接受一个参数，那么谓词被称为一元谓词。一元谓词用于对单个元素进行条件判断。</p>
</li>
<li><p>如果<code>operator()</code>接受两个参数，那么谓词被称为二元谓词。二元谓词用于对两个元素进行条件判断，例如在排序算法中用于定义排序顺序。</p>
<p>以此类推……</p>
</li>
</ul>
</li>
<li><p>lambda 表达式谓词：一种匿名函数，通常用于简单的条件判断或过滤操作。</p>
<p>&#x3D;&#x3D;（关于lambda表达式将在以后学习，这里可不做了解，直接跳过即可）&#x3D;&#x3D;</p>
<p>例如，使用lambda表达式谓词来筛选出列表中的偶数：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;vector&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;algorithm&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    std::vector&lt;<span class="type">int</span>&gt; numbers = &#123;<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>, <span class="number">6</span>&#125;;</span><br><span class="line">    std::vector&lt;<span class="type">int</span>&gt; even_numbers;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 使用Lambda表达式谓词来筛选出列表中的偶数</span></span><br><span class="line">    std::<span class="built_in">copy_if</span>(numbers.<span class="built_in">begin</span>(), numbers.<span class="built_in">end</span>(), std::<span class="built_in">back_inserter</span>(even_numbers),</span><br><span class="line">                 [](<span class="type">int</span> x) &#123; <span class="keyword">return</span> x % <span class="number">2</span> == <span class="number">0</span>; &#125;);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 输出筛选结果</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> num : even_numbers) </span><br><span class="line">    &#123;</span><br><span class="line">        std::cout &lt;&lt; num &lt;&lt; <span class="string">&quot; &quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    std::cout &lt;&lt; std::endl;  <span class="comment">// 输出: 2 4 6</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>
</li>
<li><p>逻辑表达式谓词：通过逻辑运算符（如AND、OR、NOT）将多个条件组合成一个复合条件。</p>
<p>例如，使用逻辑表达式谓词判断一个数是否在某个范围内：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">bool</span> <span class="title">is_in_range</span><span class="params">(<span class="type">int</span> number)</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> (number &gt;= <span class="number">10</span>) &amp;&amp; (number &lt;= <span class="number">20</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 使用谓词函数判断一个数是否在特定范围内</span></span><br><span class="line"><span class="type">bool</span> result = <span class="built_in">is_in_range</span>(<span class="number">15</span>);</span><br><span class="line">cout &lt;&lt; result &lt;&lt; endl;  <span class="comment">// 输出: 1 (True)</span></span><br></pre></td></tr></table></figure></div></li>
</ul>
<p>在STL（标准模板库）中，谓词广泛应用于各种算法函数，如<code>std::find_if</code>、<code>std::sort</code>、<code>std::remove_if</code>等。</p>
<p>它们允许我们根据自定义的条件对容器中的元素进行过滤、排序、搜索等操作，提高了代码的灵活性和可复用性。</p>
<p>谓词的使用在STL和现代C++编程中非常常见。</p>
<h2 id="STL的内建函数对象"><a href="#STL的内建函数对象" class="headerlink" title="STL的内建函数对象"></a>STL的内建函数对象</h2><hr>
<h3 id="基本概念-5"><a href="#基本概念-5" class="headerlink" title="基本概念"></a>基本概念</h3><hr>
<p>STL（Standard Template Library）的内建函数对象（Builtin Function Objects）是一组在C++标准库中提供的用于函数对象操作的预定义类模板。</p>
<p>函数对象是类对象，其行为类似于函数，可以像函数一样被调用。</p>
<p>这些内建函数对象封装了常见的操作，如算术运算、逻辑运算、比较运算等，可以在各种算法和容器操作中使用。</p>
<p>STL内建函数对象的概念主要包括以下几个方面：</p>
<ul>
<li><p>仿函数（Function Object）：</p>
<p>STL的内建函数对象通常被称为仿函数，因为它们是函数对象的模拟，具有函数调用运算符<code>operator()</code>，可以像函数一样被调用。</p>
<p>STL提供了一些预定义的仿函数类模板，同时也可以自定义自己的仿函数。</p>
</li>
<li><p>预定义的函数对象：</p>
<p>STL标准库中预定义了一些常用的函数对象类模板，这些函数对象用于执行各种操作，如加法、减法、乘法、除法、逻辑与、逻辑或、比较等。</p>
</li>
<li><p>函数对象适用于算法：</p>
<p>STL中的算法往往接受函数对象作为参数，这样就可以在执行算法时，通过函数对象来定义特定的操作逻辑。</p>
<p>例如，<code>std::transform</code>、<code>std::sort</code>、<code>std::find_if</code>等算法都接受函数对象，从而允许我们以不同的方式处理元素。</p>
</li>
</ul>
<p>STL内建函数对象的使用使得编写更加通用和灵活的代码变得更加容易。</p>
<p>通过使用预定义的函数对象，我们可以避免重复编写某些常见的操作逻辑，并且提高代码的可读性和可维护性。</p>
<p>此外，STL内建函数对象也为编写泛型代码提供了强大的工具，使得算法和容器操作可以适用于不同类型的数据。</p>
<p>STL内建的一些函数对象的分类：</p>
<ul>
<li>算术仿函数</li>
<li>关系仿函</li>
<li>逻辑仿函数</li>
</ul>
<p>等等……</p>
<p>这些仿函数所产生的对象，用法和一般函数完全相同，使用内建函数对象，需要包含头文件<code>#include&lt;functional&gt;</code></p>
<h3 id="算数仿函数"><a href="#算数仿函数" class="headerlink" title="算数仿函数"></a>算数仿函数</h3><hr>
<p>作用：实现四则运算</p>
<p>其中&#96;&#96;negate&#96;是一元运算，其他都是二元运算仿函数</p>
<p>模板原型：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt; T plus&lt;T&gt;			<span class="comment">//加法仿函数</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt; T minus&lt;T&gt;		<span class="comment">//减法仿函数</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt; T multiplies&lt;T&gt;	<span class="comment">//乘法仿函数</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt; T divides&lt;T&gt;		<span class="comment">//除法仿函数</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt; T modulus&lt;T&gt;		<span class="comment">//取模仿函数</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt; T negate&lt;T&gt;		<span class="comment">//取反仿函数</span></span><br></pre></td></tr></table></figure></div>

<h3 id="关系仿函数"><a href="#关系仿函数" class="headerlink" title="关系仿函数"></a>关系仿函数</h3><hr>
<p>功能描述：实现关系对比</p>
<p>模板原型：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt; <span class="type">bool</span> equal_to&lt;T&gt;		<span class="comment">//等于</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt; <span class="type">bool</span> not_equal_to&lt;T&gt;	<span class="comment">//不等于</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt; <span class="type">bool</span> greater&lt;T&gt;		<span class="comment">//大于</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt; <span class="type">bool</span> greater_equal&lt;T&gt;	<span class="comment">//大于等于</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt; <span class="type">bool</span> less&lt;T&gt;			<span class="comment">//小于</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt; <span class="type">bool</span> less_equal&lt;T&gt;	<span class="comment">//小于等于</span></span><br></pre></td></tr></table></figure></div>

<h3 id="逻辑仿函数"><a href="#逻辑仿函数" class="headerlink" title="逻辑仿函数"></a>逻辑仿函数</h3><hr>
<p>功能：实现逻辑运算</p>
<p>模板原型：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt; <span class="type">bool</span> logical_and&lt;T&gt;	<span class="comment">//逻辑与</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt; <span class="type">bool</span> logical_or&lt;T&gt;	<span class="comment">//逻辑或</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">class</span> <span class="title class_">T</span>&gt; <span class="type">bool</span> logical_not&lt;T&gt;	<span class="comment">//逻辑非</span></span><br></pre></td></tr></table></figure></div>

<h2 id="STL常用算法"><a href="#STL常用算法" class="headerlink" title="STL常用算法"></a>STL常用算法</h2><hr>
<h3 id="概述"><a href="#概述" class="headerlink" title="概述"></a>概述</h3><hr>
<p>算法主要是由头文件<code>&lt;algorithm&gt;</code>、<code>&lt;functional&gt;</code>、<code> &lt;numeric&gt;</code>组成。</p>
<ul>
<li><p><code>&lt;algorithm&gt;</code>是所有<code>STL</code>头文件中最大的一个，范围涉及到比较、交换、查找、遍历操作、复制、修改等等</p>
</li>
<li><p><code>&lt;numeric&gt;</code>体积很小，只包括几个在序列上面进行简单数学运算的模板函数</p>
</li>
<li><p><code>&lt;functional&gt;</code>定义了一些模板类,用以声明函数对象。</p>
</li>
</ul>
<h3 id="常用遍历算法"><a href="#常用遍历算法" class="headerlink" title="常用遍历算法"></a>常用遍历算法</h3><hr>
<h4 id="for-each遍历"><a href="#for-each遍历" class="headerlink" title="for_each遍历"></a><code>for_each</code>遍历</h4><hr>
<p>功能描述：实现遍历容器</p>
<p>函数原型：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line">for_each(iterator beg, iterator end, _func );<span class="comment">//遍历算法遍历容器元素</span></span><br><span class="line"><span class="comment">// beg开始迭代器</span></span><br><span class="line"><span class="comment">// end结束迭代器</span></span><br><span class="line"><span class="comment">// _func函数或者函数对象</span></span><br></pre></td></tr></table></figure></div>

<h3 id="常用查找算法"><a href="#常用查找算法" class="headerlink" title="常用查找算法"></a>常用查找算法</h3><hr>
<h4 id="find-查找"><a href="#find-查找" class="headerlink" title="find 查找"></a><code>find</code> 查找</h4><hr>
<p>功能：查找指定元素，找到返回指定元素的迭代器，找不到返回结束迭代器<code>end()</code>。</p>
<p>函数原型：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">find</span>(iterator beg, iteratbr end, value) ;<span class="comment">//按值查找元素，找到返回指定位置迭代器，找不到返回结束迭代器位置</span></span><br><span class="line"><span class="comment">// beg开始迭代器</span></span><br><span class="line"><span class="comment">// end结束迭代器</span></span><br><span class="line"><span class="comment">// value查找的元素</span></span><br></pre></td></tr></table></figure></div>

<p>注意！查找自定义数据类型，需要重载<code>operator==</code>。</p>
<h4 id="find-if按条件查找"><a href="#find-if按条件查找" class="headerlink" title="find_if按条件查找"></a><code>find_if</code>按条件查找</h4><hr>
<p>功能：按条件查找元素函数。</p>
<p>函数原型：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">find_if</span>(iterator beg, iterator end，_Pred);<span class="comment">//按值查找元素，找到返回指定位置迭代器，找不到返回结束迭代器位置</span></span><br><span class="line"><span class="comment">// beg 开始迭代器</span></span><br><span class="line"><span class="comment">//end结束迭代器</span></span><br><span class="line"><span class="comment">//_Pred函数或者谓词(返回bool类型的仿函数)</span></span><br></pre></td></tr></table></figure></div>

<h4 id="adjacent-find查找相邻重复元素"><a href="#adjacent-find查找相邻重复元素" class="headerlink" title="adjacent_find查找相邻重复元素"></a><code>adjacent_find</code>查找相邻重复元素</h4><hr>
<p>功能：查找相邻重复元素。</p>
<p>函数原型：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">adjacent_find</span>(iterator beg， iterator end ) ;<span class="comment">//查找相邻重复元素,返回相邻元素的第一个位置的迭代器</span></span><br><span class="line"><span class="comment">//beg 开始迭代器</span></span><br><span class="line"><span class="comment">// end结束迭代器</span></span><br></pre></td></tr></table></figure></div>

<h4 id="binary-search二分查找"><a href="#binary-search二分查找" class="headerlink" title="binary_search二分查找"></a><code>binary_search</code>二分查找</h4><hr>
<p>功能：查找指定元素是否存在。</p>
<p>函数原型：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">bool</span> <span class="title">binary_search</span><span class="params">(iterator beg, iterator end, value)</span></span>;<span class="comment">//查找指定的元素，查到返回true否则false</span></span><br><span class="line"><span class="comment">// beg 开始迭代器</span></span><br><span class="line"><span class="comment">// end结束迭代器</span></span><br><span class="line"><span class="comment">// value查找的元素</span></span><br></pre></td></tr></table></figure></div>

<p>注意！查找的容器或数组必须已经排好序的（有序序列），在无序序列中不可用。</p>
<h3 id="常用统计算法"><a href="#常用统计算法" class="headerlink" title="常用统计算法"></a>常用统计算法</h3><h4 id="count统计个数"><a href="#count统计个数" class="headerlink" title="count统计个数"></a><code>count</code>统计个数</h4><hr>
<p>功能：统计相同元素个数。</p>
<p>函数原型：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">count</span>( iterator beg, iterator end，valle);<span class="comment">//统计元素出现次数</span></span><br><span class="line"><span class="comment">// beg开始迭代器</span></span><br><span class="line"><span class="comment">// end结束迭代器</span></span><br><span class="line"><span class="comment">// value统计的元素</span></span><br></pre></td></tr></table></figure></div>

<p>对于自定义数据类型，需要重载<code>operator==</code>。</p>
<h4 id="count-if按条件统计个数"><a href="#count-if按条件统计个数" class="headerlink" title="count_if按条件统计个数"></a><code>count_if</code>按条件统计个数</h4><hr>
<p>功能：按条件统计元素个数</p>
<p>函数原型：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">count_if</span>(iterator beg, iterator end，_pred ) ;<span class="comment">//按条件统计元素出现次数</span></span><br><span class="line"><span class="comment">// beg开始迭代器</span></span><br><span class="line"><span class="comment">// end结束迭代器</span></span><br><span class="line"><span class="comment">// _Pred谓词</span></span><br></pre></td></tr></table></figure></div>

<h3 id="常用排序算法"><a href="#常用排序算法" class="headerlink" title="常用排序算法"></a>常用排序算法</h3><h4 id="sort排序"><a href="#sort排序" class="headerlink" title="sort排序"></a><code>sort</code>排序</h4><hr>
<p>功能：对容器内元素进行排序。</p>
<p>函数原型：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">sort</span>(iterator beg, iterator end, _Pred);<span class="comment">//按值查找元素，找到返回指定位置迭代器，找不到返回结束迭代器位置</span></span><br><span class="line"><span class="comment">// beg开始迭代器</span></span><br><span class="line"><span class="comment">// end结束迭代器</span></span><br><span class="line"><span class="comment">// _Pred 谓词</span></span><br></pre></td></tr></table></figure></div>

<p>谓词参数可不指定。不指定则默认升序排序</p>
<h4 id="random-shuffle随机排序"><a href="#random-shuffle随机排序" class="headerlink" title="random_shuffle随机排序"></a><code>random_shuffle</code>随机排序</h4><hr>
<p>功能：对容器内元素进行随机排序，打乱原有序列。</p>
<p>函数原型：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">random_shuffle</span>(iterator beg, iterator end ) ;<span class="comment">//指定范围内的元素随机调整次序</span></span><br><span class="line"><span class="comment">// beg开始迭代器</span></span><br><span class="line"><span class="comment">// end结束迭代器</span></span><br></pre></td></tr></table></figure></div>

<p>注意！需要指定随机数种子，才能使每次排序顺序都不一样。</p>
<h3 id="常用拷贝和替换算法"><a href="#常用拷贝和替换算法" class="headerlink" title="常用拷贝和替换算法"></a>常用拷贝和替换算法</h3><hr>
<h4 id="copy复制"><a href="#copy复制" class="headerlink" title="copy复制"></a><code>copy</code>复制</h4><hr>
<p>功能：将容器内指定范围的元素拷贝到另—容器中。</p>
<p>函数原型：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">copy</span>(iterator beg， iterator end，iterator dest;<span class="comment">//按值查找元素，找到返回指定位置迭代器，找不到返回结束迭代器位置ll beg开始迭代器</span></span><br><span class="line"><span class="comment">// end 结束迭代器</span></span><br><span class="line"><span class="comment">// dest目标起始迭代器</span></span><br></pre></td></tr></table></figure></div>

<p>注意！目标容器需要提前开辟空间。</p>
<h4 id="transform搬运容器"><a href="#transform搬运容器" class="headerlink" title="transform搬运容器"></a><code>transform</code>搬运容器</h4><hr>
<p>功能描述：搬运容器到另个容器中</p>
<p>函数原型：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">transform</span>(iterator beg1, iterator end1, iterator beg2, _func);<span class="comment">//beg1源容器开始迭代器</span></span><br><span class="line"><span class="comment">//end1源容器结束迭代器</span></span><br><span class="line"><span class="comment">//beg2目标容器开始迭代器</span></span><br><span class="line"><span class="comment">//_func函数或者函数对象</span></span><br></pre></td></tr></table></figure></div>

<p>注意！目标容器需要提前开辟空间。</p>
<h4 id="replace替换"><a href="#replace替换" class="headerlink" title="replace替换"></a><code>replace</code>替换</h4><hr>
<p>功能：将容器内指定范围的旧元素修改为新元素。</p>
<p>函数原型：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">replace</span>(iterator beg, iterator end,oldvalue，newvalue) ;<span class="comment">//将区间内旧元素替换成新元素</span></span><br><span class="line"><span class="comment">// beg开始迭代器</span></span><br><span class="line"><span class="comment">// end结束迭代器</span></span><br><span class="line"><span class="comment">// oldvalue 旧元素</span></span><br><span class="line"><span class="comment">// newvalue 新元素</span></span><br></pre></td></tr></table></figure></div>

<h4 id="replace-if按条件替换"><a href="#replace-if按条件替换" class="headerlink" title="replace_if按条件替换"></a><code>replace_if</code>按条件替换</h4><hr>
<p>功能：将区间内满足条件的元素，替换成指定元素。</p>
<p>函数原型：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">replace_if</span>(iterator beg, iterator end, _pred, newvalue);<span class="comment">//按条件替换元素，满足条件的替换成指定元素</span></span><br><span class="line"><span class="comment">// beg开始迭代器</span></span><br><span class="line"><span class="comment">// end结束迭代器</span></span><br><span class="line"><span class="comment">//_pred谓词</span></span><br><span class="line"><span class="comment">// newvalue替换的新元素</span></span><br></pre></td></tr></table></figure></div>

<h4 id="swap交换"><a href="#swap交换" class="headerlink" title="swap交换"></a><code>swap</code>交换</h4><hr>
<p>功能：互换两个容器的元素。</p>
<p>函数原型：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">swap</span>(container c1,container c2);<span class="comment">//互换两个容器的元素</span></span><br><span class="line"><span class="comment">// c1容器1</span></span><br><span class="line"><span class="comment">// c2容器2</span></span><br></pre></td></tr></table></figure></div>

<h3 id="常用算术生成算法"><a href="#常用算术生成算法" class="headerlink" title="常用算术生成算法"></a>常用算术生成算法</h3><hr>
<p>算术生成算法属于小型算法，使用时包含的头文件为<code>#include &lt;numeric&gt;</code>。</p>
<h4 id="accumulate累加"><a href="#accumulate累加" class="headerlink" title="accumulate累加"></a><code>accumulate</code>累加</h4><hr>
<p>功能：计算区间内容器元素累计总和。</p>
<p>函数原型：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">accumulate</span>(iterator beg, iterator end, value) ;<span class="comment">//计算容器元素累计总和</span></span><br><span class="line"><span class="comment">// beg 开始迭代器</span></span><br><span class="line"><span class="comment">// end结束迭代器</span></span><br><span class="line"><span class="comment">// value起始累加值</span></span><br></pre></td></tr></table></figure></div>

<h4 id="fill填充"><a href="#fill填充" class="headerlink" title="fill填充"></a><code>fill</code>填充</h4><hr>
<p>功能：向容器中填充指定的元素。</p>
<p>函数原型：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">fill</span>(iterator beg, iterator end,value);<span class="comment">//向容器中填充元素</span></span><br><span class="line"><span class="comment">// beg开始迭代器</span></span><br><span class="line"><span class="comment">// end结束迭代器</span></span><br><span class="line"><span class="comment">// value填充的值</span></span><br></pre></td></tr></table></figure></div>

<p>注意！目标容器需要提前开辟空间。</p>
<h3 id="常用集合算法"><a href="#常用集合算法" class="headerlink" title="常用集合算法"></a>常用集合算法</h3><hr>
<h4 id="set-intersection求交集"><a href="#set-intersection求交集" class="headerlink" title="set_intersection求交集"></a><code>set_intersection</code>求交集</h4><hr>
<p>功能：求两个容器的交集。</p>
<p>函数原型：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">set_intersection</span>(iterator beg1，iterator end1，iterator beg2，iterator end2，iterator dest);<span class="comment">//求两个集合的交集</span></span><br><span class="line"><span class="comment">// beg1容器1开始迭代器</span></span><br><span class="line"><span class="comment">// end1容器1结束迭代器</span></span><br><span class="line"><span class="comment">// beg2容器2开始迭代器</span></span><br><span class="line"><span class="comment">// end2容器2结束迭代器</span></span><br><span class="line"><span class="comment">// dest目标容器开始迭代器</span></span><br></pre></td></tr></table></figure></div>

<p>注意！</p>
<ul>
<li>目标容器需要提前开辟空间。最特殊情况大容器包含小容器开辟空间取小容器的size即可。</li>
<li>返回值为一个迭代器，指向交集的最后一个元素。之后可通过该迭代器重新指定容器大小。</li>
<li>两个集合必须是有序序列。</li>
</ul>
<h4 id="set-unionl求并集"><a href="#set-unionl求并集" class="headerlink" title="set_unionl求并集"></a><code>set_unionl</code>求并集</h4><hr>
<p>功能：求两个容器的并集。</p>
<p>函数原型：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">set_union</span>(iterator beg1, iterator end1, iterator beg2, iterator end2, iterator dest);<span class="comment">//求两个集合的并集</span></span><br><span class="line"><span class="comment">// beg1容器1开始迭代霸</span></span><br><span class="line"><span class="comment">// end1容器1结束迭代器</span></span><br><span class="line"><span class="comment">// beg2容器2开始迭代器</span></span><br><span class="line"><span class="comment">// end2容器2结束迭代器</span></span><br><span class="line"><span class="comment">// dest目标容器开始迭代器</span></span><br></pre></td></tr></table></figure></div>

<p>注意！</p>
<ul>
<li>目标容器需要提前开辟空间。最特殊情况两个容器没有公共元素，开辟空间大小取两个容器size和即可。</li>
<li>返回值为一个迭代器，指向并集的最后一个元素。之后可通过该迭代器重新指定容器大小。</li>
<li>两个集合必须是有序序列。</li>
</ul>
<h4 id="set-difference求差集"><a href="#set-difference求差集" class="headerlink" title="set_difference求差集"></a><code>set_difference</code>求差集</h4><hr>
<p>功能：求两个容器的差集。</p>
<p>函数原型：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">set_difference</span>(iterator beg1, iterator end1 ,iterator beg2, iterator end2, iterator dest);<span class="comment">//求两个集合的差集</span></span><br><span class="line"><span class="comment">// beg1容器1开始迭代器</span></span><br><span class="line"><span class="comment">// end1容器1结束迭代器</span></span><br><span class="line"><span class="comment">// beg2容器2开始迭代器</span></span><br><span class="line"><span class="comment">// end2容器2结束迭代器</span></span><br><span class="line"><span class="comment">// dest目标容器开始迭代器</span></span><br></pre></td></tr></table></figure></div>

<p>注意！</p>
<ul>
<li>目标容器需要提前开辟空间。最特殊情况，两个容器没有交集，取第一个容器的size即可（也可取两个容器中较大的size作为目标容器开辟空间）</li>
<li>返回值为一个迭代器，指向差集的最后一个元素。之后可通过该迭代器重新指定容器大小。</li>
<li>两个集合必须是有序序列。</li>
</ul>
<h3 id="其他常用算法"><a href="#其他常用算法" class="headerlink" title="其他常用算法"></a>其他常用算法</h3><hr>
<h4 id="merge合并容器"><a href="#merge合并容器" class="headerlink" title="merge合并容器"></a><code>merge</code>合并容器</h4><hr>
<p>功能：将两个容器元素合并，并存储到另—容器中</p>
<p>函数原型：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">merge</span>(iterator beg1，iterator end1， iterator beg2，iterator end2，iterator dest);<span class="comment">//容器元素合并，并存储到另一容器中</span></span><br><span class="line"><span class="comment">// beg1容器1开始迭代器</span></span><br><span class="line"><span class="comment">// end1容器1结束迭代器</span></span><br><span class="line"><span class="comment">// beg2容器2开始迭代器</span></span><br><span class="line"><span class="comment">// end2容器2结束迭代器</span></span><br><span class="line"><span class="comment">// dest目标容器开始迭代器</span></span><br></pre></td></tr></table></figure></div>

<p>注意！原始的两个容器必须是有序的且必须是一致的顺序（同为升序或降序）。</p>
<h4 id="reverse"><a href="#reverse" class="headerlink" title="reverse"></a><code>reverse</code></h4><hr>
<p>功能：将容器内元素进行反转</p>
<p>函数原型：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">reverse</span>(iterator beg，iterator end);<span class="comment">//反转指定范围的元素</span></span><br><span class="line"><span class="comment">// beg开始迭代器</span></span><br><span class="line"><span class="comment">// end结束迭代器</span></span><br></pre></td></tr></table></figure></div>

<p><a class="link"   href="https://www.bilibili.com/video/BV1z24y1b7LZ?vd_source=03da7de0e80f9ff60fdeca7b512ba3f5" >https://www.bilibili.com/video/BV1z24y1b7LZ?vd_source=03da7de0e80f9ff60fdeca7b512ba3f5 <i class="fa-regular fa-arrow-up-right-from-square fa-sm"></i></a>)</p>
<h1 id="C-文件操作"><a href="#C-文件操作" class="headerlink" title="C++文件操作"></a>C++文件操作</h1><h2 id="概述-1"><a href="#概述-1" class="headerlink" title="概述"></a>概述</h2><hr>
<p>C++中对文件操作需要包含头文件<code>#include &lt;fstream&gt;</code></p>
<p>文件类型分为两种:</p>
<ul>
<li>文本文件-文件以文本的ASCII码形式存储在计算机中</li>
<li>二进制文件-文件以文本的二进制形式存储在计算机中，用户一般不能直接读懂它们</li>
</ul>
<p>操作文件的三大类:</p>
<ul>
<li><code>ofstream</code>：写操作</li>
<li><code>ifstream</code>：读操作</li>
<li><code>fstream</code>：读写操作</li>
</ul>
<p>文件操作的一般步骤：</p>
<ul>
<li><p>包含头文件<code>#include &lt;fstream&gt;</code></p>
</li>
<li><p>根据读写需要创建对应的流对象</p>
</li>
<li><p>打开文件时要判断文件是否打开成功</p>
<p>对于写入数据，如果文件打开失败一般会创建一个新文件，因此可能总是打开成功的。</p>
</li>
<li><p>读取或写入</p>
</li>
<li><p>关闭文件调用</p>
<p>当程序终止时，系统会自动关闭刷新所有流，释放所有分配的内存，并关闭所有打开的文件。</p>
<p>但程序员应该养成一个好习惯，在程序终止前关闭所有打开的文件。</p>
</li>
</ul>
<h2 id="文件的打开方式"><a href="#文件的打开方式" class="headerlink" title="文件的打开方式"></a>文件的打开方式</h2><hr>
<p>在对一个文件进行读或写之前需要将这个文件打开，因此有了打开方式这一说法。</p>
<p>打开方式：</p>
<table>
<thead>
<tr>
<th align="left">打开方式</th>
<th align="left">解释</th>
</tr>
</thead>
<tbody><tr>
<td align="left"><code>ios::in</code></td>
<td align="left">打开文件用于读取</td>
</tr>
<tr>
<td align="left"><code>ios::out</code></td>
<td align="left">打开文件用于写入</td>
</tr>
<tr>
<td align="left"><code>ios::ate	</code></td>
<td align="left">打开文件后的初始位置：文件尾</td>
</tr>
<tr>
<td align="left"><code>ios::app</code></td>
<td align="left">追加模式，所有写入都追加到文件末尾。</td>
</tr>
<tr>
<td align="left"><code>ios::trunc</code></td>
<td align="left">如果文件存在先删除，再创建（覆写）</td>
</tr>
<tr>
<td align="left"><code>ios::binary</code></td>
<td align="left">二进制方式</td>
</tr>
</tbody></table>
<p>注意：</p>
<ul>
<li><p>文件打开方式可以配合使用，利用<code>|</code> 操作符。</p>
<p>例如：用二进制方式写文件<code>ios::binary | ios::out</code></p>
</li>
</ul>
<h2 id="文本文件"><a href="#文本文件" class="headerlink" title="文本文件"></a>文本文件</h2><hr>
<h3 id="写文件"><a href="#写文件" class="headerlink" title="写文件"></a>写文件</h3><hr>
<p>写文件步骤如下：</p>
<ul>
<li><p>包含头文件：<code>#include &lt;fstream&gt;</code></p>
</li>
<li><p>创建流对象：<code>ofstream ofs</code></p>
</li>
<li><p>打开文件</p>
<p>指定文件路径和打开方式：<code>ofs.open(&quot;文件路径&quot;, 打开方式式)</code></p>
</li>
<li><p>写数据：<code>ofs &lt;&lt;&quot;写入的数据&quot;</code></p>
</li>
<li><p>关闭文件：<code>ofs.close()</code></p>
</li>
</ul>
<p>示例（写入一个文本文件）：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;fstream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    ofstream ofs;<span class="comment">//创建流对象</span></span><br><span class="line">	ofs.<span class="built_in">open</span>(<span class="string">&quot;test.txt&quot;</span>, ios::out);<span class="comment">//指定打开文件路径和打开方式</span></span><br><span class="line">	ofs &lt;&lt; <span class="string">&quot;Hello world&quot;</span> &lt;&lt; endl;<span class="comment">//写入 Hello world 并换行</span></span><br><span class="line">    ofs &lt;&lt; <span class="string">&quot;Hello C++&quot;</span>&lt;&lt; endl;<span class="comment">//写入 Hello C++ 并换行</span></span><br><span class="line">	ofs.<span class="built_in">close</span>();<span class="comment">//关闭文件</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>要点总结</p>
<ul>
<li><p>文件操作必须包含头文件<code>fstream</code></p>
</li>
<li><p>读文件可以利用<code>ofstream</code> ，或者<code>fstream</code>类</p>
</li>
<li><p>打开文件时候需要指定操作文件的路径，以及打开方式。</p>
</li>
<li><p>利用<code>&lt;&lt;</code>以向文件中写数据</p>
</li>
<li><p>操作完毕，要关闭文件</p>
</li>
</ul>
<h3 id="读文件"><a href="#读文件" class="headerlink" title="读文件"></a>读文件</h3><hr>
<p>读文件步骤如下:</p>
<ul>
<li>包含头文件<code>#include &lt;fstream&gt;</code></li>
<li>创建流对象<code>ifstream ifs</code></li>
<li>打开文件并判断文件是否打开成功<code>ifs.open(&quot;文件路径&quot;,打开方式)</code></li>
<li>读数据：四种方式读取</li>
<li>关闭文件<code>ifs.close()</code>;</li>
</ul>
<p>读数据四种方式：</p>
<ul>
<li>第一种，将文件中的内容全部放到字符数组中，注意：数组大小必须&gt;&#x3D;文件内容的字符数量，否则程序崩溃</li>
<li>第二种，将文件中的内容一行一行的写入字符数组中，并根据数组大小，写入相应大小的内容</li>
<li>第三种，将文件中的内容写入string字符串中</li>
<li>第四种，将文件中的内容一个一个的读取并写入到一个<code>char</code>字符中，一个一个的输出，效率低</li>
</ul>
<p>代码示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//读数据，四种方式</span></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">read1</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	ifstream ifs;<span class="comment">//创建流对象</span></span><br><span class="line">	ifs.<span class="built_in">open</span>(<span class="string">&quot;test.txt&quot;</span>, ios::in);<span class="comment">//打开文件并</span></span><br><span class="line">	<span class="keyword">if</span>(!ifs.<span class="built_in">is_open</span>())<span class="comment">//判断文件是否打开成功</span></span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;文件打开失败&quot;</span> &lt;&lt; endl;</span><br><span class="line">		<span class="keyword">return</span>;</span><br><span class="line">	&#125;		</span><br><span class="line">	<span class="comment">//读数据</span></span><br><span class="line">		<span class="comment">//第一种，将文件中的内容全部放到字符数组中，注意：数组大小必须&gt;=文件内容的字符数量，否则程序崩溃</span></span><br><span class="line">	<span class="type">char</span> buf[<span class="number">1024</span>] = &#123; <span class="number">0</span> &#125;;</span><br><span class="line">	<span class="keyword">while</span> (ifs &gt;&gt; buf)</span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; buf ;</span><br><span class="line">	&#125;</span><br><span class="line">	cout &lt;&lt; endl;</span><br><span class="line">	ifs.<span class="built_in">close</span>();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">read2</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	ifstream ifs;<span class="comment">//创建流对象</span></span><br><span class="line">	ifs.<span class="built_in">open</span>(<span class="string">&quot;test.txt&quot;</span>, ios::in);<span class="comment">//打开文件并</span></span><br><span class="line">	<span class="keyword">if</span> (!ifs.<span class="built_in">is_open</span>())<span class="comment">//判断文件是否打开成功</span></span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;文件打开失败&quot;</span> &lt;&lt; endl;</span><br><span class="line">		<span class="keyword">return</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">//读数据</span></span><br><span class="line">		<span class="comment">//第二种，将文件中的内容一行一行的写入字符数组中，并根据数组大小，写入相应大小的内容</span></span><br><span class="line">	<span class="type">char</span> buf[<span class="number">1024</span>] = &#123; <span class="number">0</span> &#125;;</span><br><span class="line">	<span class="keyword">while</span> (ifs.<span class="built_in">getline</span>(buf, <span class="built_in">sizeof</span>(buf)))<span class="comment">//ifs下的getline()函数，参数一：数组首地址，参数二：数组大小</span></span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; buf &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line">	ifs.<span class="built_in">close</span>();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">read3</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	ifstream ifs;<span class="comment">//创建流对象</span></span><br><span class="line">	ifs.<span class="built_in">open</span>(<span class="string">&quot;test.txt&quot;</span>, ios::in);<span class="comment">//打开文件并</span></span><br><span class="line">	<span class="keyword">if</span> (!ifs.<span class="built_in">is_open</span>())<span class="comment">//判断文件是否打开成功</span></span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;文件打开失败&quot;</span> &lt;&lt; endl;</span><br><span class="line">		<span class="keyword">return</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">//读数据</span></span><br><span class="line">		<span class="comment">//第三种，将文件中的内容写入string字符串中</span></span><br><span class="line">	string buf;</span><br><span class="line">	<span class="keyword">while</span> (<span class="built_in">getline</span>(ifs, buf)) <span class="comment">//全局函数getline()，参数一：流，这里是文件读取流，参数二：字符串</span></span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; buf &lt;&lt; endl;</span><br><span class="line">	&#125;</span><br><span class="line">	ifs.<span class="built_in">close</span>();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">read4</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	ifstream ifs;<span class="comment">//创建流对象</span></span><br><span class="line">	ifs.<span class="built_in">open</span>(<span class="string">&quot;test.txt&quot;</span>, ios::in);<span class="comment">//打开文件并</span></span><br><span class="line">	<span class="keyword">if</span> (!ifs.<span class="built_in">is_open</span>())<span class="comment">//判断文件是否打开成功</span></span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;文件打开失败&quot;</span> &lt;&lt; endl;</span><br><span class="line">		<span class="keyword">return</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="comment">//读数据</span></span><br><span class="line">		<span class="comment">//第四种，将文件中的内容一个一个的读取并写入到一个char字符中，一个一个的输出，效率低</span></span><br><span class="line">	<span class="type">char</span> c;</span><br><span class="line">	<span class="keyword">while</span> ((c = ifs.<span class="built_in">get</span>()) != EOF)	<span class="comment">//EOF	end of file	</span></span><br><span class="line">        							<span class="comment">//使用ifs下的get()函数，获取单个字符，并赋值给字符c，然后输出，并判断是否读取到文章尾部，到尾部则停止</span></span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; c;</span><br><span class="line">	&#125;</span><br><span class="line">	ifs.<span class="built_in">close</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<h2 id="二进制文件"><a href="#二进制文件" class="headerlink" title="二进制文件"></a>二进制文件</h2><hr>
<h3 id="写文件-1"><a href="#写文件-1" class="headerlink" title="写文件"></a>写文件</h3><hr>
<p>以二进制的方式对文件进行读写，操作打开方式要指定为<code>ios:binary</code>。</p>
<p>二进制方式写文件主要利用流对象调用成员函数<code>write</code>。</p>
<p>对于一些复杂数据类型如：<code>string</code>，可以正确写入，但无法正确读取，需要避免使用。</p>
<p>整体步骤与上述文本文件读写步骤一样。</p>
<ul>
<li>函数原型:<code>ostream&amp; write( const char * buffer ,int len)</code></li>
<li>参数解释:字符指针<code>buffer</code>指向内存中一段存储空间。<code>len</code>是读写的字节数</li>
</ul>
<p>示例（以二进制形式写入自定义数据类型）：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span><span class="string">&lt;fstream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span><span class="string">&lt;string&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">person</span></span><br><span class="line">&#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="type">unsigned</span> <span class="type">int</span> age = <span class="number">0</span>;</span><br><span class="line">	<span class="type">char</span> name[<span class="number">64</span>] = <span class="string">&quot;&quot;</span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">void</span> <span class="title">write_2</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="function">ofstream <span class="title">ofs</span><span class="params">(<span class="string">&quot;test2.txt&quot;</span>, ios::out | ios::binary)</span></span>;</span><br><span class="line">	person 张三&#123; <span class="number">20</span>, <span class="string">&quot;张三&quot;</span> &#125;;</span><br><span class="line">	ofs.<span class="built_in">write</span>((<span class="type">const</span> <span class="type">char</span>*)&amp;张三, <span class="built_in">sizeof</span>(张三));</span><br><span class="line">	ofs.<span class="built_in">close</span>();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="built_in">write_2</span>();</span><br><span class="line">	<span class="built_in">system</span>(<span class="string">&quot;pause&quot;</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure></div>

<h3 id="读文件-1"><a href="#读文件-1" class="headerlink" title="读文件"></a>读文件</h3><hr>
<p>以二进制的方式对文件进行读写，操作打开方式要指定为<code>ios:binary</code>。</p>
<p>二进制方式读文件主要利用流对象调用成员函数<code>read</code>。</p>
<p>对于一些复杂数据类型如：<code>string</code>，无法正确读取，需要避免使用。</p>
<p>整体步骤与上述文本文件读写步骤一样。</p>
<ul>
<li>函数原型:<code>istream&amp; read(char *buffer,int len) </code></li>
<li>参数解释:字符指针<code>buffer</code>指向内存中一段存储空间。<code>len</code>是读写的字节数</li>
</ul>
<p>示例（将上一个示例中的自定义数据类型读回来）：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">void</span> <span class="title">read_2</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="function">ifstream <span class="title">ifs</span><span class="params">(<span class="string">&quot;test2.txt&quot;</span>, ios::in | ios::binary)</span></span>;</span><br><span class="line">	<span class="keyword">if</span> (!ifs.<span class="built_in">is_open</span>())<span class="comment">//判断文件是否打开成功</span></span><br><span class="line">	&#123;</span><br><span class="line">		cout &lt;&lt; <span class="string">&quot;文件打开失败&quot;</span> &lt;&lt; endl;</span><br><span class="line">		<span class="keyword">return</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	person p2;</span><br><span class="line">	ifs.<span class="built_in">read</span>((<span class="type">char</span>*)&amp;p2, <span class="built_in">sizeof</span>(p2));</span><br><span class="line">	ifs.<span class="built_in">close</span>();</span><br><span class="line">	cout &lt;&lt; p2.name &lt;&lt; <span class="string">&quot;  &quot;</span> &lt;&lt; p2.age &lt;&lt; endl;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<h2 id="补充说明"><a href="#补充说明" class="headerlink" title="补充说明"></a>补充说明</h2><hr>
<p>C++新标准（C++11及更高版本）引入了一些新特性和库，其中一些与文件操作相关。</p>
<p>下面是一些在C++新标准中与文件操作相关的内容：</p>
<ol>
<li><p><code>std::filesystem</code>：C++17引入了 <code>&lt;filesystem&gt;</code> 头文件，提供了对文件系统的访问和操作。它包含了一组类和函数，使您能够进行文件和目录的创建、复制、移动、重命名等操作，以及获取文件的属性和路径等信息。</p>
</li>
<li><p><code>std::ifstream::open()</code> 重载：</p>
<p>C++11引入了一个新的 <code>std::ifstream</code> 成员函数的重载形式，使您能够更方便地打开文件并指定打开模式。</p>
<p>例如，您可以使用 <code>std::ifstream::open(const std::string&amp; filename, std::ios_base::openmode mode)</code> 重载函数来打开文件并指定读取模式、写入模式、二进制模式等。</p>
</li>
<li><p><code>std::ofstream::open()</code> 重载：</p>
<p>类似地，C++11还引入了 <code>std::ofstream</code> 的 <code>open()</code> 函数重载形式，使您能够更灵活地打开文件并指定打开模式。</p>
</li>
<li><p>移动语义和右值引用：C++11引入了移动语义和右值引用的概念，使您能够更高效地处理文件流对象。通过使用 <code>std::move()</code> 函数，您可以将文件流对象的所有权从一个对象转移到另一个对象，而无需执行深拷贝。</p>
</li>
<li><p>等等……</p>
</li>
</ol>
<p>这些是C++新标准中与文件操作相关的主要内容。它们提供了更强大和灵活的文件操作功能，使您能够更方便地处理文件和文件系统。</p>
<p>请注意，具体实现可能会因编译器和标准库的版本而有所不同，因此请查阅相关的参考文档以了解详细的用法和支持情况。</p>
<h1 id="预处理器"><a href="#预处理器" class="headerlink" title="预处理器"></a>预处理器</h1><h2 id="define"><a href="#define" class="headerlink" title="#define"></a><code>#define</code></h2><h3 id="简单的文本替换"><a href="#简单的文本替换" class="headerlink" title="简单的文本替换"></a>简单的文本替换</h3><p>语法描述：</p>
<p><code>#define name stuff</code></p>
<p>有了这条指令以后，每当<code>name</code>出现在这条指令后面时，预处理器就会把它替换成<code>stuff</code>。</p>
<p>替换文本并不限于数值字面值常量。使用<code>#define</code>指令，可以把任何文本替换到程序中。</p>
<p>代码示例：</p>
<div class="highlight-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">#define reg 			register</span><br><span class="line">#define do_fprever 		for(;;)</span><br><span class="line">#define CASE			break;case;</span><br></pre></td></tr></table></figure></div>

<p>第1个定义只是为关键字<code>register</code>创建了一个简短的别名。</p>
<p>这个较短的名字使各个声明更容易通过制表符进行排列。</p>
<p>第2条声明用一个更具描述性的符号来代替一种用于实现无限循环的for语句类型。</p>
<p>最后一个<code>#define</code>定义了一种简短记法，以便在<code>switch</code>语句中使用。它自动地把一个<code>break</code>放在每个<code>case</code>之前，这使得<code>switch</code>语句看上去更像其他语言的<code>case</code>语句。</p>
<p>如果定义中的stuff非常长，它可以分成几行，除了最后一行之外，每行的末尾都要加一个反斜杠，如下面的例子所示：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> DEBUG_PRINT		printf( <span class="string">&quot;File %s line %d: &quot;</span> \</span></span><br><span class="line"><span class="meta">								<span class="string">&quot;x=%d, y=%d, z=%d&quot;</span>, \</span></span><br><span class="line"><span class="meta">								__FTLE__, __LINE_,  \</span></span><br><span class="line"><span class="meta">								x, y，z )</span></span><br></pre></td></tr></table></figure></div>

<p>这里利用了相邻的字符串常量被自动连接为一个字符串这个特性。当你调试一个存在许多涉及一组变量的不同计算过程的程序时，这种类型的声明非常有用。</p>
<p>你可以很容易地插入一条调试语句打印出它们的当前值。</p>
<p>示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line">x *= <span class="number">2</span>;</span><br><span class="line">y += x;</span><br><span class="line">z = x * y;</span><br><span class="line">DEBUG_PRINT;</span><br></pre></td></tr></table></figure></div>

<p>&#x3D;&#x3D;注意！：&#x3D;&#x3D;</p>
<p>这条语句在DEBUG_PRINT后面加了一个分号，所以你不应该在宏定义的尾部加上分号。</p>
<p>如果你这样做了，结果就会产生两条语句：一条<code>printf</code>语句后面再加一条空语句。</p>
<p>有些场合只允许出现一条语句，如果放入两条语句就会出现问题，例如：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span>(<span class="comment">//...</span></span><br><span class="line">)&#123;<span class="comment">//...</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//...</span></span><br><span class="line">DEBUG_PRINT;</span><br><span class="line"><span class="keyword">else</span></span><br><span class="line">&#123;<span class="comment">//...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>所以，不要滥用这种技巧。如果相同的代码需要出现在程序的几个地方，通常更好的方法是把它实现为一个函数。后续将详细讨论<code>#define</code>宏和函数之间的优劣。</p>
<h3 id="宏函数"><a href="#宏函数" class="headerlink" title="宏函数"></a>宏函数</h3><p><code>#define</code>机制包括了一个规定，允许把参数替换到文本中，这种实现通常称为宏或宏函数。</p>
<p>下面是宏函数的声明方式：</p>
<p><code>#define　name(parameter-list)　 stuff</code></p>
<p>其中，<code>parameter-list</code>（参数列表）是一个由逗号分隔的符号列表，它们可能出现在<code>stuff</code>中。</p>
<p>参数列表的左括号必须与<code>name</code>紧邻。如果两者之间有任何空白存在，参数列表就会被解释为<code>stuff</code>的一部分。</p>
<p><em>&#x3D;&#x3D;以下内容宏函数全部简称为宏。&#x3D;&#x3D;</em></p>
<p>当宏被调用时，名字后面是一个由逗号分隔的值的列表，每个值都与宏定义中的一个参数相对应，整个列表用一对括号包围。</p>
<p>当参数出现在程序中时，与每个参数对应的实际值都将被替换到<code>stuff</code>中。</p>
<hr>
<p>这里有一个宏，它接受一个参数：<code>#define SQUARE(x)　　x * x</code></p>
<p>如果在上述声明之后，你把<code>SQUARE( 5 )</code>置于程序中，预处理器就会用下面这个表达式替换上面的表达式：<code>5 * 5</code></p>
<p>&#x3D;&#x3D;注意！：&#x3D;&#x3D;</p>
<p>这个宏存在一个问题。</p>
<p>观察下面的代码段：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line">a = <span class="number">5</span>;</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;%d\n&quot;</span>, <span class="built_in">SQUARE</span>( a + <span class="number">1</span> ));</span><br></pre></td></tr></table></figure></div>

<p>你可能觉得这段代码将打印36这个值。事实上，它将打印11。</p>
<p>参数<code>x</code>被文本<code>a + 1</code>替换，所以这条语句实际上变成了：</p>
<div class="highlight-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">printf(&quot;%d\n&quot;, a + 1 * a + 1);</span><br></pre></td></tr></table></figure></div>

<p>可知：由替换产生的表达式并没有按照预想的次序进行求值。</p>
<p>在宏定义中加上两个括号，这个问题便很轻松地解决了：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> SQUARE(x)　　(x) * (x)</span></span><br></pre></td></tr></table></figure></div>

<p>在前面那个例子里，预处理器现在将用下面这条语句执行替换，从而产生预期的结果：</p>
<div class="highlight-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">printf(&quot;%d\n&quot;, (a + 1) * (a + 1));</span><br></pre></td></tr></table></figure></div>

<hr>
<p>这里有另外一个宏定义：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span>　DOUBLE(x)　　(x) + (x)</span></span><br></pre></td></tr></table></figure></div>

<p>定义中使用了括号，用于避免前面出现的问题。</p>
<p>但是，使用这个宏，可能会出现另外一个不同的错误。下面这段代码将打印出什么值？</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line">a = <span class="number">5</span>;</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;%d\n&quot;</span>, <span class="number">10</span> * <span class="built_in">DOUBLE</span>( a ) );</span><br></pre></td></tr></table></figure></div>

<p>看上去，它好像将打印100，但事实上它打印的是55。</p>
<p>再一次，通过观察宏替换产生的文本，我们能够发现问题所在：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;%d\n&quot;</span>, <span class="number">10</span> * (a) + (a));</span><br></pre></td></tr></table></figure></div>

<p>乘法运算在宏所定义的加法运算之前执行。</p>
<p>这个错误很容易修正：在定义宏时，你只要在整个表达式两边加上一对括号就可以了：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span>　DOUBLE(x)　　((x) + (x))</span></span><br></pre></td></tr></table></figure></div>

<p>所有用于对数值表达式进行求值的宏定义都应该用这种方式加上括号，避免在使用宏时，由于参数中的操作符或邻近的操作符之间不可预料的相互作用。</p>
<p>下面是一对有趣的宏：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span>　repeat　　do</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span>　until(x)　　while(!(x))</span></span><br></pre></td></tr></table></figure></div>

<p>这两个宏创建了一种“新”的循环，其工作过程类似于其他语言中的<br><code>repeat/until</code>循环。它按照下面这样的方式使用：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line">repeat &#123;</span><br><span class="line">	statements</span><br><span class="line">&#125;<span class="built_in">until</span>( i &gt;= <span class="number">10</span>);</span><br></pre></td></tr></table></figure></div>

<p>预处理器将用下面的代码进行替换：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">do</span> &#123;</span><br><span class="line">	statements</span><br><span class="line">&#125; <span class="keyword">while</span>( !(i &gt;= <span class="number">10</span>));</span><br></pre></td></tr></table></figure></div>

<p>表达式<code>i&gt;=10</code>两边的括号用于确保在<code>!</code>操作符执行之前先完成这个表达式的求值。</p>
<p>&#x3D;&#x3D;注意！：&#x3D;&#x3D;</p>
<p>创建一套<code>#define</code>宏，用一种看上去很像其他语言的方式编写C程序是完全可能的。</p>
<p>在绝大多数情况下，你应该避免这种诱惑，因为这样编写出来的程序使其他C程序员很难理解。</p>
<p>他们必须时常查阅这些宏的定义以便弄清实际的代码是什么意思。</p>
<p>即使每个和这个项目生命期各个阶段相关的人都熟悉那种被模仿的语言，这个技巧仍然可能引起混淆，因为准确地模仿其他语言的各个方面是极为困难的。</p>
<hr>
<p>当预处理器搜索<code>#define</code>定义的符号时，字符串常量的内容并不进行检查。你如果想把宏参数插入到字符串常量中，可以使用两种技巧：</p>
<ul>
<li>第1个技巧邻近字符串自动连接的特性使我们很容易把一个字符串分成几段，每段实际上都是一个宏参数。这里有一个这种技巧的例子：</li>
</ul>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> PRINT(FORMAT ,VALUE)		\</span></span><br><span class="line"><span class="meta">		printf ( <span class="string">&quot;The value is &quot;</span> FORMAT <span class="string">&quot;n&quot;</span> , VALUE)</span></span><br><span class="line">		<span class="built_in">PRINT</span>(<span class="string">&quot;%d&quot;</span> ,x +<span class="number">3</span> ) ;</span><br></pre></td></tr></table></figure></div>

<p>这种技巧只有当字符串常量作为宏参数给出时才能使用。</p>
<ul>
<li><p>第2个技巧使用预处理器把一个宏参数转换为一个字符串。#argument这种结构<br>被预处理器翻译为“argument”。这种翻译可以让你像下面这样编写代码：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> PRINT (FORMAT，VALUE)					\</span></span><br><span class="line"><span class="meta">		printf ( <span class="string">&quot;The value of &quot;</span>#VALUE			\</span></span><br><span class="line"><span class="meta">		<span class="string">&quot;is &quot;</span> FORMAT <span class="string">&quot; \n&quot;</span>, VALUE )	</span></span><br><span class="line">		<span class="built_in">PRINT</span>(<span class="string">&quot;%d&quot;</span>,x + <span class="number">3</span> );</span><br></pre></td></tr></table></figure></div>

<p>它将产生下面的输出：</p>
<div class="highlight-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">The value of x +3 is 25</span><br></pre></td></tr></table></figure></div></li>
</ul>
<hr>
<p><code>##</code>结构则执行一种不同的任务：它把位于它两边的符号连接成一个符号。</p>
<p>作为用途之一，它允许宏定义从分离的文本片段创建标识符。</p>
<p>下面这个例子使用这种连接把一个值添加到几个变量之一：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> ADD_To_SUM(sum_number, value ) \</span></span><br><span class="line"><span class="meta">		sum##sum_number += value</span></span><br><span class="line">		<span class="built_in">ADD_TO_SUM</span>(<span class="number">5</span>,<span class="number">25</span>) ;</span><br></pre></td></tr></table></figure></div>

<p>最后一条语句把值25加到变量sum5。</p>
<p>注意这种连接必须产生一个合法的标识符。否则，其结果就是未定义的。</p>
<h3 id="宏的优势缺陷"><a href="#宏的优势缺陷" class="headerlink" title="宏的优势缺陷"></a>宏的优势缺陷</h3><p>在程序中扩展<code>#define</code>定义符号和宏时，需要涉及几个步骤。</p>
<ul>
<li>在调用宏时，首先对参数进行检查，看看是否包含了任何由<code>#define</code>定义的符号。如果是，它们首先被替换。</li>
<li>替换文本随后被插入到程序中原来文本的位置。对于宏，参数名被它们的值所替代。</li>
<li>最后，再次对结果文本进行扫描，看看它是否包含了任何由<code>#define</code>定义的符号。如果是，就重复上述处理过程。</li>
</ul>
<p>这样，宏参数和<code>#define</code>定义可以包含其他<code>#define</code>定义的符号。但是，宏不可以出现递归。</p>
<hr>
<p>宏非常频繁地用于执行简单的计算，比如在两个表达式中寻找其中较大（或较小）的一个：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> MAX( a, b )　　( (a) &gt; (b) ? (a) : (b) )</span></span><br></pre></td></tr></table></figure></div>

<p>为什么不用函数来完成这个任务呢？</p>
<p>有两个原因：</p>
<ul>
<li><p>首先，用于调用和从函数返回的代码很可能比实际执行这个小型计算工作的代码更大，所以使用宏比使用函数在程序的规模和速度方面都更胜一筹。</p>
<p>但是，更为重要的是，函数的参数必须声明为一种特定的类型，所以它只能在类型合适的表达式上使用。</p>
</li>
<li><p>反之，上面这个宏可以用于整型、长整型、单浮点型、双浮点数以及其他任何可以用&gt;操作符比较值大小的类型。</p>
<p>换句话说，宏是与类型无关的。和使用函数相比，使用宏的不利之处在于每次使用宏时，一份宏定义代码的拷贝都将插入到程序中。</p>
<p>除非宏非常短，否则使用宏可能会大幅度增加程序的长度。</p>
</li>
</ul>
<p>&#x3D;&#x3D;还有一些任务根本无法用函数实现。&#x3D;&#x3D;</p>
<p>这个宏的第2个参数是一种类型，它无法作为函数参数进行传递。</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> MALLOC(n, type) \</span></span><br><span class="line"><span class="meta">　　　　 ( (type *)malloc( (n) * sizeof( type ) ) )</span></span><br></pre></td></tr></table></figure></div>

<p>下面这个例子中的第1条语句被预处理器转换为第2条语句。</p>
<div class="highlight-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">pi = MALLOC( 25, int );</span><br><span class="line">pi = ( ( int * )malloc( ( 25 ) * sizeof( int ) ) );</span><br></pre></td></tr></table></figure></div>

<p>同样，请注意宏定义并没有用一个分号结尾。分号出现在调用这个宏的语句中。</p>
<hr>
<p>当宏参数在宏定义中出现的次数超过一次时，如果这个参数具有副作用，那么当你使用这个宏时就可能出现危险，导致不可预料的结果。</p>
<p>副作用就是在表达式求值时出现的永久性效果。例如，这个表达式<code>x + 1</code>可以重复执行几百次，它每次获得的结果都是一样的。这个表达式不具有副作<br>用。</p>
<p>但是<code>x++</code>就具有副作用：它增加<code>x</code>的值。当这个表达式下一次执行时，它将产生一个不同的结果。</p>
<p><code>MAX</code>宏可以证明具有副作用的参数所引起的问题：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> MAX(a, b) ((a) &gt; (b) ? (a) : (b))</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    x = <span class="number">5</span> ;y = <span class="number">8</span> ;</span><br><span class="line">	z = <span class="built_in">MAX</span>(x++,y++ ) ;</span><br><span class="line">	<span class="built_in">printf</span> ( <span class="string">&quot;x=%d, y=%d，z=%d\n&quot;</span> , x, y, z );</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>其结果是：<code>x=6，y=10，z=9</code></p>
<p>关于为什么，只要检查一下用宏替换后产生的代码，这个奇怪的结果就变得一目了然了：</p>
<div class="highlight-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">z = ((x++) &gt; (y++) ? (x++) : (y++));</span><br></pre></td></tr></table></figure></div>

<p>虽然那个较小的值只增值了一次，但那个较大的值却增值了两次：第1次是在比较时，第2次在执行?符号后面的表达式时出现。</p>
<hr>
<p>下面这个宏：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> EVENPARITY(ch)					\</span></span><br><span class="line"><span class="meta">		(( count__one_bits (ch) &amp; 1 ) ?	\</span></span><br><span class="line"><span class="meta">		(ch) | PARITYBIT : (ch))</span></span><br></pre></td></tr></table></figure></div>

<p>该函数返回它的参数的二进制位模式中1的个数。这个宏的目的是产生一个具有偶校验的字符。</p>
<p>它首先计数字符中位1的个数，如果结果是一个奇数，PARITYBIT值（一个值为1的位）与该字符执行OR操作，否则该字符就保留不变。</p>
<p>但是，当这个宏以下面这种方式使用时：</p>
<p><code>ch = EVENPARITY( getchar() );</code></p>
<p>读取一个字符并计算它的校验位。但是，它的结果是失败的，因为它实际上读入了两个字符！</p>
<hr>
<p>宏和函数的不同之处：</p>
<table>
<thead>
<tr>
<th align="left"></th>
<th><code>#define</code>宏</th>
<th>函数</th>
</tr>
</thead>
<tbody><tr>
<td align="left">代码长度</td>
<td>每次使用时，宏代码都被插入到程序中。除了非常小的宏之外，程序的长度将大幅度增长</td>
<td>函数代码只出现于一个地方；每次使用这个函数时，都调用那个地方的同一份代码</td>
</tr>
<tr>
<td align="left">执行速度</td>
<td>更快</td>
<td>存在函数调用&#x2F;返回的额外开销</td>
</tr>
<tr>
<td align="left">操作符优先级</td>
<td>宏参数的求值是在所有周围表达式的上下文环境里，除非它们加上括号，否则邻近操作符的优先级可能会产生不可预料的结果</td>
<td>函数参数只在函数调用时求值一次，它的结果值传递给函数。表达式的求值结果更容易预测</td>
</tr>
<tr>
<td align="left">参数求值</td>
<td>参数每次用于宏定义时，它们都将重新求值。由于多次求值，具有副作用的参数可能会产生不可预料的结果</td>
<td>参数在函数被调用前只求值一次。在函数中多次使用参数并不会导致多种求值过程。参数的副作用并不会造成任何特殊的问题</td>
</tr>
<tr>
<td align="left">参数类型</td>
<td>宏与类型无关。只要对参数的操作是合法的，它可以使用于任何参数类型</td>
<td>函数的参数是与类型有关的。如果参数的类型不同，就需要使用不同的函数，即使它们执行的任务是相同的</td>
</tr>
</tbody></table>
<h3 id="命名约定"><a href="#命名约定" class="headerlink" title="命名约定"></a>命名约定</h3><p><code>#define</code>宏的行为和真正的函数相比存在一些不同的地方，上表对此进行了总结。</p>
<p>由于这些不同之处，所以让程序员知道一个标识符究竟是一个宏还是一个函数是非常重要的。</p>
<p>但是使用宏的语法和使用函数的语法是完全一样的，所以语言本身并不能帮助你区分这两者。</p>
<p>为宏定义（对于绝大多数由<code>#define</code>定义的符号也是如此）采纳一种命名约定是很重要的，上面这种混淆就是促使人们这样做的原因之一。</p>
<p>一个常见的约定就是把宏名字全部大写。</p>
<p>在下面这条语句中：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line">value = <span class="built_in">max</span>( a, b );</span><br></pre></td></tr></table></figure></div>

<p><code>max</code>究竟是一个宏还是一个函数并不明显。</p>
<p>你很可能不得不仔细察看源文件以及它所包含的所有头文件来找出它的真实身份。</p>
<p>另一方面，请看下面这条语句：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line">value = <span class="built_in">MAX</span>( a, b );</span><br></pre></td></tr></table></figure></div>

<p>命名约定使<code>MAX</code>的身份一清二楚。</p>
<p>如果宏使用可能具有副作用的参数时，这个约定尤为重要，因为它可以提醒程序员在使用宏之前先把参数存储到临时变量中。</p>
<h2 id="undef"><a href="#undef" class="headerlink" title="#undef"></a><code>#undef</code></h2><p>语法描述：</p>
<p><code>#undef　name</code></p>
<p>用于移除一个已有的宏定义<code>name</code>，让后续代码中无法再使用该宏。</p>
<p>示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 定义一个宏，用于计算两个数的平方</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> SQUARE(x) ((x) * (x))</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="type">int</span> number = <span class="number">5</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 使用宏计算5的平方</span></span><br><span class="line">    <span class="type">int</span> result = <span class="built_in">SQUARE</span>(number);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;5的平方是: %d\n&quot;</span>, result);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 取消宏定义</span></span><br><span class="line">    <span class="meta">#<span class="keyword">undef</span> SQUARE</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 尝试再次使用宏，现在会报错</span></span><br><span class="line">    result = <span class="built_in">SQUARE</span>(number); <span class="comment">// Uncommenting this line will cause a compilation error</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>代码中使用<code>#undef SQUARE</code>取消了宏定义，后续代码中将无法再使用<code>SQUARE</code>宏，再次尝试使用宏时会导致编译错误。</p>
<p>如果一个现存的名字需要被重新定义，那么它的旧定义首先必须用<code>#undef</code>移除。</p>
<p>示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 定义一个宏，表示一个最大值MAXNUM为100</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> MAXNUM 100</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;MAXNUM的值是: %d\n&quot;</span>, MAXNUM); <span class="comment">//打印100</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 取消宏定义</span></span><br><span class="line">    <span class="meta">#<span class="keyword">undef</span> MAXNUM</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 重新定义宏，MAXNUM为1000</span></span><br><span class="line">    <span class="meta">#<span class="keyword">define</span> MAXNUM 1000</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 再次打印</span></span><br><span class="line">     <span class="built_in">printf</span>(<span class="string">&quot;MAXNUM的值是: %d\n&quot;</span>, MAXNUM); <span class="comment">//打印1000</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure></div>

<h2 id="条件编译"><a href="#条件编译" class="headerlink" title="条件编译"></a>条件编译</h2><h3 id="选择性编译"><a href="#选择性编译" class="headerlink" title="选择性编译"></a>选择性编译</h3><p>在编译一个程序时，如果我们可以选择只对某些语句进行编译或者忽略，常常会显得很方便。</p>
<p>这些语句不应该出现在程序的产品版本中，但是你可能并不想把这些语句从源代码中物理删除，因为如果需要一些维护性修改时，你可能需要重新调试这个程序，还需要这些语句。</p>
<p>条件编译就是用于实现这个目的。使用条件编译，你可以选择代码的一部分是被正常编译还是完全忽略。</p>
<p>用于条件编译的指令包括：<code>#if</code>、<code>#endif</code>、<code>#ifdef</code>、<code>#ifndef</code>、<code>#else</code>、<code>#elif</code>。</p>
<p>下面显示了它最简单的语法形式：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">if</span>　constant-expression</span></span><br><span class="line">　　　　 statements</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br></pre></td></tr></table></figure></div>

<p>其中，<code>constant-expression</code>是一个常量表达式，由预处理器进行求值。</p>
<p>如果它的值是非零值即真值，那么<code>statements</code>部分就被正常编译，否则预处理器就“安静地删除“它们，使得<code>statements</code>部分在编译时被忽略。</p>
<p>所谓常量表达式，就是说它或者是字面值常量，或者是一个由<code>#define</code>定义的符号。</p>
<p>如果变量在执行期之前无法获得它们的值，那么它们如果出现在常量表达式中就是非法的，不同的编译器可能有不同的处理方式：忽略、报错等，因为它们的值在编译时是不可预测的。</p>
<p>例如：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">if</span> DEBUG</span></span><br><span class="line"><span class="built_in">printf</span> ( <span class="string">&quot;x=%d, y=%dn&quot;</span> , x, y ) ;</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br></pre></td></tr></table></figure></div>

<p><code>DEBUG</code>的值并没有指定，这实际上是一种未定义行为，不同的编译器可能有不同的处理方式：有的编译器会将未定义的宏视为零（假），有的编译器会发出警告，有的编译器可能会忽略这段代码或者报错。</p>
<p>为了避免这种未定义行为，正确的做法是在代码中明确地定义 <code>DEBUG</code> 宏，如果想要编译它，只要使用：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">define</span> DEBUG 1  </span></span><br></pre></td></tr></table></figure></div>

<p>如果想要忽略它，只要把这个符号定义为0就可以了。无论哪种情况，这段代码都可以保留在源文件中。</p>
<p>这样，预处理指令 <code>#if DEBUG</code> 就能正确地根据 <code>DEBUG</code> 宏的定义与否来决定是否编译相关代码。</p>
<p>当使用条件预处理指令 <code>#if</code> 时，可以结合 <code>#elif</code> 和 <code>#else</code> 子句来实现多个条件的判断。</p>
<p>完整的语法如下所示：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> OPTION 2</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="meta">#<span class="keyword">if</span> OPTION == 1</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Option 1 is selected.\n&quot;</span>);</span><br><span class="line"><span class="meta">#<span class="keyword">elif</span> OPTION == 2</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Option 2 is selected.\n&quot;</span>);</span><br><span class="line"><span class="meta">#<span class="keyword">elif</span> OPTION == 3</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Option 3 is selected.\n&quot;</span>);</span><br><span class="line">    </span><br><span class="line"><span class="comment">//......</span></span><br><span class="line">    </span><br><span class="line"><span class="meta">#<span class="keyword">else</span></span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Option is not recognized.\n&quot;</span>);</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>在这个示例中，我们定义了一个宏 <code>OPTION</code>，然后使用条件预处理指令 <code>#if</code> 和 <code>#elif</code> 来判断不同的选项。</p>
<p>根据 <code>OPTION</code> 的值，程序将输出不同的信息。</p>
<ul>
<li>如果 <code>OPTION</code> 的值为 <code>1</code>，那么编译器会选择第一个条件分支 <code>#if OPTION == 1</code>，输出 “Option 1 is selected.”</li>
<li>如果 <code>OPTION</code> 的值为 <code>2</code>，那么编译器会选择第二个条件分支 <code>#elif OPTION == 2</code>，输出 “Option 2 is selected.”</li>
<li>如果 <code>OPTION</code> 的值既不是 <code>1</code> 也不是 <code>2</code>，编译器会选择最后的 <code>#else</code> 分支，输出 “Option is not recognized.”</li>
</ul>
<p>在实际的项目中，通过调整宏的定义，可以灵活地根据不同的条件编译不同的代码，这在实现跨平台、调试开关等方面非常有用。</p>
<h3 id="检查是否定义"><a href="#检查是否定义" class="headerlink" title="检查是否定义"></a>检查是否定义</h3><p>当使用条件编译时，我们可以通过<code>#ifdef</code>和<code>#ifndef</code>来检查符号是否已经被定义。</p>
<p>下面是一个简单的例子：</p>
<p>假设我们有一个程序，其中使用了一个预定义的符号<code>DEBUG</code>来控制是否打印调试信息。</p>
<p>如果<code>DEBUG</code>已经被定义，则打印调试信息；如果没有定义，则不打印调试信息。</p>
<div class="highlight-container" data-rel="C"><figure class="iseeu highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> DEBUG <span class="comment">// 如果定义了DEBUG符号时，打印调试信息</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> DEBUG_PRINT(x) std::cout&lt;&lt;x;</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">else</span> <span class="comment">//没有定义就空实现一段代码</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> DEBUG_PRINT(x) do &#123;&#125; while (0)</span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="type">int</span> num = <span class="number">42</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 输出调试信息</span></span><br><span class="line">    DEBUG_PRINT((<span class="string">&quot;Debug: The value of num is %d\n&quot;</span>, num));</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 其他代码...</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>在这个例子中，我们使用了<code>#ifdef DEBUG</code>和<code>#ifndef DEBUG</code>来检查<code>DEBUG</code>符号是否已经被定义。</p>
<ul>
<li><p><code>#ifdef DEBUG</code> 表示：检查是否已经定义了<code>DEBUG</code>符号。</p>
<p>如果已经定义，就会编译<code>#ifdef</code>后面的代码块，这里我们定义了一个宏<code>DEBUG_PRINT(x)</code>，它会调用<code>cout</code>来打印调试信息。</p>
</li>
<li><p><code>#ifndef DEBUG</code> 表示：检查是否没有定义<code>DEBUG</code>符号。</p>
<p>如果没有定义，就会编译<code>#ifndef</code>后面的代码块，这里我们定义了一个空的<code>DEBUG_PRINT(x)</code>宏，这样在程序中调用<code>DEBUG_PRINT</code>时就不会有任何输出，达到不打印调试信息的目的。</p>
</li>
</ul>
<p>在编译程序时，可以通过在编译命令中加入<code>-DDEBUG</code>选项来定义<code>DEBUG</code>符号，或者不加该选项则表示没有定义<code>DEBUG</code>符号。</p>
<p>这样，程序在不同的编译情况下会根据<code>DEBUG</code>符号的定义与否，选择性地打印或不打印调试信息。</p>
<h3 id="防止头文件重复包含"><a href="#防止头文件重复包含" class="headerlink" title="防止头文件重复包含"></a>防止头文件重复包含</h3><p>可以使用预处理指令来防止头文件重复包含。</p>
<p>头文件重复包含可能导致编译错误或其他问题，因此需要使用预处理指令来确保头文件只被包含一次。</p>
<p>常用的预处理指令有两种：<code>#ifndef</code>和<code>#pragma once</code>。这里只将使用条件编译的方式。</p>
<p>在每个头文件的开头，我们可以使用<code>#ifndef</code>、<code>#define</code>和<code>#endif</code>组合来防止重复包含。</p>
<p>这样的组合通常被称为“头文件保护”或“宏保护”。</p>
<p>代码示例：</p>
<div class="highlight-container" data-rel="C"><figure class="iseeu highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">ifndef</span> MY_HEADER_H  <span class="comment">// 检查是否没有定义过MY_HEADER_H</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> MY_HEADER_H  <span class="comment">// 如果没有定义，则定义MY_HEADER_H</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 头文件的内容写在这里</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span>  <span class="comment">// 结束宏保护</span></span></span><br></pre></td></tr></table></figure></div>

<p>当编译器第一次遇到该头文件时，<code>MY_HEADER_H</code>还没有被定义，所以<code>#ifndef</code>条件为真，然后执行<code>#define MY_HEADER_H</code>，将<code>MY_HEADER_H</code>定义为一个标识符。</p>
<p>当其他地方再次包含这个头文件时，由于<code>MY_HEADER_H</code>已经被定义了，所以<code>#ifndef</code>条件为假，头文件的内容就会被跳过，从而避免了重复包含。</p>
<h3 id="嵌套指令"><a href="#嵌套指令" class="headerlink" title="嵌套指令"></a>嵌套指令</h3><p>当进行条件编译时，的确可以嵌套使用多个条件，这样可以根据多个条件来选择性地编译代码块。</p>
<p>我们使用一个更简单的示例来说明嵌套条件编译的概念：</p>
<p>假设有一个宏 <code>DEBUG_MODE</code>，用于控制是否打印调试信息。</p>
<p>还有一个宏 <code>TEST_MODE</code>，用于控制是否编译测试相关的代码。</p>
<p>下面是一个简单的示例代码：</p>
<div class="highlight-container" data-rel="C"><figure class="iseeu highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> DEBUG_MODE</span></span><br><span class="line"><span class="meta">#<span class="keyword">define</span> TEST_MODE</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span> </span><br><span class="line">&#123;</span><br><span class="line">    <span class="meta">#<span class="keyword">ifdef</span> DEBUG_MODE</span></span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;已启用调试模式。\n&quot;</span>);</span><br><span class="line">    <span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line">    <span class="meta">#<span class="keyword">ifdef</span> TEST_MODE</span></span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;已启用测试模式。\n&quot;</span>);</span><br><span class="line">    <span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line">    <span class="meta">#<span class="keyword">ifdef</span> DEBUG_MODE</span></span><br><span class="line">        <span class="meta">#<span class="keyword">ifdef</span> TEST_MODE</span></span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">&quot;已同时启用调试模式和测试模式。\n&quot;</span>);</span><br><span class="line">        <span class="meta">#<span class="keyword">else</span></span></span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">&quot;已启用调试模式，但未启用测试模式。\n&quot;</span>);</span><br><span class="line">        <span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line">    <span class="meta">#<span class="keyword">else</span></span></span><br><span class="line">        <span class="meta">#<span class="keyword">ifdef</span> TEST_MODE</span></span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">&quot;已启用测试模式，但未启用调试模式。\n&quot;</span>);</span><br><span class="line">        <span class="meta">#<span class="keyword">else</span></span></span><br><span class="line">            <span class="built_in">printf</span>(<span class="string">&quot;未启用调试模式和测试模式。\n&quot;</span>);</span><br><span class="line">        <span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line">    <span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>在这个示例中，我们使用了嵌套的条件编译来处理四种情况：</p>
<ol>
<li>如果只有 <code>DEBUG_MODE</code> 被定义，输出：已启用调试模式。</li>
<li>如果只有 <code>TEST_MODE</code> 被定义，输出：已同时启用调试模式和测试模式。</li>
<li>如果两者都被定义，输出：已启用调试模式，但未启用测试模式。</li>
<li>如果两者都没有被定义，输出：已启用测试模式，但未启用调试模式。</li>
</ol>
<h3 id="其他注意事项"><a href="#其他注意事项" class="headerlink" title="其他注意事项"></a>其他注意事项</h3><ul>
<li><p><code>#if defined A</code>和<code> #ifdef A</code>是等价的。</p>
</li>
<li><p><code>#if !defined A</code>和<code>#ifndef A</code>是等价的。</p>
</li>
<li><p>每一个条件编译都应以<code>#endif</code>结尾。</p>
</li>
</ul>
<h2 id="error"><a href="#error" class="headerlink" title="#error"></a><code>#error</code></h2><p><code>#error</code>指令允许你生成错误信息。编译程序时，只要遇到<code>#error</code> 就会生成一个编译错误提示消息，并停止编译。</p>
<p>语法：<br><code>#error　text of error message</code></p>
<p>代码示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="meta">#<span class="keyword">if</span> defined A</span></span><br><span class="line"></span><br><span class="line">	cout &lt;&lt; <span class="string">&quot;define 了 A&quot;</span> &lt;&lt; endl;</span><br><span class="line"><span class="meta">#<span class="keyword">elif</span> defined B</span></span><br><span class="line">	cout &lt;&lt; <span class="string">&quot;define 了 B&quot;</span> &lt;&lt; endl;</span><br><span class="line"><span class="meta">#<span class="keyword">elif</span> defined C</span></span><br><span class="line">	cout &lt;&lt; <span class="string">&quot;define 了 C &quot;</span> &lt;&lt; endl;</span><br><span class="line"><span class="meta">#<span class="keyword">else</span></span></span><br><span class="line">	<span class="meta">#<span class="keyword">error</span> No defined A B C</span></span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line">    </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>这段代码编译无法通过。</p>
<p>在程序运行时，一旦遇到断言不成立，程序即会报错终止，以方便程序调试。<code>#error</code>与<code>#if</code>搭配使用可以认为是一个用于调试宏的编译期断言。</p>
<h2 id="include"><a href="#include" class="headerlink" title="#include"></a><code>#include</code></h2><p>我们使用<code>#include</code>指令来包含头文件，以便在代码中引用其中的函数声明、宏或其他定义。</p>
<p>C&#x2F;C++语言标准允许编译器自行决定如何处理两种形式的<code>#include</code>：一种是本地形式，另一种是函数库形式。</p>
<ol>
<li><p>本地形式的<code>#include</code>：</p>
<p>这是指我们在代码中使用相对或绝对路径来引用自己或项目中的头文件。</p>
<p>例如：<code>#include &quot;my_header.h&quot;</code>，其中<code>my_header.h</code>就是本地头文件。</p>
</li>
<li><p>函数库形式的<code>#include</code>：</p>
<p>这是指我们引用标准库或第三方库提供的头文件。</p>
<p>例如：<code>#include &lt;stdio.h&gt;</code>，其中<code>stdio.h</code>就是函数库头文件。</p>
</li>
</ol>
<p>编译器在处理这两种<code>#include</code>时有一个策略：</p>
<ul>
<li>首先，编译器会对本地头文件采用一种特殊的处理方式。它会首先在源文件所在的当前目录中查找要包含的本地头文件。如果能找到，就直接使用这个本地头文件。</li>
<li>如果在当前目录中没有找到本地头文件，编译器则会按照函数库头文件的处理方式来处理它们。这就意味着编译器会在标准位置（通常是系统指定的一些标准目录）去查找本地头文件。</li>
</ul>
<hr>
<p>当我们在代码中使用<code>#include &quot;my_header.h&quot;</code>这样的本地头文件引用时，编译器会按照一种特殊的方式来处理它。</p>
<p>举个例子：</p>
<p>假设我们有一个项目，其中有两个文件，一个是<code>main.c</code>，另一个是<code>my_header.h</code>。</p>
<p><code>main.c</code>文件包含了一些代码，而<code>my_header.h</code>文件定义了一些函数和常量。</p>
<p>现在，我们在<code>main.c</code>中使用了<code>#include &quot;my_header.h&quot;</code>这样的语句来引用<code>my_header.h</code>这个本地头文件。</p>
<ul>
<li><p>如果<code>main.c</code>和<code>my_header.h</code>都在同一个文件夹内，编译器会首先在这个文件夹中查找<code>my_header.h</code>，找到了就直接使用它的内容。</p>
</li>
<li><p>如果<code>my_header.h</code>在另一个文件夹中，比如在<code>include</code>文件夹内，而<code>main.c</code>在项目的根目录中，编译器会先在当前根目录中查找<code>my_header.h</code>，如果找不到，再去<code>include</code>文件夹中找，找到了就使用。</p>
</li>
</ul>
<p>这样的处理方式让我们能够方便地组织项目中的本地头文件，如果在当前目录能找到，就直接使用，找不到再按照标准规则去查找，确保了头文件的引用可以在不同的项目结构中都能正常工作。</p>
<p>这种处理方式的好处是，编译器优先考虑了本地头文件的查找，这样在项目中我们可以方便地使用自定义的头文件。</p>
<p>如果本地头文件不存在，编译器会像处理函数库头文件一样，按照预定的规则去查找它们。</p>
<h2 id="（空指令）"><a href="#（空指令）" class="headerlink" title="#（空指令）"></a><code>#</code>（空指令）</h2><p>无效指令（空指令）就是一个<code>#</code>符号开头，但后面不跟任何内容的一行。这类指令只是被预处理器简单地删除。</p>
<p>下面例子中的无效指令通过把<code>#include</code>与周围的代码分隔开来，凸显它的存在，当然我们也可以通过插入空行取得相同的效果：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line">#</span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line">#</span><br></pre></td></tr></table></figure></div>

<h2 id="pragma"><a href="#pragma" class="headerlink" title="#pragma"></a><code>#pragma</code></h2><p><code>#pragma</code> 指令通常用于向编译器发出一些特定的指令或控制命令，以便改变编译器的行为或进行一些特定的操作。</p>
<p>&#x3D;&#x3D;它并不是C&#x2F;C++语言标准的一部分，而是由各个编译器厂商提供的扩展功能。&#x3D;&#x3D;</p>
<p>因此，&#x3D;&#x3D;不同的编译器可能支持不同的 <code>#pragma</code> 指令，并且在不同的编译器中可能会有不同的效果。&#x3D;&#x3D;</p>
<p>下面是一些常见的 <code>#pragma</code> 预处理指令和它们的用途：</p>
<ul>
<li><p><code>#pragma once</code>: 这是一个用于防止头文件重复包含的指令，和使用 <code>#ifndef</code> 和 <code>#define</code> 进行头文件保护的效果是一样的。</p>
</li>
<li><p><code>#pragma pack</code>: 这个指令用于设置结构体的字节对齐方式。</p>
<p>可以通过 <code>#pragma pack(n)</code> 将结构体的对齐方式设置为 <code>n</code> 字节。在不同的编译器中，可能会有其他指令用于控制结构体的对齐方式。</p>
</li>
<li><p><code>#pragma message</code>: 这个指令用于在编译过程中输出一条用户定义的消息。例如，用于调试或显示一些编译信息。</p>
<p>例如：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">pragma</span> message(<span class="string">&quot;Compiling my program...&quot;</span>)</span></span><br></pre></td></tr></table></figure></div>
</li>
<li><p><code>#pragma warning</code>: 这个指令用于控制编译器警告的输出。</p>
<p>可以通过 <code>#pragma warning(disable: &lt;warning_code&gt;)</code> 来禁用特定的警告，或者通过 <code>#pragma warning(error: &lt;warning_code&gt;)</code> 将警告转换为错误，中断编译。</p>
<p>例如：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">pragma</span> <span class="keyword">warning</span>(disable: 1234) <span class="comment">// 禁用警告编号为1234的警告</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">pragma</span> <span class="keyword">warning</span>(<span class="keyword">error</span>: 5678)  <span class="comment">// 将警告编号为5678的警告转换为错误</span></span></span><br></pre></td></tr></table></figure></div>

<p>示例：</p>
<p>再MSVC微软旗下的一款编译器中，认为<code>scanf</code>函数是不安全的，编译时会报错，编译无法通过，错误代码为：C4996。</p>
<p>但是可以使用<code>#pragma warning(disable: 4996)</code>来禁用这个错误提示，使得编译顺利通过。</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">pragma</span> <span class="keyword">warning</span>(disable: 4996) <span class="comment">// 禁用警告编号为4996的警告</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> std;</span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="type">int</span> a;</span><br><span class="line">    </span><br><span class="line">	<span class="built_in">scanf</span>(<span class="string">&quot;%d&quot;</span>, &amp;a);</span><br><span class="line">    </span><br><span class="line">	cout &lt;&lt; a &lt;&lt; endl;</span><br><span class="line">    </span><br><span class="line">	<span class="built_in">system</span>(<span class="string">&quot;pause&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div></li>
</ul>
<p>&#x3D;&#x3D;需要注意：#pragma&#96; 不是C&#x2F;C++的标准指令，它的行为和效果可能在不同的编译器中有所不同。&#x3D;&#x3D;</p>
<p>为了保证代码的可移植性，建议尽量避免过度依赖特定的 <code>#pragma</code> 指令。</p>
<p>对于MSVC，详细参阅：<a class="link"   href="https://learn.microsoft.com/zh-cn/cpp/preprocessor/pragma-directives-and-the-pragma-keyword?view=msvc-170" >Pragma 指令与 __pragma 和 _Pragma 关键字 | Microsoft Learn <i class="fa-regular fa-arrow-up-right-from-square fa-sm"></i></a></p>
<h2 id="预定义符号"><a href="#预定义符号" class="headerlink" title="预定义符号"></a>预定义符号</h2><h3 id="概述-2"><a href="#概述-2" class="headerlink" title="概述"></a>概述</h3><p>预定义的预处理符号或指令是在编译过程中由编译器自动定义的一些宏或符号。</p>
<p>这些预处理符号可以在代码中使用，用于编写可移植和条件化的代码。</p>
<p>以下是一些常见的预定义的预处理符号或指令：</p>
<ul>
<li><p><code>__LINE__</code>: 这个预处理符号表示当前代码所在的行号，可以在代码中使用，通常用于调试信息输出和错误定位。</p>
</li>
<li><p><code>__FILE__</code>: 这个预处理符号表示当前代码所在的文件名（包括路径），可以在代码中使用，通常用于调试信息输出。</p>
</li>
<li><p><code>__DATE__</code>: 这个预处理符号表示代码被编译的日期，以字符串形式表示（通常是”月 日 年”的格式），可以在代码中使用。</p>
</li>
<li><p><code>__TIME__</code>: 这个预处理符号表示代码被编译的时间，以字符串形式表示（通常是”时:分:秒”的格式），可以在代码中使用。</p>
</li>
<li><p><code>__cplusplus</code>: 这个预处理符号在C++编译器中定义，用于判断当前代码是否是C++环境。如果代码在C++环境中编译，<code>__cplusplus</code> 会被定义为一个非零值，否则未定义。</p>
</li>
<li><p><code>_WIN32</code>、<code>_WIN64</code>: 这些预处理符号通常在Windows平台的编译器中定义，用于判断代码是否在Windows环境中编译。</p>
<p><code>_WIN32</code> 在32位Windows系统上定义，而 <code>_WIN64</code> 在64位Windows系统上定义。</p>
</li>
<li><p><code>__GNUC__</code>: 这个预处理符号在GNU编译器（例如GCC）中定义，用于判断代码是否在GCC环境中编译。</p>
</li>
</ul>
<p>这些预定义的预处理符号或指令可以帮助程序员获取一些关于编译环境和代码位置的信息，并且在编写可移植和条件化代码时非常有用。</p>
<p>需要注意的是，这些预定义符号的名称通常以双下划线开头和结尾，以避免和用户定义的宏冲突。</p>
<h3 id="使用示例"><a href="#使用示例" class="headerlink" title="使用示例"></a>使用示例</h3><ul>
<li><p><code>__LINE__</code> 和 <code>__FILE__</code> 的使用示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;当前代码位于文件 %s 的第 %d 行。\n&quot;</span>, __FILE__, __LINE__);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>运行上述代码，输出可能类似于：</p>
<div class="highlight-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">当前代码位于文件 main.c 的第 5 行。</span><br></pre></td></tr></table></figure></div>
</li>
<li><p><code>__DATE__</code> 和 <code>__TIME__</code> 的使用示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;代码编译日期：%s\n&quot;</span>, __DATE__);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;代码编译时间：%s\n&quot;</span>, __TIME__);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>运行上述代码，输出可能类似于：</p>
<div class="highlight-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">代码编译日期：Jul 28 2023</span><br><span class="line">代码编译时间：18:30:00</span><br></pre></td></tr></table></figure></div>
</li>
<li><p><code>__cplusplus</code> 的使用示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> __cplusplus</span></span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;这是C++环境下的代码。&quot;</span> &lt;&lt; std::endl;</span><br><span class="line"><span class="meta">#<span class="keyword">else</span></span></span><br><span class="line">    std::cout &lt;&lt; <span class="string">&quot;这是C环境下的代码。&quot;</span> &lt;&lt; std::endl;</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>运行上述代码，如果编译器是C++编译器，则输出：</p>
<div class="highlight-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">这是C++环境下的代码。</span><br></pre></td></tr></table></figure></div>

<p>如果编译器是C编译器，则输出：</p>
<div class="highlight-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">这是C环境下的代码。</span><br></pre></td></tr></table></figure></div>
</li>
<li><p><code>_WIN32</code> 和 <code>_WIN64</code> 的使用示例：</p>
<div class="highlight-container" data-rel="C++"><figure class="iseeu highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="function"><span class="type">int</span> <span class="title">main</span><span class="params">()</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="meta">#<span class="keyword">ifdef</span> _WIN32</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;这是在Windows 32位环境下编译的代码。\n&quot;</span>);</span><br><span class="line"><span class="meta">#<span class="keyword">elif</span> defined(_WIN64)</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;这是在Windows 64位环境下编译的代码。\n&quot;</span>);</span><br><span class="line"><span class="meta">#<span class="keyword">else</span></span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;这不是在Windows环境下编译的代码。\n&quot;</span>);</span><br><span class="line"><span class="meta">#<span class="keyword">endif</span></span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>运行上述代码，输出根据编译环境的不同可能有所不同。例如，在Windows 64位系统下编译运行：</p>
<div class="highlight-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">这是在Windows 64位环境下编译的代码。</span><br></pre></td></tr></table></figure></div></li>
</ul>
<p>&#x3D;&#x3D;请注意，输出可能因编译环境不同而有所变化。这些预定义的预处理符号在不同的编译器和平台上可能会有不同的定义和行为。&#x3D;&#x3D;</p>
<h2 id="line"><a href="#line" class="headerlink" title="#line"></a><code>#line</code></h2><p><code>#line</code> 指令是预处理器中的一个特殊指令，用于改变源代码中的行号和文件名信息。</p>
<p>它的主要作用是帮助程序员在预处理阶段中调整行号和文件名，从而在编译错误或调试时提供更准确的信息。</p>
<p><code>#line</code> 指令的语法如下：</p>
<div class="highlight-container" data-rel="C"><figure class="iseeu highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">line</span> constant <span class="string">&quot;filename&quot;</span></span></span><br></pre></td></tr></table></figure></div>

<p>其中，<code>constant</code> 是一个整数常量，表示将当前行号修改为指定的值，<code>&quot;filename&quot;</code> 是一个字符串，表示将当前文件名修改为指定的文件名。</p>
<p>通过使用 <code>#line</code> 指令，程序员可以在宏展开或其他情况下，手动调整行号和文件名，以便更好地定位问题和调试代码。</p>
<p><code>#line</code>指令与作用域是无关的。</p>
<p>以下是一个简单的代码示例：</p>
<div class="highlight-container" data-rel="C"><figure class="iseeu highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">()</span></span><br><span class="line">&#123;</span><br><span class="line">	&#123;</span><br><span class="line">		<span class="built_in">printf</span>(<span class="string">&quot;当前文件：%s，当前行号：%d\n&quot;</span>, __FILE__, __LINE__);</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="keyword">line</span> 100 <span class="string">&quot;my_code.c&quot;</span></span></span><br><span class="line">		<span class="built_in">printf</span>(<span class="string">&quot;当前文件：%s，当前行号：%d\n&quot;</span>, __FILE__, __LINE__);</span><br><span class="line"></span><br><span class="line">		<span class="built_in">printf</span>(<span class="string">&quot;当前文件：%s，当前行号：%d\n&quot;</span>, __FILE__, __LINE__);</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="built_in">printf</span>(<span class="string">&quot;当前文件：%s，当前行号：%d\n&quot;</span>, __FILE__, __LINE__);</span><br><span class="line"></span><br><span class="line">	system(<span class="string">&quot;pause&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></div>

<p>输出结果可能类似于：</p>
<div class="highlight-container" data-rel="Plaintext"><figure class="iseeu highlight plaintext"><table><tr><td class="code"><pre><span class="line">当前文件：main.c，当前行号：6</span><br><span class="line">当前文件：my_code.c，当前行号：100</span><br><span class="line">当前文件：my_code.c，当前行号：102</span><br><span class="line">当前文件：my_code.c，当前行号：104</span><br></pre></td></tr></table></figure></div>









]]></content>
      <tags>
        <tag>学习笔记</tag>
      </tags>
  </entry>
</search>
